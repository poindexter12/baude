#!/usr/bin/env bash
# Reject release-please-incompatible messages before Git records the commit.
set -eu

repo_root="$(git rev-parse --show-toplevel)"
exec bash "$repo_root/scripts/check-commit-message.sh" "$1"
