---
phase: 06-safe-managed-worktree-lifecycle
plan: 02
type: tdd
wave: 2
depends_on: ["06-01"]
files_modified:
  - baude-core/src/git.rs
  - baude-core/src/lifecycle.rs
  - baude/src/app.rs
  - bauded/src/manager.rs
autonomous: true
requirements: [WORK-02]
must_haves:
  truths:
    - "Invalid refs, filesystem or Git-inventory path collisions, and unsafe occupancy are refused with no partial child or runtime."
    - "Concurrent creation uses fresh topology and cannot create duplicate paths, children, or runtimes."
  artifacts:
    - path: "baude-core/src/git.rs"
      provides: "Collision-safe durable managed path allocation and typed creation failures"
      contains: "PathCollision"
    - path: "baude-core/src/lifecycle.rs"
      provides: "Serialized create/activate transitions and rollback-stage outcomes"
    - path: "baude/src/app.rs"
      provides: "Local failure-safe activation adapter"
    - path: "bauded/src/manager.rs"
      provides: "Manager failure-safe activation adapter"
  key_links:
    - from: "baude-core/src/git.rs"
      to: "RepositoryKey + CheckoutKey"
      via: "managed path includes durable repository and checkout identity"
      pattern: "repository-.*checkout|ManagedBranch"
    - from: "baude-core/src/lifecycle.rs"
      to: "fresh worktree inventory"
      via: "rediscovery after reservation and immediately before add"
      pattern: "reserve|discover"
---

<objective>
Harden the activation tracer for WORK-02 so every invalid, colliding, occupied, failing, or racing request leaves Git, durable state, and runtimes consistent.

Purpose: Ensure user-controlled branch/path requests cannot bypass Git safeguards or leave partial lifecycle artifacts.
Output: Full creation-safety matrix, stable managed allocation, and commit-aware compensation in both owners.
</objective>

## Artifacts this phase produces

- Stable filesystem-safe branch worktree paths whose uniqueness comes from durable keys.
- Typed no-partial-mutation outcomes for validation, topology, Git, persistence, and spawn failures.
- Deterministic same-repository race coverage and forbidden-command audit.

<execution_context>
@/Users/joese/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/joese/.config/opencode/get-shit-done/templates/summary.md
@/Users/joese/.config/opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-CONTEXT.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-RESEARCH.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-PATTERNS.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-01-SUMMARY.md
</context>

<tasks>

<task id="06-02-01" type="auto" tdd="true">
  <name>Task 1: Refuse malformed refs and every managed-path collision</name>
  <files>baude-core/src/git.rs, baude-core/src/lifecycle.rs</files>
  <read_first>baude-core/src/git.rs lifecycle code from Plan 01 plus existing GitFixture; baude-core/src/lifecycle.rs; 06-RESEARCH.md Required Real-Git Matrix rows Ref validation, Managed paths, and Add failures; 06-PATTERNS.md Fail Closed on Unknown State</read_first>
  <behavior>
    - RED: leading-dash, `.lock`, `..`, lone `@`, `@{-1}`, spaces, control characters, malformed Git output, and command failures reject before key/path/state/runtime mutation.
    - RED: pre-existing file, directory, symlink, and missing-but-registered/prunable path all block candidate reuse.
    - RED: slash/sanitizer collisions and duplicate repository basenames still allocate distinct stable paths from `RepositoryKey` plus `CheckoutKey`.
    - RED: no tested command contains `--force`, `-B`, prune, repair, clean, reset, stash, fetch, or branch deletion.
  </behavior>
  <action>Extend the real-Git fixture and injectable command recorder first, verify the matrix fails, then make managed branch allocation use the durable repository key, bounded non-empty sanitized label, and checkout-key collision suffix. Check `symlink_metadata` and the fresh complete Git inventory independently before add; a missing path does not override an inventory record. Keep the branch slug non-authoritative. Treat command start, unexpected exit, decode, parse, and postcondition errors as typed refusal. Remove or make unreachable the legacy unsafe helper on managed lifecycle paths. Do not use direct recursive deletion as compensation.</action>
  <acceptance_criteria>
    - Every invalid ref and path ownership case is rejected before production state/runtime changes.
    - Stable allocation handles sanitizer collisions without treating a readable label as identity.
    - Git inventory remains the final occupancy authority even for absent/prunable paths.
    - The argv audit proves all prohibited bypass commands are absent.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p baude-core git::tests::lifecycle::creation_safety -- --nocapture</automated>
  </verify>
  <done>Creation safety rejects untrusted or colliding requests without bypassing Git or allocating a partial checkout.</done>
</task>

<task id="06-02-02" type="auto" tdd="true">
  <name>Task 2: Prove serialized creation rollback across App and Manager</name>
  <files>baude-core/src/lifecycle.rs, baude/src/app.rs, bauded/src/manager.rs</files>
  <read_first>baude-core/src/lifecycle.rs; baude/src/app.rs Plan 01 activation adapter and test seams; bauded/src/manager.rs Plan 01 activation adapter, save_checked, and atomic failure seams; baude-core/src/persist.rs:89-130,310-384; 06-RESEARCH.md Required Race Matrix and Required Failure/Consistency Matrix Add failures</read_first>
  <behavior>
    - RED: two same-branch creates serialize and converge on one checkout/runtime; different-branch creates rediscover after the first mutation and allocate distinct checkouts.
    - RED: Git add/postcondition failure leaves no durable child/runtime and retains any created branch.
    - RED: pre-replacement save failure compensates a newly added checkout only through verified plain Git removal; post-replacement failure keeps memory aligned with committed durable intent.
    - RED: spawn failure leaves one durable active child for retry and no runtime-map entry.
  </behavior>
  <action>Add deterministic reservation and stage-injection vectors before implementation, then complete the fixed reserve → rediscover → validate → Git add/reuse → verify → save → spawn sequence in both adapters. Use `SaveError::replacement_committed()` to distinguish rollback from committed durability degradation. A safe compensation may call only the verified plain worktree removal primitive and must retain the local branch; if compensation cannot be proven safe, retain an unavailable recovery child and report the exact stage. Same-checkout repeats focus/return one runtime. Ensure guards release under all early returns and panics are not used for external failures.</action>
  <acceptance_criteria>
    - Race vectors deterministically prove per-repository serialization and fresh rediscovery.
    - Every injected stage has an asserted Git/state/runtime postcondition rather than a generic error assertion.
    - App and Manager produce the same lifecycle outcome for equivalent facts.
    - No failure creates a duplicate child or runtime, deletes a branch, or rolls memory behind a committed replacement.
  </acceptance_criteria>
  <verify>
    <automated>cargo test lifecycle_creation_rollback -- --nocapture</automated>
  </verify>
  <done>WORK-02 failures and races are fail-closed and leave no ambiguous partial activation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| branch label → managed filesystem path | Sanitized text is attacker-controlled and may collide. |
| stale plan → external Git topology | Another process can change inventory between validation and mutation. |
| Git mutation → JSON persistence → PTY spawn | Separate commit boundaries can fail independently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Spoofing / Tampering | managed path/ref validation | mitigate | Durable-key uniqueness, bounded slug, `symlink_metadata`, full fresh inventory, typed unknown rejection, and argv audit. |
| T-06-04 | Tampering / Repudiation / DoS | creation transaction and races | mitigate | Per-repository reservation, immediate rediscovery, stage-aware rollback, verified compensation, and one checkout-key runtime association. |
</threat_model>

<verification>
- Both focused WORK-02 commands pass, preferably under 30 seconds each.
- Search production lifecycle callers to confirm no `git::create_worktree` use remains.
- `cargo test lifecycle_create_activate -- --nocapture` remains green.
</verification>

<success_criteria>
- Invalid refs, collisions, unsafe occupancy, and every unknown Git result refuse activation without partial state.
- Concurrent requests converge or serialize with no stale mutation.
- All TDD gates are recorded for both tasks.
</success_criteria>

<output>
Create `.planning/phases/06-safe-managed-worktree-lifecycle/06-02-SUMMARY.md` when done.
</output>
