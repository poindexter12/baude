---
phase: 06-safe-managed-worktree-lifecycle
plan: 05
type: tdd
wave: 4
depends_on: ["06-04"]
files_modified:
  - baude-core/src/backend/mod.rs
  - baude-core/src/backend/claude.rs
  - baude-core/src/backend/opencode.rs
  - baude-core/src/pty.rs
  - baude-core/src/lifecycle.rs
  - baude/src/app.rs
  - bauded/src/manager.rs
autonomous: true
requirements: [WORK-04]
must_haves:
  truths:
    - "A retained main checkout or managed worktree reconciles before durable activation and opens exactly one runtime in the active backend."
    - "Claude Code and OpenCode target the retained conversation ID when present, using directory-latest continuation only when no ID was ever observed."
    - "Moved, branch-changed, detached, locked, prunable, or identity-changed retained children remain unavailable and do not launch."
  artifacts:
    - path: "baude-core/src/backend/mod.rs"
      provides: "Typed Fresh/ContinueLatest/ResumeId backend spawn mode"
      contains: "ResumeId"
    - path: "baude-core/src/pty.rs"
      provides: "Opaque spawn environment transport that avoids shell interpolation"
    - path: "baude-core/src/lifecycle.rs"
      provides: "Reconcile-save-dispatch reopen transition and checkout reservation"
    - path: "baude/src/app.rs"
      provides: "Local retained checkout reopen"
    - path: "bauded/src/manager.rs"
      provides: "Manager retained checkout reopen/restart"
  key_links:
    - from: "RetainedSessionState.resume_id"
      to: "Backend::spawn_plan"
      via: "typed ResumeId mode"
      pattern: "ResumeId"
    - from: "Backend::spawn_plan"
      to: "Pty::spawn"
      via: "opaque environment/direct process data, never command interpolation"
      pattern: "env|SpawnPlan"
    - from: "reopen transition"
      to: "git::reconcile_checkout"
      via: "fresh topology before active-intent save and spawn"
      pattern: "reconcile_checkout"
---

<objective>
Implement WORK-04: securely reopen any retained checkout through fresh Git reconciliation, durable active intent, targeted backend resume, and one checkout-key runtime reservation.

Purpose: Return users to the intended conversation without launching against stale topology or duplicating agents under repeated/concurrent reopen.
Output: Typed backend resume mode, injection-safe PTY transport, and shared App/Manager reopen orchestration.
</objective>

## Artifacts this phase produces

- Typed fresh/latest/targeted backend spawn contract for Claude Code and OpenCode.
- Opaque resume-ID transport that cannot become shell syntax.
- Reconcile-before-dispatch reopen state machine with one runtime per checkout key.

<execution_context>
@/Users/joese/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/joese/.config/opencode/get-shit-done/templates/summary.md
@/Users/joese/.config/opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-CONTEXT.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-RESEARCH.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-PATTERNS.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-04-SUMMARY.md
</context>

<tasks>

<task id="06-05-01" type="auto" tdd="true">
  <name>Task 1: Carry targeted resume IDs as opaque process data</name>
  <files>baude-core/src/backend/mod.rs, baude-core/src/backend/claude.rs, baude-core/src/backend/opencode.rs, baude-core/src/pty.rs</files>
  <read_first>baude-core/src/backend/mod.rs SpawnPlan/Backend; baude-core/src/backend/claude.rs spawn_plan and exact command tests; baude-core/src/backend/opencode.rs compose_spawn_cmd/spawn_plan tests; baude-core/src/pty.rs spawn CommandBuilder; 06-RESEARCH.md Pattern 7 and shell-injection security row; 06-PATTERNS.md backend/pty assignment</read_first>
  <behavior>
    - RED: `Fresh`, `ContinueLatest`, and `ResumeId(String)` produce distinct exact plans for both backends.
    - RED: Claude targeted mode invokes `--resume` with the opaque ID; OpenCode invokes `--session`; only absent IDs select each backend's latest continuation form.
    - RED: hostile values containing spaces, quotes, semicolons, substitutions, and option-looking prefixes remain one opaque data value and never appear as executable shell syntax.
    - RED: existing permission flags, Claude event URL scope, OpenCode prompt config, and server-port behavior remain intact in every mode.
  </behavior>
  <action>Write exact plan and hostile-ID tests first. Replace the boolean resume parameter with a typed mode. Extend `SpawnPlan` and PTY creation so the resume ID is delivered through a fixed environment key or direct argv-capable process field; never concatenate a persisted ID into the shell `-c` string. Claude and OpenCode may reference only a fixed quoted environment variable in their static command templates if direct argv cannot preserve the login-shell behavior. Keep event/config exports safe and existing backend permission composition unchanged. Update all backend call sites only enough to compile; actual retained reopen selection is Tasks 2-3.</action>
  <acceptance_criteria>
    - Both backend test suites distinguish all three modes and pin their exact safe transport.
    - Hostile resume IDs cannot add flags or shell commands.
    - Targeted IDs choose `--resume`/`--session`; latest continuation occurs only for `ContinueLatest`.
    - Existing hook, permission, and OpenCode port tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p baude-core backend:: -- --nocapture</automated>
  </verify>
  <done>The backend/PTY boundary can safely target the exact retained conversation without shell interpolation.</done>
</task>

<task id="06-05-02" type="auto" tdd="true">
  <name>Task 2: Plan idempotent reopen after fresh checkout reconciliation</name>
  <files>baude-core/src/lifecycle.rs</files>
  <read_first>baude-core/src/lifecycle.rs activation/close contracts and reservation type; baude-core/src/git.rs reconcile_checkout; baude-core/src/repository.rs retained resume_id and health; 06-RESEARCH.md architecture REOPEN sequence, Required Race Matrix reopen row, Required Failure Matrix reopen rows</read_first>
  <behavior>
    - RED: unavailable/moved/branch-changed/detached/locked/prunable/replaced-identity facts produce blocked outcomes with no active-intent flip or runtime effect.
    - RED: an available inactive child plans save active intent before focus/restart/resume/spawn and selects targeted or latest mode from retained metadata.
    - RED: live runtime focuses, exited runtime restarts, absent runtime spawns, and repeated/concurrent same-checkout requests reserve and return one runtime path.
  </behavior>
  <action>Add table-driven pure transition and deterministic reservation tests first. Implement a general retained-checkout reopen plan keyed by `RepositoryKey` and `CheckoutKey`: reserve, fresh reconcile exact common directory/path/full ref/unlocked topology, mark health, persist active intent, then select focus/restart/spawn. Same-checkout in-flight reopens must converge on the reserved runtime; conflicting same-repository mutation returns typed busy. Do not auto-repair, move, unlock, change branches, or launch an unavailable child. Release reservations on every exit.</action>
  <acceptance_criteria>
    - Every topology mismatch blocks before active intent or runtime effects.
    - Available retained main and managed children share one transition contract.
    - Dispatch mode and targeted/latest selection are deterministic from retained facts.
    - Repeated/concurrent vectors prove exactly one spawn attempt.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p baude-core lifecycle::tests::reopen -- --nocapture</automated>
  </verify>
  <done>Core produces a fail-closed, idempotent reopen effect plan for any retained checkout.</done>
</task>

<task id="06-05-03" type="auto" tdd="true">
  <name>Task 3: Execute retained reopen consistently in App and Manager</name>
  <files>baude/src/app.rs, bauded/src/manager.rs</files>
  <read_first>baude/src/app.rs ensure_primary/reconcile_primary/restart/add_session and Plan 04 close adapter; bauded/src/manager.rs restore/reconcile/spawn/restart and Plan 04 close adapter; baude-core/src/lifecycle.rs reopen plan; 06-PATTERNS.md App/Manager reopen assignments</read_first>
  <behavior>
    - RED: App reopens retained main/worktree children by key, saves active intent, and focuses/restarts/spawns exactly one runtime with preserved settings.
    - RED: Manager restore/restart/reopen uses the same outcome and targeted backend mode.
    - RED: pre-replacement save failure restores inactive intent and performs no spawn; spawn failure keeps durable active intent for retry without a runtime-map entry.
  </behavior>
  <action>Write the shared adapter vectors first, then generalize App's primary-only ensure/reconcile path and Manager restore/restart path to execute the core reopen plan for any retained checkout. Persist active intent before PTY effects, pass `ResumeId` when retained, `ContinueLatest` only when absent, and restore name/shell/archive settings. Associate one runtime by checkout key after successful spawn; focus live or restart exited associations rather than duplicating. Surface reconciliation and persistence failures with the retained child intact. Keep daemon APIs compatible and defer hierarchy/action presentation.</action>
  <acceptance_criteria>
    - App and Manager vectors produce identical domain outcomes and aggregate deltas.
    - Retained main and managed worktree children both reopen in the active backend.
    - Exactly one runtime is focused/restarted/spawned under repeated requests.
    - Every unavailable or pre-commit failure produces zero launch attempts.
  </acceptance_criteria>
  <verify>
    <automated>cargo test lifecycle_reopen -- --nocapture</automated>
  </verify>
  <done>Users can safely reopen retained main or managed worktree children into the intended backend conversation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted resume ID → login-shell PTY | Untrusted opaque backend data must not become executable syntax. |
| persisted checkout intent → external Git topology | Stale paths/branches cannot authorize launch. |
| repeated caller requests → runtime owner | Concurrency must not duplicate agent processes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering / Elevation | backend/PTY targeted resume | mitigate | Typed mode, fixed env/direct argv transport, hostile-value tests, and no ID interpolation into shell text. |
| T-06-10 | Spoofing / Tampering | reopen reconciliation | mitigate | Exact fresh common-dir/path/full-ref/lock checks before active intent and launch. |
| T-06-11 | Tampering / DoS | reopen reservation/runtime map | mitigate | Repository/checkout reservation, checkout-key ownership, save-before-spawn, and deterministic duplicate-request tests. |
</threat_model>

<verification>
- Backend, core reopen, and adapter reopen filters pass separately.
- Hostile resume-ID tests prove the ID is data, not command syntax.
- Focused checks remain under 30 seconds where possible; full workspace gate is deferred to final plan verification, not feature behavior.
</verification>

<success_criteria>
- WORK-04 reopens retained main and worktree children only after fresh reconciliation.
- The exact prior conversation is targeted when known, with one runtime under concurrent requests.
- All three behavior tasks complete enforced TDD gates.
</success_criteria>

<output>
Create `.planning/phases/06-safe-managed-worktree-lifecycle/06-05-SUMMARY.md` when done.
</output>
