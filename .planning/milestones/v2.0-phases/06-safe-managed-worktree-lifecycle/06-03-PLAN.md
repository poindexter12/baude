---
phase: 06-safe-managed-worktree-lifecycle
plan: 03
type: tdd
wave: 3
depends_on: ["06-02"]
files_modified:
  - baude-core/src/git.rs
autonomous: true
requirements: [WORK-06]
must_haves:
  truths:
    - "Any tracked, staged, unstaged, deleted, untracked, ignored, conflicted, submodule, locked, prunable, malformed, or indeterminate state blocks removal."
    - "A removal preflight cannot report safe unless exact managed linked-worktree ownership and every Git inspection are conclusive."
  artifacts:
    - path: "baude-core/src/git.rs"
      provides: "Result-valued removal inspection, blocker taxonomy, plain remove primitive, and real-Git matrix"
      exports: ["RemovalSafety", "RemovalBlocker", "inspect_removal"]
  key_links:
    - from: "inspect_removal"
      to: "discover_repository"
      via: "fresh common-dir/path/full-ref/lock/prunable ownership proof"
      pattern: "discover_repository|managed_by_baude"
    - from: "inspect_removal"
      to: "git status --porcelain=v2 -z"
      via: "fail-closed status record parser"
      pattern: "porcelain=v2|ignored=matching"
---

<objective>
Build the fail-closed Git safety oracle for WORK-06 before any removal orchestration can stop a runtime or alter durable state.

Purpose: Replace boolean dirtiness with inspectable proof that unknown, hidden, conflicted, submodule, and topology states all block deletion.
Output: Typed removal blockers, strict machine-output parsers, and a comprehensive real-Git/failure matrix.
</objective>

## Artifacts this phase produces

- Result-valued worktree removal preflight with specific actionable blockers.
- Porcelain-v2 NUL status and recursive submodule inspection.
- Exact managed linked-worktree ownership/topology proof and unknown-state rejection.

<execution_context>
@/Users/joese/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/joese/.config/opencode/get-shit-done/templates/summary.md
@/Users/joese/.config/opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-CONTEXT.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-RESEARCH.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-PATTERNS.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-02-SUMMARY.md
</context>

<tasks>

<task id="06-03-01" type="auto" tdd="true">
  <name>Task 1: Parse every dirty and conflicted status as an explicit blocker</name>
  <files>baude-core/src/git.rs</files>
  <read_first>baude-core/src/git.rs topology parser, lifecycle Git executor, legacy is_dirty/remove_worktree, and GitFixture; 06-RESEARCH.md Pattern 4, Required Real-Git Matrix Status row, Pitfalls 4/6; 06-PATTERNS.md git.rs removal-preflight assignment</read_first>
  <behavior>
    - RED: staged add/delete/rename, unstaged edit/delete, untracked file/directory, ignored file/directory, conflict, and unusual NUL-safe filenames each produce the specific blocker.
    - RED: empty valid porcelain output is status-clean, but unknown record kinds, malformed fields, invalid UTF-8 where text is required, process-start failure, and nonzero exit return indeterminate errors rather than clean.
    - RED: the command uses `--no-optional-locks`, porcelain v2 `-z`, all untracked files, `--ignore-submodules=none`, and `--ignored=matching`.
  </behavior>
  <action>Write parser units and real-Git cases first, confirm the old boolean helper cannot satisfy them, then introduce inspectable `RemovalSafety`, `RemovalBlocker`, and `InspectionError` types. Parse only documented NUL-delimited porcelain-v2 record forms, including XY/submodule fields and unmerged `u` records; classify `?` and `!` separately. Reject unknown/malformed output and every command failure. Do not preserve a compatibility boolean that can map an error to clean. The status parser may keep paths as bytes where needed; diagnostics may be lossy text but never authority.</action>
  <acceptance_criteria>
    - Every dirty category has a distinct asserted blocker and empty valid output is the only status-clean result.
    - Ignored paths block even when plain Git might remove them.
    - Unknown or failing inspection cannot construct a safe target.
    - Legacy `.unwrap_or(false)` cleanliness behavior is removed from lifecycle use.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p baude-core git::tests::lifecycle::removal_preflight -- --nocapture</automated>
  </verify>
  <done>All content-level dirty, conflict, hidden-file, malformed, and command-failure cases fail closed.</done>
</task>

<task id="06-03-02" type="auto" tdd="true">
  <name>Task 2: Require managed linked topology and reject every submodule uncertainty</name>
  <files>baude-core/src/git.rs</files>
  <read_first>baude-core/src/git.rs WorktreeRecord/discover/reconcile and Task 1 removal types; baude-core/src/repository.rs SavedCheckout ownership fields; 06-RESEARCH.md Pattern 4 checks 1 and 3, Required Real-Git Matrix Submodule and Topology rows; 06-CONTEXT.md Safe Removal decisions</read_first>
  <behavior>
    - RED: main, detached, external unmanaged, locked, prunable, moved, branch-changed, replaced-common-dir, or unlinked targets cannot become safe.
    - RED: clean, dirty, untracked, uninitialized (`-`), mismatched (`+`), conflicted (`U`), nested, malformed, and command-failing submodule observations all block; only no recorded submodule advances.
    - RED: a safe result carries the exact canonical path, common directory, full branch ref, and captured branch OID needed for a later postcondition check.
  </behavior>
  <action>Extend the fixture with linked/main/external/locked/prunable/topology and recursive submodule cases before implementation. Make `inspect_removal` accept the persisted checkout ownership facts and freshly prove same common directory, exact canonical selected path/full branch, linked non-main status, `managed_by_baude=true`, and unlocked/non-prunable topology. Run `git submodule status --recursive`; any valid row blocks non-force removal, and malformed/nonzero/start failure is indeterminate. Return an opaque verified target only after all checks pass so later plain removal cannot be called with an arbitrary path. Do not prune, repair, unlock, initialize, clean, or alter submodules.</action>
  <acceptance_criteria>
    - Only an exact available baude-managed linked worktree with no submodule record can produce `Safe`.
    - External occupied worktrees remain non-removable even if clean.
    - The safe token captures branch and parent facts for post-removal preservation checks.
    - Every topology/submodule unknown blocks without changing Git, state, or runtime.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p baude-core git::tests::lifecycle::removal_topology -- --nocapture</automated>
  </verify>
  <done>WORK-06 has a conclusive managed-ownership, topology, status, and submodule gate that defaults every uncertainty to blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted ownership → destructive authorization | Durable claims must match fresh Git facts before deletion is possible. |
| Git status/submodule bytes → safety decision | External machine output may be malformed, incomplete, or hidden by config. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering / Information loss | porcelain status parser | mitigate | Explicit porcelain-v2 NUL parsing, config overrides, ignored/untracked/conflict blockers, and unknown/error rejection. |
| T-06-06 | Spoofing / Elevation / Information loss | ownership/topology/submodule preflight | mitigate | Fresh exact topology, managed-only authorization, recursive submodule block, and opaque verified target construction. |
</threat_model>

<verification>
- Both focused real-Git matrices pass independently.
- `cargo test -p baude-core git::tests::lifecycle -- --nocapture` remains green.
- Production search finds no lifecycle authorization based on `is_dirty() -> bool`.
</verification>

<success_criteria>
- Every WORK-06 blocker named by the requirement and context is automated and fail-closed.
- No inspection error can be interpreted as clean.
- TDD RED and GREEN gates are present for both parser and topology/submodule behavior.
</success_criteria>

<output>
Create `.planning/phases/06-safe-managed-worktree-lifecycle/06-03-SUMMARY.md` when done.
</output>
