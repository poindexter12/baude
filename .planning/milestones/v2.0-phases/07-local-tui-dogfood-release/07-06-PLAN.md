---
phase: 07-local-tui-dogfood-release
plan: 06
type: execute
wave: 6
depends_on: ["07-05"]
files_modified:
  - README.md
  - CHANGELOG.md
  - docs/local-tui-dogfood.md
  - .planning/phases/07-local-tui-dogfood-release/07-VALIDATION.md
autonomous: true
requirements: [REL-03]
must_haves:
  truths:
    - "README, changelog, source-install instructions, and dogfood runbook consistently describe readiness for v2.0.0-beta without claiming publication."
    - "The complete local format, Clippy, test, package, release-build, archive/extract/version, and isolated cargo-install final gate passes."
    - "Validation records only observed implementation evidence; morning certification, independent review, verification, approvals, requirement/phase completion, and publication remain pending."
    - "Morning evidence goes to 07-UAT-EVIDENCE.md, and that file is created only when screenshots, commands, or observed certification outcomes exist."
  artifacts:
    - path: "docs/local-tui-dogfood.md"
      provides: "HOME/state/repository/install-isolated manual wide/narrow dogfood runbook and morning evidence checklist"
    - path: "README.md"
      provides: "Durable hierarchy, lifecycle keys, and local beta source-install guidance"
      contains: "local-tui-dogfood"
    - path: "CHANGELOG.md"
      provides: "Unreleased beta-readiness section without a released/published claim"
      contains: "2.0.0-beta readiness"
    - path: ".planning/phases/07-local-tui-dogfood-release/07-VALIDATION.md"
      provides: "Observed local implementation evidence and honest pending morning certification"
  key_links:
    - from: "README.md"
      to: "docs/local-tui-dogfood.md"
      via: "readiness-safe local source/install instructions"
      pattern: "local-tui-dogfood"
    - from: "docs/local-tui-dogfood.md"
      to: ".planning/phases/07-local-tui-dogfood-release/07-UAT-EVIDENCE.md"
      via: "morning-only evidence destination named without pre-creating an empty artifact"
      pattern: "07-UAT-EVIDENCE.md"
---

<objective>
Complete REL-03 documentation and final local readiness evidence for v2.0.0-beta without false certification or publication claims.

Purpose: Give users an exact isolated dogfood path and prove local source/package/install readiness while preserving the morning certification boundary.
Output: Accurate docs/runbook/changelog plus evidence-only validation from the complete local final gate.
</objective>

<execution_context>
@/Users/joese/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/joese/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-local-tui-dogfood-release/07-UI-SPEC.md
@.planning/phases/07-local-tui-dogfood-release/07-RESEARCH.md
@.planning/phases/07-local-tui-dogfood-release/07-PATTERNS.md
@.planning/phases/07-local-tui-dogfood-release/07-VALIDATION.md
@.planning/phases/07-local-tui-dogfood-release/07-05-SUMMARY.md
@README.md
@CHANGELOG.md
@docs/deploy.md
@Cargo.toml
@.release-please-manifest.json
@.github/workflows/release.yml
</context>

<tasks>

<task id="07-06-01" type="auto">
  <name>Task 1: Document hierarchy and isolated morning dogfood without release claims</name>
  <reversibility rating="reversible">Documentation and unreleased changelog wording can be corrected locally before any publication.</reversibility>
  <files>README.md, CHANGELOG.md, docs/local-tui-dogfood.md</files>
  <action>Update README's overview, install, Keys, status, archive, Worktrees, and remote sections to describe repository parents, persisted oldest-first children, durable in-process selection, deterministic first-parent restart initialization, x retained close, X verified clean managed removal with branch retained, applicable existing actions, and separate flat non-destructive remote rows. Source installation/dogfood must use the explicitly built local source or `cargo install --path baude --root <isolated-root> --locked`; do not imply a GitHub beta release exists or overwrite the user's installed baude. Add an unreleased `2.0.0-beta readiness` changelog section above 0.14.0 covering durable hierarchy/lifecycle/release checks without a compare URL or released/published claim. Create `docs/local-tui-dogfood.md` as a command-first isolated runbook: temporary HOME/config/data/state/install roots, temporary bare origin/main repository, harmless fake backend, local locked build/install, open/create-or-activate/close/restart/reselect/reopen/safe-remove sequence, deterministic first-parent restart assertion, wide and narrow screenshots, RepositoryKey/CheckoutKey/order and before/after worktree inventory evidence, branch-ref survival, cleanup, and explicit morning pending checklist. Name `.planning/phases/07-local-tui-dogfood-release/07-UAT-EVIDENCE.md` as the evidence destination and state that it is created only when evidence exists. Include no push, tag, release-please execution, gh, publish, Docker push, PR, fetch, force removal, auto-clean, or branch-deletion command.</action>
  <verify>
    <automated>rg -F 'v2.0.0-beta' README.md CHANGELOG.md docs/local-tui-dogfood.md &amp;&amp; rg -F 'X' docs/local-tui-dogfood.md &amp;&amp; rg -F 'branch' docs/local-tui-dogfood.md &amp;&amp; rg -F '40x12' docs/local-tui-dogfood.md &amp;&amp; rg -F '07-UAT-EVIDENCE.md' docs/local-tui-dogfood.md</automated>
    <fails_when>Beta target, distinct remove key, branch-retention evidence, tiny/wide dogfood coverage, or exact morning evidence destination is absent, or the command exits nonzero.</fails_when>
    <automated>python3 -c 'from pathlib import Path; ps=[Path("README.md"),Path("CHANGELOG.md"),Path("docs/local-tui-dogfood.md")]; t="\n".join(p.read_text().lower() for p in ps); forbidden=["git push","gh release","cargo publish","release-please manifest","docker push","git tag"]; assert not [x for x in forbidden if x in t]; c=Path("CHANGELOG.md").read_text().lower().split("## [0.14.0]",1)[0]; assert "released" not in c and "published" not in c; d=Path("docs/local-tui-dogfood.md").read_text(); assert "first local repository parent" in d and "selection is not persisted" in d.lower() and "only when evidence exists" in d'</automated>
    <fails_when>A forbidden publication/push/tag command exists, the unreleased beta section claims released/published status, restart claims persisted selection, the evidence file is presented as anticipatory, a document is unreadable, or the command exits nonzero.</fails_when>
  </verify>
  <done>Users can understand and locally install/dogfood the exact hierarchy, restart behavior, and lifecycle safely while all text remains readiness-only and morning evidence/publication remain pending.</done>
</task>

<task id="07-06-02" type="auto">
  <name>Task 2: Run final-gate sampling and record observed evidence only</name>
  <reversibility rating="reversible">Validation evidence records observed local commands and can be corrected; it does not approve certification, requirements, or publication.</reversibility>
  <files>.planning/phases/07-local-tui-dogfood-release/07-VALIDATION.md</files>
  <action>Run every command below against the final source as final-gate sampling; this comprehensive gate may exceed 30 seconds and is not a focused feedback loop. Inspect `git diff --check`, `git status --short`, and tracked-file membership for every existing source/config/doc path changed by the phase; preserve unrelated `graphify-out/` and `opencode.json`. Fully verify the publishable core package, assemble all workspace packages with `--no-verify` because dependents cannot registry-resolve unpublished beta core, then separately prove full workspace tests/builds and host artifacts. Build and extract a two-binary host tar, assert exact versions, and perform an isolated `cargo install --path baude --root <temp> --locked` version check without changing global installation. Update 07-VALIDATION.md only with exact observed pass/fail output and test counts. Keep `nyquist_compliant: false`, approval pending, and manual wide/narrow dogfood, supported CI/Linux/runtime certification including Phase 6 process checks, independent deep review, phase verification, Nyquist approval, requirement/phase completion, UI-SPEC checker sign-off, and publication decision pending for morning. Do not create `07-UAT-EVIDENCE.md` during implementation; morning certification creates it only when evidence exists. Do not edit REQUIREMENTS checkboxes, Phase 6/7 completion state, or published history; do not push, open a PR, publish, tag, upload a release, or claim readiness if any local gate fails.</action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <fails_when>Formatting differs or the command exits nonzero.</fails_when>
    <automated>cargo clippy --workspace --all-targets -- -D warnings</automated>
    <fails_when>Any warning/error is emitted or the command exits nonzero.</fails_when>
    <automated>cargo test --workspace</automated>
    <fails_when>Any workspace test fails or the command exits nonzero.</fails_when>
    <automated>cargo package -p baude-core --locked</automated>
    <fails_when>The core package cannot assemble and pristine-verify from locked metadata, or the command exits nonzero.</fails_when>
    <automated>cargo package --workspace --locked --no-verify</automated>
    <fails_when>Any workspace package cannot assemble, path/version metadata is invalid, package contents are incomplete, or the command exits nonzero.</fails_when>
    <automated>cargo build --workspace --release --locked &amp;&amp; test "$(target/release/baude --version)" = 'baude 2.0.0-beta' &amp;&amp; test "$(target/release/bauded --version)" = 'bauded 2.0.0-beta'</automated>
    <fails_when>Locked release build fails, either binary is absent, either version differs exactly, or the command exits nonzero.</fails_when>
    <automated>tmp=$(mktemp -d); trap 'rm -rf "$tmp"' EXIT; tar -C target/release -czf "$tmp/baude-v2.0.0-beta-host.tar.gz" baude bauded &amp;&amp; mkdir "$tmp/extracted" &amp;&amp; tar -C "$tmp/extracted" -xzf "$tmp/baude-v2.0.0-beta-host.tar.gz" &amp;&amp; test "$("$tmp/extracted/baude" --version)" = 'baude 2.0.0-beta' &amp;&amp; test "$("$tmp/extracted/bauded" --version)" = 'bauded 2.0.0-beta'</automated>
    <fails_when>The host archive omits either binary, extraction fails, extracted versions differ, temporary cleanup setup fails, or the command exits nonzero.</fails_when>
    <automated>tmp=$(mktemp -d); trap 'rm -rf "$tmp"' EXIT; cargo install --path baude --root "$tmp/install" --locked &amp;&amp; test "$("$tmp/install/bin/baude" --version)" = 'baude 2.0.0-beta'</automated>
    <fails_when>Isolated local install changes global state, install fails, installed version differs, temporary cleanup setup fails, or the command exits nonzero.</fails_when>
    <automated>git diff --check &amp;&amp; git ls-files --error-unmatch baude-core/src/lifecycle.rs baude/src/main.rs baude/src/app.rs baude/src/ui.rs bauded/src/api.rs .github/workflows/ci.yml Cargo.toml baude-core/Cargo.toml baude/Cargo.toml bauded/Cargo.toml Cargo.lock release-please-config.json README.md CHANGELOG.md .release-please-manifest.json .github/workflows/release-please.yml .github/workflows/release.yml</automated>
    <fails_when>Whitespace errors exist, any existing source/config/release path is not tracked, published history/workflow is missing, or the command exits nonzero.</fails_when>
    <automated>python3 -c 'from pathlib import Path; s=Path(".planning/phases/07-local-tui-dogfood-release/07-VALIDATION.md").read_text(); required=["nyquist_compliant: false","Manual wide/narrow local TUI dogfood","Linux/runtime certification","Independent deep review","Phase verification and Nyquist approval","Release publication decision","07-UAT-EVIDENCE.md"]; assert all(x in s for x in required); assert "status: complete" not in s.lower(); p=Path(".planning/phases/07-local-tui-dogfood-release/07-UAT-EVIDENCE.md"); assert not p.exists() or (p.stat().st_size &gt; 0 and "observed" in p.read_text().lower())'</automated>
    <fails_when>Validation claims completion/Nyquist approval, omits any morning pending gate/evidence destination, a present UAT evidence file is empty or lacks observed evidence, or the command exits nonzero.</fails_when>
  </verify>
  <done>All local source/package/artifact/install final-gate samples pass and validation contains only observed evidence; every human/platform/review/verification/completion/publication gate remains explicitly pending for morning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| readiness docs → users | Documentation must not imply an unpublished beta exists remotely. |
| local install/package checks → user machine | Verification must not overwrite global installs or publish credentials/artifacts. |
| observed results → validation state | Unrun morning gates must not be represented as passed or pre-populated evidence. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking mitigation |
|-----------|----------|----------|-----------|-------------|---------------------|
| T-07-19 | Spoofing/Repudiation | High | README/changelog/runbook | mitigate | Readiness-only wording and forbidden publication-command assertions. |
| T-07-20 | Tampering/DoS | Medium | local install/artifact gate | mitigate | Temporary isolated roots, exact extracted versions, and cleanup. |
| T-07-21 | Repudiation | High | validation/UAT evidence | mitigate | Evidence-only updates, explicit morning pending list, and no UAT file until evidence exists. |
| T-07-SC | Tampering | High | package supply chain | mitigate | No new dependency; locked existing graph and package-content checks. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Local dogfood/docs/package readiness without false completion | 07-06 | COVERED | Runbook plus final gate. |
| REQ | REL-03 | Version/package/changelog/install docs consistent and non-publishing | 07-06 | COVERED | Tasks 1-2. |
| RESEARCH | certification boundary | Morning certification; evidence destination created only from evidence | 07-06 | COVERED | Validation and runbook exact assertions. |
| CONTEXT | overnight boundary | Observed implementation evidence only; certification/completion/publication pending | 07-06 | COVERED | Task 2. |
| CONTEXT | deferred | Publication, push, PR, tag, release upload | — | EXCLUDED | Explicitly forbidden. |
</source_coverage_audit>

<phase_source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status |
|--------|----|---------------------|------|--------|
| GOAL | — | Complete local durable hierarchy/lifecycle dogfood plus beta source readiness | 07-01..07-06 | COVERED |
| REQ | REPO-05, HIER-01, HIER-02, HIER-03, HIER-04 | Durable visible stable hierarchy | 07-01 | COVERED |
| REQ | WORK-01, WORK-02, WORK-03, WORK-04, WORK-05, WORK-06 | Safe lifecycle actions | 07-02 | COVERED |
| REQ | SURF-01, SURF-02 | Exact responsive contextual local surface and preserved actions | 07-03 | COVERED |
| REQ | SURF-05, REL-01 | Flat compatibility and isolated restart/dedup dogfood | 07-04 | COVERED |
| REQ | REL-02 | Exact package/release-proposal and supported CI readiness | 07-05 | COVERED |
| REQ | REL-03 | Readiness docs, local final gate, evidence boundary | 07-06 | COVERED |
| RESEARCH | architecture | Pure projection, core capabilities, App dispatch, Ratatui, real Git, exact prerelease config, non-publishing final gate | 07-01..07-06 | COVERED |
| CONTEXT | locked scope | Exact UI spec, in-process durable selection, deterministic restart, Phase 6 authority, no new dependency, morning gates pending | 07-01..07-06 | COVERED |
| CONTEXT | deferred | Dormant rows/deletion, remote hierarchy, PWA, publication/push/PR, force/auto cleanup/fetch/main switching | — | EXCLUDED |
</phase_source_coverage_audit>

<estimate>
raw_tokens: 26000
calibration_factor: 1
estimated_tokens: 26000
confidence: medium
reason: One bounded documentation task plus one mechanical but intentionally long final-gate sampling task.
</estimate>

<verification>
- Final-gate sampling may exceed 30 seconds; guarded exact tests in earlier plans remain the focused feedback loop.
- No exact Cargo filter is used without `--list | rg -Fx` anywhere in the phase; this plan intentionally runs broad workspace commands.
- Manual dogfood, supported CI/Linux/runtime certification, independent review, phase verification, Nyquist/UI approval, requirement/phase completion, and publication decision remain pending for morning.
</verification>

<success_criteria>
- README, changelog, and runbook consistently describe v2.0.0-beta readiness, deterministic restart selection, and no release publication.
- All local code, package, archive, version, and isolated install final-gate samples pass.
- Validation is evidence-based; 07-UAT-EVIDENCE.md is absent until observed evidence exists; all unrun morning gates remain pending.
</success_criteria>

<output>
Create `.planning/phases/07-local-tui-dogfood-release/07-06-SUMMARY.md` with exact docs/local gate/package/archive/install evidence. Do not create UAT evidence without observations, check requirements, complete Phase 6/7, approve Nyquist/UI, push, create a PR, tag, publish, or alter last-published history.
</output>
