---
phase: 07-local-tui-dogfood-release
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - baude/src/hierarchy.rs
  - baude/src/main.rs
  - baude/src/app.rs
  - baude/src/ui.rs
autonomous: true
requirements: [REPO-05, HIER-01, HIER-02, HIER-03, HIER-04]
must_haves:
  truths:
    - "The first production tracer renders durable repository parents and their main/default/worktree children from real App repository state, including parents and retained children with no runtime."
    - "Repository selection is RepositoryKey and checkout selection is CheckoutKey; runtime IDs, labels, paths, and row indexes never identify local hierarchy selection."
    - "Parents are name-sorted with path/key ties and children remain persisted oldest-first with key ties across process restart and every status/archive/attention/runtime change."
    - "Selection retains the same durable key across in-process refresh/status transitions; because selection is not persisted, process restart deterministically selects the first local repository parent in rendered order, else the first flat remote row, else no selection."
    - "The main checkout and a separate managed default checkout appear beneath one canonical repository parent without synthesizing dormant branch rows."
  artifacts:
    - path: "baude/src/hierarchy.rs"
      provides: "Pure durable hierarchy, row identity, structural ordering, decoration joins, selection reconciliation, and local removal fallback"
      contains: "LocalRow"
    - path: "baude/src/app.rs"
      provides: "Real RepositoryState/runtime projection and RepositoryKey/CheckoutKey selection"
      contains: "SelId"
    - path: "baude/src/ui.rs"
      provides: "Production repository and checkout sidebar tracer using established Ratatui visuals"
      contains: "draw_sidebar"
    - path: "baude/src/main.rs"
      provides: "Hierarchy module registration"
      contains: "mod hierarchy"
  key_links:
    - from: "baude/src/app.rs"
      to: "baude/src/hierarchy.rs"
      via: "App projects RepositoryState plus runtime_checkouts into ordered local rows"
      pattern: "hierarchy::"
    - from: "baude/src/ui.rs"
      to: "baude/src/app.rs"
      via: "sidebar consumes App hierarchy rows and durable selected_id"
      pattern: "hierarchy_rows|selected_id"
---

<objective>
Deliver the leading production tracer for REPO-05 and HIER-01 through HIER-04: a real App-backed, durable-key-selected repository hierarchy visible in the local TUI.

Purpose: Prove the hardest phase constraint first—runtime-independent topology and stable selection reach the actual Ratatui route before action and polish expansion.
Output: A pure hierarchy projection, App integration, durable selection, and established-design parent/child rendering.
</objective>

<execution_context>
@/Users/joese/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/joese/.config/opencode/get-shit-done/templates/summary.md
@/Users/joese/.config/opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-local-tui-dogfood-release/07-UI-SPEC.md
@.planning/phases/07-local-tui-dogfood-release/07-RESEARCH.md
@.planning/phases/07-local-tui-dogfood-release/07-PATTERNS.md
@.planning/phases/07-local-tui-dogfood-release/07-VALIDATION.md
@.planning/phases/05-durable-repository-admission/05-02-SUMMARY.md
@.planning/phases/05-durable-repository-admission/05-03-SUMMARY.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-07-SUMMARY.md
@baude-core/src/repository.rs
@baude/src/app.rs
@baude/src/ui.rs
@baude/src/main.rs

<interfaces>
Use existing `RepositoryKey`, `CheckoutKey`, `RepositoryState::{repositories,checkouts}`, `SavedRepository::{key,observed_main_worktree,health}`, and `SavedCheckout::{key,repository_key,role,managed_by_baude,observed_path,observed_branch,first_seen_order,lifecycle(),health(),session}`. App already owns `repository_state`, `runtime_checkouts: HashMap<CheckoutKey,u64>`, local `Session` values, and flat `RemoteSnapshot`. Do not add a second persisted hierarchy or schema.
</interfaces>
</context>

<tasks>

<task id="07-01-01" type="auto" tdd="true">
  <name>Task 1: Trace durable parents and checkouts through real App data into Ratatui</name>
  <reversibility rating="costly">Changing the local selection/projection interface later would require coordinated changes across navigation, rendering, every local action, and tests, but no persisted migration.</reversibility>
  <files>baude/src/hierarchy.rs, baude/src/main.rs, baude/src/app.rs, baude/src/ui.rs</files>
  <behavior>
    - Exact test `hierarchy::tests::local_hierarchy_orders_parents_and_children_by_durable_identity` starts RED and covers opposite admission/name order, main plus distinct managed default plus other worktrees, runtime-less parents/children, duplicate basenames, and deterministic path/key ties.
    - Exact test `ui::tests::hierarchy_tracer_renders_real_app_parent_and_child` starts RED and renders a real App-owned RepositoryState, proving one parent followed by main/default children, connectors, status glyph/text, and durable selected gutter—not a detached model fixture with no UI route.
    - The tracer is production-quality: `SelId::{Repository(RepositoryKey),Checkout(CheckoutKey),Remote(u64)}`, local rows originate in durable state, remote rows remain separate, and the established rounded border/cyan focus/xterm-237/two-cell-gutter vocabulary is preserved.
  </behavior>
  <action>Commit both exact RED tests before production edits. Add `hierarchy.rs` and register it in `main.rs`. Define key-valued local row records and a pure projection that first sorts repositories by case-insensitive basename, canonical main path bytes, then RepositoryKey; under each parent sort checkouts only by `(first_seen_order, CheckoutKey)`. Derive duplicate-basename shortest unique suffixes for display only. Join optional runtime/session status after structural order is fixed. Replace local runtime-only `SelId` and `ordered_ids()` usage with RepositoryKey/CheckoutKey identity while preserving `Remote(u64)` and its existing compatibility order. Route `draw_sidebar` through real App hierarchy rows and render the exact 07-UI-SPEC parent/normal-child anatomy sufficiently for the happy path, including main/default role chips and closed runtime-less rows. Do not add collapse, dormant branches, a local archived section, direct Git calls, persistence fields, dependencies, or daemon/PWA changes.</action>
  <verify>
    <automated>name='hierarchy::tests::local_hierarchy_orders_parents_and_children_by_durable_identity'; cargo test -p baude -- --list | rg -Fx -- "$name: test" &amp;&amp; cargo test -p baude "$name" -- --exact --nocapture</automated>
    <fails_when>The exact hierarchy test is absent, the list guard matches zero tests, a runtime-less durable row is missing, either sort contract is violated, or the exact test exits nonzero.</fails_when>
    <automated>name='ui::tests::hierarchy_tracer_renders_real_app_parent_and_child'; cargo test -p baude -- --list | rg -Fx -- "$name: test" &amp;&amp; cargo test -p baude "$name" -- --exact --nocapture</automated>
    <fails_when>The exact tracer test is absent, the list guard matches zero tests, the fixture bypasses real App state, parent/main/default rows are not rendered in one hierarchy, established selection/focus semantics regress, or the exact test exits nonzero.</fails_when>
  </verify>
  <done>A real user-visible local hierarchy is reachable through App and Ratatui with durable parent/checkout identity; REPO-05 and the HIER happy path are no longer isolated domain logic.</done>
</task>

<task id="07-01-02" type="auto" tdd="true">
  <name>Task 2: Keep ordering restart-stable and selection deterministic without persistence</name>
  <reversibility rating="costly">The structural order and fallback rules become the shared navigation contract used by subsequent actions, though they remain in-memory and can be coordinated without data migration.</reversibility>
  <files>baude/src/hierarchy.rs, baude/src/app.rs, baude/src/ui.rs</files>
  <behavior>
    - Exact test `hierarchy::tests::local_hierarchy_order_ignores_runtime_and_session_status` mutates waiting, working, completion, exit, archive, attention, health, and runtime presence while asserting an identical durable row-key vector and correct child decoration.
    - Exact test `hierarchy::tests::local_hierarchy_selection_survives_refresh_and_removal_falls_back_locally` retains the same durable key across in-process refresh/status changes, after removal selects next sibling, else previous sibling, else parent without crossing repositories, and after process reload chooses the first local repository parent in rendered order, else the first flat remote row, else no selection.
    - `alt+Left/Right` cycles checkout/session children only and wraps; `j/k` traverses parents and children; `ctrl+q` retains the selected durable target.
  </behavior>
  <action>Commit the exact RED tests first. Add pure selection reconciliation and post-removal fallback helpers in `hierarchy.rs`; use durable-key retention for tick/remote refresh, archive/status updates, admission, activation, close/reopen, and eventual removal success instead of row indexes or runtime IDs. Do not add a persisted selection field or schema expansion. On a newly constructed/restored process, initialize selection deterministically to the first local repository parent in the UI-SPEC rendered parent order; if no local repository exists, choose the first flat remote row in its existing compatibility order; if no row exists, use no selection. Keep repository parents and archived/closed/unavailable children visible in place, decorate attention on the correct CheckoutKey, and preserve existing flat remote behavior as a separate tail section. Do not let status, archive, runtime, health, branch/name, or attention participate in local sorting.</action>
  <verify>
    <automated>name='hierarchy::tests::local_hierarchy_order_ignores_runtime_and_session_status'; cargo test -p baude -- --list | rg -Fx -- "$name: test" &amp;&amp; cargo test -p baude "$name" -- --exact --nocapture</automated>
    <fails_when>The exact stability test is absent, the list guard matches zero tests, any volatile field changes row-key order/hides a child, status decorates the wrong CheckoutKey, or the exact test exits nonzero.</fails_when>
    <automated>name='hierarchy::tests::local_hierarchy_selection_survives_refresh_and_removal_falls_back_locally'; cargo test -p baude -- --list | rg -Fx -- "$name: test" &amp;&amp; cargo test -p baude "$name" -- --exact --nocapture</automated>
    <fails_when>The exact selection test is absent, the list guard matches zero tests, an in-process refresh loses an existing durable key, restart claims to restore unpersisted selection or does not choose first-local-parent/first-remote/none deterministically, fallback crosses repository boundaries or violates next/previous/parent order, or the exact test exits nonzero.</fails_when>
  </verify>
  <done>Parent and child ordering is structural and restart-stable, volatile changes remain attached to the correct child, in-process selection reconciles by durable key, and process restart applies the exact deterministic initial-selection rule without schema expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted repository bytes → hierarchy projection | Malformed or contradictory durable state must remain blocked by existing validation rather than becoming selectable authority. |
| App runtime/status data → durable rows | Volatile runtime IDs and presentation fields may decorate but must not replace CheckoutKey identity. |
| key selection → rendered target | Display-name/path collisions must not spoof which durable repository or checkout is selected. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking mitigation |
|-----------|----------|----------|-----------|-------------|---------------------|
| T-07-01 | Spoofing/Tampering | High | `SelId`, hierarchy projection | mitigate | Resolve and retain RepositoryKey/CheckoutKey end to end; exact tests block completion on stale/wrong-target selection. |
| T-07-02 | Tampering/DoS | High | local ordering | mitigate | Structural sort allowlist excludes all volatile fields; mutation matrix must preserve key order. |
| T-07-03 | Information/Spoofing | Medium | duplicate labels | mitigate | Short unique path suffix is presentation only; key remains authority. |
| T-07-SC | Tampering | High | Cargo supply chain | mitigate | No package install or dependency/lock change is authorized in this plan. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Durable repository parents/default sessions are visible and selectable in local TUI | 07-01 | COVERED | Production tracer reaches App and Ratatui. |
| REQ | REPO-05, HIER-01, HIER-02, HIER-03, HIER-04 | Main/default hierarchy, runtime-less visibility, structural order, stable attention | 07-01 | COVERED | Tasks 1-2. |
| RESEARCH | hierarchy projection | Pure durable projection before decoration and RepositoryKey/CheckoutKey selection | 07-01 | COVERED | No second store/schema. |
| CONTEXT | locked UI hierarchy | Exact parent/child anatomy and no status/archive reorder | 07-01 | COVERED | Tracer plus invariant expansion. |
| CONTEXT | deferred | Dormant rows/deletion, remote hierarchy, PWA | — | EXCLUDED | No task introduces them. |
</source_coverage_audit>

<estimate>
raw_tokens: 36000
calibration_factor: 1
estimated_tokens: 36000
confidence: low
reason: Four tightly coupled Rust files, two RED/GREEN cycles, and direct-buffer verification; below the 50% execution-context target.
</estimate>

<verification>
- Every exact Cargo filter first lists tests and uses `rg -Fx` against the full `name: test` line; each runnable command has an immediate `fails_when`.
- Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo test --workspace` after the wave; failures block the next wave.
</verification>

<success_criteria>
- Local topology is projected from durable App state, not sessions.
- Main and managed default children are both visible beneath one parent.
- Durable-key selection survives all in-process refresh/status transitions; structural order survives restart and volatile changes; restart uses first-local-parent, else first-remote, else none rather than claiming unpersisted selection restoration.
- Existing design language and separate flat remote section remain intact.
</success_criteria>

<output>
Create `.planning/phases/07-local-tui-dogfood-release/07-01-SUMMARY.md` with RED/GREEN evidence and exact key/order/render outcomes. Do not check off requirements or mark Phase 7 complete.
</output>
