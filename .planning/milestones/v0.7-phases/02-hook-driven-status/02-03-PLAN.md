---
phase: 02-hook-driven-status
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - bauded/src/api.rs
  - bauded/src/manager.rs
  - baude/src/ui.rs
autonomous: false
requirements: [HOOK-01, HOOK-03]
user_setup: []
must_haves:
  truths:
    - "POST /sessions/{id}/event resolves the baude id to the Claude session_id, appends to the same /tmp event file the poll loop tails, and returns 204"
    - "The daemon seeds .claude/settings.local.json AND injects $BAUDE_EVENT_URL into the spawned claude command so hooks POST back to the daemon"
    - "Daemon-spawned hooks converge on the same consume path as TUI-local sessions (one event model, both transports)"
    - "The info overlay shows the StateSource and last tool for the selected session (minimal capture-but-render-lightly surfacing)"
  artifacts:
    - path: "bauded/src/api.rs"
      provides: "POST /sessions/{id}/event route + post_event handler"
      contains: "post_event"
    - path: "bauded/src/manager.rs"
      provides: "ingest_event + $BAUDE_EVENT_URL injection + settings seed + SessionInfo state_source/last_tool"
      contains: "ingest_event"
    - path: "baude/src/ui.rs"
      provides: "StateSource + last_tool rows in the i info overlay"
  key_links:
    - from: "bauded/src/api.rs::post_event"
      to: "Manager::ingest_event"
      via: "Path<u64> handler -> 204/404"
      pattern: "ingest_event"
    - from: "bauded/src/manager.rs::spawn"
      to: "BAUDE_EVENT_URL"
      via: "command-string prefix before Pty::spawn"
      pattern: "BAUDE_EVENT_URL"
    - from: "baude/src/ui.rs"
      to: "status_with_source"
      via: "overlay row"
      pattern: "state_source|StateSource|last_tool"
---

<objective>
Wire the daemon half of the hook system and surface the new state in the TUI. Add `POST /sessions/{id}/event` feeding the shared consume path, seed settings + inject `$BAUDE_EVENT_URL` at daemon spawn, expose `state_source`/`last_tool` on `SessionInfo`, and render them minimally in the `i` info overlay. A final human-verify checkpoint confirms a real Claude session fires hooks and flips state without the silence timer.

Purpose: Completes HOOK-03's daemon transport (one event model serving both POST and file-tail) and the daemon side of HOOK-01 (idempotent seeding at `manager.rs::spawn`), and delivers the capture-but-render-lightly surfacing of StateSource + last tool. Depends on Plan 01 (`merge_hook_settings`, `append_event`, `event_path`) and Plan 02 (`status_with_source`, `StateSource`, `last_tool`).

Output: a daemon event route + `ingest_event`, `$BAUDE_EVENT_URL` injection + settings seeding in `spawn`, `state_source`/`last_tool` on `SessionInfo`, overlay rows in `ui.rs`, and a passing end-to-end UAT.
</objective>

<execution_context>
@$HOME/.claude-poindexter-poindexter/gsd-core/workflows/execute-plan.md
@$HOME/.claude-poindexter-poindexter/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hook-driven-status/02-CONTEXT.md
@.planning/phases/02-hook-driven-status/02-RESEARCH.md
@.planning/phases/02-hook-driven-status/02-PATTERNS.md
@.planning/phases/02-hook-driven-status/02-VALIDATION.md

# Self-analogs (Path<u64> handlers, spawn block, SessionInfo builder, overlay rows)
@bauded/src/api.rs
@bauded/src/manager.rs

# Upstream symbols consumed by this plan
@.planning/phases/02-hook-driven-status/02-01-SUMMARY.md
@.planning/phases/02-hook-driven-status/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ingest_event + $BAUDE_EVENT_URL injection + settings seeding to manager.rs</name>
  <files>bauded/src/manager.rs</files>
  <read_first>
    - bauded/src/manager.rs:45-84 (SessionInfo struct + status_str), :203-256 (spawn, cmd construction at 228-234), :289-312 (post_message — resolve-by-id precedent), :117-131 (restore() calls self.spawn() at :124 on daemon startup -> this is the path that re-exercises the seed -> seed MUST be idempotent), :323-334 (restart() inlines its own Pty::spawn at :330 and does NOT call spawn() / does NOT re-seed — harmless), :465-477 (session/session_mut resolution), :524-545 (session_info builder)
    - 02-PATTERNS.md "bauded/src/manager.rs (MOD)" section (spawn env-inject, ingest_event, SessionInfo + source_str)
    - 02-RESEARCH.md "Pitfall 4" ($BAUDE_EVENT_URL via command-string prefix; Pty::spawn has no env param), "Pattern 1" (idempotent seed), OQ#2 (worktree cwd is the seed target)
  </read_first>
  <behavior>
    - ingest_event(id, body): resolves baude id -> Session.meta.session_id (Claude sid), appends `body` as a line to `/tmp/baude-events-<sid>.jsonl` (via `hook::append_event`), returns Ok(()). Unknown id -> Err with the "no session {id}" message.
    - ingest_event when session_id not yet known -> Err (or no-op Err) rather than panic.
    - After spawn, the constructed command string contains `BAUDE_EVENT_URL=` pointing at the daemon's loopback default bind for that session's event route.
    - Seeding `.claude/settings.local.json` in the session cwd is idempotent across the daemon restore path (re-spawn via `restore()` -> still one baude entry per event).
  </behavior>
  <action>
    In `Manager::spawn`, before `Pty::spawn` (manager.rs:234), seed `cwd/.claude/settings.local.json` using `baude_core::hook::merge_hook_settings(&existing, &baude_core::hook::baude_hook_command())` — the same best-effort read/merge/write as the TUI path (Plan 01 Task 3), into the session's actual cwd (worktree dir for worktree sessions, per OQ#2). The merge MUST be idempotent because the daemon **restore** path (`Manager::restore`, manager.rs:117-131) calls `self.spawn(...)` at manager.rs:124 on every daemon startup, re-running the seed for each persisted session. (Note: `restart()` at manager.rs:323 does NOT go through `spawn` — it inlines its own `Pty::spawn` at manager.rs:330 and never re-seeds; that is harmless since the first spawn already seeded. Idempotency is required by `restore`, not `restart`.)

    Inject `$BAUDE_EVENT_URL` by prefixing the command string (Pty::spawn has no env-map param): `let cmd = format!("BAUDE_EVENT_URL={url} {cmd}");` where `url` is the daemon's own loopback event endpoint for this session: `http://127.0.0.1:8642/sessions/{id}/event` (the loopback default bind — `DEFAULT_BIND = "127.0.0.1:8642"` in bauded/src/main.rs:20). `Manager` does not store its bind addr (manager.rs:104-113 has no bind field), and the hook only needs same-host reachability, so use the loopback default directly. **Known limitation (out of scope for Phase 2):** a custom `--bind` port passed to the daemon is NOT honored by this injected URL — honoring it would require threading the bind addr into `Manager`, deferred. The hook running inside the session POSTs its event line here, and the daemon's `post_event` -> `ingest_event` converges it onto the same `/tmp` file the poll loop tails.

    Add `pub fn ingest_event(&mut self, id: u64, body: &str) -> Result<()>`: resolve via `self.session(id)?`, read `s.meta.session_id` (Err if absent), call `baude_core::hook::append_event(sid, body.trim_end())`, map IO error to anyhow. Model the resolve-by-id shape on `post_message` (manager.rs:289).

    Add `state_source: &'static str` and `last_tool: Option<String>` to `SessionInfo` (manager.rs:45-64). In `session_info` (manager.rs:524), switch `s.status()` to `s.status_with_source()` to get `(status, source)`; set `status: status_str(status)`, `state_source: source_str(source)` (write a `source_str(StateSource)->&'static str` mapper mirroring `status_str` at :78-84), and `last_tool: s.meta.last_tool.as_ref().map(|(t,_)| t.clone())`. Do NOT add a structured `waiting_reason` field (deferred to Phase 4).

    Add unit tests in manager.rs's test module: `ingest_event` appends to the resolved /tmp file and errors on unknown id; the spawn command string carries `BAUDE_EVENT_URL=`.
  </action>
  <verify>
    <automated>cargo test -p bauded manager:: && cargo fmt --check && cargo clippy -p bauded --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `ingest_event` appends to the session's /tmp event file and errors (not panics) on unknown id / missing session_id.
    - spawn seeds settings.local.json idempotently (idempotency exercised by the `restore` re-spawn path) and injects `BAUDE_EVENT_URL=http://127.0.0.1:8642/...` into the command string.
    - `SessionInfo` carries `state_source` and `last_tool`, populated from `status_with_source` + `meta.last_tool`.
    - `cargo test -p bauded manager::` green; clippy clean.
  </acceptance_criteria>
  <done>The daemon seeds hooks, injects the event URL, and ingests posted events onto the shared consume path; SessionInfo exposes state_source + last_tool.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add POST /sessions/{id}/event route + post_event handler to api.rs</name>
  <files>bauded/src/api.rs</files>
  <read_first>
    - bauded/src/api.rs:19-37 (router — add after /stream at :36), :94-98 (ApiError + not_found), :188-219 (archive/interrupt Path<u64>->StatusCode 204 handlers), :549-556 (in-process axum::serve test harness)
    - 02-PATTERNS.md "bauded/src/api.rs (MOD)" section (route registration, handler shape, test harness)
    - 02-RESEARCH.md "Code Examples → Daemon route", "Security Domain V5" (Path<u64> rejects non-numeric; parse body best-effort, never 500)
  </read_first>
  <behavior>
    - POST /sessions/{id}/event with a JSON event line body for a known id -> 204, and the line lands in that session's /tmp event file.
    - POST for an unknown id -> 404 (not_found), not 500.
    - A non-numeric `{id}` is rejected by the framework (Path<u64> extractor) before the handler runs.
    - A malformed body for a known session -> still best-effort appended (or 404 if session unresolvable); never a 500/panic.
  </behavior>
  <action>
    Register `.route("/sessions/{id}/event", post(post_event))` after the `/stream` route (api.rs:36). Implement `async fn post_event(State(state): State<Shared>, Path(id): Path<u64>, body: String) -> Result<StatusCode, ApiError>`: take the raw `body` (one event line already built by `baude hook`), call `lock(&state).ingest_event(id, &body).map_err(not_found)?`, return `Ok(StatusCode::NO_CONTENT)`. Model error handling on `archive` (api.rs:208) and use the existing `not_found` helper (api.rs:96). Do not add auth — the route inherits the tailnet-bound, single-user security model (V2/V3/V4 N/A per RESEARCH).

    Add an integration test reusing the in-process `axum::serve` harness (api.rs:549): create a session, set/ensure a `session_id`, POST a line to `/sessions/{id}/event`, assert 204 and that the /tmp event file received the line; POST to a bogus id asserts 404.
  </action>
  <verify>
    <automated>cargo test -p bauded api:: && cargo fmt --check && cargo clippy -p bauded --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `POST /sessions/{id}/event` returns 204 + appends to the /tmp file for a known session; 404 for unknown; non-numeric id rejected by extractor; no 500 path.
    - Integration test via in-process axum harness passes.
    - `cargo test -p bauded api::` green; clippy clean.
  </acceptance_criteria>
  <done>The daemon exposes the event ingest endpoint feeding the same consume path as the file-tail transport (HOOK-03).</done>
</task>

<task type="auto">
  <name>Task 3: Surface StateSource + last tool in the i info overlay (ui.rs)</name>
  <files>baude/src/ui.rs</files>
  <read_first>
    - baude/src/ui.rs:865-960 (the info-overlay row block — `lines.push(row("effort", ...))` etc. from Phase 1; the `row(label, value)` helper and how `m`/meta + status are accessed)
    - 02-CONTEXT.md decision 5 ("Surface StateSource + last_tool minimally in the info overlay (capture-but-render-lightly, like Phase 1)")
    - 02-PATTERNS.md "Shared Patterns" (capture-but-render-lightly precedent)
  </read_first>
  <action>
    In the `i` info overlay (ui.rs ~865-960, where Phase 1 added effort/thinking/pr rows), add two minimal rows for the selected session: a `state` source row showing the `StateSource` label (e.g. "hook" / "session-file" / "silence" — so a regression to silence is observable, per CONTEXT specifics) and a `tool` row showing the last tool name when `meta.last_tool` is present (omit the row when absent, matching the existing optional-row pattern). Use the existing `row(label, value)` helper and the same conditional-push style as the effort/thinking/pr rows. Obtain the source via `status_with_source()` on the selected session (or the `state_source` already on `SessionInfo` if the overlay renders from SessionInfo in the daemon-attached path — match whichever data source the overlay already uses).

    Keep it lightweight: no timeline, no live feed (that is Phase 3). This is capture-but-render-lightly, mirroring how vim_mode/effort were surfaced in Phase 1.
  </action>
  <verify>
    <automated>cargo build -p baude && cargo fmt --check && cargo clippy -p baude --all-targets -- -D warnings && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - The `i` overlay shows a state-source label and (when present) the last tool for the selected session.
    - Silence fallback is visibly labeled "silence" so a regression is observable.
    - `cargo test --workspace` green; clippy clean with `-D warnings`.
  </acceptance_criteria>
  <done>StateSource + last tool render minimally in the info overlay.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-to-end UAT — real Claude session fires hooks and flips state without the silence timer</name>
  <files>(verification only — no files modified)</files>
  <action>
    Human-verify checkpoint (no code changes). The executor presents the verification steps below to the user and pauses for approval. This exercises the live `claude` CLI 2.1.177 invoking the seeded hook — behavior that cannot be covered by `cargo test`.
  </action>
  <what-built>
    Full hook pipeline: settings seeding (TUI + daemon), `baude hook` subcommand, event file-tail + POST transport, hook-driven state with StateSource label, and overlay surfacing. This checkpoint verifies the live behavior that cannot be exercised in `cargo test` (real `claude` CLI 2.1.177 invoking the seeded hook).
  </what-built>
  <how-to-verify>
    1. `cargo build --workspace` then run the TUI (`baude` / `cargo run -p baude`).
    2. Pre-create a `.claude/settings.local.json` in a scratch dir with a user `statusLine` and one user-defined hook. Spawn a managed session there. Inspect the merged file: confirm the user statusLine + user hook survive intact AND baude's four hooks (UserPromptSubmit/Stop/Notification/PostToolUse) were appended, each command being the absolute `current_exe()` path + ` hook` (HOOK-01).
    3. Submit a prompt in that session. Confirm it shows "working" essentially instantly (faster than the ~2s silence window). Press `i` — confirm the overlay shows state source = "hook" (NOT "silence").
    4. When Claude runs a tool, confirm the overlay's last-tool row updates; when the turn ends (Stop), confirm the session flips to "waiting" with source still "hook".
    5. Tail `/tmp/baude-events-<sid>.jsonl` and confirm schema-1 lines were appended for the events.
    6. (Daemon path, if convenient) start `bauded`, spawn a session via the daemon, and confirm events arrive via `POST /sessions/{id}/event` (the daemon-seeded `$BAUDE_EVENT_URL`) and drive the same state.
    7. Re-spawn / restart the session and re-inspect settings.local.json: confirm baude's entries were NOT duplicated (idempotent).
    Note: if `claude --version` has advanced past 2.1.177, re-verify the hook stdin field names and update the pinned doc-comment in hook.rs.
  </how-to-verify>
  <verify>
    <human-check>State flips via hooks (overlay source=hook, faster than the silence window); user statusLine + user hook survive; baude entries not duplicated on re-spawn.</human-check>
  </verify>
  <resume-signal>Type "approved" if state flips via hooks (source=hook) and user settings survive intact + idempotent; otherwise describe what failed.</resume-signal>
  <acceptance_criteria>
    - A real Claude session shows "working" on prompt submit faster than the silence window, with overlay StateSource = hook.
    - Pre-existing user statusLine + user hook survive the merge; baude's four hooks present; no duplication on re-spawn.
    - schema-1 event lines appear in `/tmp/baude-events-<sid>.jsonl`.
  </acceptance_criteria>
  <done>Live hook-driven state transitions verified end-to-end against CLI 2.1.177; user settings preserved + idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session -> `POST /sessions/{id}/event` | Hook process (inside a managed session) posts event lines to the daemon over loopback/tailnet |
| `{id}` path param + body | Untrusted numeric id + JSON line crossing into the daemon |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Spoofing / Tampering | New unauthenticated `POST /sessions/{id}/event` | accept | Inherits the existing tailnet/loopback binding (default `127.0.0.1:8642`); no new exposure beyond the existing REST surface. ASVS L1 + VPN model is the accepted project baseline (no auth layer by design). |
| T-02-09 | Denial of Service / Tampering | `post_event` body + `{id}` parsing | mitigate | `Path<u64>` typed extractor rejects non-numeric ids at the framework layer; body handled best-effort; unknown/unresolvable -> 404 via `not_found`, never 500/panic (V5 input validation). |
| T-02-10 | Tampering | `$BAUDE_EVENT_URL` injection into the spawn command string | accept | URL is a hardcoded loopback daemon endpoint (`127.0.0.1:8642` default bind), not a secret and not user input. |
| T-02-11 | Tampering | Daemon settings seeding clobbering user config | mitigate | Same sentinel-guarded `merge_hook_settings` as TUI; sibling keys untouched; idempotent across the daemon restore re-spawn (HOOK-01). |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies this phase (RESEARCH Package Legitimacy Audit: none). No install tasks. |
</threat_model>

<verification>
- `cargo test -p bauded manager::` and `cargo test -p bauded api::` — ingest + endpoint tests green.
- `cargo build -p baude` — overlay rows compile.
- `cargo fmt --check && cargo clippy --workspace -- -D warnings && cargo test --workspace` — full CI triad green.
- Task 4 human-verify: live hook firing flips state via StateSource::Hook, user settings preserved + idempotent.
</verification>

<success_criteria>
- `POST /sessions/{id}/event` feeds the same `/tmp` consume path as the file-tail (one event model, both transports — HOOK-03).
- The daemon seeds settings idempotently and injects `$BAUDE_EVENT_URL` so daemon-managed hooks POST back (HOOK-01 + HOOK-03).
- The info overlay surfaces StateSource + last tool minimally; silence fallback is visibly labeled.
- A real Claude session drives working/waiting/done via hooks without the silence timer (UAT approved).
- CI triad green across the workspace.
</success_criteria>

<output>
Create `.planning/phases/02-hook-driven-status/02-03-SUMMARY.md` when done.
</output>
