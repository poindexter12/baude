---
phase: 10-clickable-terminal-links
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-03"]
files_modified:
  - baude/src/app.rs
  - baude/src/ui.rs
autonomous: true
requirements: [LINK-04, LINK-05, LINK-06, LINK-08]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low   # derived: 0 calibration samples (factor 1.0, not applied)

must_haves:
  truths:
    - "In hint mode, `c` or `y` copies the selected link's actual destination without opening it; Enter opens; Esc dismisses (LINK-05, LINK-06)"
    - "Every key pressed while the LinkHints overlay is open is handled or swallowed by handle_modal_key — no byte reaches the child PTY (LINK-04)"
    - "Hint mode over a screen with zero links shows a 'no links' message and opens no modal (explicit edge lift)"
    - "The gesture call site collects OSC8 AND bare-URL links at the pane's current scroll offset, bracketed by set_scrollback(scroll)..set_scrollback(0), lock dropped before modal mutation"
    - "The hint chord resolves the focused pane's parser for local Claude, local Shell, AND active remote attach — the same parser the render path draws from (LINK-04 remote leg)"
    - "An opener spawn error surfaces one set_message warning naming the failure; the session keeps running (LINK-08)"
    - "The overlay's displayed destination is middle-truncated for width only — scheme and host always visible; copy and open always use the full normalized URL (LINK-05)"
  artifacts:
    - "baude/src/app.rs: link_hints navigation (j/k/arrows + hint letters), copy dispatch through an injected copy sink (production: App::copy_to_clipboard), no-links message path, focus-aware parser resolution at the chord site"
    - "baude/src/ui.rs: completed LinkHints overlay — bottom-anchored list, `[a] destination` rows, selected-row highlight, middle truncation, scrolling past 26 links"
    - "app.rs test modules: link_hints:: (swallow/navigation/copy/preview), link_open:: (failure path via injected opener)"
  key_links:
    - "chord -> focused pane's parser (local pty OR remote attach parser) -> scrollback-bracketed collect_links -> Modal::LinkHints"
    - "c/y -> injected copy sink -> App::copy_to_clipboard (existing pbcopy path — reuse, never a second clipboard spawn)"
    - "Enter -> validate-gated destination -> injected opener; Err -> set_message -> modal closed, session alive"
  prohibitions:
    - "output alone never opens a link"
    - "opener never receives shell code (argv data only)"
    - "link handling never breaks selection/drag-copy/child mouse"
    - "opener failure never kills the session"
---

<objective>
Complete the hint-mode interaction surface on top of the 10-01 tracer: full overlay
UX (labels, navigation, truncation), destination copy (LINK-06), the no-links state,
the integrated gesture call site (OSC8 + bare passes, all focused-pane kinds), and
the LINK-08 failure-path guarantees — plus the non-interference proof that LINK-04
demands (selection, drag-copy, scrolling, child mouse untouched; nothing forwarded
while the overlay is open).

Implements CONTEXT locked decisions: per-link actions Enter/c/y/Esc; overlay always
shows the actual destination; no new mouse capture (modifier-click NOT shipped —
discretionary, recommended against by RESEARCH for v2.2); opener failure through
set_message with the session running. Discretion decisions recorded here: hint labels
are letters a-z with list scrolling beyond 26; overlay is a bottom-anchored list
panel (RESEARCH Pattern 3 recommendation); keybind ctrl+o (chosen in 10-01).

Flagged planner assumption (edge-probe UNRESOLVED, proven here): LINK-05/06 — the
overlay model carries the full destination and copy emits exactly it (Task 1 tests).

Output: phase-complete interaction layer; full workspace suite green.
</objective>

<execution_context>
@~/.poindexter/claude/gsd-core/workflows/execute-plan.md
@~/.poindexter/claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-clickable-terminal-links/10-RESEARCH.md
@.planning/phases/10-clickable-terminal-links/10-PATTERNS.md
@.planning/phases/10-clickable-terminal-links/10-01-SUMMARY.md
@.planning/phases/10-clickable-terminal-links/10-03-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Symbols | Produced by |
|----------|---------|-------------|
| `Modal::LinkHints` UX (complete) | navigation, hint letters, copy dispatch, no-links state, completed draw arm | 10-04 (this plan) |
| Opener seam (failure-proven) | `spawn_opener`/`activate_link` Err path tests | 10-01 seam; proven here |
| Vendored vt100 link API / re-emission | — | 10-01 / 10-02 |
| links module | — | 10-01 / 10-03 |
| Keybind | ctrl+o, documented | 10-01 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Overlay completion — navigation, copy, preview, no-links state</name>
  <files>baude/src/app.rs, baude/src/ui.rs</files>
  <read_first>
    - baude/src/app.rs: handle_link_hints_key + Modal::LinkHints as left by 10-01; 4374-4413 (handle_modal_key idioms); 5476-5488 (copy_to_clipboard — the ONLY clipboard path, reuse); 3586-3588 (set_message)
    - baude/src/ui.rs: LinkHints draw arm from 10-01; 2114-2171 (Modal::Help skeleton); 1607 (ConfirmRemoveWorktree — data-driven modal rows)
    - baude/src/links.rs: DetectedLink fields (destination Url, spans)
  </read_first>
  <behavior>
    - `c` and `y` on the selected entry invoke the injected copy sink with the FULL normalized destination string, close the modal, set a "copied ..." message; the opener sink is never invoked (LINK-06)
    - j/k and Up/Down move `selected` with bounds clamping; typing a hint letter (a-z) selects that entry directly; Enter opens the selected entry only
    - overlay model for N links exposes, per row, the hint letter + full destination; display truncation is a pure helper (middle-ellipsis preserving scheme+host) applied at render only — the model keeps the full URL (LINK-05)
    - chord over a parser whose screen has zero links: message set, modal stays None
    - any other key while the overlay is open: swallowed (modal unchanged or dismissed), never forwarded
  </behavior>
  <action>
    Extend handle_link_hints_key: navigation keys, hint-letter jump, copy dispatch
    through a second injected sink parameter (same FnOnce idiom as the opener —
    production passes a closure calling App::copy_to_clipboard; tests pass a
    recording spy; per PATTERNS "Injected side-effects for testability"). Do NOT
    add a new clipboard spawn — WINDOWS entry 6: reuse, don't duplicate. Complete
    the ui.rs draw arm: bottom-anchored list, `[a] https://...` rows via the Help
    skeleton, selected row bold/reversed, viewport scrolling so the selected row is
    always visible past 26 entries, `display_truncated` middle-ellipsis helper
    (pure fn, unit-tested: scheme+host retained at narrow widths). Write the
    behavior tests in app.rs `mod link_hints_tests` first (they compile against the
    existing seam and fail on assertions), then implement to green.
  </action>
  <verify>
    <automated>cargo test -p baude link_hints --locked && cargo check --workspace --all-targets --locked</automated>
    <fails_when>any link_hints test reports FAILED (copy-spy content, navigation bounds, letter jump, no-links state, truncation invariants), or compilation fails</fails_when>
  </verify>
  <acceptance_criteria>
    - Copy tests prove the sink receives the full destination, byte-identical to Url::as_str(), and the opener spy stays uncalled (per CONTEXT locked decision: c/y copies, Enter opens, Esc dismisses)
    - Truncation helper never drops scheme or host; model retains full URL
    - No new Command spawn introduced anywhere in this task (copy goes through the existing path, injected in tests)
  </acceptance_criteria>
  <done>Overlay is fully navigable; inspect/copy/open triad works per locked decisions; no-links state handled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gesture integration + non-interference proof</name>
  <files>baude/src/app.rs</files>
  <read_first>
    - baude/src/app.rs: chord handler from 10-01; 5456-5468 (scrollback-bracketed read — the exact bracket to mirror); 3862-3906 (interception + forward_key); 5403-5422 (mouse selection path — must remain untouched); 6397 (App::new test precedent)
    - baude/src/remote.rs:244, 278-281 (remote attach parser — resolve the same parser the render path draws at ui.rs:1112)
  </read_first>
  <behavior>
    - chord at a scrolled-back offset collects links from the VIEWED rows (set_scrollback(scroll) bracket), then restores set_scrollback(0); parser lock released before Modal::LinkHints is set
    - collect_links receives both passes' results (OSC8 + bare) — a screen with one OSC8 link and one bare URL yields two entries
    - focus on remote attach: the chord resolves the remote parser and hint mode works identically (per CONTEXT locked decision: identical parser path serves attach)
    - while Modal::LinkHints is open, handle_key routes EVERY KeyEvent to handle_modal_key — a test drives printable keys, ctrl chords, and Enter through handle_key and asserts the child-forwarding path was never taken
    - with modal closed, mouse selection/drag-copy behavior is byte-identical to before (no new mouse arms added anywhere in this phase)
  </behavior>
  <action>
    Finalize the chord call site: focus-aware parser resolution (Claude pane parser,
    Shell pane parser, or remote-attach parser — whichever the current mode renders),
    scrollback bracket copied from the selection-copy shape, both detection passes,
    empty -> message, non-empty -> modal. Add the non-interference tests: modal-open
    key swallowing asserted at the handle_key level (spy/flag on the forwarding
    path — follow whatever seam the existing key tests use; if none exists, assert
    via observable state: modal transitions consumed the key and no pty write
    occurred on a session-less App). Assert the mouse handler match arms are
    unchanged by this phase (no code change = review-level; the automated leg is
    that existing selection/clipboard tests still pass untouched).
  </action>
  <verify>
    <automated>cargo test -p baude link_hints --locked && cargo test -p baude --locked && cargo check --workspace --all-targets --locked</automated>
    <fails_when>a link_hints integration test reports FAILED (bracket restore, dual-pass collection, remote parser resolution, key swallowing), or any pre-existing baude test (selection, clipboard, chords) regresses</fails_when>
  </verify>
  <acceptance_criteria>
    - Scrollback offset is restored to 0 after collection in every path including empty-result (bracket test)
    - Modal-open swallowing proven at handle_key granularity — the structural LINK-04 guarantee has a regression test, backing the phase backstop (output alone can never activate: the only entry to hint mode is this chord)
    - Remote-attach focus reaches the same collect path (LINK-03/04 remote leg at the gesture layer)
    - All pre-existing selection/drag-copy/clipboard tests pass unmodified (LINK-04 non-interference)
  </acceptance_criteria>
  <done>Gesture is fully integrated for all pane kinds; interception and non-interference are regression-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Opener failure surface + phase-gate full suite</name>
  <files>baude/src/app.rs</files>
  <read_first>
    - baude/src/app.rs: activate_link/spawn_opener from 10-01; 3606-3634 (warn_seed_failures aggregation caveat — single-shot here, one message); 5082-5123 (open_editor Ok/Err message precedent)
  </read_first>
  <action>
    Add `mod link_open_tests`: injected opener returning Err(io::Error) -> exactly one
    set_message containing the error and an indication the session is unaffected;
    modal closed; App still processes a subsequent key normally. Injected opener
    returning Ok -> "opening ..." message with the truncated display form. Verify the
    production spawn_opener still holds the 10-01 shape (argv arg, three null stdio
    handles, reaper thread) — extend it only if a failure test exposes a gap. No test
    may construct a real Command. Then run the phase-gate full suite.
  </action>
  <verify>
    <automated>cargo test -p baude link_open --locked && cargo test --workspace --locked</automated>
    <fails_when>a link_open test reports FAILED (Err path message/liveness, Ok path message), or the full workspace suite (554 pre-existing + all phase-10 tests) has any failure</fails_when>
    <human-check>Optional dogfood (feeds SHIP-03, not this gate): run baude in a real terminal, `printf '\e]8;;https://example.com\e\\click\e]8;;\e\\ and https://exa\nmple bare\n'` in a shell pane, press ctrl+o — overlay lists destinations; c copies; Enter opens the browser; selection/drag still work</human-check>
  </verify>
  <acceptance_criteria>
    - Err path: one warning message, session demonstrably still alive (subsequent event handled), no panic, no retry loop (LINK-08 non-fatal failure, per CONTEXT locked decision: failure surfaces via set_message)
    - Full workspace suite green under --locked — the phase's automated gate before /gsd-verify-work
  </acceptance_criteria>
  <done>LINK-08's failure guarantee is regression-tested; phase suite green end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| overlay actions -> opener / clipboard subprocesses | user-gestured data crosses to OS processes |
| modal key handling -> child PTY | keys must NOT cross while overlay is open |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-14 | Elevation | spawn_opener argv position | low | mitigate | validated URLs always begin "http" — can never be parsed as an opener flag (structural, RESEARCH Pitfall 8); spawns stay injected in tests (WINDOWS entry 6) |
| T-10-15 | Spoofing | overlay display truncation | medium | mitigate | middle truncation is render-only and preserves scheme+host; copy/open always use the full normalized URL; unit test on the helper |
| T-10-16 | Denial | opener child lifecycle | low | mitigate | detached spawn + reaper thread (no zombie accumulation); spawn Err -> one set_message, session continues |
| T-10-17 | Tampering | modal key path -> child PTY | high | mitigate | LinkHints handled in handle_modal_key before any forwarding; Task 2 regression test drives keys through handle_key and asserts none reach the child |
</threat_model>

<verification>
- `cargo test --workspace --locked` green — wave-3 / phase gate
- All five ROADMAP success criteria have automated backing except the real-browser/real-terminal dogfood residue (manual, feeds SHIP-03 per RESEARCH Validation Architecture)
- No changes outside baude/src/{app,ui}.rs (parallel-safety and blast-radius check)
</verification>

<success_criteria>
- LINK-04: gesture documented (10-01) + interception and non-interference regression-tested
- LINK-05: destination always inspectable pre-open (model + truncation invariants)
- LINK-06: copy-without-open proven via injected sink over the existing clipboard path
- LINK-08: argv-data opening (10-01) + non-fatal failure proven
</success_criteria>

<output>
Create `.planning/phases/10-clickable-terminal-links/10-04-SUMMARY.md` when done.
</output>
