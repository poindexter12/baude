---
phase: 10-clickable-terminal-links
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vendor/vt100/ (new vendored crate: src/*, Cargo.toml, LICENSE, README.md)
  - Cargo.toml
  - Cargo.lock
  - baude-core/Cargo.toml
  - baude/Cargo.toml
  - baude/src/main.rs
  - baude/src/links.rs
  - baude/src/app.rs
  - baude/src/ui.rs
  - .planning/WINDOWS.md
autonomous: true
requirements: [LINK-01, LINK-04, LINK-05, LINK-07, LINK-08]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low   # derived: 0 calibration samples (factor 1.0, not applied)

must_haves:
  truths:
    - "Pressing the hint chord (ctrl+o) over a pane whose screen contains an OSC8 link opens a LinkHints overlay listing the link's parsed destination, never its visible label (LINK-01, LINK-05)"
    - "Enter on the selected hint passes the validated URL as one argv argument to the injected opener; both Ok and Err resolve to set_message and the session keeps running (LINK-08)"
    - "An OSC8 sequence with an empty URI closes the link run; empty targets are never collected as activatable (edge lift)"
    - "Only targets accepted by validate_http_url — parsed http/https, no control chars or whitespace pre- OR post-percent-decode — are activatable (LINK-07)"
    - "The hint chord and its actions are documented in the help overlay's global section (LINK-04: documented gesture)"
  artifacts:
    - "vendor/vt100/ — workspace member forked from vt100 0.15.2 with: OSC8 arm in osc_dispatch (params[2..] rejoined with b\";\"), Attrs.link: Option<u16>, per-Screen link intern table, Cell::link_id(), Screen::link_target(id)"
    - "baude/src/links.rs — DetectedLink, collect_links(&Screen) (OSC8 pass), validate_http_url(raw) -> Option<url::Url>"
    - "baude/src/app.rs — Modal::LinkHints { links, selected }, ctrl+o chord, handle_link_hints_key with injected opener, spawn_opener/activate_link seam"
    - "baude/src/ui.rs — draw_modal arm for LinkHints + help overlay line"
    - "vendor/vt100/README.md — upstream version + diff surface; .planning/WINDOWS.md entry 6 names the opener as injected-by-construction"
  key_links:
    - "ctrl+o chord -> collect_links(&Screen) -> Modal::LinkHints (only entry path into hint mode)"
    - "Attrs.link -> Screen intern table -> Screen::link_target(id) — cell label text never consulted for the destination"
    - "Enter -> validate_http_url gate -> activate_link(injected spawn_opener) -> Command::new(OPENER).arg(url)"
    - "baude-core/src/lib.rs `pub use vt100;` re-export unchanged — fork keeps crate name vt100, downstream imports untouched"
  prohibitions:
    - "output alone never opens a link"
    - "opener never receives shell code (argv data only)"
    - "non-HTTP(S)/malformed/control-bearing targets never activatable"
    - "opener failure never kills the session"
  backstop: { statement: "No byte sequence arriving as terminal output can trigger link activation without the explicit gesture", verification: backstop }
---

<objective>
Tracer for Phase 10: prove the whole clickable-links architecture end-to-end on one
path — OSC8 bytes parsed in a vendored vt100 fork, per-cell link id, gesture-time
collection, hint overlay showing the actual destination, validated open through an
injected opener seam — then document the gesture and record fork provenance.

Purpose: the vendored-fork decision (CONTEXT "Claude's Discretion: which crate layer
owns the OSC8 parser extension", resolved by RESEARCH primary recommendation) is the
architectural risk of this phase. One production-quality slice validates it before
waves 2-3 build out grid fidelity, bare-URL detection, and overlay UX on top.

Implements CONTEXT locked decisions: keyboard link-hint mode as the explicit gesture;
OSC8 parsed in the terminal-state layer with per-cell link ids; overlay shows actual
destination (LINK-05 structural); http/https-only validation with pre/post-decode
control rejection; argv-data opener, detached, never shell; failure via set_message.
Modifier-click is NOT shipped (discretionary; RESEARCH anti-patterns recommends
against for v2.2).

Flagged planner assumptions (edge-probe UNRESOLVED, proven here): LINK-05/06 — the
overlay model carries the destination string (Task 1 test asserts it); LINK-08 — the
injected opener seam receives exactly the validated URL (Task 1 spy test asserts it).

Output: vendored fork wired as workspace member, minimal but production-quality link
plumbing, tracer tests green, gesture documented, provenance recorded.
</objective>

<execution_context>
@~/.poindexter/claude/gsd-core/workflows/execute-plan.md
@~/.poindexter/claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-clickable-terminal-links/10-RESEARCH.md
@.planning/phases/10-clickable-terminal-links/10-PATTERNS.md
@.planning/REQUIREMENTS.md (lines 50-60: LINK-01..LINK-08)
</context>

## Artifacts this phase produces

| Artifact | Symbols | Produced by |
|----------|---------|-------------|
| Vendored vt100 link API | `Attrs.link: Option<u16>`, `Cell::link_id()`, `Screen::link_target(id)`, OSC8 arm in `osc_dispatch` | 10-01 (this plan); hardened in 10-02 |
| `Modal::LinkHints` | `Modal::LinkHints { links: Vec<links::DetectedLink>, selected: usize }` + `handle_link_hints_key` | 10-01 (this plan); completed in 10-04 |
| links module | `baude/src/links.rs`: `DetectedLink`, `collect_links(&vt100::Screen)`, `validate_http_url(&str) -> Option<url::Url>` | 10-01 (this plan); bare-URL pass + matrix in 10-03 |
| Keybind | ctrl+o (global chord, documented in help overlay) | 10-01 (this plan) |
| Opener seam | `spawn_opener(url)` + `activate_link<F: FnOnce(&str) -> io::Result<()>>` (injected; route_event idiom) | 10-01 (this plan); failure-path tests in 10-04 |
| Formatted-output OSC8 re-emission | fork `grid.rs`/`screen.rs` writer + round-trip test | 10-02 |
| Bare-URL detection | `collect_links` bare pass (wrap-join, punctuation trim) | 10-03 |
| Overlay UX + copy | hint labels, navigation, `c`/`y` copy, no-links state | 10-04 |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "inspect and open an OSC8 link" — one path only</name>
  <files>vendor/vt100/ (new), Cargo.toml, Cargo.lock, baude-core/Cargo.toml, baude/Cargo.toml, baude/src/main.rs, baude/src/links.rs (new), baude/src/app.rs, baude/src/ui.rs</files>
  <precondition>vt100 0.15.2 source exists in the local cargo registry cache: `ls "$HOME"/.cargo/registry/src/*/vt100-0.15.2/src/screen.rs` succeeds (verified present this session at index.crates.io-1949cf8c6b5b557f). If missing, run `cargo fetch` at the workspace root first; halt if it still cannot be found.</precondition>
  <reversibility rating="costly">Vendoring vt100 is a costly-but-two-way door: reverting means restoring `vt100 = "0.15"` in baude-core/Cargo.toml, dropping the workspace member, and deleting vendor/ — link features are lost but no data, schema, or published API is affected (baude does not cargo-publish; verified in RESEARCH via release.yml).</reversibility>
  <read_first>
    - baude/src/app.rs:386-411 (Modal enum), 3862-3906 (handle_key interception + chord shapes + forward_key), 4374-4413 (handle_modal_key dispatch, mem::replace idiom), 5082-5123 (open_editor spawn skeleton — copy stdio/spawn/message shape, NOT its shell indirection), 5456-5468 (scrollback-bracketed screen read), 5491-5524 (clipboard_tests pure-parser test shape), 6397 (App::new("/not-a-repository") test precedent)
    - baude/src/ui.rs:99-117 (draw_modal is final draw call), 1496 (draw_modal), 2114-2171 (Modal::Help render skeleton)
    - baude-core/src/hook.rs:431-449 (route_event injected-closure idiom — copy whole idiom incl. doc comment style)
    - baude-core/src/lib.rs:6 (pub use vt100), baude-core/Cargo.toml:26 (vt100 dep line), Cargo.toml:3 (workspace members), baude/Cargo.toml:10-20 (dependency block style)
    - 10-RESEARCH.md "Code Examples" (OSC8 dispatch arm, validate_http_url, spawn_opener/activate_link, parser-level test shape) and "Pattern 2" (verified osc_dispatch + Attrs excerpts)
    - vt100 registry source: ~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/vt100-0.15.2/src/{screen.rs,attrs.rs,cell.rs}
  </read_first>
  <behavior>
    - Fork test `hyperlink::target_not_label_and_wrap_survival` (shape from RESEARCH Code Examples): process `\x1b]8;;https://real.example/x\x1b\\click here\x1b]8;;\x1b\\` on a 4x10 parser; the labeled cell carries a link id; `link_target(id)` returns the URI, NOT "click here"; the soft-wrapped fragment on row 1 shares the id; empty-URI sequence closes the run (cells written after it have no link id).
    - Fork test `hyperlink::semicolon_uri_rejoined`: URI `https://ex.com/a;b` survives vte's `;` split — `link_target` returns the full URI (params[2..] rejoined).
    - baude test `links::tracer_end_to_end`: parser fed the OSC8 bytes above → `collect_links(screen)` yields exactly one DetectedLink whose destination is the parsed URI → passing it through the activation path with an injected spy closure records exactly `https://real.example/x` as the single argv string; a second case with destination `file:///etc/passwd` is rejected by `validate_http_url` and never reaches the spy.
    - baude test `link_hints::chord_without_session_sets_message`: `App::new("/not-a-repository")`, ctrl+o via handle_key → modal stays `Modal::None`, message set (no panic, nothing spawned).
  </behavior>
  <action>
    RED first (commit `test(10-01): ...`): vendor the fork and add the API surface as
    inert stubs so the tests above COMPILE but FAIL on assertions (a compile error is
    INVALID_RED — the gate requires assertion-level failure).

    1. Vendor: copy the registry source dir verbatim to vendor/vt100/ (keep LICENSE,
       Cargo.toml, src/, and upstream tests/ if the package ships them; delete
       .cargo-checksum.json). Append "vendor/vt100" to workspace members in Cargo.toml;
       change baude-core/Cargo.toml vt100 dep to `{ path = "../vendor/vt100" }` (keep
       package name vt100 — the `pub use vt100;` re-export in baude-core/src/lib.rs:6
       must not change). Run `cargo check --workspace` ONCE without --locked to
       regenerate Cargo.lock (registry->path source change); all later gates use
       --locked. Run `cargo fmt` on the vendored crate once at import (RESEARCH Open
       Q4). If upstream tests/ were not shipped in the crate package, note that in
       Task 2's README instead of failing.
    2. Stubs for RED: `pub link: Option<u16>` on Attrs (attrs.rs) with Default None;
       `Cell::link_id() -> Option<u16>` (cell.rs); `Screen::link_target(&self, id: u16)
       -> Option<&str>` returning None (screen.rs); links.rs with `DetectedLink`
       (destination: url::Url, plus (row, start_col, end_col) span fields and a
       source discriminant Osc8|Bare), `collect_links` returning empty,
       `validate_http_url` per the RESEARCH Code Examples function (full: pre-decode
       control/whitespace reject on the raw string, percent_decode_str + post-decode
       control recheck, url::Url::parse, http|https allowlist, returns the parsed
       normalized Url — the same value later displayed AND passed to argv). Add
       `mod links;` to baude/src/main.rs; add `url = "2"` and `percent-encoding = "2"`
       to baude/Cargo.toml [dependencies] with a WHY comment (both already in
       Cargo.lock via ureq — zero new packages).
    3. Run the named tests, confirm they fail on assertions, persist RED evidence
       (`gsd_run check tdd-red-evidence`), commit.

    GREEN (commit `feat(10-01): ...`):
    4. Fork: add the OSC8 arm to osc_dispatch per RESEARCH Code Examples — match
       `Some(&b"8")`, rejoin `params[2..]` with `b";"`, empty URI => `attrs.link =
       None`, else intern via `intern_link(id_param, uri)` deduped by (id, uri) into a
       per-Screen `Vec<(String, String)>` with u16 index; `parse_id_param` extracts
       `id=` from the `:`-separated param field. Wire Attrs.link into cell writes so
       printed cells carry the current link id. Implement link_target against the
       intern table. (Caps, erase-stripping, and formatted re-emission are 10-02 —
       do NOT build them here; keep the table append-only and the diff minimal.)
    5. links.rs: implement the OSC8 pass of collect_links — walk visible cells, group
       maximal runs sharing link_id, dedupe whole-screen by interned entry, resolve via
       link_target, gate every candidate through validate_http_url (failed candidates
       are simply not collected). Bare-URL pass is 10-03; leave a seam (private fn
       stub returning no candidates) with a comment pointing at plan 10-03.
    6. app.rs: add `Modal::LinkHints { links: Vec<links::DetectedLink>, selected: usize }`
       in the existing enum style; add the ctrl+o chord in the global-chords block
       (occupied set verified in RESEARCH Pitfall 5: ctrl+q, ctrl+\/ctrl+4, ctrl+e,
       ctrl+n, alt+arrows — ctrl+o is free) using the `if ctrl && matches!(...)`
       shape from app.rs:3892-3901. Chord handler: only when focus is Claude/Shell
       with a live parser — lock parser, `set_scrollback(scroll)`, `collect_links`,
       `set_scrollback(0)`, drop lock BEFORE mutating modal state (PATTERNS
       "Scrollback-bracketed screen reads"); empty result => set_message("no links
       visible"); else open Modal::LinkHints. Add
       `handle_link_hints_key<F: FnOnce(&str) -> std::io::Result<()>>(&mut self, key, open)`
       dispatched from handle_modal_key via the mem::replace idiom (app.rs:4412-4413):
       Enter => activate selected via `open` with the normalized `Url::as_str()`;
       Esc => dismiss. (`c`/`y` copy and navigation are 10-04.) Production call site
       passes `spawn_opener`; implement spawn_opener per RESEARCH Code Examples:
       cfg-gated OPENER const ("open" macOS / "xdg-open" elsewhere),
       `Command::new(OPENER).arg(url)`, Stdio::null() on all three handles, spawn,
       reaper thread on the returned child; both Ok and Err arms => set_message,
       session continues (open_editor precedent minus its shell indirection —
       the URL is a single argv argument, direct `Command::new(OPENER)`).
    7. ui.rs: minimal draw_modal arm for LinkHints — Clear + rounded-border Paragraph
       (Modal::Help skeleton, ui.rs:2114-2171), one Line per link showing the
       destination (`Url::as_str()`), selected row bold. Layout polish is 10-04.
    8. Run the RED tests to green; run the verify command; commit.
  </action>
  <verify>
    <automated>cargo test -p vt100 hyperlink --locked && cargo test -p baude links:: --locked && cargo test -p baude link_hints --locked && cargo check --workspace --all-targets --locked</automated>
    <fails_when>any `hyperlink`/`links::`/`link_hints` test reports FAILED (assertion on target-vs-label, rejoin, spy argv, or chord-without-session), or any workspace target fails to compile (non-zero cargo exit)</fails_when>
  </verify>
  <acceptance_criteria>
    - Fork tests prove: labeled cell's destination comes from link_target, never cell text; wrapped fragment shares the id; empty URI closes the run; `;`-bearing URI reconstructed (per CONTEXT locked decision: OSC8 parsed in the terminal-state layer, per-cell ids)
    - links::tracer_end_to_end proves: collect->validate->injected-open delivers exactly the parsed URI as one argv string; a non-http target never reaches the spy (per CONTEXT locked decisions on validation and argv opening)
    - RED evidence persisted with verdict RED_EVIDENCE_OK before any GREEN commit; test(10-01) commit precedes feat(10-01)
    - Cargo.lock re-resolves vt100 to the path source; `cargo test --workspace --locked` still passes 554 pre-existing tests (no regression from the fork swap)
    - No test reaches a real Command spawn (opener injected; WINDOWS entry 6 discipline)
  </acceptance_criteria>
  <done>One happy path works end-to-end and is committed: OSC8 bytes -> cell link id -> gesture-time collection -> overlay model with actual destination -> validated open through the injected seam. Fork is a workspace member; workspace green.</done>
</task>

<task type="auto">
  <name>Task 2: Document the gesture; record fork provenance and opener disposition</name>
  <files>baude/src/ui.rs, vendor/vt100/README.md (new), .planning/WINDOWS.md</files>
  <read_first>
    - baude/src/ui.rs:2142-2151 (help overlay "global (any pane)" section — two-space + padded-key + description format)
    - .planning/WINDOWS.md entry 6 (uncontained subprocess inventory)
    - 10-RESEARCH.md Open Question 4 (fork hygiene recommendation)
  </read_first>
  <action>
    1. Help overlay: add `Line::raw("  ctrl+o      link hints (inspect/copy/open urls)")`
       to the global section — this is LINK-04's "documented explicit gesture" (SHIP-02
       will cite it). Follow the existing padding format exactly.
    2. vendor/vt100/README.md: record upstream crate name/version (vt100 0.15.2),
       upstream source (crates.io registry cache copy), MIT license retained, why the
       fork exists (OSC 8 hyperlink support absent upstream through 0.16.2), and the
       baude-specific diff surface by file (attrs.rs link field; screen.rs OSC8 arm +
       intern table + link_target; cell.rs link_id; grid.rs formatted re-emission —
       note: lands in plan 10-02). Note whether upstream tests/ shipped in the
       registry package (from Task 1 finding).
    3. .planning/WINDOWS.md entry 6: add the link opener to the uncontained-subprocess
       inventory with disposition "injected-by-construction — production call site
       passes spawn_opener; every test passes a closure spy; not test-reachable".
       Keep the existing pbcopy/open_editor text intact (reuse, don't duplicate).
  </action>
  <verify>
    <automated>grep -q "link hints" baude/src/ui.rs && grep -q "0.15.2" vendor/vt100/README.md && grep -qi "opener" .planning/WINDOWS.md && cargo check -p baude --locked</automated>
    <fails_when>any grep exits non-zero (help line, provenance, or WINDOWS disposition missing), or the ui.rs edit fails to compile</fails_when>
  </verify>
  <acceptance_criteria>
    - Help overlay documents the chord in the global section (LINK-04 documented-gesture leg; keybind choice ctrl+o recorded — Claude's-discretion decision, collision-checked against the occupied set)
    - README names upstream version and every forked file's purpose (RESEARCH Open Q4)
    - WINDOWS entry 6 records the opener's injected disposition without duplicating existing entries
  </acceptance_criteria>
  <done>Gesture documented in-product; fork provenance and opener containment disposition recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child/remote PTY byte stream -> forked vt100 parser | untrusted terminal output crosses into terminal state (OSC8 params, labels) |
| hint overlay Enter -> OS platform opener | validated URL crosses from TUI to the operating system |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Spoofing | fork osc_dispatch / overlay render | high | mitigate | destination resolved only via Screen::link_target(id); cell label text never consulted; tracer test asserts label != target |
| T-10-02 | Elevation | validate_http_url scheme gate | high | mitigate | allowlist exactly http/https after url::Url::parse; failures never collected (fail closed) |
| T-10-03 | Elevation | spawn_opener | high | mitigate | Command::new(OPENER).arg(url) — URL is a single argv argument; no shell interpreter anywhere on the link-open path |
| T-10-04 | Tampering | validate_http_url control/whitespace gate | high | mitigate | reject raw-string control/whitespace pre-parse AND recheck post-percent-decode (full matrix in 10-03) |
| T-10-05 | Denial | fork link intern table | medium | mitigate | caps land in 10-02 (URI <= 2083 bytes, bounded table); this plan keeps the table u16-indexed and minimal |
| T-10-SC | Tampering | vendored vt100 source (supply chain) | high | mitigate | source copied from the local registry cache of the exact Cargo.lock-pinned, already-shipping vt100 0.15.2; provenance in vendor/vt100/README.md; zero new registry packages (url/percent-encoding already transitive — RESEARCH Package Legitimacy Audit: both OK, no ASSUMED/SUS/SLOP) |
</threat_model>

<verification>
- `cargo test --workspace --locked` green (554 pre-existing + new tracer tests) — wave-1 merge gate
- Fork swap is invisible to downstream code: no import changes outside Cargo manifests
- Tracer commits show RED (test) before GREEN (feat)
</verification>

<success_criteria>
- The single happy path (OSC8 -> overlay destination -> validated injected open) is committed and test-proven
- Gesture documented; fork provenance recorded; WINDOWS entry 6 updated
- Requirements advanced: LINK-01 (target-not-label), LINK-04 (documented gesture + structural modal interception), LINK-05 (overlay shows destination), LINK-07 (validation gate live), LINK-08 (argv-data injected opener)
</success_criteria>

<output>
Create `.planning/phases/10-clickable-terminal-links/10-01-SUMMARY.md` when done.
</output>
