---
phase: 09-hook-seeding-safety
plan: 04
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - baude-core/src/backend/claude.rs
  - baude/src/app.rs
  - bauded/src/manager.rs
autonomous: true
requirements: [HREG-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An existing .mcp.json that cannot be read, cannot be parsed, or parses to a non-object is left byte-identical with a warning naming the file (D-03)"
    - "The .mcp.json command field remains a raw, unquoted argv string — Claude Code spawns stdio MCP servers directly, not through a shell (D-09)"
    - "bauded surfaces seed warnings via prefixed eprintln at both spawn call sites; the TUI restart path surfaces them like the add-session path (D-02)"
    - "A seeding failure never aborts a spawn in either binary (D-04)"
    - "An app-level spawn attempt over a malformed settings.local.json leaves the file byte-identical and surfaces a warning naming it in the TUI (D-10)"
  artifacts:
    - path: "baude-core/src/backend/claude.rs"
      provides: "guarded seed_mcp_config returning Vec<SeedWarning>; prepare_cwd collects both seeds"
      contains: "read_settings_guarded"
    - path: "bauded/src/manager.rs"
      provides: "seed-warning eprintln surface at spawn_with_mode_internal and restart_with_mode"
      contains: "seed warning"
    - path: "baude/src/app.rs"
      provides: "restart-path warning surface + app-level malformed-settings integration tests"
      contains: "seed_warning_"
  key_links:
    - from: "baude-core/src/backend/claude.rs"
      to: "baude-core/src/hook.rs"
      via: "seed_mcp_config reuses hook::read_settings_guarded from 09-01"
      pattern: "read_settings_guarded"
    - from: "bauded/src/manager.rs"
      to: "baude-core/src/backend/mod.rs"
      via: "both daemon call sites consume prepare_cwd's Vec<SeedWarning>"
      pattern: "prepare_cwd\\(&cwd\\)"
  prohibitions:
    - statement: "The .mcp.json permission-mcp command MUST NOT be quoted — it is argv data for a direct spawn, and inserted quote characters would break the server launch (self-inflicted DoS)"
      category: safety
      requirement_id: HREG-03
      disposition: flagged-unverified
    - statement: "Seeding MUST NOT abort a session spawn on any read, parse, or write failure — warnings inform, never block"
      category: safety
      requirement_id: HREG-03
      disposition: flagged-unverified
---

<objective>
Complete HREG-03: extend the 09-01 guard to `.mcp.json` with the identical warning shape
(D-03) while keeping its command field unquoted argv data (D-09), wire the three remaining
`prepare_cwd` consumers (TUI restart path, both bauded call sites) to surface warnings
(D-02), and land the app-level malformed-settings integration test (D-10's second half).

Purpose: 09-01 proved the warning seam on one path; this plan expands it to every spawn
path and the second clobber site so no seeding route can silently destroy user settings.

Output: guarded `seed_mcp_config`, all four call sites surfacing warnings, app-level
`seed_warning_*` integration tests under Phase-8 fixtures.

(Decision map D-01..D-13 is defined in 09-01-PLAN.md and cited by number here.)
</objective>

<execution_context>
@~/.poindexter/claude/gsd-core/workflows/execute-plan.md
@~/.poindexter/claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-hook-seeding-safety/09-CONTEXT.md
@.planning/phases/09-hook-seeding-safety/09-RESEARCH.md (Pattern 3 — .mcp.json stays unquoted; "Malformed-settings app-level test")
@.planning/phases/09-hook-seeding-safety/09-PATTERNS.md (claude.rs, app.rs, manager.rs sections)
@.planning/phases/09-hook-seeding-safety/09-01-SUMMARY.md

<interfaces>
<!-- From 09-01 (consume, do not redefine): -->
```rust
// baude-core/src/hook.rs
pub struct SeedWarning { pub file: PathBuf, pub reason: SeedWarningReason }
pub enum SeedWarningReason { Unreadable(std::io::Error), Unparseable(serde_json::Error), NonObjectRoot, WriteFailed(std::io::Error) }
pub(crate) fn read_settings_guarded(path: &Path) -> Result<Value, SeedWarning>;
pub fn seed_settings(cwd: &Path) -> Vec<SeedWarning>;
// baude-core/src/backend/mod.rs
fn prepare_cwd(&self, cwd: &Path) -> Vec<crate::hook::SeedWarning>;
// baude/src/app.rs
fn warn_seed_failure(&mut self, warning: &baude_core::hook::SeedWarning);  // private, from 09-01
```

Existing (verified this session):
```rust
// baude-core/src/backend/claude.rs
fn prepare_cwd(&self, cwd: &Path) -> ...   // :74-79 — calls seed_settings + (prompt mode) seed_mcp_config
fn seed_mcp_config(cwd: &Path)             // :110-122 — the second clobber site
// baude-core/src/permission.rs
pub fn mcp_config_path(cwd: &Path) -> PathBuf                      // :213
pub fn merge_mcp_config(existing: &Value, exe: &str) -> Value      // :175
// call sites still ignoring the return after 09-01:
//   baude/src/app.rs:5245        (App::restart_session_with_mode)
//   bauded/src/manager.rs:1158   (Manager::spawn_with_mode_internal)
//   bauded/src/manager.rs:2136   (Manager::restart_with_mode)
// bauded warning precedent: eprintln!("save state: {error}") at manager.rs:652-656
```
</interfaces>
</context>

## Artifacts this phase produces

This plan's contributions (see 09-01 for the phase-wide list):

- `seed_mcp_config(cwd: &Path) -> Vec<SeedWarning>` — signature change, baude-core/src/backend/claude.rs (stays private)
- `ClaudeBackend::prepare_cwd` collecting warnings from BOTH seeds
- bauded `eprintln!("seed warning: {warning}")` surface at manager.rs:1158 and :2136
- TUI restart-path surface at app.rs:5245 (reuses `warn_seed_failure`)
- New tests: `backend::claude::tests` mcp-guard units; `app::tests::seed_warning_*` integration tests

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard .mcp.json with the shared helper — command stays argv data</name>
  <files>baude-core/src/backend/claude.rs</files>
  <read_first>
    - baude-core/src/backend/claude.rs :55-144 (prepare_cwd, seed_mcp_config, existing tests mod shape)
    - baude-core/src/hook.rs — read_settings_guarded + SeedWarning from 09-01 (signatures in interfaces block)
    - baude-core/src/permission.rs :151-175 (mcp_server_config doc — command/args split — and merge_mcp_config)
    - .planning/phases/09-hook-seeding-safety/09-RESEARCH.md Pattern 3 (why no quoting: direct spawn, A2)
  </read_first>
  <behavior>
    - `mcp_guard_unparseable_left_untouched_and_warned`: `.mcp.json` containing `{not json`
      → seed_mcp_config returns one warning with file ending `.mcp.json`, reason
      `Unparseable(_)`, bytes identical.
    - `mcp_guard_non_object_root_left_untouched`: `[1,2]` → `NonObjectRoot`, bytes identical.
    - `mcp_guard_missing_file_fresh_seed`: no file → empty Vec, written file parses with
      `mcpServers.baude.command` present.
    - `mcp_command_is_raw_argv_not_shell_quoted`: after a fresh seed, the written
      `mcpServers.baude.command` string equals `std::env::current_exe()` displayed EXACTLY
      — in particular it does not start with a single-quote character (D-09: argv data for
      a direct spawn; quoting it would break the launch).
  </behavior>
  <action>
    Convert `seed_mcp_config(cwd) -> Vec<SeedWarning>` using
    `crate::hook::read_settings_guarded(&crate::permission::mcp_config_path(cwd))` — the
    identical warning shape D-03 requires: guarded-read Err → return vec![warning], file
    untouched; Ok → `merge_mcp_config` + write; write error → vec![WriteFailed] (D-04).
    The `current_exe` pre-resolution keeps its best-effort early return (empty Vec — no
    warning; unchanged shipped behavior). Do NOT pass `exe` through any quoting function —
    it is argv data (D-09, research answer A2). Update `prepare_cwd` to collect:
    seed_settings warnings, extended with seed_mcp_config warnings when prompt mode runs
    it. Add the four behavior tests to claude.rs's tests mod using a unique temp cwd
    (same fixture shape as hook.rs seed tests).
  </action>
  <verify>
    <automated>cargo test -p baude-core --lib backend::claude:: && cargo check --workspace --all-targets --locked</automated>
    <fails_when>non-zero exit, or any line matching "test result: FAILED" / "error[E" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - seed_mcp_config's read path contains no `.ok()` swallow and no `unwrap_or_else` coercion; it routes through hook::read_settings_guarded
    - The exe string reaches merge_mcp_config unmodified (source assertion: no quote_posix_single call anywhere in claude.rs)
    - prepare_cwd returns warnings from both seeds in prompt mode, settings-only otherwise
    - All four mcp_guard/mcp_command tests pass; workspace check exits 0
  </acceptance_criteria>
  <done>The second clobber site is guarded with the identical warning shape, and the .mcp.json command field is pinned as raw argv data by a test.</done>
</task>

<task type="auto">
  <name>Task 2: Surface warnings on the three remaining spawn paths</name>
  <files>baude/src/app.rs, bauded/src/manager.rs</files>
  <read_first>
    - baude/src/app.rs :5217-5250 (restart_session_with_mode; call site :5245) and the 09-01 warn_seed_failure helper
    - bauded/src/manager.rs :1150-1165 (spawn_with_mode_internal; call site :1158), :2127-2142 (restart_with_mode; call site :2136), :652-656 (the eprintln precedent)
  </read_first>
  <action>
    Wire the three call sites 09-01 left ignoring the return (D-02):
    - app.rs:5245 (`restart_session_with_mode`): loop the returned Vec through the existing
      `warn_seed_failure`, exactly like the add-session site.
    - manager.rs:1158 (`spawn_with_mode_internal`) and :2136 (`restart_with_mode`):
      `for warning in be.prepare_cwd(&cwd) { eprintln!("seed warning: {warning}"); }` — 
      the prefixed-eprintln shape of the `save state:` precedent; bauded has no logging
      crate, so CONTEXT's "warn-level logging" concretely means this (RESEARCH A3).
      Deliberately NO per-path dedup across bauded's restore loop: the save-state precedent
      also repeats, and a warning per re-spawn is the signal (resolves RESEARCH Open
      Question 2 — noise accepted over hidden state).
    Keep every surrounding line untouched: warnings inform, never block (D-04) — no early
    return, no Result change in any spawn function.
  </action>
  <verify>
    <automated>cargo check --workspace --all-targets --locked && cargo test -p bauded --bins manager::tests::fixture_isolation_</automated>
    <fails_when>non-zero exit, or any line matching "test result: FAILED" / "error[E" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - No call site discards prepare_cwd's return anymore (source assertion: every `be.prepare_cwd(&cwd)` occurrence in app.rs and manager.rs feeds a warning consumer)
    - Both bauded surfaces use the literal prefix "seed warning: " followed by the SeedWarning Display
    - No spawn function gained an early return or changed Result type on the seeding path
    - Workspace check + existing manager fixture-isolation smoke tests exit 0
  </acceptance_criteria>
  <done>Every one of the four spawn paths (TUI add + restart, daemon spawn + restart) surfaces seed warnings through its binary's established channel.</done>
</task>

<task type="auto">
  <name>Task 3: App-level malformed-settings integration tests</name>
  <precondition>Phase-8 fixture infrastructure exists on this branch: baude_core::testing::TestRedirect, workspace::override_for_test, and the guarded App construction used by app.rs tests (08-03/08-08)</precondition>
  <files>baude/src/app.rs</files>
  <read_first>
    - baude/src/app.rs :5577-5740 (worker_isolation_app_does_not_launch_ambient_readers — the guarded App construction + fixture shape to copy), :2790-2860 (add_session_with_mode_internal and its wrappers at :1690-1706 and :2770-2790)
    - .planning/phases/09-hook-seeding-safety/09-RESEARCH.md "Malformed-settings app-level test"
    - bauded/src/manager.rs manager tests for the stub-command convention (sessions spawned with "sh -c 'sleep 30'" / "true")
  </read_first>
  <action>
    Add `seed_warning_malformed_settings_survives_spawn_attempt` and
    `seed_warning_non_object_settings_survives_spawn_attempt` to app.rs's tests mod
    (D-10 app half):
    1. Phase-8 owner fixture per the worker_isolation test's shape: unique temp root,
       `TestRedirect::new(&root)` + `workspace::override_for_test` held as locals for the
       whole test, plus `TestRedirect::with_hook_command` supplying a production-shaped
       command (per RESEARCH, override strings stay verbatim; a legacy-form
       `<root>/bin/baude hook` is fine — both forms are recognized after 09-03, and this
       test's assertions do not depend on quoting).
    2. Create a session cwd under the fixture root containing `.claude/settings.local.json`
       with bytes `{not json` (test A) / `[1,2]` (test B); record the bytes.
    3. Construct the guarded App and drive a REAL spawn entry point that reaches
       `be.prepare_cwd(&cwd)` at :2841 — the standalone wrapper
       (`add_standalone_session_with_mode`) or the checkout wrapper, whichever the guarded
       fixture can satisfy with less setup, with a stub command per the manager-test
       convention. The spawn attempt MAY return Err after seeding (PTY/backend constraints
       in a test process) — the assertions bind to the seeding effects, not spawn success;
       what is NOT acceptable is bypassing App and calling prepare_cwd directly, which
       would not be an app-level spawn test (D-10).
    4. Assert: file bytes identical to step 2; `app.message` is Some and its text contains
       the settings path (warn_seed_failure ran).
    5. Cleanup per fixture convention.
  </action>
  <verify>
    <automated>cargo test -p baude --bins seed_warning_</automated>
    <fails_when>non-zero exit, "test result: FAILED", or a summary line showing "0 passed; 0 failed" (filter matched nothing)</fails_when>
  </verify>
  <acceptance_criteria>
    - Both tests reach prepare_cwd through an App spawn entry point (source assertion: the tests call an add/restart session method on App, never backend::active().prepare_cwd directly)
    - Both malformed variants ({not json and [1,2]) asserted byte-identical via raw bytes after the spawn attempt
    - app.message content includes the settings.local.json path in both tests
    - Tests run under TestRedirect + workspace override with all paths inside the fixture root
    - `cargo test -p baude --bins seed_warning_` exits 0 with 2 tests passing
  </acceptance_criteria>
  <done>The app-level half of D-10 is automated: a real TUI spawn attempt over a malformed settings file leaves it byte-identical and surfaces a warning naming it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user project dir → baude seeding | `.mcp.json` is user-owned content baude writes into in prompt mode |
| .mcp.json command field → Claude Code process spawn | the command string becomes argv[0] of a directly-spawned MCP server |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-10 | Tampering (data loss) | seed_mcp_config write path | high | mitigate | Guarded read via shared helper (Task 1); byte-identical unit tests for unparseable and non-object roots |
| T-09-11 | Denial of Service (self-inflicted) | .mcp.json command field | medium | mitigate | Leave the exe unquoted — argv data for a direct spawn (D-09); pinned by mcp_command_is_raw_argv_not_shell_quoted |
| T-09-12 | Denial of Service (spawn abort) | four prepare_cwd call sites | medium | mitigate | Task 2 keeps every spawn function's control flow unchanged; warnings are fire-and-forget |
| T-09-13 | Information Disclosure | seed warning text on bauded stderr | low | accept | Local daemon stderr only; names the user's own project file |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `cargo test -p baude-core --lib backend::claude::`, `cargo test -p baude --bins seed_warning_`, `cargo test -p bauded --bins manager::tests::fixture_isolation_` all green.
- `cargo check --workspace --all-targets --locked` exits 0; `cargo fmt --check` and `cargo clippy --workspace --all-targets` clean.
- Phase gate (after all Wave 2 plans): full workspace suite under `scripts/assert-real-roots-untouched.sh` per the Phase-8 convention, before /gsd-verify-work.
</verification>

<success_criteria>
- Phase criterion 1 fully satisfied: BOTH files (settings.local.json and .mcp.json) survive unsafe states untouched with actionable warnings, on every spawn path of both binaries.
- .mcp.json command remains a working direct-spawn argv string (no quoting regression).
- D-10's app-level spawn test exists and passes under Phase-8 isolation.
</success_criteria>

<output>
Create `.planning/phases/09-hook-seeding-safety/09-04-SUMMARY.md` when done.
</output>
