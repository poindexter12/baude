---
phase: 09-hook-seeding-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - baude-core/src/persist.rs
  - bauded/src/manager.rs
autonomous: true
requirements: [WLOCK-01, WLOCK-03, WLOCK-04]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Reopening a workspace whose lock file remains on disk after the OS lock was released succeeds and re-stamps the current pid (D-12)"
    - "A bauded save under a lock held by another owner surfaces an error carrying the holder pid and the lock path, and never removes the other owner's lock file (D-12)"
    - "Both new tests run under Phase-8 isolation (isolated persist roots / ManagerFixture) and touch no real user root (D-13)"
  artifacts:
    - path: "baude-core/src/persist.rs"
      provides: "regression test: leftover lock file does not block reopen after OS lock release"
      contains: "leftover"
    - path: "bauded/src/manager.rs"
      provides: "regression test: held lock surfaces pid diagnostic through bauded's save path"
      contains: "held_lock"
  key_links:
    - from: "bauded/src/manager.rs"
      to: "baude-core/src/persist.rs"
      via: "save_checked -> persist::save_current_at_test -> atomic_save_current -> hold_state_lock"
      pattern: "save_checked"
  prohibitions:
    - statement: "A test MUST NOT 'fix' contention by deleting a lock file — no code path removes another owner's lock file (WLOCK-02 contract)"
      category: safety
      requirement_id: WLOCK-04
      disposition: flagged-unverified
---

<objective>
Turn the two v2.1.3 inspection-only lock behaviors into automated regression tests under
Phase-8 isolation (D-12, D-13, phase success criterion 4): (a) reopening a workspace whose
lock FILE remains on disk after the OS lock was released succeeds; (b) `bauded`
encountering a held lock refuses with the pid diagnostic.

Purpose: WLOCK-01..04 shipped verified by inspection only; these tests pin the contract so
Phases 10-12 cannot regress it silently. No production behavior changes — the phase
boundary excludes any change to the WLOCK contract beyond adding tests.

Output: one test beside the persist lock module, one test in bauded's manager tests mod.
</objective>

<execution_context>
@~/.poindexter/claude/gsd-core/workflows/execute-plan.md
@~/.poindexter/claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-hook-seeding-safety/09-CONTEXT.md
@.planning/phases/09-hook-seeding-safety/09-RESEARCH.md (Pitfalls 4 and 6, "Existing lock-test vocabulary to reuse")
@.planning/phases/09-hook-seeding-safety/09-PATTERNS.md (persist.rs and manager.rs sections)

<interfaces>
<!-- Existing surfaces the tests consume — no production code changes in this plan. -->

baude-core/src/persist.rs (all read this session, line refs verified):
```rust
fn lock_path(destination: &Path) -> PathBuf            // :472 — ".{name}.lock" sibling
fn hold_state_lock(destination: &Path) -> Result<(), StateLockError>  // :568 — re-entrant cache at :578-579
fn lock_holder_pid(path: &Path) -> Option<u32>         // :544
pub fn release_state_lock_for_test(destination: &Path) // :557-566 (test-support)
pub enum StateLockError { Held { path, holder: Option<u32> }, Io { .. } }  // :482
// Held Display: "another baude (pid {pid}) already owns this workspace; its lock is {path}"
```

bauded/src/manager.rs:
```rust
struct ManagerFixture { _identity, _redirect, root, workspace }  // :2652 owner-struct fixture
fn persist_at_for_test(&mut self, root, workspace, failure)      // :2371 routes save_checked to a fixture root
pub fn save(&mut self)          // :652 — eprintln!("save state: {error}")
fn save_checked(&mut self) -> Result<(), persist::SaveError>     // :658
// SaveError Display forwards its anyhow source → StateLockError::Held's Display
```
</interfaces>
</context>

## Artifacts this phase produces

This plan's contributions (see 09-01 for the phase-wide list):

- `persist::tests::reopen_claims_lock_when_leftover_file_outlives_released_os_lock` — new test, baude-core/src/persist.rs
- `manager::tests::held_lock_save_refuses_with_pid_diagnostic` — new test, bauded/src/manager.rs

No production symbols change in this plan.

<tasks>

<task type="auto">
  <name>Task 1: Reopen-with-leftover-lock-file regression test</name>
  <precondition>baude-core/src/persist.rs tests mod contains isolated_root and the raw-OpenOptions external-holder pattern at :1633-1678 (Phase 08 landed on this branch)</precondition>
  <files>baude-core/src/persist.rs</files>
  <read_first>
    - baude-core/src/persist.rs :472-478 (lock_path), :544-548 (lock_holder_pid), :557-566 (release_state_lock_for_test), :568-609 (hold_state_lock incl. re-entrant cache), :1633-1678 (second_workspace_owner_cannot_load_while_writer_lock_is_held + held_state_lock_records_holder_pid — the vocabulary to copy)
    - .planning/phases/09-hook-seeding-safety/09-RESEARCH.md "Pitfall 6" (the re-entrant cache trap)
  </read_first>
  <action>
    Add `reopen_claims_lock_when_leftover_file_outlives_released_os_lock` to persist.rs's
    tests mod, WLOCK-04's untested sequence (D-12): prior owner exits, OS lock evaporates,
    lock FILE stays, reopen must succeed.

    Flow (copies the :1633-1678 vocabulary):
    1. `let root = isolated_root("leftover-lock");` + `test_workspace("claude")`;
       `destination = root.join(workspace.state_file("state"))`; `lock_path(&destination)`;
       `create_dir_all` the destination parent.
    2. Simulate the PRIOR owner with a RAW file handle — `OpenOptions::new().read(true)
       .write(true).create(true).truncate(false).open(&lock_path)` then
       `lock.try_lock().unwrap()` — NEVER via hold_state_lock: the re-entrant cache at
       :578-579 would return Ok early and the test would exercise the cache, not the
       leftover-file path (Pitfall 6). Stamp a fake pid (e.g. 999999) with writeln + flush.
    3. `drop(lock)` — the OS lock releases; assert the lock FILE still exists on disk and
       `lock_holder_pid` still reads Some(999999) (the stale stamp).
    4. Assert `hold_state_lock(&destination)` returns `Ok(())` — reopen succeeds despite
       the leftover file — and the file is re-stamped with the CURRENT pid
       (`read_to_string(&lock_path).trim() == std::process::id().to_string()`).
    5. Cleanup: `release_state_lock_for_test(&destination)` then `remove_dir_all(root)`.
       The test never deletes the lock file itself while another holder could own it.
  </action>
  <verify>
    <automated>cargo test -p baude-core --lib persist::</automated>
    <fails_when>non-zero exit, or any line matching "test result: FAILED" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - The test acquires the first claim via raw OpenOptions + try_lock, never via hold_state_lock (source assertion: hold_state_lock appears only in the step-4 reopen assertion)
    - Asserts, in order: lock file exists after drop; stale pid readable; hold_state_lock Ok; current pid re-stamped
    - Uses isolated_root and cleans up via release_state_lock_for_test + remove_dir_all
    - `cargo test -p baude-core --lib persist::` exits 0 with the new test listed as passing
  </acceptance_criteria>
  <done>WLOCK-04's reopen-after-release sequence is pinned by a test that fails if a leftover lock file ever starts blocking reopen or the re-stamp stops happening.</done>
</task>

<task type="auto">
  <name>Task 2: bauded held-lock pid-diagnostic regression test</name>
  <precondition>bauded/src/manager.rs tests mod contains ManagerFixture (:2652) and persist_at_for_test (:2371) (Phase 08-06 landed on this branch)</precondition>
  <files>bauded/src/manager.rs</files>
  <read_first>
    - bauded/src/manager.rs :2652-2720 (ManagerFixture), :2371-2381 (persist_at_for_test), :652-700 (save/save_checked), an existing persistence test using persist_at_for_test (e.g. around :2997-3040) for Manager construction shape
    - baude-core/src/persist.rs :472-478 (lock_path shape — private, mirror it in the test), :482-530 (StateLockError + Display)
    - .planning/phases/09-hook-seeding-safety/09-RESEARCH.md "Pitfall 4" and "Open Questions" #1 (option (a) is the locked recommendation)
  </read_first>
  <action>
    Add `held_lock_save_refuses_with_pid_diagnostic` to manager.rs's tests mod,
    implementing RESEARCH Open Question 1 option (a): assert bauded's EXISTING first-save
    contention surface, not a new startup claim — bauded today never calls
    claim_workspace_state_lock, and adding one would change the WLOCK contract, which the
    phase boundary excludes ("no change beyond adding tests"; WLOCK-01's bauded startup
    residual stays documented).

    Flow:
    1. `let fixture = ManagerFixture::new("held-lock");` and
       `let state_root = fixture.subdir("state");`.
    2. `let mut manager = Manager::new("true".into(), true);` then
       `manager.persist_at_for_test(&state_root, fixture.workspace(), None);` (existing
       persistence-test shape).
    3. Compute the lock path the save will contend on, mirroring persist's private
       lock_path: `destination = state_root.join(fixture.workspace().state_file(STATE_BASE))`;
       lock file = destination.with_file_name(format!(".{}.lock", destination file_name)).
       `create_dir_all` the parent.
    4. Simulate ANOTHER owner: raw OpenOptions open + `try_lock().unwrap()` on that lock
       path (the persist.rs :1638-1645 pattern — two file descriptions in one process DO
       conflict), stamp fake pid 424242, flush, and KEEP the handle alive across the save.
    5. `let error = manager.save_checked().unwrap_err();` assert:
       (a) `!error.replacement_committed()` (refusal happened before any replacement);
       (b) `error.to_string()` contains "already owns this workspace", "pid 424242", and
       the lock file name — the StateLockError::Held Display carrying the pid diagnostic
       and lock path (WLOCK-03's diagnostic content at bauded's surface; the recovery
       GUIDANCE sentences live only in the TUI refusal block, out of scope here).
    6. Assert the lock file still exists and still reads pid 424242 — the refused save
       removed nothing (WLOCK-02 contract). Drop the holder handle; fixture Drop cleans
       the root.
  </action>
  <verify>
    <automated>cargo test -p bauded --bins manager::tests::held_lock_</automated>
    <fails_when>non-zero exit, "test result: FAILED", or "0 passed" with "0 failed" in the filtered run's summary line (filter matched nothing)</fails_when>
  </verify>
  <acceptance_criteria>
    - The external holder is a raw OpenOptions + try_lock handle held for the duration of the save attempt, stamped with a literal fake pid
    - save_checked's error Display is asserted to contain the ownership phrase, the fake pid, and the lock file name
    - The test asserts the lock file survives the refused save with its stamp intact
    - No production code changes in bauded or baude-core (test-only diff)
    - `cargo test -p bauded --bins manager::tests::held_lock_` exits 0 and runs at least 1 test
  </acceptance_criteria>
  <done>bauded's held-lock refusal surface (pid + lock path via StateLockError::Held) is pinned by a test under ManagerFixture isolation, with the other owner's lock file untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| second baude/bauded instance → workspace state | the single-writer lock is the only thing preventing cross-instance state clobber |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Tampering | workspace state file (cross-instance) | high | mitigate | Regression tests pin try_lock-based refusal (Task 2) and legitimate reopen (Task 1), so future phases cannot weaken the single-writer contract unnoticed |
| T-09-05 | Denial of Service | leftover lock file blocking reopen | medium | mitigate | Task 1 pins that a stale lock FILE alone never blocks reopen (contention is decided by try_lock, never file existence — WLOCK-04) |
| T-09-06 | Repudiation | anonymous lock holder | low | accept | pid stamping is best-effort by design; Task 1/2 assert the stamped path only |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `cargo test -p baude-core --lib persist::` and `cargo test -p bauded --bins manager::tests::held_lock_` green.
- Diff is test-only: no non-test lines change in persist.rs or manager.rs.
- `cargo fmt --check` and `cargo clippy --workspace --all-targets` clean.
</verification>

<success_criteria>
- Both v2.1.3 inspection-only behaviors are automated: leftover-lock reopen (persist) and held-lock pid diagnostic (bauded), each under Phase-8 isolation.
- Neither test deletes or truncates a lock file another holder could own.
</success_criteria>

<output>
Create `.planning/phases/09-hook-seeding-safety/09-02-SUMMARY.md` when done.
</output>
