# Deferred Items — Phase 11

Out-of-scope discoveries logged during execution (not fixed; pre-existing, unrelated to phase tasks).

## From 11-01 execution (2026-09-16)

- **Pre-existing clippy failures (RESOLVED in phase 12, plan 12-01):** `cargo clippy --all-targets -- -D warnings` (the CI invocation) failed with exit 101 on (a) `clippy::cargo`-group package-metadata lints (`missing package.readme/keywords/categories` for `baude`, `baude-core`, `bauded`) and (b) ~40 errors in the vendored `vendor/vt100` crate (`derivable_impls`, `default_constructed_unit_structs`, and similar). None originated in `baude/src/keys.rs`, `baude/src/main.rs`, or `baude/src/app.rs` (verified: zero clippy diagnostics attributed to those files). **Correction to this entry's original cause:** the toolchain hypothesis recorded here was wrong. No toolchain is pinned anywhere in this repo (there is no `rust-toolchain` file, and the workflows reference `rustup` only for `rustup target add`), so CI runs whatever Rust the GitHub runner image ships and would have failed this gate at least as hard as local did. The real cause was that `vendor/vt100` is a first-class workspace member whose own in-source lint header (`#![warn(clippy::{cargo,pedantic,nursery})]`) made it fail to compile under `-D warnings`; because `baude-core` depends on it and `baude` on `baude-core`, **baude's own crates were never linted at all**, masking two genuine lints in phase-10 code: `clippy::manual_flatten` at `baude/src/links.rs:201` and `clippy::manual_clamp` at `baude/src/ui.rs:2120`. Phase 12 resolved all three sites (fork lint header relaxed inside one commented `FORK (baude)` block, plus the two mechanical rewrites in baude's own source); the gate now exits 0 with `Checking baude v2.1.5` present in its output. Closed in `.planning/phases/12-validation-and-v2-2-0-release/12-01-PLAN.md`.
  status: acknowledged
