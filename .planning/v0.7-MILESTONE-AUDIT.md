---
milestone: v0.7
audited: 2026-06-15
status: tech_debt
scores:
  requirements: 14/14 code-complete (9 fully verified, 5 human-UAT-pending)
  phases: 4/4 code-complete
  integration: 5/5 cross-phase seams wired (CLEAN)
  flows: clean — no broken flows
gaps: {}  # No critical blockers, no unsatisfied/orphaned requirements, no broken integration flows.
tech_debt:
  - phase: all (v0.7)
    items:
      - "Deferred verification: 5 requirements code-complete + integration-wired but pending HUMAN UAT (visual/live-claude renders, not code gaps): ACT-03 (PWA activity strip visual), ACT-04 (TUI `v` overlay visual), PERM-01/PERM-02 (live `claude` 2.1.178 --permission-prompt-tool MCP wire contract — the §F CONTRACT gate), PERM-03 (PWA approve/deny card visual). All tracked in per-phase UAT.md files."
  - phase: 02-hook-driven-status
    items:
      - "Known limitation (documented, non-fatal): daemon-seeded $BAUDE_EVENT_URL hardcodes the loopback DEFAULT_BIND (127.0.0.1:8642), so a custom --bind port is not honored by daemon-seeded hooks. Same-host reachability holds; deferred by design."
  - phase: 04-remote-permission-approval
    items:
      - "MCP wire contract is MEDIUM-confidence (no complete official example, claude-code #1175) — the bridge encodes the ASSUMED §F contract, isolated to parse_frame/parse_tool_call/build_approve_result so a live-claude divergence is a 3-function fix."
      - "First-real-phone Web Push verification (bell/permission push on a device) is still outstanding from v0.5 — a separate deferred milestone task, not a v0.7 code gap."
  - phase: backlog
    items:
      - "BL-02: model/permission-mode/planning-mode not shown for every session (Phase 1 follow-up — capture-vs-render gap to investigate)."
      - "BL-03: wire GSD phase/state into the sidebar (new feature idea, placement TBD)."
---

# v0.7 Native Claude Integration — Milestone Audit

**Status:** tech_debt (no blockers; all code-complete + integration-clean; accumulated deferred human-UAT verification + documented limitations to review)

## Requirements Coverage (14/14 code-complete)

| Req | Phase | Code | Verified | Note |
|-----|-------|------|----------|------|
| STL-01/02/03 | 1 | ✓ | ✓ passed* | Full status-line capture + overlay |
| HOOK-01/02/03 | 2 | ✓ | ✓ passed (Claude-validated live) | Hook event stream, StateSource, dual transport |
| ACT-01/02 | 3 | ✓ | ✓ passed (data feed Claude-validated live) | Ring buffer + daemon API/SSE |
| ACT-03 | 3 | ✓ | ⏳ human UAT | PWA activity strip — visual render |
| ACT-04 | 3 | ✓ | ⏳ human UAT | TUI `v` overlay — visual render |
| PERM-01 | 4 | ✓ | ⏳ human UAT | Mode switch wired; live MCP wire contract pending (§F gate) |
| PERM-02 | 4 | ✓ | ⏳ human UAT | Permission API + bridge round-trip Claude-validated live; live-claude wire shape pending |
| PERM-03 | 4 | ✓ | ⏳ human UAT | PWA approve/deny card — visual render |
| PERM-04 | 4 | ✓ | ✓ passed (Claude-validated live) | waiting_reason + distinct push |

\* P1 verified human_needed at the time (overlay render); foundation consumed by P2-P4.

## Cross-Phase Integration: CLEAN (5/5 seams)

1. P2 hook events → P3 activity ring — one event model, one tail loop, one `/tmp` file. WIRED.
2. P2 Notification → P4 `waiting_reason` → distinct push + PWA card. WIRED.
3. Shared spawn-command env injection serves both hook + permission-mcp; both subcommands dispatched in both binaries (no daemonize trap). WIRED.
4. Daemon route table: `/event` + `/activity` + `/activity-stream` + `/permission` coexist. WIRED.
5. SessionInfo accreted fields (state_source, last_tool, activity, waiting_reason) + RemoteInfo `#[serde(default)]` mirror — clean back-compat. WIRED.

No broken flows. No orphaned requirements. Workspace builds clean; full suite green (baude-core 100, bauded 58, baude 2).

## Validation summary

All four phases' data paths were drive-validated live by Claude (headlessly, against the real binaries/daemon). **4 real integration bugs found and fixed** that the unit tests missed:
- `bauded hook` booted a 2nd daemon instead of emitting events (P2).
- daemon ingest required a poll-resolved session_id, dropping early events (P2).
- permission bridge POSTed `text/plain` → daemon `Json` extractor 415 → prompt mode silently denied every tool (P4).
- (+ code-review: 6 P2, 2 P3, 4 P4 warnings — all fixed.)

## Deferred / Tech Debt

See frontmatter `tech_debt`. The headline item is **deferred human verification** of 5 code-complete requirements (visual TUI/PWA renders + the live-`claude` MCP wire contract) — observation gaps, not code gaps — plus the documented `--bind` limitation, the MEDIUM-confidence MCP wire contract, and the outstanding first-phone Web Push check.
