---
phase: 12-validation-and-v2-2-0-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vendor/vt100/src/lib.rs
  - baude/src/links.rs
  - baude/src/ui.rs
  - .planning/phases/11-negotiated-multiline-input/deferred-items.md
autonomous: true
requirements: [SHIP-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`cargo clippy --all-targets -- -D warnings` exits 0 from a clean tree (D-04)."
    - "Clippy's output contains a `Checking baude v2.1.5` line, proving baude's own crates were actually linted rather than skipped behind a failed dependency."
    - "The full CI-parity bracket — fmt, clippy, real-roots self-test, roots-before, the serial workspace suite, roots-after — passes locally with every exit code observed directly."
    - "`cargo build --workspace --release --locked` succeeds with the vendored fork in the workspace (D-10)."
    - "The phase-11 deferred ledger no longer asserts that CI runs on a pinned toolchain."
  artifacts:
    - vendor/vt100/src/lib.rs
    - baude/src/links.rs
    - baude/src/ui.rs
    - .planning/phases/11-negotiated-multiline-input/deferred-items.md
  key_links:
    - "The fork's in-source lint header is what gates whether baude's own crates are linted at all — relaxing it is the only thing that makes the two real baude lints visible."
    - "`scripts/assert-real-roots-untouched.sh before`/`after` brackets the suite; without both halves the isolation claim is unproven."
    - "`.github/workflows/ci.yml:15-43` is the definition of parity — every local gate string must match it character for character."
  prohibitions:
    - "The clippy gate is never made green by widening the allow scope beyond the vendored fork. No allow attribute for a clippy lint group may be added to baude/, baude-core/, or bauded/ sources."
    - "The three `baude`/`baude-core`/`bauded` manifests are not given publishing metadata (readme/keywords/categories) and the `test-support` feature is not renamed — those lints exist only because the fork opted into `clippy::cargo`."
---

<objective>
Take the clippy gate from exit 101 to exit 0 with the three proven edits, then
prove the entire CI-parity bracket green locally — before the branch is ever
pushed. This is the tracer for the whole phase: it is the thinnest end-to-end
path through every layer the phase touches (the vendored fork, baude's own
source, the gate ladder, the release build), and a red result here would cost a
full CI round trip later under `enforce_admins: true`.

Decisions implemented: **D-01** (one plan runs the full local ladder),
**D-04** (the vendored-vt100/metadata clippy lints are resolved now so the gate
is a clean exit 0), **D-10** (the locked release build is the packaging
verification — no packaging rework), **D-14** (Claude's discretion on fix vs
allow-list; research proved the narrow route and this plan takes it).

Purpose: SHIP-01's local half. Three of `main`'s required status contexts
(`check (macos-14)`, `check (ubuntu-22.04)`, `docker`) run these same commands,
and `enforce_admins: true` means no maintainer can merge around a red one.
Output: a clean tree that clippy, fmt, the serial workspace suite, the
real-roots bracket, and the locked release build all accept.
</objective>

<execution_context>
@~/.poindexter/claude/gsd-core/workflows/execute-plan.md
@~/.poindexter/claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-validation-and-v2-2-0-release/12-CONTEXT.md
@.planning/phases/12-validation-and-v2-2-0-release/12-RESEARCH.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the clippy gate green end to end — all three edits or none</name>
  <read_first>
    - `.planning/phases/12-validation-and-v2-2-0-release/12-RESEARCH.md` section `## Clippy Remediation` (the full classification of all 42 diagnostics and the two-step proof) and section `## Code Examples` → "The vendored-fork lint header (the proven fix)" — the replacement block is given there verbatim.
    - `vendor/vt100/src/lib.rs` lines 30-50 — the current upstream lint header. The three group-`warn` lines sit at 36, 37, 38; the two narrow `warn` lines (`as_conversions`, `get_unwrap`) at 39-40 and the seven `allow` lines at 41-47 are kept as-is.
    - `baude/src/links.rs` lines 195-212 — the anchor-span loop.
    - `baude/src/ui.rs` lines 2114-2124 — the link-hints row-count expression.
    - `.github/workflows/ci.yml` line 20 — the exact clippy invocation CI runs. Match it character for character; do not substitute a `--workspace` form.
  </read_first>
  <files>vendor/vt100/src/lib.rs, baude/src/links.rs, baude/src/ui.rs</files>
  <precondition>`git status --porcelain vendor/vt100/src/lib.rs baude/src/links.rs baude/src/ui.rs` prints nothing. Research patched and then reverted these three files during the research session, and `target/` is stale relative to that experiment — a leftover patch or a cached green would make this gate read as passing on work that was never committed.</precondition>
  <behavior>
    These are mechanical rewrites of behavior-bearing baude source that is already covered by phase-10 tests. No new test file is written; the existing focused suites are the contract, and they must stay green across the edit.
    - Anchor-span detection (`baude/src/links.rs`): for a candidate whose visible cells span `cells[i..end]`, `end_col` still resolves to the column of the last cell whose row equals the anchor's row. Cells on other rows and `None` cells still contribute nothing. Covered by the link-detection tests in `baude/src/links.rs` and by `vendor/vt100/tests/link_fidelity.rs` (16 tests).
    - Link-hints row count (`baude/src/ui.rs`): the overlay still renders at least 1 and at most 10 rows. 0 links renders 1 row; 3 links renders 3; 25 links renders 10. The bounds are the literals 1 and 10, so the clamp form cannot panic.
    - The vendored fork's runtime behavior is unchanged — only its in-source lint header moves.
  </behavior>
  <action>
    Per **D-04** and **D-14**, apply exactly three edits. Research proved this is a closed-form fix: with only the fork relaxed the entire remaining clippy output is the two baude diagnostics, and with those two allowed the run reaches `Finished` with nothing else hiding behind them. Partial application is the documented failure mode (RESEARCH Pitfall 2) — cargo stops dependent crates when a dependency fails, so the error tail reads as though the fork were the only problem.

    1. `vendor/vt100/src/lib.rs`: replace the three group-level `warn` attribute lines (currently lines 36-38, the `cargo` / `pedantic` / `nursery` trio) with the fork-marker comment plus group-level relaxations exactly as written in 12-RESEARCH.md `## Code Examples`. Copy that block verbatim — comment text included, since the comment is what keeps the fork's diff-vs-upstream surface auditable per `vendor/vt100/README.md`. Leave the two narrow `warn` lines (`as_conversions`, `get_unwrap`) and all seven existing `allow` lines untouched. This is the only edit to the fork; do not touch `vendor/vt100/Cargo.toml` (a `[lints]` table lowers to CLI flags and cannot override an in-source attribute) and do not remove `vendor/vt100` from the workspace members list (that would reshape `Cargo.lock` on the eve of a release).

    2. `baude/src/links.rs` around line 201: apply clippy's own `manual_flatten` suggestion to the anchor-span loop — iterate the flattened `Option` cells directly instead of matching inside the loop body. The surrounding comment about off-screen schemes stays.

    3. `baude/src/ui.rs` line 2120: replace the chained min/max row-count expression with the single clamp form clippy suggests, bounds 1 and 10.

    Then run the focused suites named in `<verify>`. Do not run the full workspace suite here — that is Task 2's job.
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings && cargo test -p baude links -- --test-threads=1 && cargo test -p vt100 --test link_fidelity && cargo test -p vt100 --test kitty_keyboard</automated>
    <fails_when>Clippy exits non-zero (today it exits 101 with `error: could not compile \`vt100\` (lib) due to 40 previous errors`), or exits 0 but its output never contains a line beginning `Checking baude v2.1.5` — which means baude's own crates were skipped behind a failed dependency and the gate is green for the wrong reason. Also fails if any focused link or kitty-keyboard test regresses, which is the signal that a mechanical rewrite changed behavior.</fails_when>
    <automated>! grep -rn --include='*.rs' -E 'allow\(clippy::(all|pedantic|nursery|cargo)\)' baude/src baude-core/src bauded/src</automated>
    <fails_when>A clippy group relaxation leaked out of the vendored fork into baude's own crate sources — the prohibited route to a green gate. A Rust comment merely mentioning such an attribute also trips this, which is the safe direction: it fails loudly rather than passing silently.</fails_when>
  </verify>
  <acceptance_criteria>
    - `cargo clippy --all-targets -- -D warnings` exits 0.
    - Clippy's output contains `Checking baude v2.1.5` (proof the dependent crates were reached).
    - `grep -rn --include='*.rs' -E 'allow\(clippy::(all|pedantic|nursery|cargo)\)' baude/src baude-core/src bauded/src` returns zero hits.
    - `grep -n 'FORK (baude)' vendor/vt100/src/lib.rs` returns at least one hit.
    - `cargo test -p baude links -- --test-threads=1`, `cargo test -p vt100 --test link_fidelity`, and `cargo test -p vt100 --test kitty_keyboard` all exit 0.
    - `git show --stat --name-only HEAD` for this task's commit lists exactly `vendor/vt100/src/lib.rs`, `baude/src/links.rs`, and `baude/src/ui.rs`, and no `Cargo.toml` or `Cargo.lock`.
  </acceptance_criteria>
  <done>The clippy gate exits 0 from a clean tree, baude's own crates were demonstrably linted, the relaxation is confined to one commented block in the vendored fork, and the phase-10 link and keyboard suites are green. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full CI-parity bracket plus the locked release build</name>
  <read_first>
    - `.github/workflows/ci.yml` lines 15-43 — the `check` job's step order. The local ladder must be the same six commands in the same order, including `--test-threads=1` (serial execution is deliberate, issue #58: the manager close-rollback test fails only on loaded 3-core runners).
    - `.planning/phases/12-validation-and-v2-2-0-release/12-RESEARCH.md` section `## CI Parity` → "The precise local command set that reproduces CI" and the measured-result table (645 tests, ~4.5 min serial, all four roots untouched).
    - `scripts/assert-real-roots-untouched.sh` — the `--self-test` / `before` / `after` contract built in phase 8.
  </read_first>
  <files>(no files modified — this task is a gate)</files>
  <action>
    Per **D-01**, run the complete ladder locally in CI's order, capturing each exit code directly. Never route a gate through a pipe to `grep` or `tail` and then read `$?` — that masks the real failure and is a recorded project policy. The `&&` chain in `<verify>` preserves each exit code; use it as written.

    Order: `cargo fmt --check` → `cargo clippy --all-targets -- -D warnings` → `bash scripts/assert-real-roots-untouched.sh --self-test` → `bash scripts/assert-real-roots-untouched.sh before` → `cargo test -- --test-threads=1` → `bash scripts/assert-real-roots-untouched.sh after`. Then, per **D-10**, `cargo build --workspace --release --locked` — the packaging verification CONTEXT locks in place of any packaging rework, proving the vendored path dependency survives a locked release build.

    Expect roughly 4.5 minutes for the serial suite and a few more for the release build; `target/` is stale relative to Task 1's edits so the first clippy and build will be cold. Do not trust a cached green from before Task 1.

    One operational note the `&&` chain cannot express: CI runs the `after` roots check with `if: always()`. If the workspace suite fails, still run `bash scripts/assert-real-roots-untouched.sh after` by hand before investigating the failure — a suite that both failed and leaked into a real root is a materially different problem from one that only failed.

    Record in the plan SUMMARY: each gate's name and observed exit code, the total test count from the suite's own output, and the four real-root paths the bracket fingerprinted on this host. That record is SHIP-01's local evidence and is cited later by the phase-8 re-stamp checklist item, because a green `before`/`after` bracket around a full suite run is exactly the measurement phase 8's verifier was forbidden to perform.
  </action>
  <verify>
    <automated>cargo fmt --check && cargo clippy --all-targets -- -D warnings && bash scripts/assert-real-roots-untouched.sh --self-test && bash scripts/assert-real-roots-untouched.sh before && cargo test -- --test-threads=1 && bash scripts/assert-real-roots-untouched.sh after && cargo build --workspace --release --locked</automated>
    <fails_when>Any gate in the ladder exits non-zero. Specific signals: `cargo fmt --check` prints a diff; clippy returns anything but `Finished`; the roots self-test reports fewer than 40 checks or any failure; the `after` check prints anything other than the pass line asserting all four real roots were left untouched; the workspace suite reports any failure or a test count materially below the 645 research measured; the locked release build fails to resolve against `Cargo.lock`.</fails_when>
  </verify>
  <acceptance_criteria>
    - All seven commands exit 0, each exit observed directly rather than through a pipe.
    - The suite's own output reports 0 failures.
    - `assert-real-roots-untouched.sh after` reports that all four real roots were left untouched.
    - The SUMMARY records each gate name, its exit code, the test count, and the four fingerprinted root paths.
  </acceptance_criteria>
  <done>The full CI-parity bracket and the locked release build are green locally, with per-gate exit codes recorded in the SUMMARY as SHIP-01's local evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the phase-11 deferred ledger's pinned-toolchain claim</name>
  <read_first>
    - `.planning/phases/11-negotiated-multiline-input/deferred-items.md` line 7 — locate the pre-existing-clippy-failures entry and the clause at its end that speculates about a toolchain difference on CI.
    - `.planning/phases/12-validation-and-v2-2-0-release/12-RESEARCH.md` section `## CI Parity` — "No toolchain is pinned anywhere": there is no `rust-toolchain` file and the workflows reference `rustup` only for `rustup target add`. Also `## State of the Art` → "Deprecated / stale in this repo".
  </read_first>
  <files>.planning/phases/11-negotiated-multiline-input/deferred-items.md</files>
  <action>
    The ledger records a wrong cause, and the next reader will trust it. Replace the pinned-toolchain hypothesis with what was actually true: there is no pinned toolchain in this repo, CI uses whatever Rust the GitHub runner image ships, and the vendored fork's compile failure under `-D warnings` was masking two genuine lints in baude's own source (`baude/src/links.rs` and `baude/src/ui.rs`) because the dependent crates were never reached. Note that phase 12 resolved all three sites and cite this plan as where it was closed. Keep the ledger's existing entry format and do not restructure unrelated entries.
  </action>
  <verify>
    <automated>! grep -niE 'presumed green' .planning/phases/11-negotiated-multiline-input/deferred-items.md</automated>
    <fails_when>The stale speculative clause is still present in the ledger, which would leave a future reader with a wrong causal story for why the clippy gate was red. This gate targets the speculation's own phrasing rather than the subject it speculated about, so the corrected prose cannot trip it.</fails_when>
    <automated>grep -nE 'links\.rs' .planning/phases/11-negotiated-multiline-input/deferred-items.md && grep -nE 'ui\.rs' .planning/phases/11-negotiated-multiline-input/deferred-items.md && grep -nE '12-01|phase 12|Phase 12' .planning/phases/11-negotiated-multiline-input/deferred-items.md</automated>
    <fails_when>The corrected entry does not name the two baude source files whose lints were masked, or does not point at where the item was actually closed — leaving the deferral dangling and the real cause unrecorded.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -niE 'presumed green' .planning/phases/11-negotiated-multiline-input/deferred-items.md` returns zero hits.
    - The ledger entry names the real cause (no toolchain is pinned anywhere in this repo; the fork's compile failure masked two baude lints) and names `baude/src/links.rs` and `baude/src/ui.rs`.
    - The entry points at phase 12 as where it was closed.
    - No other entry in the ledger is restructured.
  </acceptance_criteria>
  <done>The phase-11 deferred ledger records the true cause of the red clippy gate and points at its closure in phase 12. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored fork → baude's own crates | `vendor/vt100` is a first-class workspace member; its in-source lint header governs whether baude's crates are linted at all. A change here has blast radius beyond the fork. |
| local test suite → the developer's real filesystem | The suite runs against injected fixture roots, but a resolver bug would reach `~/.config/baude`, `~/.poindexter/claude`, `~/.local/share/baude/worktrees`, or `~/Code`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | `vendor/vt100/src/lib.rs` lint header | medium | mitigate | Confine the relaxation to one commented `FORK (baude)` block inside the fork; Task 1's second gate greps baude's own crate sources and fails if any clippy group relaxation leaked there. Future re-vendoring must re-review rather than trust the allow. |
| T-12-02 | Information Disclosure | workspace test suite vs. real user roots | high | mitigate | Bracket the suite with `scripts/assert-real-roots-untouched.sh before` / `after` (40 self-checks); a non-zero `after` fails Task 2. This is the phase-8 harness, unchanged. |
| T-12-03 | Tampering | stale `target/` from the research session's patch-and-revert | medium | mitigate | Task 1 carries a `<precondition>` asserting the three source files are clean, and Task 2's action forbids trusting a cached green from before Task 1's edits. |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs zero external packages. `12-RESEARCH.md` `## Package Legitimacy Audit` records "not applicable": no `cargo add`, no new `[dependencies]`, and the one third-party surface (`vendor/vt100`) is an already-committed fork audited in phase 10 with provenance in `vendor/vt100/README.md`. No install task exists, so no legitimacy checkpoint is warranted. |
</threat_model>

<verification>
- `cargo clippy --all-targets -- -D warnings` exits 0 and its output contains `Checking baude v2.1.5`.
- The six-command CI-parity bracket exits 0 end to end, each exit observed directly.
- `cargo build --workspace --release --locked` exits 0.
- No clippy group relaxation exists outside `vendor/vt100`.
- The phase-11 deferred ledger no longer speculates that CI was green where local was red, and names the real cause instead.
</verification>

<success_criteria>
The local half of SHIP-01 is proven: every gate the required `check` contexts
run is green on this machine, from a clean tree, with the fix confined to one
commented block in the vendored fork plus two mechanical rewrites in baude's own
source. The branch is now safe to push.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Notes |
|----------|------|-------|
| `vendor/vt100/src/lib.rs` — `FORK (baude)` clippy allow marker | source edit | The fork's diff-vs-upstream surface; one commented block. |
| `baude/src/links.rs` — flattened anchor-span loop | source edit | Mechanical; covered by existing phase-10 tests. |
| `baude/src/ui.rs` — clamped link-hints row count | source edit | Mechanical; bounds are literals 1 and 10. |
| `.planning/phases/11-negotiated-multiline-input/deferred-items.md` | ledger correction | Replaces the pinned-toolchain hypothesis with the real cause. |
| Per-gate exit-code record | SUMMARY section | SHIP-01's local evidence; cited by the phase-8 re-stamp checklist item in plan 12-04. |
</artifacts>

<output>
Create `.planning/phases/12-validation-and-v2-2-0-release/12-01-SUMMARY.md` when done
</output>
