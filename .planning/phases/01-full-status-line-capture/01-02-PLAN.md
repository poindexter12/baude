---
phase: 01-full-status-line-capture
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - baude-core/src/meta.rs
autonomous: true
requirements: [STL-02]
must_haves:
  truths:
    - "ClaudeMeta carries effort, thinking, vim_mode, pr (PrInfo), and worktree (WorktreeInfo) as optional/additive fields"
    - "read_bridge_file populates the new fields from a schema:2 bridge file using Value accessors, leaving them None when absent"
    - "A pre-existing reader shape (legacy / schema:1 file with only the four old fields) still parses without error — legacy fields present, new fields None"
    - "Model precedence: bridge value wins when present; the transcript-derived model survives when the bridge omits it (guarded by if let Some)"
    - "Reader logic never branches on the schema discriminant"
  artifacts:
    - path: "baude-core/src/meta.rs"
      provides: "PrInfo and WorktreeInfo reader-side sub-structs; new Option fields on ClaudeMeta"
      contains: "struct PrInfo"
    - path: "baude-core/src/meta.rs"
      provides: "extended read_bridge_file reading effort/thinking/vim_mode/pr/worktree/model"
      contains: "self.effort"
    - path: "baude-core/src/meta.rs"
      provides: "#[cfg(test)] mod tests for v2 round-trip, legacy/schema:1, and nested-object reads"
      contains: "mod tests"
  key_links:
    - from: "baude-core/src/meta.rs::read_bridge_file"
      to: "baude-core/src/bridge.rs bridge file keys (effort, thinking, vim_mode, pr, worktree, model)"
      via: "reads the keys written by build_bridge in plan 01-01, via Value .as_*() accessors"
      pattern: "v\\[\"effort\"\\]"
    - from: "read_bridge_file"
      to: "self.model"
      via: "if let Some(m) = v[\"model\"].as_str() — bridge wins when present, transcript value survives otherwise"
      pattern: "if let Some\\(m\\) = v\\[\"model\"\\]"
---

<objective>
Grow the `ClaudeMeta` reader (baude-core/src/meta.rs) with the new optional fields written by the bridge in plan 01-01, populating them in `read_bridge_file()` with the same `Value`-accessor pattern the rate-window reads already use. Implements the reader half of STL-02 (additive deserialization + back-compat).

Purpose: `ClaudeMeta` is the in-memory model the TUI (and daemon) consume. The new fields must be additive and optional so an older reader still parses a `schema: 2` file and the new reader still parses a legacy/`schema: 1` file — the back-compat guarantee that is an emergent property of `Value` parsing on both sides.

Output: `PrInfo`/`WorktreeInfo` reader-side sub-structs, five new `Option`/`Default` fields on `ClaudeMeta`, extended `read_bridge_file()` reads, explicit model precedence (bridge-wins-when-present), and a net-new `#[cfg(test)] mod tests` for v2 round-trip / legacy / nested-object reads.
</objective>

<execution_context>
@$HOME/.claude-poindexter/gsd-core/workflows/execute-plan.md
@$HOME/.claude-poindexter/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-full-status-line-capture/01-RESEARCH.md
@.planning/phases/01-full-status-line-capture/01-PATTERNS.md
@.planning/phases/01-full-status-line-capture/01-VALIDATION.md

@baude-core/src/meta.rs
@bauded/src/transcript.rs

# Bridge file key contract produced by plan 01-01 (read its SUMMARY for the exact emitted keys):
@.planning/phases/01-full-status-line-capture/01-01-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Grow ClaudeMeta with the new optional fields and read them back (RED → GREEN → REFACTOR)</name>
  <files>baude-core/src/meta.rs</files>
  <read_first>
    - baude-core/src/meta.rs lines 38-100 (ClaudeMeta struct + RateWindow sub-struct + poll order — note read_bridge_file runs AFTER read_transcript_tail at lines 94/98, so the bridge can overwrite the transcript-derived model) and lines 240-290 (read_bridge_file reader, read_json free function). The load-bearing analogs: every ClaudeMeta field is already `Option`/`Default`; `RateWindow` (lines 48-52) is the in-file template for a small typed reader-side sub-struct; the `window` closure (lines 257-262) is the nested-object read template (`w.is_object().then(|| ...)`).
    - .planning/phases/01-full-status-line-capture/01-PATTERNS.md "baude-core/src/meta.rs" section — struct, sub-struct, and reader target shapes; the explicit model-precedence note (bridge wins when present, `if let Some` guard prevents overwriting with None).
    - bauded/src/transcript.rs lines 300-321 — temp-file test idiom: `std::env::temp_dir().join(format!("...-{}", std::process::id()))`, write fixture, assert, `remove_dir_all(&dir).ok()`.
    - The 01-01-SUMMARY.md bridge key contract — confirm the exact keys the writer emits (`effort`, `thinking`, `vim_mode`, `pr` object, `worktree` object, `model`).
  </read_first>
  <behavior>
    Write these tests FIRST. Because `read_bridge_file` is private and keys off `self.session_id` + `crate::bridge::bridge_path(sid)` (which hardcodes `/tmp/baude-usage-<sid>.json`), use a unique pid-keyed session_id so the test writes a real bridge file at the path `bridge_path` returns, sets `meta.session_id`, calls the reader (add a `#[cfg(test)]`-visible thin helper if needed to invoke the private fn), then asserts and cleans up the temp file.
    - `reads_v2_bridge`: write a schema:2 fixture with all new fields (effort, thinking, vim_mode, pr object with number/url/review_state, worktree object, model) → after read, ClaudeMeta.effort/thinking/vim_mode/pr/worktree all populated; pr.number/url/review_state and worktree.name/path/branch match; legacy fields (cost, context, rate windows) still read.
    - `reads_legacy_bridge`: write a schema:1 (or schema-absent) fixture containing ONLY the four legacy fields → legacy fields present; new fields all `None` (proves additive back-compat; the older-reader direction is covered by the `Value`-accessor design, this is the new-reader-reads-old-file direction).
    - `does_not_branch_on_schema`: a fixture with `schema: 99` and the new fields present → new fields still read (proves no `if schema == 2` gate).
    - `pr_absent_is_none`: fixture with no `pr` key → ClaudeMeta.pr is `None` (and a fixture where `pr` exists but `review_state` is absent → PrInfo.review_state is `None`, pr still Some).
    - `model_bridge_wins_then_survives`: set `meta.model = Some("from-transcript")`, read a bridge file WITH `model` → model becomes the bridge value; then set `meta.model = Some("keep-me")`, read a bridge file WITHOUT `model` → model stays "keep-me" (guard prevents None overwrite).
  </behavior>
  <action>
    RED: Add a `#[cfg(test)] mod tests` to meta.rs with the behaviors above. Run; it must fail because the new fields/sub-structs do not exist. Commit `test(01-02): add failing tests for additive ClaudeMeta bridge reads`.

    GREEN: Add reader-side sub-structs `PrInfo { number: Option<u64>, url: Option<String>, review_state: Option<String> }` and `WorktreeInfo { name: Option<String>, path: Option<String>, branch: Option<String> }`, both `#[derive(Default, Clone)]` (Clone not Copy — they hold `Option<String>`), modeled on `RateWindow` (lines 48-52). Add to `ClaudeMeta`: `effort: Option<String>`, `thinking: Option<bool>`, `vim_mode: Option<String>`, `pr: Option<PrInfo>`, `worktree: Option<WorktreeInfo>` (additive; `#[derive(Default)]` covers init). In `read_bridge_file`, AFTER the existing rate-window reads, append: `if let Some(s) = v["effort"].as_str() { self.effort = Some(s.to_string()); }`, the same for `thinking` (`.as_bool()`) and `vim_mode` (`.as_str()`); read `pr` and `worktree` with an `if v["..."].is_object()` guard mirroring the `window` closure, populating the sub-structs; and add model precedence `if let Some(m) = v["model"].as_str() { self.model = Some(m.to_string()); }` (bridge wins when present, guard preserves transcript value otherwise). Do NOT branch on `schema`; read every field optionally. Run tests until green. Commit `feat(01-02): grow ClaudeMeta with effort/thinking/vim/pr/worktree from bridge`.

    REFACTOR (if needed): tidy for clippy/fmt (watch needless field-init, redundant clone). Keep `read_json` as the testable seam — do not switch to `#[derive(Deserialize)]` on the bridge file. Commit `refactor(01-02): ...` only if changes were made.
  </action>
  <verify>
    <automated>cargo test -p baude-core meta && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - `PrInfo` and `WorktreeInfo` exist (`#[derive(Default, Clone)]`); `ClaudeMeta` has `effort`, `thinking`, `vim_mode`, `pr`, `worktree` as optional/additive fields.
    - `read_bridge_file` populates all five from a schema:2 file and leaves them `None` when absent.
    - Legacy/schema:1 fixture parses: old fields present, new fields `None`.
    - No reader branch on `schema` (the schema:99 test passes).
    - Model precedence test passes: bridge wins when present, transcript value survives when bridge omits model.
    - `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check` clean.
  </acceptance_criteria>
  <done>STL-02 reader half complete: ClaudeMeta carries the new optional fields, reads them additively from a schema:2 bridge file, back-compat holds both directions, model precedence is explicit, and a `#[cfg(test)] mod tests` proves it — all three CI gates green for baude-core.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /tmp/baude-usage-<sid>.json → meta.rs::read_bridge_file | The bridge file (written by the same process tree in plan 01-01) is read into the in-memory ClaudeMeta model. Parsed via serde_json with `.as_*()` accessors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | Malformed / wrong-type fields in the bridge file (e.g. `effort` as a number, `pr` as a string) | mitigate | All reads use `.as_str()` / `.as_bool()` / `.as_u64()` / `is_object()` which return `None` on type mismatch — field stays `None`, no panic. The `does_not_branch_on_schema` and `pr_absent_is_none` tests exercise absent/odd shapes. No `unwrap()` on new fields. |
| T-01-05 | Denial of Service | Oversized bridge file | accept | `read_json` reads the file via `fs::read_to_string` (pre-existing, unchanged); the writer is the local bridge. No new unbounded read introduced. |
| T-01-06 | Information Disclosure | pr.url / model strings stored in memory | accept | No new exposure surface in this plan — fields are held in `ClaudeMeta` and only rendered locally in plan 03 via ratatui text. No logging of payload strings added. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies — Cargo.toml not modified. RESEARCH Package Legitimacy Audit: N/A (zero external packages). Adding a crate is a red flag — stop. |

No `high`-severity threats are introduced by this plan (security_block_on: high).
</threat_model>

<verification>
- `cargo test -p baude-core meta` — new meta tests green.
- `cargo test -p baude-core` — bridge + meta tests both green together.
- `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check` — clean.
</verification>

<success_criteria>
- `ClaudeMeta` carries effort/thinking/vim_mode/pr/worktree as additive optional fields.
- A schema:2 bridge file fully populates them; a legacy file leaves them `None`; no reader logic branches on `schema`.
- Model precedence is bridge-wins-when-present without clobbering the transcript value with `None`.
- All three CI gates pass for baude-core.
</success_criteria>

<output>
Create `.planning/phases/01-full-status-line-capture/01-02-SUMMARY.md` when done.
</output>
