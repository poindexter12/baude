---
phase: 01-full-status-line-capture
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - baude/src/ui.rs
autonomous: true
requirements: [STL-03]
must_haves:
  truths:
    - "Selecting a session and pressing `i` shows that session's effort, thinking mode, and PR state in the local info overlay"
    - "Each new row is omitted (not shown as a placeholder) when the underlying ClaudeMeta field is absent"
    - "The remote info branch is untouched (Phase 1 is local-TUI only)"
    - "vim_mode is NOT rendered (captured-but-not-rendered per the locked scope decision)"
  artifacts:
    - path: "baude/src/ui.rs"
      provides: "effort, thinking, and pr conditional rows in the local Modal::Info branch"
      contains: "row(\"effort\""
  key_links:
    - from: "baude/src/ui.rs local Modal::Info branch"
      to: "ClaudeMeta.effort / ClaudeMeta.thinking / ClaudeMeta.pr"
      via: "conditional lines.push(row(...)) guarded by if let Some on m.effort / m.thinking / m.pr"
      pattern: "if let Some\\(pr\\) = &m\\.pr"
---

<objective>
Surface the session's effort, thinking mode, and PR state in the local `i` info overlay (baude/src/ui.rs, the local `Modal::Info` branch). Implements STL-03.

Purpose: Completes the user-facing goal of Phase 1 — pressing `i` on a selected session shows the new first-party fields the bridge now captures.

Scope (locked): LOCAL `Modal::Info` branch only (the `app.selected()` path, lines ~834-908). Do NOT touch the remote info branch (lines ~780-832) or `SessionInfo`/`RemoteInfo` — remote/PWA parity is deferred to Tier 2. Do NOT render `vim_mode` — it is captured (plan 01-01/02) but not rendered (STL-03 lists only effort/thinking/pr).

Output: three conditional rows (`effort`, `thinking`, `pr`) pushed into the overlay's `lines` vec, reusing the existing `row`/`opt` closures.
</objective>

<execution_context>
@$HOME/.claude-poindexter/gsd-core/workflows/execute-plan.md
@$HOME/.claude-poindexter/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-full-status-line-capture/01-RESEARCH.md
@.planning/phases/01-full-status-line-capture/01-PATTERNS.md
@.planning/phases/01-full-status-line-capture/01-VALIDATION.md

@baude/src/ui.rs

# ClaudeMeta fields consumed here are defined in plan 01-02 (effort, thinking, pr: PrInfo):
@.planning/phases/01-full-status-line-capture/01-02-SUMMARY.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add effort/thinking/pr rows to the local info overlay</name>
  <files>baude/src/ui.rs</files>
  <read_first>
    - baude/src/ui.rs lines 834-908 — the local `Modal::Info` branch. Load-bearing analogs: the `row(label, value)` closure (lines 837-842) and `opt(&Option<String>)` closure (line 844), already defined — reuse, do not redefine; the conditional-push analog `if let Some(u) = &m.last_usage { lines.push(row(...)) }` (lines 871-882) — new optional rows follow this exact shape so absent fields are omitted rather than shown as `—`; the initial `lines` vec literal ends with `Line::raw("")` at line 869; overlay height auto-sizes from `lines.len()` at line 896 (no manual height bump needed).
    - .planning/phases/01-full-status-line-capture/01-PATTERNS.md "baude/src/ui.rs" section — the conditional-row target shape and the exact insertion point (after the vec literal at line 870, before the `last_usage` block at line 871).
    - 01-02-SUMMARY.md — confirm the field names/types on ClaudeMeta: `effort: Option<String>`, `thinking: Option<bool>`, `pr: Option<PrInfo>` with `PrInfo { number: Option<u64>, url, review_state: Option<String> }`.
  </read_first>
  <action>
    Insert three conditional rows into the local `Modal::Info` `lines` vec immediately after the vec literal (after line 870) and before the `last_usage` block, using the existing `row` closure and the `if let Some` conditional-push idiom (so absent fields are omitted, not shown as `—`):
    - `effort`: `if let Some(e) = &m.effort { lines.push(row("effort", e.clone())); }`
    - `thinking`: `if let Some(t) = m.thinking { lines.push(row("thinking", if t { "on".into() } else { "off".into() })); }`
    - `pr`: `if let Some(pr) = &m.pr { let n = pr.number.map(|n| format!("#{n}")).unwrap_or_else(|| "?".into()); let st = pr.review_state.clone().unwrap_or_else(|| "—".into()); lines.push(row("pr", format!("{n} ({st})"))); }`
    Do NOT modify the remote branch (lines ~780-832). Do NOT add a `vim_mode` row. Do NOT redefine `row`/`opt`/`dim`/`val`. Match the existing closure style so clippy stays quiet under `-D warnings`.
  </action>
  <verify>
    <automated>cargo build -p baude && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
    <human-check>Run baude, select a session whose bridge file has effort/thinking/pr present (or seed /tmp/baude-usage-<sid>.json with those fields), press `i`, and confirm the overlay shows `effort`, `thinking` (on/off), and `pr (#N (state))` rows; confirm rows are omitted (not shown as `—`) when those fields are absent; confirm vim mode is NOT shown.</human-check>
  </verify>
  <acceptance_criteria>
    - The local `Modal::Info` branch pushes `effort`, `thinking`, and `pr` rows guarded by `if let Some` so absent fields are omitted.
    - No row for `vim_mode`; the remote branch is unchanged.
    - `cargo build -p baude`, `cargo clippy --all-targets -- -D warnings`, and `cargo fmt --check` all pass.
  </acceptance_criteria>
  <done>STL-03 complete: pressing `i` on a selected session shows effort, thinking, and PR state in the local info overlay, with rows omitted when absent; CI gates green and the end-of-phase human verification confirms render.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ClaudeMeta strings → ratatui overlay | Payload-derived strings (model, effort, pr.url-adjacent values) are rendered in the local TUI overlay. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering / Spoofing the display | Untrusted field content (e.g. ANSI escapes in `pr.review_state` / effort) rendered in the overlay | mitigate | Rows are built as ratatui `Span`/`Line` via the existing `row` closure — ratatui renders them as text, NOT as raw terminal control sequences, so embedded escapes are not interpreted. No `print!` of payload strings outside ratatui. Only effort/thinking/pr are rendered; pr.url is not rendered. |
| T-01-08 | Denial of Service | A pathologically long field string widening the overlay | accept | Pre-existing overlay behavior; `centered(area, 76, ...)` fixes width at 76 and ratatui clips. No new risk beyond existing rows. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies — Cargo.toml not modified; ratatio/format! already in scope. Adding a crate is a red flag — stop. |

No `high`-severity threats are introduced by this plan (security_block_on: high).
</threat_model>

<verification>
- `cargo build -p baude` — compiles.
- `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check` — clean.
- Full suite at phase gate: `cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test`.
- End-of-phase human verification: the `i` overlay renders effort/thinking/pr (per human_verify_mode: end-of-phase, captured as `<human-check>`).
</verification>

<success_criteria>
- Pressing `i` on a selected session with effort/thinking/pr present renders those three rows.
- Absent fields omit their row entirely; vim mode is not rendered; remote branch untouched.
- All three CI gates pass.
</success_criteria>

<output>
Create `.planning/phases/01-full-status-line-capture/01-03-SUMMARY.md` when done.
</output>
