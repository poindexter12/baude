---
phase: 05-durable-repository-admission
plan: 03
type: tdd
wave: 1
depends_on:
  - 05-01
  - 05-02
files_modified:
  - baude/src/app.rs
  - bauded/src/manager.rs
  - baude-core/src/git.rs
autonomous: true
requirements:
  - REPO-02
  - REPO-03
  - PERS-03
must_haves:
  truths:
    - "Successful local admission or clone durably records one repository and one primary checkout before launching exactly one session through the active workspace backend."
    - "Reopening via launch directory, Open, clone completion, alias, or linked worktree focuses a live primary or resumes/restarts its retained exited child instead of creating a duplicate parent, child, or process."
    - "Persisted repository and checkout intent is rediscovered and reconciled with current Git topology immediately before reuse, worktree creation, save-before-spawn, or launch."
    - "Unavailable default/topology or failed persistence leaves durable actionable state where possible and launches nothing; a failed spawn leaves saved active intent available for retry."
    - "Restore loads/migrates hierarchy first, restarts only prior active primaries, leaves intentionally idle parents sessionless, and never overwrites malformed state."
    - "Closing a primary runtime retains its repository and checkout child and clears active intent for explicit reopening."
  artifacts:
    - path: "baude/src/app.rs"
      provides: "Production tracer slice for local repository admission, reconciliation, save-before-spawn, idempotent primary dispatch, restore, open, launch-dir, clone, close, and reopen"
      contains: "admit_repository"
    - path: "bauded/src/manager.rs"
      provides: "Safe consumer adaptation for strict shared versioned persistence and blocking load errors without Phase 8 hierarchy APIs"
      contains: "persistence_blocked"
  key_links:
    - from: "baude/src/app.rs::admit_repository"
      to: "baude_core::git::discover_repository / resolve_default_branch / ensure_default_worktree"
      via: "fresh snapshot before mutation or launch"
      pattern: "discover_repository"
    - from: "baude/src/app.rs::admit_repository"
      to: "baude_core::persist"
      via: "validated aggregate atomic save before `Pty::spawn`"
      pattern: "save.*add_session|save.*spawn"
    - from: "baude/src/app.rs::ensure_primary"
      to: "App.sessions"
      via: "stable `(RepositoryKey, CheckoutRole::PrimaryDefault)` ownership and pure dispatch decision"
      pattern: "PrimaryDispatch"
    - from: "bauded/src/manager.rs::restore"
      to: "baude_core::persist::LoadOutcome"
      via: "Missing/Legacy/Current handling plus save-disable guard after error"
      pattern: "LoadOutcome"
---

<objective>
Wire the production-quality tracer slice through the existing local App and active backend: restore/migrate durable intent, reconcile Git, atomically reserve the primary, then focus/restart/spawn exactly once. Adapt the daemon owner to the strict shared state API without crossing the Phase 8 boundary.

Purpose: REPO-02, REPO-03, and PERS-03 become observable only when the tested core contracts control every local admission path and runtime lifecycle.
Output: Idempotent App admission/restore/close/reopen behavior and non-destructive daemon persistence compatibility.
</objective>

<execution_context>
@/Users/joese/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/joese/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-durable-repository-admission/05-CONTEXT.md
@.planning/phases/05-durable-repository-admission/05-RESEARCH.md
@.planning/phases/05-durable-repository-admission/05-PATTERNS.md
@.planning/phases/05-durable-repository-admission/05-VALIDATION.md
@.planning/phases/05-durable-repository-admission/05-01-SUMMARY.md
@.planning/phases/05-durable-repository-admission/05-02-SUMMARY.md
@baude/src/app.rs
@bauded/src/manager.rs

<interfaces>
Existing App/runtime seams to preserve:
- `App::add_session(cwd, repo_root, branch, is_worktree, resume, shell_open) -> Result&lt;u64&gt;` resolves the active backend, prepares cwd, spawns a PTY, allocates a runtime id, and selects it.
- `App::restore`, `App::save`, `open_repo_session`, clone completion, and launch-directory auto-add are currently separate flat admission paths and must converge.
- `App.sessions: Vec&lt;Session&gt;` remains the runtime collection; durable repository records must not own a backend or PTY.
- `Manager::restore` / `Manager::save` share `persist` using `STATE_BASE = "daemon-state"`; `persist: false` remains the unrelated-test guard.

Contracts consumed from prior plans:
- `git::{discover_repository, resolve_default_branch, ensure_default_worktree, RepositorySnapshot, DefaultBranchUnavailable, DefaultWorktreeOutcome}`.
- `repository::{RepositoryState, RepositoryKey, CheckoutKey, CheckoutRole, CheckoutHealth}`.
- `persist::{LoadOutcome, LoadError, load_for_workspace, save_for_workspace}` with strict migration and atomic save.

New local orchestration contracts:
- `PrimaryDispatch::{Focus(runtime_id), Restart(runtime_id), Spawn, Idle}` from a pure decision over durable active intent and runtime status.
- `App::admit_repository(path)` and `App::ensure_primary(checkout_key)` as the only local admission/primary lifecycle path.
- App and Manager `persistence_blocked` guards prevent automatic saves after a load failure.
</interfaces>
</context>

<tasks>

<task id="05-03-01" type="auto" tdd="true">
  <name>Task 1: RED-GREEN idempotent primary-session admission tracer</name>
  <files>baude/src/app.rs, baude-core/src/git.rs</files>
  <read_first>
    - baude/src/app.rs startup/persistence, `add_session`, `open_repo_session`, clone completion, remove, and restart sections
    - .planning/phases/05-durable-repository-admission/05-01-SUMMARY.md
    - .planning/phases/05-durable-repository-admission/05-02-SUMMARY.md
    - .planning/phases/05-durable-repository-admission/05-CONTEXT.md Primary Session Lifecycle decisions
    - .planning/phases/05-durable-repository-admission/05-RESEARCH.md sections "Pattern 5", "Save Before Spawn", and "Session ensure" validation matrix
    - .planning/phases/05-durable-repository-admission/05-VALIDATION.md row 05-03-01 and Wave 0 primary-dispatch seam
  </read_first>
  <behavior>
    - First successful admission reserves one repository and one primary checkout, atomically saves active intent, then performs one spawn through the active workspace backend.
    - Repeated launch-dir/Open/clone/alias admission of the same repository focuses one live primary and creates no duplicate durable/runtime object.
    - Exited retained primary dispatches restart/resume; absent active dispatches spawn; absent idle dispatches no-op.
    - Save failure occurs before any spawn and leaves no process; spawn failure after save retains durable active intent for retry.
    - Unavailable default persists the parent/health cause, displays cause plus recovery action, and launches nothing.
    - Main-on-default, linked-default, and newly-created-default outcomes all launch in the verified primary checkout and use the current workspace backend rather than a persisted backend.
  </behavior>
  <action>
First extract a pure `PrimaryDispatch` decision or injectable spawn/save seam and write the `repository_admission` RED tests using harmless stand-ins, never Claude Code/OpenCode. Add the durable aggregate and runtime child association to `App`; exact collection shape follows Rust conventions, but uniqueness must be `(RepositoryKey, CheckoutRole::PrimaryDefault)`. Implement `admit_repository` as discover → find/create workspace-scoped parent → resolve default → reuse/create primary checkout → rediscover/reconcile → set active intent → validate → atomic save → `ensure_primary`. Reuse existing backend command resolution, `prepare_cwd`, spawn plan, PTY construction, runtime IDs, metadata, and focus behavior through `add_session` or an extracted equivalent. Live focuses; exited restarts with resume enabled per the locked decision; absent-active spawns; idle does nothing. Never spawn before the aggregate save, never identify by display name/cwd alone, and never persist a backend. Persist the unavailable parent and an actionable TUI message for unresolved/detached/unborn default state. This is the production tracer slice for REPO-02 and REPO-03.
  </action>
  <acceptance_criteria>
    - The App test seam proves save-before-spawn ordering and all four dispatch states without launching a real coding agent.
    - Every local entry path can call one `admit_repository` implementation; no path performs direct duplicate spawn logic.
    - Exactly one primary durable child and at most one live runtime exist per canonical repository in the active workspace.
    - Backend resolution remains dynamic through the active workspace and unavailable state launches nothing.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p baude repository_admission -- --nocapture</automated>
  </verify>
  <done>One successful admission spans path → Git → durable save → active-backend runtime, and repeat admission deterministically focuses/restarts instead of duplicating.</done>
</task>

<task id="05-03-02" type="auto" tdd="true">
  <name>Task 2: RED-GREEN restore-time and pre-launch Git reconciliation</name>
  <files>baude/src/app.rs, baude-core/src/git.rs</files>
  <read_first>
    - baude/src/app.rs `restore`, `save`, `remove_session`, and `restart_session`
    - baude-core/src/git.rs discovery implementation and inline real-Git fixture/tests
    - .planning/phases/05-durable-repository-admission/05-01-SUMMARY.md Git discovery contracts
    - .planning/phases/05-durable-repository-admission/05-02-SUMMARY.md persistence/load contracts
    - .planning/phases/05-durable-repository-admission/05-CONTEXT.md reconciliation, unavailable retention, restore, close, and reopen decisions
    - .planning/phases/05-durable-repository-admission/05-RESEARCH.md reconciliation and session-ensure test matrices
    - .planning/phases/05-durable-repository-admission/05-VALIDATION.md row 05-03-02
  </read_first>
  <behavior>
    - Restore handles Missing, Legacy, and Current explicitly, builds durable hierarchy first, then ensures only primary children whose active intent is true.
    - Missing path, moved/common-dir mismatch, externally changed branch, detached checkout, locked/prunable observation, and stale selected worktree update health and block reuse/launch while retaining intent.
    - Reconciliation runs immediately before each reuse, managed default-worktree mutation, restart, and launch; an identity mismatch cannot be authorized by a persisted path.
    - Closing a primary runtime clears active intent and removes/kills only runtime state while retaining repository and checkout records.
    - Reopening an idle retained primary sets intent, saves, reconciles, and launches once; restart preserves retained session settings.
    - Malformed load sets `persistence_blocked`, shows source/cause/recovery, creates no runtime, and makes all automatic saves no-op/error without replacing evidence.
  </behavior>
  <action>
Extend Plan 05-01's inline real-Git fixture with the failing `git::tests::reconciliation` matrix, and add App restore/close/reopen tests under Plan 05-02's injected state root. Replace flat restore's missing-path skip and unconditional save: load/migrate the aggregate, preserve unavailable records, reconcile each intended primary, and only then dispatch active primaries. Add one fresh-snapshot reconciliation function used before reuse, default-worktree mutation, restart, and launch; compare stable parent/child ownership with observed common-dir, registered path, and full branch ref, and retain typed degraded health instead of pruning or repairing. On strict load error, set a persistence-disabled guard and actionable message; no process or automatic save may follow. Change primary close semantics to clear active intent and retain parent/child; explicit reopen saves intent before ensure. This implements all locked reconciliation/restore/close decisions and PERS-03 without adding Phase 6 removal controls or Phase 7 hierarchy UI.
  </action>
  <acceptance_criteria>
    - Every stale/indeterminate topology case blocks mutation/launch and retains durable intent with typed health.
    - Restore launches only previously active primaries and does not launch intentionally idle parents.
    - Primary close/reopen retains identity and session settings across restart.
    - A malformed state file cannot be automatically saved over by either restore or later App save calls.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p baude-core git::tests::reconciliation -- --nocapture &amp;&amp; cargo test -p baude repository_admission -- --nocapture</automated>
  </verify>
  <done>Persisted intent is reconciled against current Git immediately before every relevant action, stale facts fail closed, and retained primaries close/reopen without identity loss.</done>
</task>

<task id="05-03-03" type="auto" tdd="true">
  <name>Task 3: RED-GREEN admission-route convergence and strict daemon persistence safety</name>
  <files>baude/src/app.rs, bauded/src/manager.rs</files>
  <read_first>
    - baude/src/app.rs `restore`, launch-directory auto-add, `open_repo_session`, clone completion, and session close/restart routes
    - bauded/src/manager.rs `Manager`, `restore`, `save`, `create`, and spawn paths
    - .planning/phases/05-durable-repository-admission/05-CONTEXT.md Phase Boundary and active-backend isolation decisions
    - .planning/phases/05-durable-repository-admission/05-PATTERNS.md App and Manager assignments
    - .planning/phases/05-durable-repository-admission/05-VALIDATION.md Sampling Rate
  </read_first>
  <behavior>
    - RED route tests prove launch-directory admission, New/Open completion, and successful clone completion each delegate to `admit_repository`; combining routes for one repository creates no duplicate primary, while remote routing remains unchanged.
    - RED Manager tests prove Missing, Legacy, and Current outcomes are handled explicitly; a selected daemon legacy source preserves every valid session field and a second restore is idempotent.
    - Malformed, truncated, unsupported, or unreadable daemon state sets `persistence_blocked`, returns/displays actionable source and cause context, launches nothing, and preserves the exact original bytes across later automatic-save attempts.
    - `persist: false` continues to perform no state-file I/O in unrelated Manager tests, and Manager resolves the active backend only when spawning rather than persisting backend identity.
    - No local or daemon test introduces repository hierarchy endpoints, remote projections, managed-worktree lifecycle controls, dormant-branch UI, or PWA/TUI hierarchy rendering.
  </behavior>
  <action>
RED before GREEN: first add focused `admission_routes` App tests and `manager_persistence` Manager tests under injected state roots, run the task verification command, and confirm failures identify missing route delegation and strict Manager load/save behavior rather than fixture or compile defects. Then route local launch-directory admission, New/Open completion, and clone completion through `admit_repository`; preserve existing remote routing unchanged because daemon hierarchy/action APIs are Phase 8. Keep clone itself unchanged and perform admission only after clone success. Update Manager only enough to compile against and safely consume the shared versioned/result-valued persistence contract: add durable aggregate ownership and a load-failure save guard, explicitly handle Missing/Legacy/Current, preserve selected fallback migration and all valid session fields, retain unavailable records, and never unconditionally save after load error. Continue resolving the active backend at spawn time and retain `persist: false` for unrelated tests. Do not add repository hierarchy endpoints, remote projections, dormant branches, managed lifecycle controls, or PWA/TUI hierarchy rendering. Run the same focused filters GREEN after implementation; reserve the full workspace suite for the plan/wave verification below.
  </action>
  <acceptance_criteria>
    - All local admission routes converge on `admit_repository`; repeated route combinations do not duplicate a primary.
    - Remote `open_repo_session` behavior and existing active-backend selection remain unchanged.
    - Manager handles strict outcomes/migration without overwriting malformed daemon state and without exposing new hierarchy APIs.
    - Existing valid local and daemon session fields still restore through the active workspace backend.
    - No Phase 6-9 deferred capability appears in code or tests.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p baude admission_routes &amp;&amp; cargo test -p bauded manager_persistence</automated>
  </verify>
  <done>Every in-scope local route uses the tracer, and both state owners safely consume the durable format without crossing the phase boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted intent → live Git/process action | Stale paths and ownership observations must be revalidated before worktree or PTY mutation. |
| App durable aggregate → active backend PTY | Saving and uniqueness decisions must precede process launch to avoid duplicate/untracked agents. |
| State load error → automatic lifecycle | Malformed evidence must block restore and future automatic saves. |
| Local TUI → configured daemon | Existing remote routing is preserved; Phase 5 must not treat client-local paths as future daemon hierarchy identity. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering / Elevation | High | `admit_repository` reconciliation | mitigate/block | Fresh canonical discovery and full parent/child identity comparison immediately before reuse, Git mutation, restart, or launch; stale/mismatched facts become unavailable. |
| T-05-10 | Tampering / Denial of Service | High | primary dispatch | mitigate/block | Stable repository+primary-role uniqueness, pure dispatch matrix, durable reservation before spawn, and repeated-entry tests ensure at most one live primary. |
| T-05-11 | Repudiation / Tampering | High | App/Manager load failure | mitigate/block | Typed source/cause message plus `persistence_blocked` guard; tests prove no restore-triggered process or save after malformed load. |
| T-05-12 | Tampering | High | save/spawn ordering | mitigate/block | Validate and atomically save active intent before PTY spawn; injected save failure proves zero spawn, spawn failure leaves retryable intent. |
| T-05-13 | Spoofing | Medium | backend/session association | mitigate | Resolve backend only from active workspace at spawn/restart; never persist backend on repository or checkout records. |
| T-05-SC | Tampering | Low | Cargo supply chain | accept | No package install or manifest/lockfile change; consume only core symbols created in Wave 0. |
</threat_model>

<verification>
- Task feedback: run the exact task filter after each task; TDD tasks require expected RED then GREEN commits.
- Wave gate: `cargo test`.
- Phase gate: `cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test`.
- Confirm no process launched in save-failure, unavailable-default, stale-topology, or malformed-load tests.
- Confirm `git diff -- Cargo.toml Cargo.lock` is empty.
</verification>

<success_criteria>
- REPO-02 admission/clone yields one usable active-backend default-primary session only after durable save.
- REPO-03 every repeat route focuses or resumes the same durable primary without duplication.
- PERS-03 current Git topology is authoritative before every in-scope reuse, mutation, or launch, while stale intent is retained unavailable.
- Strict local and daemon state consumers preserve malformed evidence and migrated valid sessions.
- Full CI triad passes with no Phase 6-9 scope leakage.
</success_criteria>

## Artifacts this phase produces

- `baude-core/src/git.rs`: `RepositorySnapshot`, `WorktreeRecord`, `DefaultBranch`, `DefaultBranchUnavailable`, `DefaultWorktreeOutcome`, `discover_repository`, `resolve_default_branch`, `ensure_default_worktree`.
- New file `baude-core/src/repository.rs`: `RepositoryKey`, `CheckoutKey`, `CheckoutRole`, `RepositoryHealth`, `CheckoutHealth`, `SavedRepository`, `SavedCheckout`, `RetainedSessionState`, `RepositoryState`.
- `baude-core/src/persist.rs`: `SCHEMA_VERSION`, `StateFile`, `LoadOutcome`, `LoadError`, strict workspace load/save APIs, selected-source migration, isolated-root seam, and atomic replacement helper.
- `baude-core/src/lib.rs`: `pub mod repository`.
- `baude/src/app.rs`: `PrimaryDispatch`, `App::admit_repository`, `App::ensure_primary`, durable aggregate ownership, reconciliation/restore logic, and persistence-blocked guard.
- `bauded/src/manager.rs`: durable aggregate ownership and persistence-blocked compatibility handling; no new daemon hierarchy API.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Admit once and return to a durable Git-reconciled parent with one usable default-branch session without mutating the existing checkout | 01-03 | COVERED | Git truth, durable intent, and tracer wiring are all included. |
| REQ | REPO-01 | Canonical parent across main/subdir/symlink/linked inputs | 01 | COVERED | `discover_repository` alias matrix. |
| REQ | REPO-02 | Exactly one usable default session in active backend | 03 | COVERED | Save-before-spawn tracer and dispatch tests. |
| REQ | REPO-03 | Repeat open focuses/reopens, no duplicates | 03 | COVERED | Stable primary-role uniqueness across all entry paths. |
| REQ | REPO-04 | Preserve non-default main and ensure separate default worktree | 01 | COVERED | Inventory-first reuse/create plus non-mutation assertions. |
| REQ | REPO-06 | Actionable unresolved default; no switch/fetch/guess | 01 | COVERED | Typed unavailable matrix and command audit. |
| REQ | PERS-01 | Durable workspace-scoped repository/child/session state | 02 | COVERED | Current schema round trip and validation. |
| REQ | PERS-02 | Idempotent local/daemon flat-state migration | 02 | COVERED | Single selected source, all fields, deterministic order. |
| REQ | PERS-03 | Reconcile intent with Git before action | 03 | COVERED | Fresh snapshot before every in-scope action. |
| REQ | PERS-04 | Atomic writes and fail-visible malformed state | 02 | COVERED | Strict load and failure-injected replacement. |
| RESEARCH | — | Byte-safe porcelain-z discovery and exact symbolic-ref default resolution | 01 | COVERED | No lossy topology parser or branch heuristic. |
| RESEARCH | — | Opaque workspace-scoped keys; persist intent/reconcile facts | 02-03 | COVERED | Core model plus App reconciliation. |
| RESEARCH | — | Selected-source migration, strict versioning, atomic replacement | 02 | COVERED | All persistence matrices included. |
| RESEARCH | — | Idempotent primary ensure and save before spawn | 03 | COVERED | Production tracer slice. |
| CONTEXT | — | All locked Default-Branch Resolution decisions | 01,03 | COVERED | Main-first upstream preference, reuse/main/create, unavailable no-launch. |
| CONTEXT | — | All locked Repository Identity decisions | 01-02 | COVERED | Common-dir/main record, aliases, opaque keys, workspace isolation. |
| CONTEXT | — | All locked Persistence and Migration decisions | 02-03 | COVERED | Field retention, blocking malformed load, atomic save, unavailable retention. |
| CONTEXT | — | All locked Primary Session Lifecycle decisions | 03 | COVERED | Immediate launch, focus/restart, retained close, active-intent restore. |
| CONTEXT | deferred | Phase 6 lifecycle, Phase 7 hierarchy, Phase 8 daemon projections, Phase 9 PWA | NONE | EXCLUDED | Explicitly deferred and prohibited from Phase 5 plans. |

<output>
Create `.planning/phases/05-durable-repository-admission/05-03-SUMMARY.md` when done.
</output>
