---
phase: 08-test-isolation-and-fixture-ownership
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - baude-core/src/workspace.rs
  - baude/src/main.rs
  - bauded/src/main.rs
  - baude/src/app.rs
autonomous: true
requirements: [TISO-02, TISO-03]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Two fixtures running concurrently on different threads each resolve their own workspace identity, and neither observes the other's (D-05)."
    - "`workspace::active()` has the same signature and the same return type it has today, so no call site changed (D-07)."
    - "A test that reaches `active()` with no override set and no containment fails, instead of silently resolving the developer's real configured workspace (D-08)."
    - "`workspace::initialize` takes the config as a parameter; the identity path performs no config read of its own (D-06)."
  artifacts:
    - "baude-core/src/workspace.rs with a thread-local `&'static Workspace` override"
    - "A concurrency test proving per-fixture identity independence"
  key_links:
    - "workspace::active() -> testing::workspace_override() -> OnceLock production fallback"
    - "baude/src/main.rs load_config() -> workspace::initialize(&config, hint)"
---

<objective>
Make workspace identity resolvable per fixture instead of once per process, so concurrent
fixtures cannot share or race one identity seeded from the developer's real environment.

Purpose: `ACTIVE: OnceLock<Workspace>` is the last process-wide cache in the isolation
path. Whichever fixture calls `active()` first pins the identity for the whole test binary,
and that identity is derived from the developer's real config. Because
`git::managed_default_worktree_path` composes `<base>/<workspace>/repository-<key>`, a
pinned identity also pins the directory every later fixture writes into.

Output: a thread-local override checked ahead of the retained `OnceLock`, and `initialize`
taking its config by parameter.
</objective>

<execution_context>
@~/.poindexter/claude/gsd-core/workflows/execute-plan.md
@~/.poindexter/claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-test-isolation-and-fixture-ownership/08-CONTEXT.md
@.planning/phases/08-test-isolation-and-fixture-ownership/08-RESEARCH.md
@.planning/phases/08-test-isolation-and-fixture-ownership/08-01-SUMMARY.md
@baude-core/src/workspace.rs
</context>

<interface_from_prior_plan>
Plan 01 delivers, in `baude_core::testing`, behind `cfg(any(test, feature =
"test-support"))`:

- `Redirects.workspace: Option<&'static crate::workspace::Workspace>` — the field already
  declared for this plan; `testing.rs` does not need editing.
- `workspace_override() -> Option<&'static Workspace>` — the reader this plan consumes.
- `pub struct TestRedirect` — RAII, `#[must_use]`, reverts on drop.
- `pub(crate) fn assert_contained(resolved: &Path, what: &str)`.
</interface_from_prior_plan>

<research_deviations>
**`active()` returns `&'static Workspace`, which a `thread_local!` cannot hand out.**
D-07 locks the signature, and a thread-local cannot produce a `&'static` reference to its
own contents. The resolution, compiled and verified in RESEARCH Finding 3, is to leak the
per-fixture `Workspace` with `Box::leak` and store the resulting `&'static` reference —
which is `Copy`, so a `Cell` suffices and no `RefCell` borrow can escape.

This is a genuine leak, and it is acceptable here for two reasons that must both hold:
it is bounded by fixture count rather than by call count, and it lives entirely inside
`cfg(any(test, feature = "test-support"))` code, so it never reaches a shipped binary.
`Workspace`'s fields are already `'static`-compatible (`String`, `&'static dyn Backend`,
`Option<String>`, `Option<u16>`), so nothing about the type needs to change.

**D-08 is implemented as a containment check, not an override-presence check.** CONTEXT.md
says a test reaching `active()` with no override "panics via the escape guard." Taken as
override-presence, that would abort the existing dogfood test, which isolates by `HOME` and
`XDG_DATA_HOME` rather than by thread-local and legitimately resolves identity from
environment. Plan 01's `assert_contained` is the containment form, and this plan uses it:
identity may fall through to the production path when the resolution demonstrably sits
inside `BAUDE_TEST_FIXTURE_ROOT`, and fails otherwise. The intent of D-08 — no silent
fallback to the developer's configured identity — is preserved exactly.
</research_deviations>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread-local `&'static` workspace override ahead of the retained `OnceLock`</name>
  <files>baude-core/src/workspace.rs</files>
  <read_first>
    - `baude-core/src/workspace.rs:46-56` — the `Workspace` struct and its field types, which determine whether `Box::leak` is viable.
    - `baude-core/src/workspace.rs:199` — `ACTIVE: OnceLock<Workspace>`, and `:206-221` — `initialize` and `active`.
    - `baude-core/src/workspace.rs:58-71` — `DEFAULT`, `state_file`, and `legacy_state_file`; plan 05 depends on these names.
    - `baude-core/src/git.rs:1776` — `managed_default_worktree_path`, the path composition that must not shift.
  </read_first>
  <behavior>
    - Two threads spawned concurrently, each setting its own workspace override, each resolve their own identity and never the other's.
    - Neither of those threads observes the identity that a third, override-free thread would resolve.
    - With an override held, `active()` returns it; after the guard drops, `active()` resolves as it did before.
    - An inner override scope shadows an outer one and the outer value is restored when the inner scope ends.
    - With no override and with containment unsatisfied, `active()` aborts the test.
    - `git::managed_default_worktree_path` composes the same string it composes today for a given identity and key.
  </behavior>
  <action>
Add a `Cell<Option<&'static Workspace>>` reader path to `workspace::active()` without
changing its signature (D-07). `active()` first consults
`testing::workspace_override()` under the `cfg(any(test, feature = "test-support"))` gate
and returns it when present. When absent, it calls `testing::assert_contained` against the
identity-bearing path before falling through to the existing `ACTIVE` `OnceLock`
resolution, which is retained verbatim as the production path (D-05).

Add the override setter to `workspace.rs` as the constructor for a per-fixture identity: it
builds a `Workspace`, hands it to `Box::leak` to obtain the `&'static` reference, stores
that reference in the `Redirects` field plan 01 already declared, and returns a guard that
restores the previous value on drop. Store the reference, never the box — the reference is
`Copy`, which is what lets a `Cell` hold it and keeps any borrow from escaping the
thread-local.

Keep the resolution order strictly override-first. `active()` has roughly 27 call sites
across all three crates and the internal change is what keeps them untouched; do not thread
a parameter through any of them.

Write the concurrency test as two threads spawned inside one `#[test]`, each constructing
its own override *inside its own thread body*. Thread-locals do not cross a
`std::thread::spawn` boundary, so an override constructed on the parent thread would not be
visible in either child and the test would prove the opposite of what it claims. Gate the
two threads on a barrier so the overlap is real rather than incidental.

Do not write a test asserting that an override set by one `#[test]` is invisible to the
next. libtest already allocates a fresh thread per test, so that assertion passes on
today's unchanged code and certifies nothing. The property that actually fails today is the
concurrent one above: with a process-wide `OnceLock`, both threads resolve the same
identity.

Add a direct assertion that `git::managed_default_worktree_path` composes the same path for
a given identity and repository key as it does today. The identity feeds that composition,
so a change in resolution order that altered it would silently relocate every managed
worktree.
  </action>
  <verify>
    <automated>cargo test -p baude-core --lib workspace:: && cargo test -p baude-core --lib git::tests && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - The concurrency test passes, and fails when the override read is removed from `active()` — it is falsifiable against the current implementation.
    - `active()`'s declared signature and return type are textually unchanged (D-07).
    - The `#[should_panic]` no-override, no-containment case aborts with a message naming workspace identity.
    - `managed_default_worktree_path` composes an unchanged path for a fixed identity and key.
  </acceptance_criteria>
  <done>Concurrent fixtures resolve independent workspace identities and no call site of `active()` was edited.</done>
  <reversibility rating="costly">`Box::leak` behind a thread-local changes the lifetime story of the single most widely read value in the crate (~27 call sites). Reverting is mechanical but the concurrency test must prove independence before the `OnceLock` stops being the only source.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject config into `initialize()` and update the production call sites</name>
  <files>baude-core/src/workspace.rs, baude/src/main.rs, bauded/src/main.rs, baude/src/app.rs</files>
  <read_first>
    - `baude-core/src/workspace.rs:206` — `initialize` and the `resolve_with_hint` call it wraps, including where the config is read today.
    - `baude/src/main.rs:281` and `:299` — `load_config()` is already computed before `initialize` is called, so the production site passes it with no extra read.
    - `bauded/src/main.rs` — the corresponding `initialize` call site.
    - `baude/src/app.rs:719` — `App::new`'s real-config read, which must keep working under the dogfood test's environment-based isolation.
  </read_first>
  <behavior>
    - `initialize` called with a literal config resolves identity from that config and performs no filesystem config read.
    - Production start-up resolves the same identity it resolves today for the same config and environment.
    - A fixture can construct an identity from a literal config without any file existing on disk.
  </behavior>
  <action>
Change `workspace::initialize` to take the config by parameter alongside its existing hint
(D-06), and pass it through to `resolve_with_hint` in place of the read it performs today.
This removes the real-config read from the identity path entirely rather than merely
redirecting it, which is the distinction D-06 draws and the reason it is preferred over
relying on plan 01's config redirect alone.

Update the production call sites. In `baude/src/main.rs` the config is already loaded into
a local a few lines above the `initialize` call, so pass that same value — do not add a
second `load_config()`. Update `bauded/src/main.rs` the same way, loading the config once
if it does not already do so. Leave `baude/src/app.rs:719` reading the real config through
`persist::load_config()`; that read is now contained by plan 01's redirect and by the
dogfood child's environment isolation, and reshaping it is not in scope.

Add the behavior tests above. The "no filesystem config read" property is best asserted by
constructing an identity from a literal config while a `TestRedirect` points at an empty
fixture root containing no config file at all, and checking the resolved identity matches
the literal — if any read were still happening it would either fail or resolve a default
rather than the literal.

Confirm the dogfood test still passes after this change. It boots through the production
start-up path, so a signature change that altered resolution order would surface there
first.
  </action>
  <verify>
    <automated>cargo test -p baude-core --lib workspace:: && cargo test -p baude --lib && cargo test -p bauded --lib && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `initialize` accepts the config as a parameter and no longer reads it internally (D-06).
    - Identity resolves from a literal config with no config file present anywhere under the fixture root.
    - `baude/src/main.rs` passes its existing `load_config()` local — there is no second config read on the start-up path.
    - `local_tui_dogfood_real_git_flow_survives_restart_without_duplicates` still passes.
  </acceptance_criteria>
  <done>The identity path reads no configuration of its own; fixtures supply a literal and production supplies its already-loaded config.</done>
  <reversibility rating="costly">A public signature change on `baude-core`, consumed by both binaries. Mechanically reversible, but every call site moves with it.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture -> process-wide identity cache | A single `OnceLock` write by the first fixture determines the directory every later fixture in that binary writes into |
| developer real config -> test identity | Identity seeded from `load_config()` imports the developer's configured workspace name into the test's path composition |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05 | Tampering | `workspace::ACTIVE` `OnceLock` | high | mitigate | Thread-local override consulted before the `OnceLock`; concurrency test proves two fixtures resolve independently (task 1) |
| T-08-06 | Information Disclosure | identity seeded from the developer's real config | high | mitigate | `initialize` takes config by parameter so fixtures supply a literal and the identity path performs no config read (task 2) |
| T-08-07 | Denial of Service | `Box::leak` per fixture | low | accept | The leak is bounded by fixture count, not call count, and is confined to test-support-gated code that never ships; documented in the task action |
| T-08-11 | Tampering | `managed_default_worktree_path` composition | medium | mitigate | Direct assertion that the composed path is unchanged for a fixed identity and key (task 1) |
</threat_model>

<verification>
- `cargo test -- --test-threads=1` passes from the repo root.
- `cargo clippy --all-targets -- -D warnings` is clean.
- `cargo fmt --check` is clean.
- `cargo build --workspace --release --locked` succeeds.
</verification>

<success_criteria>
- Workspace identity is resolvable per fixture rather than through a process-wide cache seeded from the developer's real environment.
- Concurrent fixtures cannot share or race one identity.
- `active()`'s signature and all of its call sites are unchanged.
</success_criteria>

<output>
Create `.planning/phases/08-test-isolation-and-fixture-ownership/08-03-SUMMARY.md` when done
</output>
