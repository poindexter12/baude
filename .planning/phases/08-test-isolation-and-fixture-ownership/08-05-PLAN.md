---
phase: 08-test-isolation-and-fixture-ownership
plan: 05
type: execute
wave: 4
depends_on: ["08-03", "08-04"]
files_modified:
  - baude-core/src/worktree_scan.rs
autonomous: true
requirements: [TISO-04]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A candidate referenced by any workspace's persisted state is reported Live and can never be removed (D-14 corrected)."
    - "A saved repository key protects its workspace's repo parent and children even when its observed main worktree is external; any cross-workspace path reference also blocks removal."
    - "Any unreadable potentially referencing state file or incomplete inventory blocks clearing every candidate across the scan, rather than clearing other workspaces."
    - "Removal happens only when confirmation is passed as an explicit parameter with no removing default, and every evidence item is re-derived from scratch immediately before each removal (D-15, D-16). Plan 07 surfaces that parameter as a second distinct command-line flag."
  artifacts:
    - "State cross-referencing across every workspace's state files, including the legacy unsuffixed names"
    - "A prune path that re-verifies before removing"
  key_links:
    - "worktree_scan state inventory -> persist::load_named_at (non-locking) -> SavedRepository keys/paths and SavedCheckout.observed_path"
    - "serialized ScanReport -> prune_at(roots, report, confirmation) -> fresh evidence comparison"
    - "worktree_scan::prune re-derivation -> the same evidence collectors scan uses"
---

<objective>
Cross-reference persisted workspace state as ownership-negative evidence and implement the
re-verifying prune path. Plan 07 completes TISO-04 by exposing both through a CLI
subcommand a developer can run headless.

Purpose: plan 04 can classify a candidate but has no source of positive non-ownership, so
nothing it produces is actionable. State cross-referencing supplies that, and the two-step
approval is what keeps a preview from becoming a deletion.

Output: the state cross-reference and a prune path gated behind explicit opt-in that
re-derives its evidence at removal time. Plan 07 puts the `baude worktrees` CLI surface on
top of both.
</objective>

<execution_context>
@~/.poindexter/claude/gsd-core/workflows/execute-plan.md
@~/.poindexter/claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-test-isolation-and-fixture-ownership/08-CONTEXT.md
@.planning/phases/08-test-isolation-and-fixture-ownership/08-RESEARCH.md
@.planning/phases/08-test-isolation-and-fixture-ownership/08-04-SUMMARY.md
@.planning/phases/08-test-isolation-and-fixture-ownership/08-03-SUMMARY.md
@baude-core/src/workspace.rs
</context>

<interface_from_prior_plan>
From plan 04, in `baude_core::worktree_scan` (production code, no cfg gate):
- `Evidence` — one variant per independent signal, including the not-referenced-by-state,
  referenced-by-state, and state-unreadable variants this plan populates.
- `Verdict` — `Live` / `Indeterminate { … }` / `Removable { proof }`, with `Indeterminate`
  as the default.
- The evidence-to-verdict function implementing the predicate agreed at plan 04's decision
  checkpoint.
- Read-only enumeration over `git::real_worktrees_base()` with shape filtering, non-following
  metadata classification, and symlink refusal.

Existing assets: `persist::load_named_at(root, file)` (`persist.rs:1007`, widened to
`pub(crate)` by plan 04) for strict non-locking reads. Do not use
`load_for_workspace_strict_at` (`persist.rs:338`), which acquires a state lock. Use
`workspace::state_file` / `legacy_state_file` / `DEFAULT` (`workspace.rs:58-71`),
`git::inspect_removal` / `RemovalBlocker` / `RemovalSafety` (`git.rs:2499`),
`git::remove_verified_worktree` and `verified_remove_arguments` (`git.rs:2566`, `:2673`).
</interface_from_prior_plan>

<research_deviations>
**State is the ownership-negative evidence source, inverting CONTEXT.md's reasoning.**
CONTEXT.md dismisses state because it "has zero record of the leaked directories." That
silence is the point: `SavedCheckout` records `observed_path`, so a directory state *does*
reference is definitively live and must never be a candidate. Two directories in the live
dataset — `claude/repository-1` and `claude/repository-5` — match the leak shape exactly,
were created by production after the v2.1.4 fix landed, and correspond to real repositories
in `state-claude.json`. Without the state cross-reference they are indistinguishable from
the 1431 genuine leaks.

The cross-reference must cover **every** workspace's state files, not only the active one.
The naming is `state-<workspace>.json` and `daemon-state-<workspace>.json`, plus the legacy
unsuffixed `state.json` and `daemon-state.json` which belong to the default `claude`
workspace. The developer's real config directory holds all eight of these today. Missing
one file class silently converts "not referenced" into "not checked."
</research_deviations>

<prune_transport_contract>
Plan 07 must pass the saved `ScanReport` to `prune_at`, not freshly scanned candidates.
The actions below implement the all-four semantics proposed at the human gate. If the human
selects another option, record the actual decision and synchronize both plan 05 and plan 07
before implementing it; the default remains unapproved until that response exists. The
report transport, complete state protection, re-verification and separate confirmation are
required regardless. Tests remove only newly created synthetic fixtures.
</prune_transport_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-reference every workspace's persisted state, fail closed on unreadable files</name>
  <files>baude-core/src/worktree_scan.rs</files>
  <read_first>
    - `baude-core/src/workspace.rs:58-71` — `DEFAULT`, `state_file`, and `legacy_state_file`; these produce the exact filename set to read.
    - `baude-core/src/repository.rs:318-335` — `SavedCheckout` and `observed_path`.
    - `baude-core/src/persist.rs:1007` — `load_named_at`, the strict non-locking loader; contrast `:338-349`, where the workspace-aware loader acquires a lock and must not be used by scan.
  </read_first>
  <behavior>
    - A candidate whose path appears as an `observed_path` in any workspace's state is reported Live.
    - A candidate referenced only by a non-default workspace's state is still reported Live — the cross-reference is not limited to the active workspace.
    - A candidate referenced only by a legacy unsuffixed state file is still reported Live.
    - A candidate referenced only by a daemon state file is still reported Live.
    - A referencing workspace with no directory under the worktrees base is still discovered through the config-directory state inventory.
    - A persisted child path protects its repo-N ancestor; a reference to an ancestor protects a candidate descendant, including cross-workspace references.
    - Empty `claude/repository-1` and `repository-5` parents are Live when SavedRepository keys 1 and 5 exist in claude state, despite external observed main-worktree paths and unmanaged checkouts.
    - An unreadable or unsupported state file, failed config inventory, or unresolvable reference blocks clearing all candidates, including other workspaces.
    - A genuinely absent state file is a checked absence only after a successful complete config-directory inventory; suffix and legacy files are checked independently.
  </behavior>
  <action>
Implement state cross-referencing as a mandatory exclusion from removal, using plan 04's
explicit `ScanRoots` (D-14's safety checkpoint and TISO-04). Inventory the config directory
itself, not only workspace names encountered under the worktrees base. Discover all
`state-<workspace>.json` and `daemon-state-<workspace>.json` files and always check
`state.json` and `daemon-state.json` as the default claude workspace. Union discovered
workspace names with worktree workspace names when deriving expected file names through
`Workspace::state_file` / `legacy_state_file`. Validate filename/workspace segments; an
ambiguous state-like entry or failed directory inventory is uncertainty, not an ignored
file. Read every discovered file independently, including legacy files alongside suffixed
files; fallback loading would hide live references in a second file.

Use `persist::load_named_at` for strict non-locking reads, never startup/migration APIs
which acquire locks. Distinguish parsed, absent, and unreadable/unparseable/unsupported.
A successfully enumerated absent config root or missing expected filename is a checked
absence; permission errors, vanished inventory entries during reading, symlinks, or legacy
formats that cannot be validated make the inventory incomplete. Do not migrate state,
create directories, acquire locks, or rewrite files during scan.

Build both kinds of exclusion evidence from each parsed RepositoryState:

- Repository-key ownership: a `SavedRepository.key` in workspace W protects
  `<base>/W/repository-<key>` and every child of that parent. A SavedCheckout's
  `repository_key` also protects that parent. Keys are workspace-scoped, never global.
  This protects the recorded empty repo-1/repo-5 production parents even when all
  `observed_main_worktree` paths are external and `managed_by_baude` is false.
- Path ownership: collect all `SavedCheckout.observed_path`, repository
  `observed_main_worktree` / `observed_common_dir`, and
  `SavedStandaloneSession.canonical_path` from
  every workspace. Convert `PersistedPath` via `to_path_buf` losslessly. Compare normalized
  canonical paths by components, including equality and either ancestor/descendant
  direction. A child reference blocks removal of its repo parent; a reference in a
  different workspace still blocks the candidate it overlaps. Do not use string prefixes
  (repository-1 and repository-10 are distinct). Preserve the source workspace, filename,
  key, and match reason in ReferencedByState evidence.

An unresolved/canonicalization-failed reference is uncertainty, never a mismatch. A
failed or potentially incomplete state read can conceal a cross-workspace reference, so
attach StateUnreadable to every otherwise-clearable candidate in the scan and withhold
NotReferencedByState globally. Positive Live evidence still wins, but no candidate becomes
Removable. Only a complete successful inventory with no key/path match may emit
NotReferencedByState, carrying sorted checked files/workspaces and absent outcomes for
auditing and later proof comparison. Re-run this complete inventory during prune.

Build realistic synthetic regression fixtures for recorded repo keys 1 and 5 with empty
managed parents, external fixture main checkouts, and unmanaged saved checkouts. Add a
workspace state file without a matching worktrees directory, a child-only cross-workspace
reference, simultaneous suffixed/legacy files, malformed state in another workspace,
missing files, and a prefix-collision key. Assert both injected roots are unchanged,
including absence of new locks. No historical developer-state reads are needed.
  </action>
  <verify>
    <automated>cargo test -p baude-core --lib worktree_scan::</automated>
    <fails_when>The scanner filter is empty or exits nonzero; a state-only workspace, recorded repo parent, or descendant reference fails to block removal; incomplete state inventory clears any candidate; scan writes a file/lock; or the report/prune tests accept changed proof, an unpreviewed path, a symlink, a foreign-root report, or deletion without confirmation.</fails_when>
  </verify>
  <acceptance_criteria>
    - A candidate referenced by a non-default workspace's state, by a legacy unsuffixed file, or by a daemon state file is reported Live in each case.
    - Unreadable state or an incomplete inventory blocks clearing all workspaces; known Live candidates remain Live.
    - State-only workspaces, descendant references, and empty repo parents with recorded keys 1 and 5 are covered by dedicated fixtures and never Removable.
    - An absent state file is recorded as a checked absence, distinct from an unreadable one.
    - The not-referenced-by-state signal carries the complete sorted file/workspace inventory and checked absences; scan creates no state locks.
  </acceptance_criteria>
  <done>Persisted state supplies positive non-ownership, and an unreadable state file can never contribute to clearing a candidate.</done>
  <reversibility rating="reversible">Additional evidence collection feeding an existing verdict function; nothing acts on the result yet.</reversibility>
</task>

<task type="checkpoint:decision" resolved="all-four">
  <name>Confirm prune execution semantics</name>
  <decision>Confirm the prune execution semantics before writing the first code in this phase that deletes data</decision>
  <context>
**RESOLVED — HUMAN ANSWER RECORDED 2026-09-14: option `all-four`.**

The user was asked this question directly via AskUserQuestion in the parent session and
answered `all-four`. This is a real, explicit human decision; the blocking-human gate is
satisfied and has been removed. Plan 04's predicate checkpoint was answered in the same
session with `as-proposed`. Do NOT re-prompt. Implement all four semantics:

1. Prune re-derives EVERY evidence item from scratch rather than trusting the recorded
   report verdict.
2. The re-derived proof must MATCH the proof recorded at scan time — a candidate that
   newly qualifies between scan and prune is refused as firmly as one that stopped
   qualifying.
3. Removal requires `--prune` AND a separate `--yes` flag. `--prune` alone runs full
   re-verification and removes nothing.
4. Candidates still containing a gitdir go through the existing verified-removal path
   (`inspect_removal` / `remove_verified_worktree`), not direct directory removal.

NOTE: this authorizes the CODE PATH only. No historical deletion is authorized. Phase 08
acceptance is PREVIEW-ONLY; nothing under the developer's real data root is removed by
this phase's execution or verification.

The original decision text follows for context.

---

Plan 04's checkpoint must have an actual human response recorded before this point; this
revision does not approve it. That checkpoint settles *which evidence* clears a candidate.
This one remains a separate human gate for *what happens when a developer acts on the
report*. No auto-advance or planning revision counts as consent, and no historical deletion
is authorized by either implementation checkpoint.

The inspected report reaches prune through a serialized `ScanReport`: plan 07 prints it
with `scan --json`, the operator saves and inspects that file, and a later invocation
passes it with `--prune --report <path>`. This is transport of the already-required preview,
not an extra user decision. A fresh destructive scan is not a substitute.

Task 2 is the first code in Phase 8 that removes a directory under the developer's real
data root, and no revert recovers what it deletes.

The proposed semantics, all four together:

1. Prune re-derives every evidence item from scratch — shape, filesystem classification,
   state cross-reference, git inventory — rather than trusting the scan's recorded verdict.
2. The re-derived proof must *match* the proof recorded at scan time. A candidate that newly
   qualifies between scan and prune is refused as firmly as one that stopped qualifying, on
   the grounds that the developer approved a specific reported set rather than a predicate.
3. Removal requires the prune flag *and* a separate explicit confirmation flag. Prune without
   confirmation runs the full re-verification and reports, removing nothing.
4. Candidates that still contain a gitdir are removed through the existing verified-removal
   path and its `inspect_removal` analysis, not by direct directory removal.

The live context: 1433 candidates exist under the real root right now, 1286 of them empty,
and two directories matching the same shape hold real checkouts for repositories the
developer actively uses.
  </context>
  <options>
    <option id="all-four">
      <name>Approve all four semantics as proposed</name>
      <pros>Every known failure mode between reading a report and acting on it is closed; the default path still removes nothing</pros>
      <cons>A developer who scans, then legitimately creates a new leak, must re-scan before pruning it</cons>
    </option>
    <option id="drop-proof-match">
      <name>Approve without clause 2 — re-derive, but accept any candidate that re-derives as `Removable`</name>
      <pros>Fewer surprising refusals; a stale report is still safe because re-derivation is full</pros>
      <cons>Prune acts on candidates the developer never saw in a report, which makes the approval a predicate approval rather than a set approval</cons>
    </option>
    <option id="interactive">
      <name>Approve all four, plus require per-candidate interactive confirmation rather than one flag</name>
      <pros>Nothing is removed without the developer seeing that specific path at the moment of removal</pros>
      <cons>Unusable against a 1433-entry dataset, and the phase's stated CLI shape is scriptable and headless</cons>
    </option>
  </options>
  <resume-signal>Reply with the option id (`all-four`, `drop-proof-match`, `interactive`), or state different semantics. Task 2 does not begin until one is chosen.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prune path that re-derives every evidence item immediately before removal</name>
  <files>baude-core/src/worktree_scan.rs</files>
  <read_first>
    - `baude-core/src/git.rs:2499` — `inspect_removal` and the `RemovalBlocker` / `RemovalSafety` shapes; reuse them for any candidate git still registers.
    - `baude-core/src/git.rs:2566` and `:2673` — `remove_verified_worktree` and `verified_remove_arguments`.
    - `baude-core/src/git.rs:2594` — the existing symlink refusal on the removal path.
  </read_first>
  <behavior>
    - Prune given a candidate previously classified Removable re-runs the full evidence derivation before touching it.
    - A candidate that was Removable at scan time but acquires a blocker before prune runs is refused, and the refusal names the blocker.
    - A candidate whose re-derived evidence no longer matches the evidence recorded at scan time is refused.
    - A candidate that is Indeterminate or Live at re-verification is refused.
    - A candidate that became a symlink between scan and prune is refused.
    - Prune invoked without explicit confirmation removes nothing and reports what it would have done.
  </behavior>
  <action>
Define the public, serde-serializable `ScanReport` contract here so plan 07 transports the
same value the core validates: `format_version` (initial value 1), normalized absolute
worktrees/config roots (lossless path encoding), sorted candidate records with exact
relative path components, workspace/repository key, verdict and complete ordered evidence
proof, plus the sorted checked state inventory. `scan_at(&ScanRoots)` returns this report.
Use existing serde/serde_json dependencies. Reject unknown format versions and malformed
records; canonical evidence ordering makes serialization round-trips compare semantically
rather than by JSON whitespace or directory iteration order.

Expose `prune_at(roots: &ScanRoots, preview: &ScanReport, confirmed: bool)` with no default
confirmation. Its input is the report from the earlier inspected scan, never the output of
a new destructive scan. The CLI passes roots resolved independently of the report. Validate
both root bindings, strict relative path shape, duplicate paths, containment and symlink
refusal before any action. Treat report contents as untrusted comparison data: a report
cannot introduce a path outside its independently resolved base or grant authority by
editing a verdict. Iterate only report candidates previously marked Removable; new
candidates found while re-verifying are reported but not added to the removal set.

For each candidate re-derive every evidence item from scratch, including a complete state
inventory, filesystem classification and git inventory. The previous proof is only a
comparison, never authorization (D-15). Serialization includes all proof data needed to
compare, not just display counts. Test a report round-trip, then insert a new removable
fixture candidate and prove prune does not touch it. Also reject malformed reports,
root-swapped reports and forged escaping candidate paths before any deletion.

Require the re-derived verdict to be `Removable` *and* its proof to match the proof recorded
at scan time. A candidate that newly qualifies between scan and prune is refused just as
firmly as one that stopped qualifying — the developer approved a specific reported set, not
a predicate.

Where a candidate still contains a gitdir, route the removal through the existing
verified-removal path and its `inspect_removal` analysis rather than removing the directory
directly, so this code inherits the fail-closed semantics already established there. Re-check
the non-following metadata immediately before the removal call itself, not merely at the top
of the re-derivation, and refuse on any link.

Take confirmation as an explicit parameter with no default that removes. Without it, prune
runs the full re-verification and reports the outcome, removing nothing — that is the
preview-only default the project constraint requires (D-16), and it also gives a developer a
way to see the re-verification result before committing to it.

Return a per-candidate outcome record for every candidate, including the refused ones and the
reason for each refusal, so the caller can print a complete account rather than only the
successes.
  </action>
  <verify>
    <automated>cargo test -p baude-core --lib worktree_scan::</automated>
    <fails_when>The scanner filter is empty or exits nonzero; a state-only workspace, recorded repo parent, or descendant reference fails to block removal; incomplete state inventory clears any candidate; scan writes a file/lock; or the report/prune tests accept changed proof, an unpreviewed path, a symlink, a foreign-root report, or deletion without confirmation.</fails_when>
  </verify>
  <acceptance_criteria>
    - A candidate that acquires a blocker between scan and prune is refused, and the refusal names the blocker.
    - A candidate whose re-derived proof differs from the scan-time proof is refused.
    - Prune without explicit confirmation removes nothing, proven by a before/after tree comparison on a fixture.
    - A candidate replaced by a symlink between scan and prune is refused.
    - Every candidate, refused or removed, appears in the returned outcome record with a reason.
  </acceptance_criteria>
  <done>Removal is reachable only through a full re-derivation that agrees with the approved report, and the default path still removes nothing.</done>
  <reversibility rating="one-way">This is the first code in the phase that deletes data under the developer's real data root. A defect here is unrecoverable, which is why the re-derivation, the proof comparison, and the explicit confirmation are all required rather than any one of them.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prune entry point -> real data root deletion | The only function in this codebase that can remove directories under the developer's real data root; plan 07 is the only caller |
| persisted state file -> ownership conclusion | A state file is an untrusted input to the decision: it may be absent, stale, truncated by an interrupted atomic write, or unparseable |
| scan report -> prune action | Time passes between the report a developer reads and the removal they approve; conditions can change in that window |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-16 | Tampering | unreadable state read as "not referenced" | critical | mitigate | Complete config-directory inventory plus key and ancestor/descendant path exclusions; any unreadable potentially referencing state blocks clearing all candidates across workspaces (task 1) |
| T-08-25 | Tampering | imported preview report | high | mitigate | Versioned lossless report, independently bound roots, strict relative candidate shapes, duplicate rejection and fresh proof comparison; report input never authorizes removal by itself (task 2) |
| T-08-05 | Tampering | TOCTOU between scan and prune | high | mitigate | Every evidence item re-derived from scratch at removal time, with the re-derived proof required to match the approved one (task 2) |
| T-08-04 | Tampering | candidate replaced by a symlink before removal | high | mitigate | Non-following metadata re-checked immediately before the removal call, not only at the top of re-derivation; symlink refused (task 2) |
| T-08-18 | Tampering | single-flag accidental deletion | high | mitigate | The prune entry point takes confirmation as an explicit parameter with no removing default; plan 07 surfaces it as a second distinct flag (task 2) |
| T-08-15 | Repudiation | removals with no account of what was refused and why | medium | mitigate | Per-candidate outcome record covering refusals and their reasons, surfaced by the caller (task 2) |
</threat_model>

<verification>
The task scanner filters are the fast local gates; missing tests, nonzero exits or any
unsafe state/report/prune assertion fail. Full workspace tests, clippy, formatting and
release compilation are end-of-phase integration gates after plan 06's isolation boundary,
not part of the fast latency claim. Any nonzero exit fails integration. No historical
candidate is removed during phase execution.
</verification>

<success_criteria>
- Persisted state supplies positive non-ownership, and an unreadable state file can never contribute to clearing a candidate.
- Removal requires verified ownership plus separate approval, and a missing gitdir alone never authorizes it.
- The scan report and the prune decision are independently re-derived, so approving a report does not approve a predicate.
</success_criteria>

<deferred>
- The empty-parent production leak (`create_dir_all` with no cleanup in `ensure_default_worktree` and `activate_branch_with_post_add_hook`) is out of scope for this phase and is recorded as a v2.2 follow-up. This scanner will keep finding new candidates until it is fixed, so a zero-candidate scan is not an exit criterion for anything.
- Actually deleting the 1433 existing leaked directories is a separate, explicitly approved operation and is not part of phase execution (CONTEXT.md deferred item).
- Cleaning the orphaned interrupted-atomic-write temp files in the real config dir, and hardening the write path that produced them (CONTEXT.md deferred item).
- Rotating the VAPID keypair already present in the real config dir (CONTEXT.md deferred item).
</deferred>

<output>
Create `.planning/phases/08-test-isolation-and-fixture-ownership/08-05-SUMMARY.md` when done
</output>
