---
phase: 08-test-isolation-and-fixture-ownership
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - baude-core/Cargo.toml
  - baude/Cargo.toml
  - bauded/Cargo.toml
  - baude-core/src/lib.rs
  - baude-core/src/testing.rs
  - baude-core/src/persist.rs
  - baude-core/src/git.rs
  - baude-core/src/hook.rs
  - baude/src/app.rs
  - bauded/src/api.rs
  - bauded/src/manager.rs
autonomous: true
requirements: [TISO-01, TISO-03]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A test in baude's test binary that resolves the managed worktree root, the config dir, or the state dir without a redirect and without containment fails the test run (D-09, D-10, D-11)."
    - "The same failure occurs on the very first test executed in a binary, with no earlier fixture having run (D-09)."
    - "A fixture holding one guard value redirects the config dir, the state dir, the managed worktree root, and the hook command together (D-04)."
    - "Dropping that guard restores whatever the resolvers returned before it was constructed (D-01)."
    - "A release build of baude and bauded contains none of the guard or redirect code (D-12)."
  artifacts:
    - baude-core/src/testing.rs
    - "baude-core/Cargo.toml with a test-support feature"
    - "baude/Cargo.toml and bauded/Cargo.toml dev-dependency feature wiring"
  key_links:
    - "persist::config_base() -> testing::config_dir_override() -> testing::assert_contained()"
    - "git::worktrees_base() -> testing::worktrees_base_override() -> testing::assert_contained()"
    - "baude/bauded [dev-dependencies] baude-core features=[test-support] -> cfg(feature = \"test-support\") inside baude-core"
---

<objective>
Establish the cross-crate test-support gate and the unified redirect guard, then prove
end-to-end that a downstream test binary is guarded — the single riskiest assumption in
this phase (RESEARCH Finding 1 refutes the locked `#[cfg(test)]` mechanism).

Purpose: every other plan in Phase 8 hangs off `baude_core::testing`. If the gate does not
reach `baude` and `bauded` test binaries, the phase ships a guard that is absent from
exactly the two binaries that leaked 1431 directories.

Output: `baude-core/src/testing.rs`, the `test-support` cargo feature wired through three
manifests, and the config / worktrees / hook resolvers reading from one guard.
</objective>

<execution_context>
@~/.poindexter/claude/gsd-core/workflows/execute-plan.md
@~/.poindexter/claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-test-isolation-and-fixture-ownership/08-CONTEXT.md
@.planning/phases/08-test-isolation-and-fixture-ownership/08-RESEARCH.md
@baude-core/src/git.rs
@baude-core/src/hook.rs
@baude-core/src/persist.rs
</context>

<decision_id_map>
CONTEXT.md records decisions as prose bullets under headings. This phase assigns them
stable IDs; all eight plans cite these.

| ID | Decision (CONTEXT.md) | Status |
|----|----------------------|--------|
| D-01 | Config redirect is an RAII guard type that resets on drop | Honored |
| D-02 | Refactor `bauded/src/push.rs` to call `persist::config_dir()` | Honored (plan 02) |
| D-03 | `meta::claude_config_dir()` isolated by thread-local redirect | Honored (plan 02) |
| D-04 | Unify all four redirects under one guard struct | Honored |
| D-05 | Thread-local workspace override first, `OnceLock` as production fallback | Honored (plan 03) |
| D-06 | Inject config into `initialize()` as a parameter | Honored (plan 03) |
| D-07 | `active()` keeps its current signature | Honored (plan 03) |
| D-08 | Reaching `active()` with no override panics via the escape guard | Honored exactly, before cache lookup (plan 03) |
| D-09 | Guard armed from the first instruction of every test binary, not by fixture side effect | Honored, **mechanism corrected** |
| D-10 | Panic on escape, not `Result` | Honored |
| D-11 | Guard covers worktrees base, config dir, claude config dir, state dir, push/VAPID store | Honored |
| D-12 | Zero production cost; guard absent from shipped binaries | Honored, **mechanism corrected** |
| D-13 | CLI subcommand, not TUI | Honored (plan 07) |
| D-14 | Ownership proven by strict path-shape match | Safety correction proposed at plan 04's pending human gate; not approved by planning |
| D-15 | Two-step approval; `--prune` re-verifies at removal time | Honored (plans 05/07); exact prune semantics still need the plan 05 human response |
| D-16 | Preview only by default, never delete | Honored (plans 04, 05, 07) |
| D-17 | Discretion: is `BAUDE_TEST_FIXTURE_ROOT` enough for the dogfood child | Resolved: contained paths with child config env pinned (01), literal identity override (03), regression at 06 |
| D-18 | Discretion: extract the 10 copied `manager.rs` fixture preambles | Resolved: extract (plan 06) |
| D-19 | Discretion: CLI noun/verb naming | Resolved: `baude worktrees scan` (plan 07) |
</decision_id_map>

<research_deviations>
Two locked decisions are implemented by a different mechanism than CONTEXT.md names. Both
corrections come from falsification probes run in this environment (RESEARCH Findings 1
and 2), and both are stated here rather than applied silently.

**Deviation 1 — D-09 / D-12: `#[cfg(test)]` cannot arm a downstream test binary.**
CONTEXT.md locks "armed unconditionally under `#[cfg(test)]` … `#[cfg(test)]`-only
compilation." The `test` cfg is set per crate by `rustc --test`. When cargo builds
`baude`'s test harness, `baude-core` is compiled as an ordinary library with `test` false.
A `#[cfg(test)]`-only guard in `baude-core` would therefore be **absent from `baude`'s and
`bauded`'s test binaries** — the two binaries whose fixtures produced the leak. The probe
output is in RESEARCH Finding 1 (`cfg_test_in_dep=false` inside the downstream test run).

The intent of D-09 and D-12 is preserved exactly by `cfg(any(test, feature =
"test-support"))` with the feature enabled from `[dev-dependencies]`: `test` covers
`baude-core`'s own harness, the feature covers the two downstream harnesses, and the same
probe confirms the feature is off in both `cargo run` and `cargo run --release`. Compilation failures and gated behavior are measured directly in task 1; task 3 measures
the release-build/feature-selection half of D-12 rather than assuming it.

**Deviation 2 — D-01's stated rationale is refuted; the guard is still correct.**
CONTEXT.md justifies the RAII guard with "arming leaks across tests within a binary."
That is true of the process-wide `AtomicBool`, but false of the thread-locals: libtest
allocates a fresh thread per `#[test]` even under `--test-threads=1` (the command CI runs),
so a thread-local override never reaches the next test. The guard is still built — it gives
nested scoping and it lets the arming flag be deleted — but this plan deliberately writes
**no** test of the form "an override set by test A is invisible to test B." That assertion
passes on today's unchanged code and would certify nothing (RESEARCH Pitfall 4). Task 3
verifies the property that actually fails today instead: an unguarded resolution in a test
binary aborts the test.
</research_deviations>

<crate_shape>
**`baude` and `bauded` are binary-only crates.** Neither has a `src/lib.rs` and neither
manifest declares a `[lib]` section, so `cargo test -p baude --lib` and
`cargo test -p bauded --lib` both fail outright with *"no library targets found in package"*
— a dead gate that reports nothing about the code under test. Every verify command in this
phase that targets those two packages uses **`--bins`**, which selects the auto-discovered
binary target built from `src/main.rs` and runs the `#[cfg(test)]` modules compiled into it.
`baude-core` **is** a library and keeps `--lib`.

This applies to all eight plans in Phase 8. When a filter is needed, the shapes are:

| Package | Correct form |
|---------|--------------|
| `baude` | `cargo test -p baude --bins [filter]` |
| `bauded` | `cargo test -p bauded --bins [filter]` |
| `baude-core` | `cargo test -p baude-core --lib [filter]` |

Before relying on any of these, run the chosen form once with `--no-run` and confirm it
resolves a target rather than erroring.

**Never read a gate's exit status through a pipe.** `cmd 2>&1 | tail -n` exits with `tail`'s
status, so a failed build reports success and the gate passes on broken code. This repo has
been bitten by exactly that pattern. Run the gate directly, or capture its status into a
variable on its own line and test that variable afterwards — the shape task 2 of plan 06
uses. No verify command in this phase pipes a gate into anything.
</crate_shape>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a baude test cannot reach the real config dir" — one path only</name>
  <files>baude-core/Cargo.toml, baude/Cargo.toml, bauded/Cargo.toml, baude-core/src/lib.rs, baude-core/src/testing.rs, baude-core/src/persist.rs, baude/src/app.rs</files>
  <read_first>
    - `baude-core/src/git.rs:1727-1760` — `WORKTREES_BASE_OVERRIDE` thread-local, its setter, the arming flag, and the existing escape assert. This is the shape being generalized.
    - `baude-core/src/persist.rs:840-852` — `config_base()` / `config_dir()`, the XDG -> `dirs::home_dir()` -> fallback chain being made redirect-aware.
    - `baude-core/src/git.rs:2706-2718` and `:2903` — `GitFixture` and its error-swallowing `Drop`; the repo's RAII convention.
    - `Cargo.toml:1-16` — `resolver = "2"` and the `baude-core = { path = "baude-core", version = "=2.1.5" }` workspace dependency the dev-dependency must inherit from.
  </read_first>
  <action>
Wire ONE entry point — `persist::config_base()` — through every layer this phase touches,
and prove the wiring reaches `baude`'s test binary. No other resolver is converted in this
task; task 2 expands.

Manifests. Add `[features] test-support = []` to `baude-core/Cargo.toml`. In both
`baude/Cargo.toml` and `bauded/Cargo.toml` add a `[dev-dependencies]` entry
`baude-core = { workspace = true, features = ["test-support"] }`, re-declaring features
while inheriting the rest from the workspace dependency. Leave the existing
`[dependencies]` entries untouched — per Cargo resolver v2 the dev-dependency feature is
unified only when a test or `--all-targets` build is in play, which is what keeps the gate
off in release builds.

New module `baude-core/src/testing.rs`, declared in `baude-core/src/lib.rs` behind
`#[cfg(any(test, feature = "test-support"))]`. Use this predicate for shared support in
`baude-core` only. `baude` and `bauded` do not declare that feature: their own fixture code
belongs under `#[cfg(test)]`; dependency features are evaluated inside the dependency, not
inside the downstream crate. The module declares,
in full now even though only the config field is consumed in this task (D-04 — one place to
arm, one place to reset; later plans must not have to edit this module):

  - a `Redirects` struct, `Default` + `Clone`, with fields `worktrees_base:
    Option<PathBuf>`, `config_dir: Option<PathBuf>`, `claude_config_dir: Option<PathBuf>`,
    `hook_command: Option<String>`, and `workspace: Option<&'static
    crate::workspace::Workspace>`;
  - a `thread_local!` `RefCell<Redirects>` holding the current values;
  - `pub struct TestRedirect` carrying the previous `Redirects`, constructed by
    `TestRedirect::new(root: impl AsRef<Path>)` which derives every field from one fixture
    root and swaps it in, returning the guard (D-01). Attribute it `#[must_use]` with a
    message naming the revert-on-drop behavior — an unbound construction arms and disarms
    in the same statement and silently unredirects the fixture, which is the most likely
    way to reintroduce the leak (RESEARCH Pitfall 2). Because CI runs
    `cargo clippy --all-targets -- -D warnings`, the attribute escalates that mistake to a
    build failure. Callers must bind the guard to a named local so it lives for the whole
    test body; an unnamed binding is dropped immediately.
  - `impl Drop for TestRedirect` restoring the saved `Redirects` wholesale;
  - `TestRedirect::with_hook_command(command: impl Into<String>) -> Self`: snapshot the
    current redirects, change only the hook command, and restore the whole snapshot on drop.
    Nested use preserves the outer root and identity while exercising a different install path;
  - `TestRedirect::with_workspace(workspace: &'static crate::workspace::Workspace) -> Self`:
    snapshot the current redirects and change only the workspace. Plan 03's literal-config
    fixture constructor returns this same guard type, not a second redirect store (D-04);
  - `pub(crate) fn replace_workspace_override(workspace: &'static crate::workspace::Workspace)`:
    require an existing workspace override, then replace only that field. Plan 03 uses it
    for injected initialization inside a fixture's held guard; it must panic if no identity
    scope exists, never implicitly arm an override or write the production cache;
  - make `TestRedirect` thread-affine (a `PhantomData<Rc<()>>` marker suffices) so a move to
    another thread cannot restore the wrong thread's redirects;
  - one reader per field — `config_dir_override()`, `claude_config_dir_override()`,
    `worktrees_base_override()`, `hook_command_override()`, `workspace_override()` — each
    returning an owned/`Copy` value so no borrow escapes the `thread_local!`;
  - `fn fixture_root() -> Option<PathBuf>` resolving `BAUDE_TEST_FIXTURE_ROOT` **once** into
    a `OnceLock` and returning a clone thereafter. Caching is sound because no code in this
    repo ever calls `set_var` on that variable in-process: the only writer is the dogfood
    test, which sets it on a child `Command`'s environment, and the child reads it at
    start-up. Caching also removes any temptation to mutate it later.
  - `pub struct NoFixtureRoot` — an RAII, `#[must_use]` guard setting a **thread-local**
    flag that makes `fixture_root()` report `None` for the current thread only, restoring
    the previous flag on drop. This is how the deliberate-escape tests observe the
    no-fixture-root condition **without mutating process-wide state**: an environment
    variable is process-wide, so clearing one to test the escape path changes what every
    concurrently running test sees, and a mutex cannot fix that because non-participating
    tests never take the lock. The thread-local flag is invisible outside its own thread, so
    the escape tests need no serialization, no mutex, and no `--test-threads=1`.
  - `pub(crate) fn assert_contained(resolved: &Path, what: &str)` implementing the
    containment predicate: call `fixture_root()`; if `None`, panic naming `what`, the
    resolved path, and the remedy (hold a `TestRedirect` on this thread); if `Some(root)`,
    assert `resolved.starts_with(root)` and panic naming both paths otherwise. Panic, not
    `Result` — a test that reached the real data dir has already failed and a `Result`
    would let call sites swallow it (D-10).

`NoFixtureRoot` suppresses only the containment exemption, never the escape guard. Scope
this deliberate escape probe tightly: it lives
behind the same `cfg(any(test, feature = "test-support"))` predicate, it is `#[must_use]`,
and its doc comment states that its only legitimate use is a `#[should_panic]` test asserting
the escape path aborts. A fixture that holds one is defeating the phase.

`assert_contained` is a *containment* predicate, not an *override-presence* predicate, and
that distinction is load-bearing in two directions. It satisfies D-09 without any arming
state at all — the guard becomes a property of the compiled binary rather than of execution
history, so it holds for the first test in a binary as much as the thousandth. And it lets
the re-exec'd dogfood child pass: that child is the same test binary re-invoked with
`--exact`, isolated by `HOME`/`XDG_DATA_HOME` rather than by thread-locals, so every real
path it resolves already lands inside `BAUDE_TEST_FIXTURE_ROOT`. That resolves D-17 —
`BAUDE_TEST_FIXTURE_ROOT` is sufficient, provided the guard checks containment against it
rather than merely noting it is set.

Resolver. Split `persist::config_base()` so the real XDG -> `dirs::home_dir()` -> fallback
chain is reproduced verbatim in a private `real_config_base()`, then have `config_base()`
return the override when one is set, and otherwise call `real_config_base()` followed by
`assert_contained(&real, "config dir")` under the cfg gate. Reproduce that fallback chain
character for character — the four real-root resolvers in this repo share a shape but have
different terminal fallbacks (`"."` here, `/tmp` in `git.rs`), and collapsing them changes
production behavior. `config_dir()` returns `config_base()` unchanged, so this one
redirect also moves the state files that live in the same directory (RESEARCH Pitfall 7),
which is how D-11's "state dir" clause is satisfied without a separate redirect.

Keep `persist::persistence_root_for_test` exactly as it is. It and the new redirect serve
different jobs and must not be collapsed: the redirect provides ambient containment for a
whole fixture, the explicit root provides deliberate failure injection for tests that need
to point one call at a path of their choosing. State this in the module doc comment on
`testing.rs` so a later reader does not "unify" them.

Downstream proof. Add `test_support_gate_is_active` in `baude/src/app.rs`'s existing
`#[cfg(test)]` module. Hold `baude_core::testing::TestRedirect::new` for a unique synthetic
root and assert `baude_core::persist::config_dir()` equals `<root>/config`; nest a second
redirect and assert the outer value returns on drop. This observes dependency-gated code
through its public resolver rather than evaluating a downstream feature predicate. It fails
to compile when the dependency support module disappears, and fails its path assertions if
the dependency's redirect wiring is broken. Use no `App::new`, identity lookup, or git
creation on this tracer. In core add `persist::tests::config_dir_honours_redirect` with the
same synthetic-root behavior. Start with the failing assertions before implementation.
  </action>
  <verify>
    <automated>cargo test -p baude --bins test_support_gate_is_active && cargo test -p baude-core --lib persist::tests::config_dir_honours_redirect</automated>
    <fails_when>Either build fails, either named test is absent, or a config resolution/nested restoration differs from its synthetic fixture path. These checks execute the dependency's gated resolver; missing feature wiring is a compile failure, not an expected success.</fails_when>
  </verify>
  <acceptance_criteria>
    - `cargo test -p baude --bins test_support_gate_is_active` passes, proving the feature reaches a downstream test binary (D-09, Deviation 1). `--bins`, not `--lib`: see `<crate_shape>`.
    - `persist::tests::config_dir_honours_redirect` passes without invoking identity or filesystem consumers not isolated yet.
    - Release-build exclusion (D-12) is measured by task 3's compile-only gate, not by running an unisolated test binary.
  </acceptance_criteria>
  <done>One path — a `baude` test resolving `persist::config_base()` — runs through the manifest feature, the `testing` module, the redirect, and the containment assert, and is committed.</done>
  <reversibility rating="reversible">A cargo feature plus one new module; removing the feature entry and the module declaration restores the prior build exactly.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the guard to the worktrees root and the hook command, and delete the arming flag</name>
  <files>baude-core/src/git.rs, baude-core/src/hook.rs, baude-core/src/persist.rs, baude-core/src/testing.rs, baude/src/app.rs, bauded/src/api.rs, bauded/src/manager.rs</files>
  <read_first>
    - `baude-core/src/git.rs:1735-1760` — the arming `static ... AtomicBool`, `set_worktrees_base_for_test`, and `worktrees_base()`'s current assert. Read all of it before deleting anything.
    - `baude-core/src/hook.rs:91-111` — `HOOK_COMMAND_OVERRIDE` and its setter, the second instance of the pattern being folded in.
    - `baude-core/src/persist.rs:551-560` — `release_state_lock_for_test`, currently private and `test`-gated.
  </read_first>
  <behavior>
    - A test that constructs a `TestRedirect` and then calls the managed-worktree-root resolver gets a path under its fixture root.
    - A test holding a `NoFixtureRoot` probe and no `TestRedirect` aborts when it calls the managed-worktree-root resolver.
    - The first test executed in a binary behaves identically to the thousandth — no earlier call is needed to make the guard effective.
    - A test that constructs a `TestRedirect`, then constructs a second one for a different root inside an inner scope, sees the inner root inside that scope and the outer root again after it ends.
    - `git::real_worktrees_base()` returns the production path with no guard applied, from production and test code alike.
  </behavior>
  <action>
Convert `git::worktrees_base()` and `hook.rs`'s command resolution to read from
`baude_core::testing` instead of their own thread-locals, and remove the process-wide
arming state entirely.

In `git.rs`: extract the real resolution into `pub fn real_worktrees_base()`, reproducing
the existing XDG -> `dirs::home_dir()` -> `/tmp` fallback chain verbatim (note this
resolver's terminal fallback differs from `persist.rs`'s — do not share one helper).
`real_worktrees_base` is **ungated and public**: plan 04's scanner is production code whose
entire job is to target the real root, so it must not be behind the test-support cfg. Then
rewrite `worktrees_base()` to return `testing::worktrees_base_override()` joined as today
when present, and otherwise call `real_worktrees_base()` followed by
`testing::assert_contained(&real, "managed worktree root")` under the cfg gate.

Delete the process-wide arming boolean and every read and write of it, and delete the
per-module thread-local it guarded. The containment predicate replaces it: there is no
longer any state to arm, so there is no longer a window before arming in which a test runs
unguarded. That window is the named defect in Success Criterion 3 — `baude-core`'s own
tests never armed it at all.

**Delete `set_worktrees_base_for_test` and convert its call sites.** It is `pub` today and
has twelve callers across both downstream crates; the blast radius is exactly these lines,
enumerated here so it is measured rather than discovered:

| File | Lines |
|------|-------|
| `baude/src/app.rs` | 5561 |
| `bauded/src/api.rs` | 713 |
| `bauded/src/manager.rs` | 2615, 2698, 2729, 2811, 2843, 2933, 3049, 3136, 3215, 3521 |

Re-grep for `set_worktrees_base_for_test` before starting and again after converting: the
count must go from twelve call sites plus one definition to zero. A thin wrapper is **not**
an acceptable shortcut here — every site currently calls it as a bare statement and discards
the unit return, so a wrapper returning a `TestRedirect` would be dropped in the same
statement and leave each fixture running completely unredirected while still compiling. That
is the exact failure mode `#[must_use]` exists to catch, and it is how the original leak
would be reintroduced wholesale.

Convert each site to an owned guard that survives until the fixture's consumer is done.
The two repository-building helpers return paths today; a local guard inside either helper
would drop before the test acts. In `app.rs`, make `admission_repo` and
`admission_repo_cloned` return a fixture owner containing the root, selected repo path, and
`TestRedirect`. Update every caller to retain that owner in a named binding and obtain paths
from it; the cloned variant must retain the original owner when changing its selected path.
In `api.rs`, return an equivalent owner from `initialized_repo`, and keep it alive across
all handler awaits at each caller. Do not rely on implicit deref or a temporary return value
to extend lifetimes. The ten `manager.rs` sites are already in test bodies: named guard
locals there live through the body, and plan 06 absorbs them into its shared helper.

Each site passes the fixture root, not its `data` child, because the guard derives
`<root>/data`. Preserve the exact resulting managed paths. These are mechanical test-owner
migrations; keep scenario logic and persistence-failure injection at the existing call sites.

Note that `git.rs` shadows `Result`. Any new function you add there must spell its return
type as a fully-qualified `std::result::Result<_, _>`, as `inspect_removal` already does.

In `hook.rs`: remove its private override storage and free setter after migrating every
consumer to `testing::hook_command_override()` / the unified guard (D-04). The hook call
inventory is distinct from the worktrees inventory: `app.rs:5569` in `admission_repo`,
`app.rs:5667` in the install-path reconciliation regression, `api.rs:714`, and the ten
manager preambles at 2616, 2699, 2730, 2812, 2844, 2934, 3050, 3137, 3216, 3522.
The ordinary preamble command is supplied by `TestRedirect::new(root)`; preserve its exact
`<root>/bin/baude hook` shape. At `app.rs:5667`, retain the `newer` command pointing at
`bin2/baude` and bind an inner `TestRedirect::with_hook_command(&newer)` through reseeding
and all reconciliation assertions. Assert that dropping it restores the original command
without changing config, worktrees, or workspace overrides. Audit the full symbol inventory
before removing the public function so no caller is left behind. Update the explanatory
comment in the core hook tests to name the new scoped API.

Add a pure nested-command test to `testing.rs` that checks both distinct commands and
restoration; it runs in wave 1 without calling the app reconciliation scenario. That full
scenario runs at plan 06's isolation boundary.

In `persist.rs`: make `release_state_lock_for_test` `pub` and move it under
`cfg(any(test, feature = "test-support"))`. CONTEXT.md lists it as a reusable asset, but it
is neither `pub` nor reachable across crates today, so `baude` and `bauded` fixtures cannot
call it — this makes the listing true.

Add the behavior tests named above to `baude-core/src/testing.rs`'s test module. Expose
`git::worktrees_base` as `pub(crate)` for these resolver-only assertions so they do not call
managed-path composition, which also reads identity and belongs to plan 03. The
unguarded-resolution case must be `#[should_panic]` with an expected-message substring; it
holds a `NoFixtureRoot` probe and constructs **no** `TestRedirect` — that is what proves the
"armed from the first instruction" property. It must not clear `BAUDE_TEST_FIXTURE_ROOT` or
any other environment variable: the probe is thread-local precisely so this test needs no
mutex, no serial guard, and no single-threaded run. Do not add a test asserting that an override set by one test is
invisible to another; libtest already gives every test its own thread, so such an assertion
passes on unchanged code and certifies nothing (Deviation 2).
  </action>
  <verify>
    <automated>cargo test -p baude-core --lib testing:: && cargo check --workspace --all-targets --locked</automated>
    <fails_when>A core guard test fails to panic on escape, a nested redirect/custom command fails restoration, a named test is absent, or any caller still depends on a removed setter and fails compilation. The compile-only check does not run the downstream fixtures.</fails_when>
  </verify>
  <acceptance_criteria>
    - All inventoried legacy worktree-setter declarations and invocations are migrated, and the compile-only gate proves consumers use retained guards.
    - Every converted call site binds the guard to a named local; no site constructs a `TestRedirect` as a bare statement.
    - The `#[should_panic]` unguarded-resolution test passes and is the first test in its module, with no guard constructed before it, and mutates no environment variable.
    - Nested `TestRedirect` scopes resolve to the inner root inside the inner scope and the outer root after it ends.
    - All thirteen hook-setter call sites are migrated; the distinct second-install command is still exercised and nested restoration is tested.
    - `cargo check --workspace --all-targets --locked` compiles every migrated caller; full downstream execution and clippy are integration gates in plan 06 after all isolation prerequisites.
    - `git::real_worktrees_base` is callable from a plain `cargo build --release` compilation unit.
  </acceptance_criteria>
  <done>The managed worktree root, the config dir, the state dir, and the hook command all resolve through one guard; no arming flag remains in `git.rs`.</done>
  <reversibility rating="costly">Deleting the process-wide arming flag changes the failure mode of every guarded resolver at once. Reverting means reintroducing the flag and its late-arming window, so the containment predicate must be proven by the `#[should_panic]` test before the flag is removed.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the gate is compiled into all three test binaries and absent from the shipped ones</name>
  <files>baude/src/app.rs, bauded/src/manager.rs, baude-core/src/testing.rs</files>
  <read_first>
    - `baude/src/app.rs:7641-7663` — the dogfood test and its re-exec of `current_exe()` with `BAUDE_TEST_FIXTURE_ROOT`, `XDG_DATA_HOME`, and `HOME` set. This is the one existing consumer whose behavior must not change.
  </read_first>
  <behavior>
    - In `baude`'s test binary, config resolution under `NoFixtureRoot` without `TestRedirect` panics before filesystem access.
    - In `bauded`'s test binary, the same resolution panics.
    - In `baude-core`'s own test binary, the same resolution panics without any earlier fixture arming it.
    - The dogfood child's environment pins every config path inside its fixture root; plan 06 executes that regression after all identity and resolver prerequisites.
  </behavior>
  <action>
Add the deliberate escape test to each downstream crate's test module — one in
`baude/src/app.rs`, one in `bauded/src/manager.rs` — each `#[should_panic]`, each holding a
`NoFixtureRoot` probe and resolving `persist::config_dir()` with no `TestRedirect`. Name
both downstream cases `unguarded_resolution_panics`, and use the same suffix for the
core escape case so the task's filters select real tests. Config resolution is the shared
public path that does not require identity initialization. These are the
tests that would have caught the original leak, and they are the only direct evidence that
Deviation 1's correction works in the binaries that matter: a test that *should* abort but
instead passes is the warning sign that the gate vanished downstream (RESEARCH Pitfall 1).

These tests mutate **no** process-wide state. Do not clear `BAUDE_TEST_FIXTURE_ROOT`, and do
not reach for a mutex or a serial guard to make clearing it safe — a mutex only serializes
the tests that take it, so any concurrently running non-participating test still observes the
cleared variable and aborts under the new containment assert. CI's `--test-threads=1` would
hide that; a local `cargo test` would not. The thread-local `NoFixtureRoot` probe from task 1
gives the same observation with no cross-test reach.

**Fix the dogfood child's `CLAUDE_CONFIG_DIR` before running it.** The claim that its
`~/.claude` resolution is contained by `HOME` alone is wrong: `meta::claude_config_dir`
(`baude-core/src/meta.rs:24`) resolves `CLAUDE_CONFIG_DIR` **first**, falling back to
`dirs::home_dir().join(".claude")` and then to `"."`. The re-exec at `baude/src/app.rs:7657`
sets `BAUDE_TEST_FIXTURE_ROOT`, `XDG_DATA_HOME` and `HOME`, but not `CLAUDE_CONFIG_DIR` — so
on any machine where the developer exports it (this one does, as documented setup) the child
inherits it, resolves the developer's real config directory, and trips `assert_contained`.
Add `.env("CLAUDE_CONFIG_DIR", root.join("home").join(".claude"))` and
`.env("XDG_CONFIG_HOME", root.join("home").join(".config"))` to that `Command`, beside
the existing `HOME` and `XDG_DATA_HOME` lines. Both variables can otherwise inherit a real
root from the parent. Keep these assignments on the child `Command`, never mutate the
parent environment. Plan 03 also supplies a literal identity override inside the re-exec
child; path containment alone does not authorize use of a cached test identity (D-08).

That is a fixture correction, not a guard weakening, and the distinction is the whole point:
the child spawns real `claude` processes, and pointing them at a fixture config dir is the
isolation the dogfood test was always supposed to have — it simply relied on an unset
variable to get it. The prohibition still stands in its intended form: if the guard fires
because it was written as an override-presence check rather than a containment check, fix the
guard. Do not add a test-name special case, do not exempt the dogfood path from
`assert_contained`, and do not relax containment to "warn". Setting the child's environment
so its real resolutions land inside `BAUDE_TEST_FIXTURE_ROOT` is the sanctioned remedy.

Do not run the dogfood or broad fixture suites in wave 1. Plan 06 runs them only after
plans 02 and 03 complete the Claude/push resolvers, per-fixture identities and consumer
migrations (REQUIREMENTS.md execution constraint). Record the child environment edits now;
passing those later scenarios is plan 06's integration acceptance, not this task's gate.

Finally, record the production-cost measurement for D-12 as a durable check rather than a
one-time observation: confirm a release build of both binaries succeeds and that neither
enables the feature, by building with the workspace release profile and confirming the
build graph does not select `test-support`.
  </action>
  <verify>
    <automated>cargo test -p baude --bins unguarded_resolution_panics && cargo test -p bauded --bins unguarded_resolution_panics && cargo test -p baude-core --lib unguarded_resolution_panics && cargo build --workspace --release --locked</automated>
    <fails_when>Any of the three named escape tests is absent or fails its expected-panic assertion (including succeeding without a panic), or the release build exits nonzero. Separately inspect the release build graph: selection of the core test-support feature is a D-12 failure even if compilation succeeds.</fails_when>
  </verify>
  <acceptance_criteria>
    - A `#[should_panic]` escape test passes in each of the three test binaries.
    - No escape test calls `set_var` or `remove_var` on any environment variable; each uses the thread-local `NoFixtureRoot` probe.
    - The three filtered escape tests run without a serial flag; no unrelated fixture scenario is executed before plans 02/03 finish isolation.
    - The dogfood child's environment pins both config variables under its fixture root; the full regression is owned by plan 06 (D-17).
    - `CLAUDE_CONFIG_DIR` and `XDG_CONFIG_HOME` are both set on the dogfood child's `Command`.
    - `cargo build --workspace --release --locked` succeeds (D-12).
    - Removing the dependency feature wiring makes the downstream support imports fail compilation; leaving imports intact but bypassing the guarded resolver makes the expected-panic assertion fail.
  </acceptance_criteria>
  <done>All three test binaries fail a test on an unguarded real-path resolution, and the release build is unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test binary -> developer home directory | A test process has full write access to `~/.config/baude`, `~/.claude`, and `~/.local/share/baude`; nothing in the OS distinguishes a fixture write from a production write |
| cargo feature resolution -> shipped binary | The boundary that must keep test-support code out of release artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure | `persist::config_base`, `git::worktrees_base` | high | mitigate | `assert_contained` panics on any real-path resolution outside `BAUDE_TEST_FIXTURE_ROOT`, under the test-support cfg (task 1, task 2) |
| T-08-02 | Tampering | cross-crate cfg gate | high | mitigate | `cfg(any(test, feature = "test-support"))` plus the three `test_support_gate_is_active` / escape tests that fail if the gate vanishes downstream (task 1, task 3) |
| T-08-03 | Elevation of Privilege | release build of `baude` / `bauded` | medium | mitigate | Feature declared only in `[dev-dependencies]`; resolver v2 leaves it off for `cargo build --release`; verified by task 3's release build and feature-graph check |
| T-08-09 | Denial of Service | `TestRedirect` constructed unbound | medium | mitigate | `#[must_use]` on the guard, escalated to a hard failure by CI's `cargo clippy --all-targets -- -D warnings` |
| T-08-23 | Tampering | `NoFixtureRoot` escape probe used outside a `#[should_panic]` test | medium | mitigate | Thread-local only, so it cannot reach a concurrent test; test-support-gated, `#[must_use]`, and documented as legitimate only for escape assertions (task 1, task 3) |
| T-08-SC | Tampering | cargo package installs | n/a | accept | This phase adds zero registry packages — the only manifest changes are a feature declaration and dev-dependency wiring of the existing in-repo path dependency, so no slopsquatting surface exists and no legitimacy checkpoint applies |
</threat_model>

<verification>
Wave 1 executes only the contained resolver filters in the task gates. A missing named
test or nonzero command exit fails verification. Compile-only release/check commands may
run now; they execute no fixture. Full suite, dogfood, and clippy are scheduled by plan 06
after plans 02/03 complete isolation. A failed integration check there is not waived by
this plan's narrower gates. Formatting failure also blocks completion.
</verification>

<scope_budget>
**HUMAN DECISION RECORDED (2026-09-14): scope ACCEPTED as-is.** The user was asked
directly whether to split this plan and answered: run 08-01 as one 11-file plan, do not
split it, the setter migration stays atomic. The scope warning below is settled, not open.
Do not re-prompt, do not re-decompose, do not drop a caller. Execute all 11 files in one
plan.

The scope warning history follows for context: 11 modified files exceed the
recommended 10-file threshold. Calibration was queried again in revision iteration 2:
factor 1, applied false, sample_count 0, confidence low. The 62,000-token projection is
below the 100,000-token smart-zone budget but is not measured evidence of safe sizing.

Small reassignments were checked. Moving either downstream manifest prevents that binary's
migrated callers from importing TestRedirect; moving lib.rs removes the shared module;
config/persist is the tracer path; moving git/hook or app/API/manager caller changes leaves
a deleted setter in use or drops its guard before the consumer. Moving only the escape
proof/dogfood edits removes none of those files from this plan's union. Leaving compatibility
setters or partial migrations would violate the settled complete-migration/lifetime contract.
A true split requires an additive guard/API introduction plan followed by a complete migration
plan and dependency rewiring, not a one-file reassignment. That larger re-decomposition is
not applied silently during this scoped revision. Keep all 11 paths visible and retain this
warning; a human may approve that split or the bounded atomic scope. Stop for a split if
execution exceeds budget, never omit a caller. Plan 08 separately contains the newly found
worker boundary; it does not pretend to reduce this plan's scope.
</scope_budget>

<success_criteria>
- A test in any of the three binaries that resolves the config dir, the state dir, or the managed worktree root without a redirect and without containment fails that test, on the first test executed as much as the last.
- One `TestRedirect` value redirects config, state, managed worktrees, and the hook command together, and reverts all four on drop.
- No test-support code is reachable from a release build.
</success_criteria>

<output>
Create `.planning/phases/08-test-isolation-and-fixture-ownership/08-01-SUMMARY.md` when done
</output>
