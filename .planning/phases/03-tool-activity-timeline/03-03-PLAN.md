---
phase: 03-tool-activity-timeline
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - bauded/web/app.js
  - bauded/web/style.css
  - bauded/web/sw.js
autonomous: false
requirements: [ACT-03]
must_haves:
  truths:
    - "In the PWA chat view a collapsible activity strip sits below the chat history / above the composer, collapsed by default"
    - "Expanding the strip shows the recent ~30 tool events, one line each (icon + tool/type + relative time), newest at bottom, scrollable"
    - "New events append live over the activity SSE channel without a page reload, using GET-then-SSE backfill (no event falls between the snapshot and the live tail)"
    - "Deployed PWA clients refetch the updated app.js/style.css because the service-worker CACHE version is bumped"
  artifacts:
    - path: "bauded/web/app.js"
      provides: "openActivity(sid) GET-then-SSE, state.activity/activityOpen/aes/aesBuffer, strip render in renderChat, toggle handler, SSE cleanup on view exit"
      contains: "openActivity"
    - path: "bauded/web/style.css"
      provides: ".activity-strip collapsed/expanded one-line-row scrollable styles"
    - path: "bauded/web/sw.js"
      provides: "CACHE version bump (baude-v2 → baude-v3)"
      contains: "baude-v3"
  key_links:
    - from: "app.js::openActivity"
      to: "/sessions/${sid}/activity-stream + /sessions/${sid}/activity"
      via: "EventSource into a buffer, then GET history, append, drain buffer"
      pattern: "activity-stream"
    - from: "app.js strip render"
      to: "esc() on every event field"
      via: "XSS-safe innerHTML interpolation"
      pattern: "esc\\("
---

<objective>
Render the live tool-activity feed in the PWA (ACT-03): a collapsible strip below the chat history / above the composer, collapsed by default, showing the recent ~30 events (icon + tool/type + relative time, newest at bottom, scrollable) and appending live over the standalone activity SSE channel. Mirror `openChat`'s GET-then-SSE-with-buffer discipline exactly so nothing falls between the snapshot and the live tail. Bump the service-worker `CACHE` so deployed phones refetch the embedded assets (Pitfall 4).

Purpose: This is the phone-facing surface of the feed — "editing src/foo.rs → running cargo test → …" updating live while you watch a session work remotely.

Output: `openActivity`/strip render/toggle in `app.js`, `.activity-strip` styles in `style.css`, the `sw.js` cache bump. PWA is vanilla JS/CSS embedded via `include_bytes!` — NO build step, NO JS test runner; ACT-03 is manual-only UAT by construction (do NOT add a JS harness).
</objective>

<execution_context>
@$HOME/.claude-poindexter/gsd-core/workflows/execute-plan.md
@$HOME/.claude-poindexter/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tool-activity-timeline/03-CONTEXT.md
@.planning/phases/03-tool-activity-timeline/03-RESEARCH.md
@.planning/phases/03-tool-activity-timeline/03-PATTERNS.md
@.planning/phases/03-tool-activity-timeline/03-VALIDATION.md

# Files you modify — read these specific seams:
# app.js:8-24 (state object — add activity/activityOpen/aes/aesBuffer), 28-32 (esc — XSS),
#   34 (humanMs — relative time), 130-134 (closeStream — cleanup analog), 143-169 (openChat —
#   the GET-then-SSE-with-buffer template to MIRROR), 289-297 (toggleScreen — toggle analog),
#   455-514 (renderChat/screenDrawer/msgHtml — strip mount point + handler-binding block 503-507)
@bauded/web/app.js
# style.css — the #screen drawer styles (collapsible scrollable panel) are the analog for .activity-strip
@bauded/web/style.css
# sw.js:4-5 (CACHE = "baude-v2" + SHELL) and 11-17 (activate evict) — bump CACHE only
@bauded/web/sw.js
</context>

<artifacts_this_phase_produces>
PWA-side only (consumes the plan-02 endpoints):
- `openActivity(sid)` — GET-then-SSE backfill against `/sessions/{id}/activity` + `/sessions/{id}/activity-stream`.
- The collapsible activity strip UI + toggle (collapsed by default).
- `sw.js` CACHE bumped to `baude-v3`.
</artifacts_this_phase_produces>

<tasks>

<task type="execute">
  <name>Task 1: activity strip — openActivity, render, toggle, SSE cleanup, cache bump</name>
  <files>bauded/web/app.js, bauded/web/style.css, bauded/web/sw.js</files>
  <read_first>
    - app.js:8-24 (state), 28-32 (`esc`), 34 (`humanMs`), 130-134 (`closeStream`), 143-169 (`openChat` — mirror exactly), 289-297 (`toggleScreen`), 455-514 (`renderChat`/`screenDrawer`, handler-binding block 503-507).
    - style.css — the `#screen` drawer rules (collapsed/expanded scrollable container) to mirror for `.activity-strip`.
    - sw.js:4-5 + 11-17.
    - 03-PATTERNS.md lines 257-301 (copy-ready openActivity + render + toggle + cache bump) and 03-RESEARCH.md Pitfall 4 + Pitfall 5 + the XSS escaping shared pattern (PATTERNS 369-371).
  </read_first>
  <action>
    Add `activity: []`, `activityOpen: false`, `aes: null`, `aesBuffer: null` to the `state` object (app.js:8-24). Add `openActivity(sid)` mirroring `openChat` (143-169) EXACTLY: create `new EventSource('/sessions/${sid}/activity-stream')` into `state.aesBuffer` first (onmessage pushes to the buffer while it exists, else pushes to `state.activity` + `render()`, guarded by `state.sid !== sid` like openChat), THEN `await api('/sessions/${sid}/activity?limit=30')`, set `state.activity = recent`, drain `state.aesBuffer` into `state.activity`, null the buffer, `render()`. Add SSE cleanup wherever `closeStream` is called on navigation: `if (state.aes) { state.aes.close(); state.aes = null; } state.aesBuffer = null;` so the second EventSource never leaks across views (Pitfall 5). Render the strip in `renderChat` (455-514): mount it below `#chat` / above `#composer`, collapsed when `!state.activityOpen` (toggle button mirrors `toggleScreen` at 289-297 — flip `state.activityOpen`, `render()`); when expanded show the recent ~30 as one line each — icon + tool/type + relative time via `humanMs()` — newest at bottom, scrollable, auto-scroll when near bottom. CRITICAL (Security/XSS): pass EVERY event field (`e.event`, `e.tool`, `e.notification_type`) through `esc()` (app.js:28) before innerHTML interpolation — never interpolate raw. Wire the toggle button's onclick in the same handler-binding block as `screenbtn`/`archbtn` (503-507). Add `.activity-strip` styles to style.css (collapsed/expanded, one-line rows, scrollable recent-~30 container) mirroring the `#screen` drawer. Bump `sw.js` `CACHE = "baude-v2"` → `"baude-v3"` (the activate handler already evicts non-matching caches; `/app.js`+`/style.css` already in SHELL — no new entry needed). Icon set / strip class id / relative-time format are at Claude's discretion per CONTEXT. PWA assets are `include_bytes!`-embedded, so a `cargo build` is required for edits to take effect.
  </action>
  <verify>
    <automated>cargo build -p bauded && grep -q 'baude-v3' bauded/web/sw.js && grep -q 'openActivity' bauded/web/app.js && grep -q 'activity-stream' bauded/web/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `openActivity` connects SSE-into-buffer then GETs `?limit=30` then drains — the openChat seam, no gap.
    - The strip is collapsed by default, toggles open, shows recent ~30 newest-at-bottom, appends live on SSE.
    - Every event field is `esc()`-escaped before innerHTML (XSS mitigation).
    - The strip EventSource is closed on view exit (no leak across navigations).
    - `sw.js` CACHE is `baude-v3`; `cargo build -p bauded` clean (embedded assets rebuilt).
  </acceptance_criteria>
  <done>The activity strip renders + appends live behind a collapse toggle; cache bumped; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PWA activity strip UAT (live session)</name>
  <action>Human-verify only — there is no automated path (vanilla-JS PWA, no test runner, no build step; ACT-03 is manual by construction). Pause execution and walk the human through the verification steps below on a live session (ideally the phone, to exercise the service-worker cache bump). Do NOT fabricate a result — wait for the resume signal.</action>
  <what-built>The collapsible PWA activity strip: GET-then-SSE backfill against `/sessions/{id}/activity` + `/sessions/{id}/activity-stream`, collapsed by default, recent ~30 newest-at-bottom, live append, XSS-escaped fields, and the `sw.js` cache bump to `baude-v3`. (Vanilla-JS PWA has no test runner / no build step — ACT-03 is manual-only by construction per the project constraint; this UAT was NOT fabricated and cannot run in the automated executor.)</what-built>
  <how-to-verify>
    1. `cargo build -p bauded` then run `bauded` and open the PWA (the served origin) on a device — ideally the phone, to also exercise the service-worker cache bump.
    2. Hard-refresh once (or confirm the SW evicted `baude-v2` → `baude-v3`); open a session's chat view. Confirm the activity strip is present below the chat / above the composer and is COLLAPSED by default.
    3. Expand the strip. Confirm it shows the recent tool sequence (one line per event: icon + tool/type + relative time), newest at the bottom, scrollable.
    4. With the session active, drive several tool calls in Claude. Confirm new rows APPEND live (no page reload) and that the snapshot+live seam has no gap or duplicate (GET-then-SSE buffer discipline).
    5. Confirm a tool/notification string with HTML-ish characters renders as literal text (not interpreted) — XSS escaping via `esc()`.
    6. Navigate away from the chat view and back; confirm no duplicate/stale EventSource (the strip ES closed on exit).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. strip empty while file grows → /activity-stream tail bug from plan 02; strip blank on phone but fine on desktop → stale SW cache, re-check the CACHE bump).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| event field strings → PWA innerHTML | `e.tool`/`e.notification_type`/`e.event` (origin: tool names / Claude notifications) cross into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering / Information Disclosure | XSS via tool/notification strings rendered in the strip | mitigate | Escape every event field with the existing `esc()` helper (app.js:28) before innerHTML; never interpolate `e.tool`/`e.notification_type`/`e.event` raw. Verified in the UAT (step 5). |
| T-03-10 | Denial of Service | two concurrent EventSources per chat view (browser per-host connection cap) | accept | Two SSE on a single-view phone client is well within the HTTP/1.1 ~6-connection cap (03-RESEARCH.md Pitfall 5 / A1, `[ASSUMED]`, non-blocking); the strip ES is scoped to the chat view and closed on exit so it never leaks. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase — vanilla JS, no build step, no JS dependencies (03-RESEARCH.md Package Legitimacy Audit: none). |
</threat_model>

<verification>
- Automated: `cargo build -p bauded` clean (embedded assets compile); `sw.js` contains `baude-v3`; `app.js` contains `openActivity` + `activity-stream`.
- Manual (ACT-03, by construction): the Task 2 human-verify UAT — strip renders, appends live, collapsed-by-default, XSS-safe, cache refetch on phone.
- `cargo fmt --check && cargo clippy --workspace -- -D warnings && cargo test --workspace` — full CI triad green (no Rust logic changed here, but keep the gate green).
</verification>

<success_criteria>
- ACT-03 satisfied: the PWA chat view shows a collapsible activity strip ("editing src/foo.rs → running cargo test → …") that updates live without reload.
- Strip is collapsed by default, recent ~30 newest-at-bottom, XSS-escaped, SSE-cleaned on exit.
- Service-worker cache bumped so deployed clients refetch.
</success_criteria>

<output>
Create `.planning/phases/03-tool-activity-timeline/03-03-SUMMARY.md` when done.
</output>
