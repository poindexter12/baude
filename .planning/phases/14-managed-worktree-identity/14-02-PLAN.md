---
phase: 14
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - baude-core/src/git.rs
  - baude-core/src/worktree_scan.rs
  - baude-core/src/lifecycle.rs
  - baude-core/src/launch.rs
  - baude-core/src/workspace.rs
  - baude/src/app.rs
  - bauded/src/api.rs
autonomous: true
requirements:
  - WTID-01
  - WTID-02
  - WTID-03

estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - Managed worktree paths use digest keys (repository-<12 hex>) for new repos, preserve legacy counter dirs for migration
    - Path composition functions accept &str instead of u64 for repository key parameter
    - Scan's repository_key parser accepts both legacy decimal (repository-1) and new hex (repository-abcdef123456) formats
    - Collision detection in ensure_repository compares marker canonical dir with newcomer's canonical dir
    - Marker adoption at startup walks managed directories and writes markers for legacy repos without one
  artifacts:
    - git.rs: managed_default_worktree_path(key: &str, checkout_key: u64) → PathBuf
    - git.rs: managed_branch_worktree_path(key: &str, checkout_key: u64, branch: &str) → PathBuf
    - worktree_scan.rs: repository_key(name: &str) → Option<String> (handles decimal and hex)
    - lifecycle.rs: ensure_repository returns Result<(RepositoryKey, Option<CollisionReport>), LifecycleError>
    - launch.rs: adopt_markers(workspace_name: &str) → Result<(), anyhow::Error>
  key_links:
    - git.rs path functions called by repository allocation and checkout creation; must accept string keys
    - worktree_scan::repository_key used to parse directory names; must round-trip with format!
    - lifecycle::ensure_repository is the admission gate where collisions are detected and reported
    - launch::adopt_markers runs at startup before app/daemon processes repositories
  prohibitions:
    - Never use decimal parsing only for repository_key; legacy dirs would be unrecognized
    - Never break the round-trip: format!("repository-{key}") must equal the original directory name
    - Never move or rename legacy repository-<counter> directories during migration
    - Never report collision as fatal error; newcomer gets fresh path and admission continues
---

<objective>
**Path composition, resolver integration, and scan schema update:** Wire the repository_identity resolver into path composition functions, update worktree_scan to use string keys comprehensively (touching prune schema), implement collision detection in admission flow with non-destructive fallback, and implement lazy marker adoption. All call sites updated to use the resolver.

**Purpose:** Digest keys go into paths for new repositories via resolver. Legacy counter dirs are found via marker scan and preserved. Collision detection allocates newcomers to distinct paths. Scan schema is versioned and marker-aware for prune safety. Marker adoption is lazy (admission or scan time, not eager startup) to preserve Phase 15's first-frame goal.

**Output:** Production-quality path resolution, comprehensive scanner migration, and non-destructive collision handling with lazy adoption.
</objective>

## Artifacts this phase produces

**Updated function signatures:**
- `baude-core/src/git.rs` — `pub fn managed_default_worktree_path(repository_key: &str, checkout_key: u64) -> PathBuf`; `pub fn managed_branch_worktree_path(repository_key: &str, checkout_key: u64, branch: &str) -> PathBuf`

**Updated call sites across multiple files (comprehensive propagation):**
- `baude-core/src/git.rs` — path functions and ~5 test calls updated to pass &str keys
- `baude-core/src/lifecycle.rs` — `ensure_repository` updated to call `resolve_physical_key`, use returned physical_key in path composition (1 real admission site line ~1356)
- `baude-core/src/workspace.rs` — ~3 test calls updated to pass &str; includes path composition test that verifies both legacy and new formats
- `baude/src/app.rs` — ~4 test/real calls updated; includes both default and branch worktree call sites
- `bauded/src/api.rs` — ~1 test call updated

**Updated data structures:**
- `baude-core/src/worktree_scan.rs` — `repository_key()` function returns `Option<String>` (parses both legacy decimal and new hex); `Candidate` struct field `repository_key: String`; `ScanReport` versioning bumped and marker-aware evidence added

**Scanner/Prune schema changes:**
- `baude-core/src/worktree_scan.rs` — `REPORT_FORMAT_VERSION` incremented; `Candidate` and evidence structures updated for string keys; prune evidence now excludes `.baude-marker.json` alone (marker does not count as contents that block cleanup)

**New functions:**
- `baude-core/src/lifecycle.rs` — `ensure_repository` now calls `repository_identity::resolve_physical_key`, allocates physical_key, checks for collision, returns `Result<(RepositoryKey, Option<CollisionReport>), LifecycleError>`
- `baude-core/src/launch.rs` — new function `pub fn adopt_markers_lazy(workspace_name: &str) -> Result<Vec<MarkerAdoptionResult>>` (optional, deferred to admission/scan time)

<execution_context>
@~/.iarx/claude/gsd-core/workflows/execute-plan.md
@~/.iarx/claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-managed-worktree-identity/14-CONTEXT.md
@.planning/phases/14-managed-worktree-identity/14-RESEARCH.md
@.planning/phases/14-managed-worktree-identity/14-PATTERNS.md
@.planning/phases/14-managed-worktree-identity/14-01-SUMMARY.md

# Source files
@baude-core/src/git.rs
@baude-core/src/worktree_scan.rs
@baude-core/src/lifecycle.rs
@baude-core/src/launch.rs
@baude-core/src/repository.rs
@baude-core/src/persist.rs
@baude-core/src/testing.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Path composition functions accept &str keys and wire resolver; all call sites updated (RED → GREEN)</name>
  <files>baude-core/src/git.rs, baude-core/src/workspace.rs, baude-core/src/lifecycle.rs, baude/src/app.rs, bauded/src/api.rs</files>

  <read_first>
  - baude-core/src/git.rs lines 1748-1819 (managed_default_worktree_path, managed_branch_worktree_path)
  - baude-core/src/lifecycle.rs lines 1307-1365 (ensure_repository, repository allocation)
  - baude-core/src/repository_identity.rs (resolve_physical_key resolver from Plan 14-01)
  - baude-core/src/workspace.rs lines 528-550 (path composition test)
  - baude/src/app.rs lines 1914-1947 (admit_repository), 1993, 7060, 7099, 7860 (path calls)
  - bauded/src/api.rs line ~868 (path call)
  </read_first>

  <behavior>
    **RED commit:** Create failing tests:
    1. Test `path_composition_accepts_string_digest` — git::managed_default_worktree_path("abcdef123456", 1) returns path containing `repository-abcdef123456/primary-1`.
    2. Test `branch_path_accepts_string_digest` — git::managed_branch_worktree_path("abcdef123456", 2, "feature/foo") returns path containing `repository-abcdef123456/feature_f-2`.
    3. Test `legacy_path_composition_roundtrips` — "1" (string) composes to `repository-1/primary-1`; format!("repository-{key}") round-trips correctly.
    4. Test `resolver_determines_physical_key` — calls ensure_repository, which calls repository_identity::resolve_physical_key internally, receives (RepositoryKey, Option<CollisionReport>) tuple.
    5. Test `resolver_finds_legacy_dir_via_marker` — creates legacy `repository-1` directory with marker, calls resolve_physical_key, asserts returned physical_key matches marker.

    All tests fail due to signature changes and resolver not yet wired.

    **GREEN commit:** Implement:
    - Path functions accept `&str` keys; update all call sites
    - lifecycle::ensure_repository calls resolve_physical_key, uses returned physical_key
    - ensure_repository checks for collision at resolved path, returns (RepositoryKey, Option<CollisionReport>)
  </behavior>

  <action>
    1. Update git.rs: `managed_default_worktree_path(repository_key: &str, checkout_key: u64) -> PathBuf` and similarly for branch path. Both use `format!("repository-{repository_key}")`.

    2. Update lifecycle.rs ensure_repository to:
       - Import repository_identity resolver module
       - Call `resolve_physical_key(workspace_root, canonical_common_dir_bytes)` after discovering repository
       - Extract physical_key from Resolved { physical_key, source }
       - Use physical_key in path composition calls (instead of numeric RepositoryKey)
       - Check if path exists and has marker; read marker and compare canonical_common_dir bytes
       - If mismatch, construct CollisionReport and return Ok((key, Some(report)))
       - Change return type to `Result<(RepositoryKey, Option<CollisionReport>), LifecycleError>`

    3. Update all call sites across git.rs, lifecycle.rs, workspace.rs, app.rs, api.rs (~12 locations) to pass &str keys instead of u64

    **Commits:**
    1. `test(14-02): add failing tests for string-key path composition and resolver integration` — tests fail due to signatures
    2. `feat(14-02): wire resolver into path composition; update all call sites to use &str keys` — updates multiple files
  </action>

  <verify>
    <automated>cd /Users/joese/.local/share/baude/worktrees/smoke/repository-1/primary-3 && cargo test -p baude-core --lib workspace::tests managed_default_worktree_path_accepts_string_digest managed_branch_worktree_path_accepts_string_digest legacy_counter_path_composition_still_works path_composition_is_unchanged -- --nocapture 2>&1 | tee /tmp/path_tests.log && [ "$(grep -c '^test ' /tmp/path_tests.log)" -ge 4 ] && grep -q "test result: ok" /tmp/path_tests.log</automated>
    <fails_when>Exit code non-zero, fewer than 4 tests run, or "test result: ok" not found</fails_when>
  </verify>

  <done>
    - git.rs managed_default_worktree_path signature is `(repository_key: &str, checkout_key: u64) -> PathBuf`
    - git.rs managed_branch_worktree_path signature is `(repository_key: &str, checkout_key: u64, branch: &str) -> PathBuf`
    - Both functions format the key directly in the path: `format!("repository-{repository_key}")`
    - The path shape is unchanged: `repository-<key>/<role>-<checkout_key>`
    - All four workspace::tests pass (new digest tests and legacy tests and pinned shape test)
    - All call sites in git.rs, lifecycle.rs, workspace.rs, app.rs, bauded/api.rs are updated to pass &str
    - `cargo build --workspace` and `cargo test --workspace` both succeed with no compiler errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Comprehensive worktree_scan schema update: string keys, marker-aware evidence, REPORT_FORMAT_VERSION bump (RED → GREEN)</name>
  <files>baude-core/src/worktree_scan.rs</files>

  <read_first>
  - baude-core/src/worktree_scan.rs lines 1012-1015 (repository_key function)
  - baude-core/src/worktree_scan.rs lines 744-771 (Candidate struct)
  - baude-core/src/worktree_scan.rs lines 773-798 (ScanReport, REPORT_FORMAT_VERSION, report versioning logic)
  - baude-core/src/worktree_scan.rs lines 1052-1088 (evidence classification, contents evidence, directory walk)
  - baude-core/src/worktree_scan.rs lines 1283-1290 (prune version mismatch handling)
  - baude-core/src/marker.rs (MarkerMetadata from Plan 14-01)
  </read_first>

  <behavior>
    **RED commit:** Create failing tests:
    1. Test `repository_key_parses_legacy_decimal` — calls repository_key("repository-1"), asserts returns Some("1").
    2. Test `repository_key_parses_new_hex_digest` — calls repository_key("repository-abcdef123456"), asserts returns Some("abcdef123456").
    3. Test `repository_key_rejects_malformed` — calls repository_key("repository-gggggg123456"), asserts returns None.
    4. Test `repository_key_roundtrips` — format!("repository-{key}") == original name for all valid keys.
    5. Test `marker_file_does_not_count_as_contents` — directory with only `.baude-marker.json` is treated as empty (not contents-blocking).
    6. Test `scan_report_version_changed` — new version reflects string-key + marker-aware schema.

    Tests fail due to signature changes and missing marker-awareness logic.

    **GREEN commit:** Implement:
    - repository_key returns Option<String>, parses decimal (with round-trip check) and hex (12-char ASCII hex)
    - Candidate.repository_key: String; all internal references updated
    - REPORT_FORMAT_VERSION incremented
    - Evidence classification treats .baude-marker.json as non-blocking
    - All state/evidence references using repository_key updated to String
  </behavior>

  <action>
    1. Update `repository_key(name: &str) -> Option<String>`:
       - Strip "repository-" prefix
       - Try parse as u64, check round-trip format!("repository-{num}") == name
       - If fails, try 12-char hex (all ASCII hex digits [0-9a-f])
       - Return Some(key_string) or None

    2. Update Candidate struct: `repository_key: String` (instead of u64)

    3. Update all references to candidate.repository_key throughout worktree_scan.rs from numeric to String:
       - repository_key_from_name function calls
       - state references using repository_key
       - evidence/predicate/verdict logic
       - sorting operations
       - any format! calls building path strings

    4. Update evidence classification: When walking directory to check if it's empty, skip `.baude-marker.json`:
       - In the contents-discovery loop (~lines 1052-1088), add check: `if child_name == ".baude-marker.json" { continue; }`
       - Marker file alone does not count as contents that block cleanup

    5. Increment REPORT_FORMAT_VERSION (e.g., "2" → "3")

    6. Update prune version-mismatch message to reference new version

    7. Update state read/write paths that reference repository_key: now String not u64

    **Commits:**
    1. `test(14-02): add failing tests for string-key scanning and marker-aware evidence` — tests fail due to signatures
    2. `feat(14-02): comprehensive worktree_scan schema migration to string keys, marker-aware evidence, version bump` — migrates u64→String everywhere, increments REPORT_FORMAT_VERSION
  </action>

  <verify>
    <automated>cd /Users/joese/.local/share/baude/worktrees/smoke/repository-1/primary-3 && cargo test -p baude-core --lib worktree_scan::tests repository_key marker_file_does_not_count_as_contents scan_report_version -- --nocapture 2>&1 | tee /tmp/scan_tests.log && [ "$(grep -c '^test ' /tmp/scan_tests.log)" -ge 6 ] && grep -q "test result: ok" /tmp/scan_tests.log && cargo build --all 2>&1 | grep -q "Finished\|Compiling"</automated>
    <fails_when>Exit code non-zero, fewer than 6 tests run, "test result: ok" not found, or build fails</fails_when>
  </verify>

  <done>
    - worktree_scan.rs repository_key function signature is `(name: &str) -> Option<String>`
    - Parses both "repository-1" (legacy decimal) and "repository-abcdef123456" (new hex)
    - Returns None for malformed input (invalid hex, round-trip mismatch)
    - Round-trip property holds: format!("repository-{key}") == original name
    - Candidate struct's repository_key field is now String instead of u64
    - All internal references to candidate.repository_key updated to String
    - Evidence classification treats .baude-marker.json as non-blocking (marker alone not contents-blocking)
    - REPORT_FORMAT_VERSION incremented to reflect schema change (string keys + marker-aware evidence)
    - All tests pass (repository_key parsing, marker-aware evidence, version handling)
    - `cargo build --all` succeeds with no errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Collision detection in ensure_repository and marker adoption at startup (RED → GREEN)</name>
  <files>baude-core/src/lifecycle.rs, baude-core/src/launch.rs</files>

  <read_first>
  - baude-core/src/lifecycle.rs lines 1307-1365 (ensure_repository function, repository allocation and recording logic)
  - baude-core/src/launch.rs lines 66-141 (start_workspace function, lock claim and binding record steps)
  - baude-core/src/marker.rs (read_marker, write_marker, MarkerMetadata — from Plan 01)
  - baude-core/src/git.rs (discover_repository_identity function)
  </read_first>

  <behavior>
    **RED commit:** Create failing tests in lifecycle::tests and launch::tests:
    1. Test `ensure_repository_detects_collision` — calls ensure_repository with a newcomer repo, pre-populated a managed directory with a marker for a different repo, asserts returns Ok((key, Some(collision_report))) where report.owner_canonical_dir != newcomer's canonical_dir.
    2. Test `ensure_repository_allocates_new_path_on_collision` — asserts that on collision, returned key differs from what would have been allocated to the owner (newcomer gets its own path).
    3. Test `adopt_markers_writes_marker_for_legacy_dir` — creates a legacy repository-<counter> directory with a primary-* checkout, calls adopt_markers, asserts marker file exists and deserializes correctly.
    4. Test `adopt_markers_skips_existing_markers` — creates a directory with existing marker, calls adopt_markers twice, asserts marker content unchanged (idempotent).
  </behavior>

  <action>
    **In baude-core/src/lifecycle.rs, update ensure_repository:**
    1. Call repository_identity::resolve_physical_key(workspace_root, canonical_common_dir_bytes) to get physical_key
    2. Use physical_key in git::managed_default_worktree_path (not numeric RepositoryKey)
    3. Check if computed path exists
    4. If it exists, call marker::read_marker(path) to get ownership
    5. If marker exists and marker.canonical_common_dir != newcomer's canonical_common_dir (bytes compare), construct CollisionReport and return Ok((key, Some(report)))
    6. If marker is missing or matches the newcomer, continue with normal admission (return Ok((key, None)))
    7. Record repository in state; per design answer, state now has common_dir_to_physical_key mapping
    8. Change return type from Result<RepositoryKey, LifecycleError> to Result<(RepositoryKey, Option<CollisionReport>), LifecycleError>

    **In baude-core/src/launch.rs, implement lazy marker adoption:**
    1. Create public function `pub fn adopt_markers_lazy(workspace_name: &str) -> Result<Vec<MarkerAdoptionEvent>, Box<dyn std::error::Error>>`
    2. Walk managed worktree base directory for the workspace
    3. For each `repository-*` subdirectory:
       a. Check if `.baude-marker.json` exists; if yes, skip (already migrated)
       b. If not, try to discover owner by reading git common dir from ALL `primary-*` or `*-*` child checkouts
       c. If checkouts have UNANIMOUS common dir (all match), construct MarkerMetadata and write marker
       d. If checkouts DISAGREE on common dir, log error event (collision detected), do NOT write marker
       e. If no checkouts found, log unknown-owner event, do NOT write marker
    4. Log all events (success, collision, unknown) in MarkerAdoptionEvent structs
    5. Return events (not silent) so caller can surface them in startup notes or scan output
    6. Do NOT call adopt_markers_lazy during start_workspace (lazy, deferred to admission or scan time)

    Write RED tests before implementing.

    **Commits:**
    1. `test(14-02): add failing tests for collision detection and lazy marker adoption` — four tests fail
    2. `feat(14-02): implement collision detection and lazy marker adoption with event logging` — updates lifecycle.rs return type, adds adopt_markers_lazy to launch.rs
  </action>

  <verify>
    <automated>cd /Users/joese/.local/share/baude/worktrees/smoke/repository-1/primary-3 && cargo test -p baude-core --lib lifecycle::tests ensure_repository_detects_collision ensure_repository_allocates_new_path_on_collision -- --nocapture 2>&1 | tee /tmp/collision_tests.log && [ "$(grep -c '^test ' /tmp/collision_tests.log)" -ge 2 ] && grep -q "test result: ok" /tmp/collision_tests.log && cargo test -p baude-core --lib launch::tests adopt_markers_writes_marker_for_legacy_dir adopt_markers_skips_existing_markers -- --nocapture 2>&1 | tee /tmp/adopt_tests.log && [ "$(grep -c '^test ' /tmp/adopt_tests.log)" -ge 2 ] && grep -q "test result: ok" /tmp/adopt_tests.log</automated>
    <fails_when>Exit code non-zero, fewer than 4 tests total run, or "test result: ok" not found in either log</fails_when>
  </verify>

  <done>
    - lifecycle.rs ensure_repository signature is `(...) -> Result<(RepositoryKey, Option<CollisionReport>), LifecycleError>`
    - ensure_repository calls repository_identity::resolve_physical_key to get physical_key
    - Collision detection reads marker, compares canonical_common_dir bytes with newcomer
    - Returns Some(CollisionReport) when collision detected (different canonical dirs)
    - Returns None when no collision or marker matches
    - launch.rs exports adopt_markers_lazy(workspace_name: &str) → Result<Vec<MarkerAdoptionEvent>>
    - adopt_markers_lazy is NOT called during startup (lazy, deferred to admission/scan time per Phase 15 goal)
    - Marker adoption requires UNANIMOUS common dir across all child checkouts (any disagreement → error event, no write)
    - Marker adoption logs all events (success, collision, unknown-owner) in return vector (NOT silent)
    - All four tests pass (collision detection detection, new-path allocation, lazy adoption events)
    - `cargo test -p baude-core --lib lifecycle` and `cargo test -p baude-core --lib launch` both pass
    - `cargo build --all` succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Path parsing | repository_key function must accept both legacy and new formats; malformed input must be rejected. |
| Marker read during collision detection | Malformed marker must not crash; treat as ownership unknown and continue. |
| Legacy dir discovery | Must not follow symlinks out of the managed worktree base during checkout discovery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-06 | Tampering | Path parsing round-trip | medium | mitigate | Round-trip check `format!("repository-{key}") == name` prevents aliases and parsing ambiguities. |
| T-14-07 | Tampering | Collision detection bypass | medium | mitigate | Marker is source of truth; if missing, discover owner from git checkouts inside directory (fail-closed). |
| T-14-08 | Denial of Service | Legacy discovery walks unbounded directories | medium | mitigate | Only examine subdirectories matching primary-* or *-* patterns; limit to one level. |
| T-14-09 | Information Disclosure | Canonical dir in collision report | medium | mitigate | Report is internal; user sees only "repository-<digest>" display in TUI. Canonical dir logged only in daemon debug output. |
</threat_model>

<verification>
Path composition accepts string keys for both legacy and new formats. Scan parser recognizes both decimal and hex and round-trips correctly. Collision detection compares canonical dirs and returns non-fatal reports. Marker adoption at startup bridges legacy and new schemes. All tests pass in isolation and under TestRedirect isolation.
</verification>

<success_criteria>
- Managed path functions accept &str keys instead of u64, preserving repository-<key>/<role>-<key> shape
- Scan repository_key parser accepts decimal (legacy) and 12-char hex (new) formats
- Round-trip property holds for all valid keys
- Collision detection compares marker canonical dir with newcomer's canonical dir
- Resolver determines physical key (legacy or fresh digest) and wires into path composition
- Lazy marker adoption returns events (not silent) with unanimous-checkout validation
- All tests pass (path composition, scan parsing, collision detection, resolver integration, lazy adoption)
- `cargo build --all` and `cargo test --all --lib` both succeed
</success_criteria>

## Review Dispositions (Cycle 1)

| Finding | Severity | Disposition | Action in Plan |
|---------|----------|-------------|-----------------|
| No legacy/digest directory resolver | HIGH | RESOLVED | Task 1 now wires repository_identity::resolve_physical_key into path composition; resolver checks state, markers, then fresh digest |
| State-reset recovery unimplemented | HIGH | RESOLVED | Resolver scans markers when state is missing, returns physical_key for legacy dirs found via marker scan |
| Collision continuation has no mechanism | HIGH | RESOLVED | Resolver returns physical_key directly; ensure_repository uses this physical_key in path composition, enabling non-destructive fallback |
| Scanner change larger than described (u64→String everywhere) | HIGH | RESOLVED | Task 2 now comprehensively migrates Candidate, state references, evidence, sorting, all worktree_scan internals from u64 to String |
| REPORT_FORMAT_VERSION bump missing | HIGH | RESOLVED | Task 2 includes REPORT_FORMAT_VERSION increment and bumps schema version to reflect string keys + marker-aware evidence |
| Marker blocks cleanup (blocks prune) | HIGH | RESOLVED | Task 2 treats .baude-marker.json as non-blocking in evidence classification; marker alone does not prevent cleanup |
| "Discover owner from any checkout" unsafe | HIGH | RESOLVED | Task 3 adopt_markers_lazy requires UNANIMOUS common dir across all checkouts; disagreement → error event, no marker write |
| Silent adoption errors conflict with migration | HIGH | RESOLVED | Task 3 adopt_markers_lazy logs all events and returns them (not silent); caller can surface in startup notes/scan |
| Eager startup scanning threatens Phase 15 | HIGH | RESOLVED | Task 3 does NOT call adopt_markers_lazy during startup (lazy, deferred to admission/scan time per design answer) |
| files_modified missing workspace.rs, app.rs, bauded/api.rs | HIGH | RESOLVED | All three files added to files_modified at top of plan |

<output>
Create `.planning/phases/14-managed-worktree-identity/14-02-SUMMARY.md` when done
</output>
