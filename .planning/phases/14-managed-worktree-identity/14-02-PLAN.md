---
phase: 14
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - baude-core/src/git.rs
  - baude-core/src/worktree_scan.rs
  - baude-core/src/lifecycle.rs
  - baude-core/src/launch.rs
autonomous: true
requirements:
  - WTID-01
  - WTID-02
  - WTID-03

estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - Managed worktree paths use digest keys (repository-<12 hex>) for new repos, preserve legacy counter dirs for migration
    - Path composition functions accept &str instead of u64 for repository key parameter
    - Scan's repository_key parser accepts both legacy decimal (repository-1) and new hex (repository-abcdef123456) formats
    - Collision detection in ensure_repository compares marker canonical dir with newcomer's canonical dir
    - Marker adoption at startup walks managed directories and writes markers for legacy repos without one
  artifacts:
    - git.rs: managed_default_worktree_path(key: &str, checkout_key: u64) → PathBuf
    - git.rs: managed_branch_worktree_path(key: &str, checkout_key: u64, branch: &str) → PathBuf
    - worktree_scan.rs: repository_key(name: &str) → Option<String> (handles decimal and hex)
    - lifecycle.rs: ensure_repository returns Result<(RepositoryKey, Option<CollisionReport>), LifecycleError>
    - launch.rs: adopt_markers(workspace_name: &str) → Result<(), anyhow::Error>
  key_links:
    - git.rs path functions called by repository allocation and checkout creation; must accept string keys
    - worktree_scan::repository_key used to parse directory names; must round-trip with format!
    - lifecycle::ensure_repository is the admission gate where collisions are detected and reported
    - launch::adopt_markers runs at startup before app/daemon processes repositories
  prohibitions:
    - Never use decimal parsing only for repository_key; legacy dirs would be unrecognized
    - Never break the round-trip: format!("repository-{key}") must equal the original directory name
    - Never move or rename legacy repository-<counter> directories during migration
    - Never report collision as fatal error; newcomer gets fresh path and admission continues
---

<objective>
**Path composition and collision detection integration:** Update path composition to use digest-based string keys, extend scan to recognize both legacy and new formats, and implement collision detection in the admission flow. Marker adoption at startup handles legacy directory migration.

**Purpose:** Digest keys go into paths for new repositories. Legacy counter dirs are recognized via marker discovery. Collision detection is non-destructive and informational. Startup marker adoption bridges the gap between old and new schemes.

**Output:** Production-quality path changes, key parsing, and collision detection that handles both old and new directory schemes.
</objective>

<execution_context>
@~/.iarx/claude/gsd-core/workflows/execute-plan.md
@~/.iarx/claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-managed-worktree-identity/14-CONTEXT.md
@.planning/phases/14-managed-worktree-identity/14-RESEARCH.md
@.planning/phases/14-managed-worktree-identity/14-PATTERNS.md
@.planning/phases/14-managed-worktree-identity/14-01-SUMMARY.md

# Source files
@baude-core/src/git.rs
@baude-core/src/worktree_scan.rs
@baude-core/src/lifecycle.rs
@baude-core/src/launch.rs
@baude-core/src/repository.rs
@baude-core/src/persist.rs
@baude-core/src/testing.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Path composition functions use digest-based string keys (RED → GREEN)</name>
  <files>baude-core/src/git.rs, baude-core/src/workspace.rs</files>
  
  <read_first>
  - baude-core/src/git.rs lines 1748-1819 (current managed_default_worktree_path and managed_branch_worktree_path)
  - baude-core/src/workspace.rs lines 528-550 (path composition test pinning repository-<key> shape)
  </read_first>
  
  <behavior>
    **RED commit:** Create failing tests in workspace::tests:
    1. Test `managed_default_worktree_path_accepts_string_digest` — calls git::managed_default_worktree_path("abcdef123456", 1) and asserts returned path contains `repository-abcdef123456/primary-1`.
    2. Test `managed_branch_worktree_path_accepts_string_digest` — calls git::managed_branch_worktree_path("abcdef123456", 2, "feature/foo") and asserts path contains `repository-abcdef123456/feature_f-2`.
    3. Test `legacy_counter_path_composition_still_works` — calls with "1" (string) and asserts path contains `repository-1/primary-1` (verifies legacy paths round-trip).
    4. Test `path_composition_is_unchanged` — existing test should still pass (pinning the repository-<key>/<role>-<key> shape).
  </behavior>

  <action>
    In baude-core/src/git.rs, update function signatures:
    
    Change `managed_default_worktree_path(repository_key: u64, checkout_key: u64) -> PathBuf` to `managed_default_worktree_path(repository_key: &str, checkout_key: u64) -> PathBuf`. Update the format! call to use the string directly: `format!("repository-{repository_key}")` instead of `format!("repository-{repository_key}")` where the old key was u64.
    
    Change `managed_branch_worktree_path(repository_key: u64, checkout_key: u64, branch: &str) -> PathBuf` to `managed_branch_worktree_path(repository_key: &str, checkout_key: u64, branch: &str) -> PathBuf`. Update format! call identically.
    
    The path shape remains unchanged: `repository-{key}/<role>-{checkout_key}`. Update all existing call sites in the codebase to pass string references instead of u64 values. These call sites will be updated in later tasks but must compile here.
    
    Write the RED tests in workspace::tests that verify both new (digest) and legacy (decimal string) keys produce valid paths.
  </action>

  <verify>
    <automated>cd /Users/joese/.local/share/baude/worktrees/smoke/repository-1/primary-3 && cargo test -p baude-core --lib workspace::tests managed_default_worktree_path_accepts_string_digest managed_branch_worktree_path_accepts_string_digest legacy_counter_path_composition_still_works path_composition_is_unchanged -- --nocapture 2>&1 | tee /tmp/path_tests.log && [ "$(grep -c '^test ' /tmp/path_tests.log)" -ge 4 ] && grep -q "test result: ok" /tmp/path_tests.log</automated>
    <fails_when>Exit code non-zero, fewer than 4 tests run, or "test result: ok" not found</fails_when>
  </verify>

  <done>
    - git.rs managed_default_worktree_path signature is `(repository_key: &str, checkout_key: u64) -> PathBuf`
    - git.rs managed_branch_worktree_path signature is `(repository_key: &str, checkout_key: u64, branch: &str) -> PathBuf`
    - Both functions format the key directly in the path: `format!("repository-{repository_key}")`
    - The path shape is unchanged: `repository-<key>/<role>-<checkout_key>`
    - All four workspace::tests pass (new digest tests and legacy tests and pinned shape test)
    - `cargo build --all` succeeds (call sites may have compiler errors, fixed in next tasks)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scan repository_key parser handles both legacy decimal and new hex formats (RED → GREEN)</name>
  <files>baude-core/src/worktree_scan.rs</files>
  
  <read_first>
  - baude-core/src/worktree_scan.rs lines 1012-1015 (current repository_key function)
  - baude-core/src/worktree_scan.rs lines 744-771 (Candidate struct and repository_key field type)
  - baude-core/src/worktree_scan.rs lines 2471-2500 (existing test for path composition)
  </read_first>
  
  <behavior>
    **RED commit:** Create failing tests in worktree_scan::tests:
    1. Test `repository_key_parses_legacy_decimal` — calls repository_key("repository-1") and asserts returns Some("1").
    2. Test `repository_key_parses_new_hex_digest` — calls repository_key("repository-abcdef123456") and asserts returns Some("abcdef123456").
    3. Test `repository_key_rejects_malformed_hex` — calls repository_key("repository-gggggg123456") and asserts returns None (not valid hex).
    4. Test `repository_key_roundtrips_format_call` — for each valid key (decimal and hex), asserts that `format!("repository-{key}") == original_name`.
  </behavior>

  <action>
    In baude-core/src/worktree_scan.rs, update the repository_key function from returning Option<u64> to returning Option<String>. Change the logic to:
    1. Strip the "repository-" prefix.
    2. Try parsing as decimal u64 first (legacy format).
    3. If that succeeds, check round-trip: format!("repository-{num}") must equal the original name. If yes, return Some(num.to_string()).
    4. If decimal parsing fails or round-trip fails, try parsing as hex: check that suffix is exactly 12 characters and all chars are ASCII hex digits.
    5. If hex parsing succeeds, return Some(suffix.to_string()).
    6. If both fail, return None.
    
    Update the Candidate struct's repository_key field from u64 to String. This is a data structure change that propagates through the file.
    
    Write RED tests before implementing the change.
  </action>

  <verify>
    <automated>cd /Users/joese/.local/share/baude/worktrees/smoke/repository-1/primary-3 && cargo test -p baude-core --lib worktree_scan::tests repository_key -- --nocapture 2>&1 | tee /tmp/scan_tests.log && [ "$(grep -c '^test ' /tmp/scan_tests.log)" -ge 4 ] && grep -q "test result: ok" /tmp/scan_tests.log</automated>
    <fails_when>Exit code non-zero, fewer than 4 tests run, or "test result: ok" not found</fails_when>
  </verify>

  <done>
    - worktree_scan.rs repository_key function signature is `(name: &str) -> Option<String>`
    - Returns Some for both "repository-1" (legacy decimal) and "repository-abcdef123456" (new hex)
    - Returns None for malformed input like "repository-gggggg123456" (not valid hex)
    - Round-trip property holds: format!("repository-{key}") == original name for all valid keys
    - Candidate struct's repository_key field is now String instead of u64
    - All four repository_key tests pass
    - `cargo test -p baude-core --lib worktree_scan` passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Collision detection in ensure_repository and marker adoption at startup (RED → GREEN)</name>
  <files>baude-core/src/lifecycle.rs, baude-core/src/launch.rs</files>
  
  <read_first>
  - baude-core/src/lifecycle.rs lines 1307-1365 (ensure_repository function, repository allocation and recording logic)
  - baude-core/src/launch.rs lines 66-141 (start_workspace function, lock claim and binding record steps)
  - baude-core/src/marker.rs (read_marker, write_marker, MarkerMetadata — from Plan 01)
  - baude-core/src/git.rs (discover_repository_identity function)
  </read_first>
  
  <behavior>
    **RED commit:** Create failing tests in lifecycle::tests and launch::tests:
    1. Test `ensure_repository_detects_collision` — calls ensure_repository with a newcomer repo, pre-populated a managed directory with a marker for a different repo, asserts returns Ok((key, Some(collision_report))) where report.owner_canonical_dir != newcomer's canonical_dir.
    2. Test `ensure_repository_allocates_new_path_on_collision` — asserts that on collision, returned key differs from what would have been allocated to the owner (newcomer gets its own path).
    3. Test `adopt_markers_writes_marker_for_legacy_dir` — creates a legacy repository-<counter> directory with a primary-* checkout, calls adopt_markers, asserts marker file exists and deserializes correctly.
    4. Test `adopt_markers_skips_existing_markers` — creates a directory with existing marker, calls adopt_markers twice, asserts marker content unchanged (idempotent).
  </behavior>

  <action>
    In baude-core/src/lifecycle.rs, update ensure_repository to handle collisions non-destructively:
    1. After allocating a new repository key and computing the digest-based path (via git::managed_default_worktree_path with the digest string).
    2. Check if the computed path exists.
    3. If it exists, call `marker::read_marker(path)` to get ownership.
    4. If marker exists and its canonical_common_dir differs from newcomer's, construct a CollisionReport and return Ok((key, Some(report))).
    5. If marker is missing or matches the newcomer, continue with normal admission (return Ok((key, None))).
    6. Record the repository in state (existing logic).
    7. Change return type from Result<RepositoryKey, LifecycleError> to Result<(RepositoryKey, Option<CollisionReport>), LifecycleError>.
    
    In baude-core/src/launch.rs, add an adopt_markers function and call it after claiming the state lock:
    1. After the lock claim succeeds, before recording the binding.
    2. Walk the managed worktree base directory for the workspace.
    3. For each `repository-*` subdirectory:
       a. Check if `.baude-marker.json` exists; if yes, skip (already migrated).
       b. If not, try to discover the owner by reading git common dir from any `primary-*` or `*-*` checkout inside the directory.
       c. If a common dir is found, construct MarkerMetadata and write the marker via `marker::write_marker`.
       d. If no checkout is found, leave the directory without a marker (unknown owner).
    4. Silently continue on all errors; markers are advisory, and startup must not block on them.
    
    Write RED tests before implementing these changes.
  </action>

  <verify>
    <automated>cd /Users/joese/.local/share/baude/worktrees/smoke/repository-1/primary-3 && cargo test -p baude-core --lib lifecycle::tests ensure_repository_detects_collision ensure_repository_allocates_new_path_on_collision -- --nocapture 2>&1 | tee /tmp/collision_tests.log && [ "$(grep -c '^test ' /tmp/collision_tests.log)" -ge 2 ] && grep -q "test result: ok" /tmp/collision_tests.log && cargo test -p baude-core --lib launch::tests adopt_markers_writes_marker_for_legacy_dir adopt_markers_skips_existing_markers -- --nocapture 2>&1 | tee /tmp/adopt_tests.log && [ "$(grep -c '^test ' /tmp/adopt_tests.log)" -ge 2 ] && grep -q "test result: ok" /tmp/adopt_tests.log</automated>
    <fails_when>Exit code non-zero, fewer than 4 tests total run, or "test result: ok" not found in either log</fails_when>
  </verify>

  <done>
    - lifecycle.rs ensure_repository signature is `(...) -> Result<(RepositoryKey, Option<CollisionReport>), LifecycleError>`
    - Collision detection reads marker and compares canonical dir with newcomer
    - Returns Some(CollisionReport) when paths collide, None when no collision
    - launch.rs exports adopt_markers(workspace_name: &str) → Result<()>
    - adopt_markers walks managed directories, discovers owner from checkouts, writes markers for legacy dirs
    - Marker adoption is silent (errors logged but not propagated; advisory)
    - All four tests pass (two collision detection, two marker adoption)
    - `cargo test -p baude-core --lib lifecycle` and `cargo test -p baude-core --lib launch` both pass
    - `cargo build --all` succeeds (acceptance_criteria validated on next task)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Path parsing | repository_key function must accept both legacy and new formats; malformed input must be rejected. |
| Marker read during collision detection | Malformed marker must not crash; treat as ownership unknown and continue. |
| Legacy dir discovery | Must not follow symlinks out of the managed worktree base during checkout discovery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-06 | Tampering | Path parsing round-trip | medium | mitigate | Round-trip check `format!("repository-{key}") == name` prevents aliases and parsing ambiguities. |
| T-14-07 | Tampering | Collision detection bypass | medium | mitigate | Marker is source of truth; if missing, discover owner from git checkouts inside directory (fail-closed). |
| T-14-08 | Denial of Service | Legacy discovery walks unbounded directories | medium | mitigate | Only examine subdirectories matching primary-* or *-* patterns; limit to one level. |
| T-14-09 | Information Disclosure | Canonical dir in collision report | medium | mitigate | Report is internal; user sees only "repository-<digest>" display in TUI. Canonical dir logged only in daemon debug output. |
</threat_model>

<verification>
Path composition accepts string keys for both legacy and new formats. Scan parser recognizes both decimal and hex and round-trips correctly. Collision detection compares canonical dirs and returns non-fatal reports. Marker adoption at startup bridges legacy and new schemes. All tests pass in isolation and under TestRedirect isolation.
</verification>

<success_criteria>
- Managed path functions accept &str keys instead of u64, preserving repository-<key>/<role>-<key> shape
- Scan repository_key parser accepts decimal (legacy) and 12-char hex (new) formats
- Round-trip property holds for all valid keys
- Collision detection compares marker canonical dir with newcomer's canonical dir
- Marker adoption at startup walks directories, discovers owner from checkouts, writes markers for legacy repos
- All four new tests pass (path composition, scan parsing, collision detection, marker adoption)
- `cargo build --all` and `cargo test --all --lib` both succeed
</success_criteria>

<output>
Create `.planning/phases/14-managed-worktree-identity/14-02-SUMMARY.md` when done
</output>
