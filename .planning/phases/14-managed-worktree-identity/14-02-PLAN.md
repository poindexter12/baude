---
phase: 14
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - baude-core/src/repository.rs
  - baude-core/src/git.rs
  - baude-core/src/worktree_scan.rs
  - baude-core/src/lifecycle.rs
autonomous: true
requirements:
  - WTID-01
  - WTID-02

estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - SavedRepository stores physical_key: String (legacy counter or digest); path composition uses this string
    - RepositoryState::physical_key(key: RepositoryKey) -> Option<&str> maps logical key to physical key
    - Managed worktree paths use digest keys (repository-<12 hex>) for new repos, preserve legacy counter dirs for migration
    - Path composition functions accept &str instead of u64 for repository key parameter
    - Scan's repository_key parser accepts both legacy decimal (repository-1) and new hex (repository-abcdef123456) formats
    - Collision detection in ensure_repository compares marker canonical dir with newcomer's canonical dir
    - Unknown-owner (missing/malformed/symlink/oversized marker) directories are treated as collisions (allocated separate path, not reused)
    - Checkout-key allocation skips any n whose composed path already exists on disk (state-reset recovery)
  artifacts:
    - repository.rs: SavedRepository struct gains `pub physical_key: String` field with `#[serde(default)]` for backward compat
    - repository.rs: RepositoryState::physical_key(&self, key: RepositoryKey) -> Option<&str> accessor
    - git.rs: managed_default_worktree_path(key: &str, checkout_key: u64) → PathBuf
    - git.rs: managed_branch_worktree_path(key: &str, checkout_key: u64, branch: &str) → PathBuf
    - worktree_scan.rs: repository_key(name: &str) → Option<String> (handles decimal, hex, and hex+suffix)
    - lifecycle.rs: ensure_repository returns Result<(RepositoryKey, Option<CollisionReport>), LifecycleError>
  key_links:
    - repository.rs SavedRepository.physical_key is read via RepositoryState::physical_key() accessor before any path composition
    - git.rs path functions called by repository allocation and checkout creation; must accept string keys
    - worktree_scan::repository_key used to parse directory names; must round-trip with format!
    - lifecycle::ensure_repository is the admission gate where collisions are detected, unknown owners rejected, and physical_key is determined
  prohibitions:
    - Never use decimal parsing only for repository_key; legacy dirs would be unrecognized
    - Never break the round-trip: format!("repository-{key}") must equal the original directory name
    - Never move or rename legacy repository-<counter> directories during migration
    - Never report collision as fatal error; newcomer gets fresh path and admission continues
    - Never reuse an unknown-owner (missing/malformed/symlink marker) directory; treat as collision and allocate distinct path
    - Never allocate a checkout to a path that already exists on disk (pre-existing ownership proves recovery)
    - Never call adopt_markers_lazy during startup; adoption happens only at admission time per design answer D
---

<objective>
**Physical key resolution, path composition, and scan schema update:** Store physical_key (legacy counter or digest) in SavedRepository; wire it into path composition; update worktree_scan to use string keys comprehensively (touching prune schema); implement collision detection in admission flow with non-destructive fallback.

**Purpose:** Physical keys are deterministic (digest for new repos, legacy counter for old) and persisted per entry. Path composition uses string keys via the accessor. Collision detection allocates newcomers to distinct paths. Scan schema is versioned for prune safety. Adoption happens at admission time, not at startup.

**Output:** Production-quality key storage, path resolution, comprehensive scanner migration, and non-destructive collision handling.
</objective>

## Artifacts this phase produces

**Updated data structures:**
- `baude-core/src/repository.rs` — `SavedRepository` struct (entry in RepositoryState.repositories map) gains field `pub physical_key: String` with `#[serde(default)]` for backward compatibility (pre-v2.3 entries without physical_key load with `physical_key = key.to_string()` as legacy fallback during load)

**New functions and accessors:**
- `baude-core/src/repository.rs` — new accessor `pub fn physical_key(&self, key: RepositoryKey) -> Option<&str>` on RepositoryState that returns the physical_key string from the SavedRepository entry (or None if key not found)

**Updated function signatures:**
- `baude-core/src/git.rs` — `pub fn managed_default_worktree_path(repository_key: &str, checkout_key: u64) -> PathBuf`; `pub fn managed_branch_worktree_path(repository_key: &str, checkout_key: u64, branch: &str) -> PathBuf`

**Updated call sites within baude-core:**
- `baude-core/src/git.rs` — path functions and ~4 test calls (lines 4442-4444, 4454) updated to pass &str keys instead of u64
- `baude-core/src/lifecycle.rs` — `ensure_repository` updated to compute physical_key (via marker discovery and digest), use it in path composition and state record; admission site at line ~1356: `git::managed_branch_worktree_path(physical_key, ...)`
- `baude-core/src/workspace.rs` — ~3 test calls (lines 522, 663, 669) updated to pass &str keys; path composition test updated

**Updated scanner data structures:**
- `baude-core/src/worktree_scan.rs` — `repository_key()` function returns `Option<String>` (parses exactly three formats: `^[0-9]+$` legacy, `^[0-9a-f]{12}$` digest, `^[0-9a-f]{12}-[0-9]+$` digest+suffix); `Candidate` struct field `repository_key: String` (changed from u64); `ScanReport` versioning bumped

**Scanner/Prune schema changes:**
- `baude-core/src/worktree_scan.rs` — `REPORT_FORMAT_VERSION` incremented; `Candidate` and evidence structures updated for string keys; prune evidence now excludes `.baude-marker.json` alone (marker does not count as contents that block cleanup)

**New functions and data structures:**
- `baude-core/src/lifecycle.rs` — `CollisionReport` struct: `pub struct CollisionReport { pub requested_path: String, pub allocated_path: Option<String>, pub owner_display: String }` (per design answer 3)
- `baude-core/src/lifecycle.rs` — `ensure_repository` now allocates `repository-<12hex>-<n>` on collision (smallest n ≥ 2 that is free or owned by us); returns `Result<(RepositoryKey, Option<CollisionReport>), LifecycleError>` (per design answer 3)
- `baude-core/src/launch.rs` — new function `pub fn adopt_markers_lazy(workspace_name: &str) -> Result<Vec<MarkerAdoptionEvent>>` (NOT called at startup; lazy, called by admission/scan per design answer 11)

<execution_context>
@~/.iarx/claude/gsd-core/workflows/execute-plan.md
@~/.iarx/claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-managed-worktree-identity/14-CONTEXT.md
@.planning/phases/14-managed-worktree-identity/14-RESEARCH.md
@.planning/phases/14-managed-worktree-identity/14-PATTERNS.md
@.planning/phases/14-managed-worktree-identity/14-01-SUMMARY.md

# Source files
@baude-core/src/git.rs
@baude-core/src/worktree_scan.rs
@baude-core/src/lifecycle.rs
@baude-core/src/launch.rs
@baude-core/src/repository.rs
@baude-core/src/persist.rs
@baude-core/src/testing.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Path composition functions accept &str keys and wire resolver; all call sites updated (RED → GREEN)</name>
  <files>baude-core/src/git.rs, baude-core/src/workspace.rs, baude-core/src/lifecycle.rs, baude/src/app.rs, bauded/src/api.rs</files>

  <read_first>
  - baude-core/src/git.rs lines 1748-1819 (managed_default_worktree_path, managed_branch_worktree_path)
  - baude-core/src/lifecycle.rs lines 1307-1365 (ensure_repository, repository allocation)
  - baude-core/src/repository.rs (compute_repository_digest from 14-01; resolve_physical_key will be added here)
  - baude-core/src/workspace.rs lines 528-550 (path composition test)
  - baude/src/app.rs lines 1914-1947 (admit_repository), 1993, 7060, 7099, 7860 (path calls)
  - bauded/src/api.rs line ~868 (path call)
  </read_first>

  <behavior>
    **RED commit:** Create failing tests:
    1. Test `git::tests::managed_default_worktree_path_accepts_string_digest` — git::managed_default_worktree_path("abcdef123456", 1) returns a path ending in `repository-abcdef123456/primary-1`.
    2. Test `git::tests::managed_branch_worktree_path_accepts_string_digest` — git::managed_branch_worktree_path("abcdef123456", 2, "feature/foo") returns a path ending in `repository-abcdef123456/feature_f-2`.
    3. Test `git::tests::legacy_counter_path_composition_still_works` — the legacy counter key "1" composes to `repository-1/primary-1`, byte-identical to the pre-Phase-14 path for the same inputs.
    4. Test `git::tests::path_composition_is_unchanged` — pins the shape `repository-<key>/<role>-<checkout_key>` for a digest key, a suffixed key `abcdef123456-2`, and a legacy key; only the key text changes.
    5. Test `lifecycle::tests::ensure_repository_uses_physical_key_from_saved_entry` — adds a SavedRepository with physical_key "legacy-counter", calls ensure_repository, asserts path composition uses that physical_key.
    6. Test `repository::tests::physical_key_accessor_returns_entry_value` — creates RepositoryState with SavedRepository.physical_key set, calls state.physical_key(key), asserts returns the stored physical_key string.

    All tests fail due to signature changes and physical_key not yet stored.

    **GREEN commit:** Implement:
    - Path functions accept `&str` keys; update all call sites
    - SavedRepository gains physical_key: String field; load defaults empty entries to key.to_string()
    - RepositoryState::physical_key(key) accessor returns &str from SavedRepository
    - lifecycle::ensure_repository sets physical_key from marker or digest, stores in SavedRepository, uses in path composition
  </behavior>

  <action>
    **Step 1: Extend SavedRepository with physical_key field** — In baude-core/src/repository.rs, per design answer C:
    - Add field to SavedRepository: `pub physical_key: String` with `#[serde(default)]` for backward compatibility.
    - On load, entries without physical_key default to empty string; the accessor fills in `key.to_string()` (legacy counter) as fallback.
    - Add accessor to RepositoryState: `pub fn physical_key(&self, key: RepositoryKey) -> Option<&str>` that looks up the entry and returns its physical_key string (or None if key not found).
    - Remove any BTreeMap<Vec<u8>, String> or BTreeMap<String, String> mapping (not needed per design answer C).

    **Step 2: Update git.rs path functions** — Change signatures:
    - `managed_default_worktree_path(repository_key: &str, checkout_key: u64) -> PathBuf`
    - `managed_branch_worktree_path(repository_key: &str, checkout_key: u64, branch: &str) -> PathBuf`
    - Both use `format!("repository-{repository_key}")` instead of formatting from numeric u64.

    **Step 3: Update call sites within baude-core to pass &str keys** — Update these ~7 locations:
    - `baude-core/src/git.rs` lines 4442-4454: test calls change from numeric to string keys (e.g., `7` → `"7"`)
    - `baude-core/src/workspace.rs` lines 522, 663, 669: test calls change from numeric to string keys
    - These are TEST calls only; real admission path is updated in Task 3 (lifecycle.rs).

    **Step 4: Implement physical_key determination in lifecycle.rs ensure_repository** — Per design answers C, D, E, F, G:
    - In lifecycle.rs ensure_repository (not a separate function):
      1. Discover canonical_common_dir via git::discover_repository_identity
      2. Compute digest: `repository::compute_repository_digest(canonical_common_dir.as_bytes())`
      3. Check state.repositories for existing entry with matching canonical_common_dir (encoded): if found, reuse its existing physical_key
      4. If not in state, check for marker on filesystem at `repository-<digest>/.baude-marker.json`: if Valid marker with matching canonical_common_dir, use that digest as physical_key
      5. If no marker but checkouts exist in the directory, read git common dir from ALL child checkouts; if UNANIMOUS match (all agree), adopt that directory and use its counter as physical_key
      6. If marker missing and checkouts differ/absent (unknown owner), treat as collision, don't reuse
      7. If fresh (not found anywhere), use digest as physical_key
    5. Check if the chosen directory exists on disk:
       - If exists AND has marker with matching canonical_common_dir: reuse it (no collision)
       - If exists AND has marker with DIFFERENT canonical_common_dir: collision detected, allocate `repository-<physical_key>-<n>` (smallest n ≥ 2 not occupied)
       - If exists AND has INVALID/UNKNOWN marker: collision (treat as unknown owner), allocate suffix
       - If not exists: create it (std::fs::create_dir)
    6. Write marker via marker::write_marker into the chosen directory with canonical_common_dir (only if not already there with matching content)
    7. Record repository in state with computed physical_key
    8. Return (RepositoryKey, Some(CollisionReport)) if collision, or (RepositoryKey, None) if no collision

    **Commits:**
    1. `test(14-02): add failing tests for physical_key storage and string-key path composition` — tests fail due to signatures and missing field
    2. `feat(14-02): add SavedRepository.physical_key, implement physical-key determination in ensure_repository, update call sites to use &str keys` — updates repository.rs, git.rs, lifecycle.rs, workspace.rs
  </action>

  <verify>
    <automated>set -o pipefail && cd /Users/joese/.local/share/baude/worktrees/smoke/repository-1/primary-3 && cargo test -p baude-core --lib -- --nocapture managed_default_worktree_path_accepts_string_digest managed_branch_worktree_path_accepts_string_digest legacy_counter_path_composition_still_works path_composition_is_unchanged ensure_repository_uses_physical_key_from_saved_entry physical_key_accessor_returns_entry_value 2>&1 | tee /tmp/path_tests.log && [ "$(grep -c '^test ' /tmp/path_tests.log)" -ge 6 ] && grep -q "test result: ok" /tmp/path_tests.log</automated>
    <fails_when>Exit code non-zero, fewer than 6 tests run, or "test result: ok" not found</fails_when>
  </verify>

  <done>
    - baude-core/src/repository.rs SavedRepository struct has field `pub physical_key: String` with `#[serde(default)]`
    - Load logic: entries without physical_key default to key.to_string() (legacy fallback)
    - No BTreeMap mapping in state (design answer C: physical_key is stored with SavedRepository entry)
    - baude-core/src/repository.rs RepositoryState exports `pub fn physical_key(&self, key: RepositoryKey) -> Option<&str>` accessor
    - git.rs managed_default_worktree_path signature is `(repository_key: &str, checkout_key: u64) -> PathBuf`
    - git.rs managed_branch_worktree_path signature is `(repository_key: &str, checkout_key: u64, branch: &str) -> PathBuf`
    - Both functions format the key directly in the path: `format!("repository-{repository_key}")`
    - The path shape is unchanged: `repository-<key>/<role>-<checkout_key>`
    - All six Task 1 tests pass: four path-composition tests in git::tests (digest, branch, legacy counter, pinned shape) and two state tests in lifecycle::tests and repository::tests
    - All ~7 call sites in git.rs (4), workspace.rs (3) are updated to pass &str keys (baude/src/app.rs and bauded/src/api.rs calls move to Task 3 per Task boundaries)
    - lifecycle.rs ensure_repository determines physical_key via marker/digest/checkout-discovery and stores it in SavedRepository on admission
    - `cargo build --workspace` succeeds with no compiler errors
    - `cargo test -p baude-core --lib` passes (all path and state tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Comprehensive worktree_scan schema update: string keys, marker-aware evidence, REPORT_FORMAT_VERSION bump (RED → GREEN)</name>
  <files>baude-core/src/worktree_scan.rs</files>

  <read_first>
  - baude-core/src/worktree_scan.rs lines 1012-1015 (repository_key function)
  - baude-core/src/worktree_scan.rs lines 744-771 (Candidate struct)
  - baude-core/src/worktree_scan.rs lines 773-798 (ScanReport, REPORT_FORMAT_VERSION, report versioning logic)
  - baude-core/src/worktree_scan.rs lines 1052-1088 (evidence classification, contents evidence, directory walk)
  - baude-core/src/worktree_scan.rs lines 1283-1290 (prune version mismatch handling)
  - baude-core/src/marker.rs (MarkerMetadata from Plan 14-01)
  </read_first>

  <behavior>
    **RED commit:** Create failing tests:
    1. Test `repository_key_parses_legacy_decimal` — calls repository_key("repository-1"), asserts returns Some("1").
    2. Test `repository_key_parses_new_hex_digest` — calls repository_key("repository-abcdef123456"), asserts returns Some("abcdef123456").
    3. Test `repository_key_rejects_malformed` — calls repository_key("repository-gggggg123456"), asserts returns None.
    4. Test `repository_key_roundtrips` — format!("repository-{key}") == original name for all valid keys.
    5. Test `marker_file_does_not_count_as_contents` — directory with only `.baude-marker.json` is treated as empty (not contents-blocking).
    6. Test `scan_report_version_changed` — new version reflects string-key + marker-aware schema.

    Tests fail due to signature changes and missing marker-awareness logic.

    **GREEN commit:** Implement:
    - repository_key returns Option<String>, parses decimal (with round-trip check) and hex (12-char ASCII hex)
    - Candidate.repository_key: String; all internal references updated
    - REPORT_FORMAT_VERSION incremented
    - Evidence classification treats .baude-marker.json as non-blocking
    - All state/evidence references using repository_key updated to String
  </behavior>

  <action>
    1. Update `repository_key(name: &str) -> Option<String>` (per design answer 2):
       - Strip "repository-" prefix (if present)
       - Accept exactly three formats:
         - Legacy decimal: `^[0-9]+$` — parse as u64, verify round-trip format!("repository-{num}") == name, return Some(num_str)
         - New digest: `^[0-9a-f]{12}$` — exactly 12 hex chars, return Some(digest)
         - Digest+suffix: `^[0-9a-f]{12}-[0-9]+$` — digest (12 hex) then hyphen then decimal suffix, return Some(full_key)
       - Reject all other formats (return None)

    2. Update Candidate struct: `repository_key: String` (instead of u64)

    3. Update all references to candidate.repository_key throughout worktree_scan.rs from numeric to String:
       - repository_key_from_name function calls
       - state references using repository_key
       - evidence/predicate/verdict logic
       - sorting operations
       - any format! calls building path strings

    4. Update evidence classification: When walking directory to check if it's empty, skip `.baude-marker.json`:
       - In the contents-discovery loop (~lines 1052-1088), add check: `if child_name == ".baude-marker.json" { continue; }`
       - Marker file alone does not count as contents that block cleanup

    5. Increment REPORT_FORMAT_VERSION (e.g., "2" → "3")

    6. Update prune version-mismatch message to reference new version

    7. Update state read/write paths that reference repository_key: now String not u64

    **Commits:**
    1. `test(14-02): add failing tests for string-key scanning and marker-aware evidence` — tests fail due to signatures
    2. `feat(14-02): comprehensive worktree_scan schema migration to string keys, marker-aware evidence, version bump` — migrates u64→String everywhere, increments REPORT_FORMAT_VERSION
  </action>

  <verify>
    <automated>set -o pipefail && cd /Users/joese/.local/share/baude/worktrees/smoke/repository-1/primary-3 && cargo test -p baude-core --lib -- --nocapture repository_key marker_file_does_not_count_as_contents scan_report_version 2>&1 | tee /tmp/scan_tests.log && [ "$(grep -c '^test ' /tmp/scan_tests.log)" -ge 6 ] && grep -q "test result: ok" /tmp/scan_tests.log && cargo build --workspace</automated>
    <fails_when>Exit code non-zero, fewer than 6 tests run, "test result: ok" not found, or build fails</fails_when>
  </verify>

  <done>
    - worktree_scan.rs repository_key function signature is `(name: &str) -> Option<String>`
    - Parses both "repository-1" (legacy decimal) and "repository-abcdef123456" (new hex)
    - Returns None for malformed input (invalid hex, round-trip mismatch)
    - Round-trip property holds: format!("repository-{key}") == original name
    - Candidate struct's repository_key field is now String instead of u64
    - All internal references to candidate.repository_key updated to String
    - Evidence classification treats .baude-marker.json as non-blocking (marker alone not contents-blocking)
    - REPORT_FORMAT_VERSION incremented to reflect schema change (string keys + marker-aware evidence)
    - All tests pass (repository_key parsing, marker-aware evidence, version handling)
    - `cargo build --all` succeeds with no errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Collision detection matrix and unknown-owner handling in ensure_repository (RED → GREEN)</name>
  <files>baude-core/src/lifecycle.rs</files>

  <read_first>
  - baude-core/src/lifecycle.rs lines 1307-1365 (ensure_repository function)
  - baude-core/src/marker.rs (read_marker, write_marker, MarkerRead, MarkerMetadata, InvalidReason — from Plan 01)
  - baude-core/src/git.rs (discover_repository_identity function, managed_default_worktree_path)
  </read_first>

  <behavior>
    **RED commit:** Create failing tests in lifecycle::tests:
    1. Test `ensure_repository_unknown_owner_missing_marker` — directory exists but no marker and no checkouts inside; asserts collision detected, newcomer allocated suffix.
    2. Test `ensure_repository_unknown_owner_invalid_marker` — directory has oversized marker (invalid); asserts collision, newcomer gets suffix.
    3. Test `ensure_repository_unknown_owner_symlink_marker` — marker is symlink; asserts collision, newcomer allocated suffix.
    4. Test `checkout_allocation_skips_existing_dirs_after_state_reset` — resets state, allocates checkout to primary-1, then resets state again, allocates another checkout; asserts second checkout skips primary-1 and uses primary-2.
    5. Test `ensure_repository_foreign_marker_collision` — directory has marker with different canonical_common_dir; asserts collision reported with owner info.
    6. Test `ensure_repository_matching_marker_no_collision` — directory has marker with matching canonical_common_dir; asserts no collision, path reused.

    All tests fail due to collision matrix logic not yet implemented.

    **GREEN commit:** Implement:
    - All six collision matrix outcomes (Valid/same, Valid/different, Missing, Invalid, + suffixed path allocation)
    - Unknown-owner outcomes: treat as collisions, allocate suffix
    - Checkout-key allocation: skip existing paths on disk
    - Marker write: only if needed (don't rewrite matching marker)
  </behavior>

  <action>
    **(1) Implement collision detection matrix in lifecycle.rs ensure_repository** — Per design answer E, implement this matrix after computing physical_key and target path:
    
    | Marker | Checkout evidence | Outcome |
    | Valid, same common dir | any | ours: reuse, no write |
    | Valid, different dir | any | collision → allocate `repository-<physical_key>-<n>` |
    | Missing | a checkout inside proves OUR common dir | adopt: write marker, reuse |
    | Missing | a checkout proves ANOTHER common dir | collision |
    | Missing | no checkout, or unreadable | unknown owner → collision |
    | Invalid (symlink/oversized/malformed/scheme too new) | any | unknown owner → collision, allocate suffix |
    
    Implementation steps:
    1. Call marker::read_marker(target_dir) → MarkerRead (Missing | Valid(meta) | Invalid(reason))
    2. If Valid(meta) with matching canonical_common_dir → no collision, reuse path, no write needed
    3. If Valid(meta) with different canonical_common_dir → collision, record owner info, allocate suffix
    4. If Invalid(reason) → unknown owner, treat as collision, allocate suffix
    5. If Missing:
       a. Try to discover owner from child checkouts: read git common dir from every child (primary-*, *-*) via git::discover_repository_identity
       b. If all checkouts agree and match OUR canonical_common_dir → adopt: write marker, reuse path
       c. If checkouts exist but disagree or don't match → unknown owner, treat as collision
       d. If no checkouts found → unknown owner, treat as collision
    6. On collision, allocate `repository-<physical_key>-<n>` for smallest n ≥ 2 that is either free or already owned by us (check marker in suffixed dir)
    7. On collision, construct CollisionReport with requested_path, allocated_path, owner_common_dir (from marker or checkout), owner_display (via repository::repository_display_name), requester_common_dir, requester_display
    
    **(2) Implement checkout-key allocation safety** — Per design answer F:
    - After computing target checkout path (e.g., `repository-<key>/primary-<n>`), check if path exists on disk before using it
    - In allocation loop, skip any n whose path already exists
    - This ensures state-reset recovery: after state file reset, allocation starts at 1 but skips occupied directories
    - Test: allocate to primary-1, reset state, allocate again, assert second checkout uses primary-2 (primary-1 path exists, so skipped)

    **(3) Persistence ordering** — Per design answer G:
    - Order: compute physical_key → create_dir → write marker → mutate in-memory state → caller persists state
    - If persist fails afterwards, marker still proves ownership and next admission recreates the entry (same recovery path as state reset)
    - Document in action: "Marker and directory are durable first; state persistence is final step"

    **Commits:**
    1. `test(14-02): add failing tests for collision matrix, unknown-owner detection, and checkout-key safety` — tests fail at compile time
    2. `feat(14-02): implement full collision detection matrix with unknown-owner handling and checkout-key allocation safety` — updates lifecycle.rs, tests pass
  </action>

  <verify>
    <automated>set -o pipefail && cd /Users/joese/.local/share/baude/worktrees/smoke/repository-1/primary-3 && cargo test -p baude-core --lib -- --nocapture ensure_repository_unknown_owner_missing_marker ensure_repository_unknown_owner_invalid_marker ensure_repository_unknown_owner_symlink_marker checkout_allocation_skips_existing_dirs_after_state_reset ensure_repository_foreign_marker_collision ensure_repository_matching_marker_no_collision 2>&1 | tee /tmp/collision_tests.log && [ "$(grep -c '^test ' /tmp/collision_tests.log)" -ge 6 ] && grep -q "test result: ok" /tmp/collision_tests.log && cargo build --workspace</automated>
    <fails_when>Exit code non-zero, fewer than 6 tests run, or "test result: ok" not found</fails_when>
  </verify>

  <done>
    - lifecycle.rs ensure_repository signature is `(...) -> Result<(RepositoryKey, Option<CollisionReport>), LifecycleError>` (no change in signature from Task 1)
    - Collision detection reads marker via marker::read_marker, compares canonical_common_dir bytes with newcomer
    - Returns Some(CollisionReport) when collision detected (different canonical dirs, unknown owner, or invalid marker)
    - Returns None when no collision or marker matches
    - Unknown-owner handling: missing marker with no/conflicting checkouts, invalid marker (symlink/oversized/malformed), all treated as collisions
    - On collision, allocates `repository-<physical_key>-<n>` for smallest n ≥ 2 not occupied or owned by us (via marker check)
    - Checkout-key allocation skips paths that already exist on disk (state-reset recovery without scan)
    - Marker adoption at discovery: if marker missing but checkouts prove UNANIMOUS common dir, marker is written; if checkouts disagree/missing, collision
    - Persistence ordering: physical_key computed → directory created → marker written → state mutated → caller persists
    - All six tests pass (unknown owner matrix: missing, invalid, symlink; checkout safety; foreign marker; matching marker)
    - `cargo test -p baude-core --lib lifecycle` passes
    - `cargo build --workspace` succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Path parsing | repository_key function must accept both legacy and new formats; malformed input must be rejected. |
| Marker read during collision detection | Malformed marker must not crash; treat as ownership unknown and continue. |
| Legacy dir discovery | Must not follow symlinks out of the managed worktree base during checkout discovery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-06 | Tampering | Path parsing round-trip | medium | mitigate | Round-trip check `format!("repository-{key}") == name` prevents aliases and parsing ambiguities. Exactly three formats accepted. |
| T-14-07 | Tampering | Collision detection: marker vs newcomer | high | mitigate | ensure_repository reads marker and compares canonical_common_dir bytes; allocation of distinct suffixed dir (repository-<12hex>-n) is atomic via create_dir; no race window after one succeeds. |
| T-14-08 | Denial of Service | Legacy discovery walks unbounded directories | medium | mitigate | Only examine subdirectories matching primary-* or *-* patterns; limit to one level. Adoption event logging prevents silent failures. |
| T-14-09 | Information Disclosure | Canonical dir in collision report | medium | mitigate | Report is internal; user sees only "repository-<digest>" display in TUI. Canonical dir logged only in daemon debug output. |
</threat_model>

<verification>
Path composition accepts string keys for both legacy and new formats. Scan parser recognizes both decimal and hex and round-trips correctly. Collision detection compares canonical dirs and returns non-fatal reports. Marker adoption at startup bridges legacy and new schemes. All tests pass in isolation and under TestRedirect isolation.
</verification>

<success_criteria>
- Managed path functions accept &str keys instead of u64, preserving repository-<key>/<role>-<key> shape
- Scan repository_key parser accepts decimal (legacy) and 12-char hex (new) formats
- Round-trip property holds for all valid keys
- Collision detection compares marker canonical dir with newcomer's canonical dir
- Resolver determines physical key (legacy or fresh digest) and wires into path composition
- Lazy marker adoption returns events (not silent) with unanimous-checkout validation
- All tests pass (path composition, scan parsing, collision detection, resolver integration, lazy adoption)
- `cargo build --all` and `cargo test --all --lib` both succeed
</success_criteria>

## Review Dispositions (Cycle 2)

| Finding | Severity | Disposition | Action in Plan |
|---------|----------|-------------|-----------------|
| No resolver from canonical dir to physical key | HIGH | RESOLVED | Physical key determination happens inside ensure_repository (not a separate function per design answer C); physical_key is stored in SavedRepository. |
| Collision continuation undefined | HIGH | RESOLVED | ensure_repository allocates repository-<12hex>-<n> for smallest n ≥ 2 that is free or owned by us (design answer E); tests added for all matrix outcomes. |
| Parser conflicts (fallback digests vs fixed 12-hex) | HIGH | RESOLVED | Fixed: repository_key parser accepts exactly three shapes per design answer N: ^[0-9]+$, ^[0-9a-f]{12}$, ^[0-9a-f]{12}-[0-9]+$ (no variable-length fallback). |
| State schema BTreeMap invalid for persistence | HIGH | RESOLVED | Per design answer C: dropped BTreeMap entirely; physical_key stored as field in SavedRepository (not a parallel map). Load fallback: entries without physical_key default to key.to_string(). |
| Scanner change scope underestimated | HIGH | RESOLVED | Task 2 comprehensively migrates u64 → String in Candidate, all state references, evidence, sorting, predicate/verdict, scan internals. |
| REPORT_FORMAT_VERSION not bumped | HIGH | RESOLVED | Task 2 increments REPORT_FORMAT_VERSION; schema change covers string keys + marker-aware evidence. |
| Marker blocks cleanup | HIGH | RESOLVED | Task 2 excludes .baude-marker.json from contents evidence; marker alone does not prevent cleanup. |
| Unknown-owner behavior incomplete | HIGH | RESOLVED | Task 3 implements full matrix per design answer E: missing/invalid/symlink markers all treated as unknown-owner collisions (never reused, allocated suffix). |
| Adoption only at admission, not scan | MEDIUM | RESOLVED | Design answer D: adoption happens during ensure_repository (not scan, not startup). Lazy discovery of legacy dirs via checkout consensus. |
| Checkout-key recovery missing | HIGH | RESOLVED | Design answer F: allocation loop skips any n whose composed path already exists on disk; state-reset recovery doesn't reuse occupied checkouts. Test added. |

## Review Dispositions (Cycle 3)

| Finding | Severity | Disposition | Action in Plan |
|---------|----------|-------------|-----------------|
| Keys conflated: logical RepositoryKey vs physical key | HIGH | RESOLVED | Per design answer C: SavedRepository.physical_key: String stores the physical key (legacy or digest); RepositoryState::physical_key() accessor maps RepositoryKey → &str for path composition. Logical and physical are now distinct. |
| Scanner-to-state mapping underspecified | HIGH | RESOLVED | Scanner uses repository_key: String (parses decimal or hex); state stores physical_key on SavedRepository; no intermediate mapping table needed (design answer C). |
| Scan-time adoption violates read-only contract | HIGH | RESOLVED | Design answer D: adoption happens ONLY in ensure_repository during admission, never during scan. scan_at remains unconditionally read-only. |
| State-reset recovery incomplete for checkouts | HIGH | RESOLVED | Design answer F: checkout allocation skips existing paths on disk; after reset, primary-2, primary-3, etc. are reused if they exist, preventing collision with legacy checkouts. Test added. |
| Daemon legacy adoption missing | HIGH | RESOLVED | Design answer D: adoption happens in ensure_repository (shared by both TUI and daemon), not in separate paths. Both frontends call ensure_repository, same adoption logic. |
| Unknown-owner behavior vague | HIGH | RESOLVED | Design answer E: full matrix table added to Task 3 action; missing/invalid/symlink all treated as collisions, never reused. Tests cover all outcomes. |
| Marker creation before durable state saves | MEDIUM | RESOLVED | Design answer G: order is physical_key computed → directory created → marker written → state mutated → persist (caller's responsibility). If persist fails, marker proves ownership, next run recreates. |

<output>
Create `.planning/phases/14-managed-worktree-identity/14-02-SUMMARY.md` when done
</output>
