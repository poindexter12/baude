---
phase: 14
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified:
  - baude-core/Cargo.toml
  - baude-core/src/marker.rs
  - baude-core/src/repository.rs
  - baude-core/src/lib.rs
autonomous: true
requirements:
  - WTID-01
  - WTID-02

estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 1
  confidence: med

must_haves:
  truths:
    - Two repositories with identical canonical common dir always compute the same digest (deterministic)
    - A marker file written to a directory can be read back identically across processes
    - Collision detection reads marker from path and compares canonical dir with newcomer's dir
    - Marker write is atomic: temp file + sync + rename prevents corruption on crash
    - Missing marker file returns None, not error (fail-closed)
  artifacts:
    - baude-core/src/marker.rs exports pub fn write_marker, pub fn read_marker, pub struct MarkerMetadata
    - baude-core/src/repository.rs exports pub fn compute_repository_digest(common_dir_bytes: &[u8]) -> Result<String>
    - baude-core/src/lib.rs declares pub mod marker in alphabetical order
    - Tests prove digest stability, marker roundtrip, collision detection logic, marker ownership claim
  key_links:
    - marker::write_marker is called only by lifecycle::ensure_repository (new-dir claim after create_dir, and legacy adoption into an EXISTING directory); the exclusive create_new open is the ownership claim
    - repository::compute_repository_digest is called by lifecycle::ensure_repository and repository::resolve_physical_key (built in 14-02) to compute digest once (no fallback)
    - worktree_scan::repository_key must parse the 12-char hex result
  prohibitions:
    - Never overwrite a marker file without atomic guarantees (temp → sync → rename)
    - Never treat a missing marker file as an error — return Ok(None)
    - Never assume a digest is unique without monitoring — scan detects and reports collisions
    - Never use non-canonicalized paths for canonical dir comparisons
---

<objective>
**Marker module and digest resolution:** Implement the core infrastructure for stable repository identity based on git canonical common directory hashing. One end-to-end path proves that two repositories with the same common dir compute the same digest, markers are written and read atomically, and collisions are detected by comparing canonical dirs.

**Purpose:** Digest-keyed directories eliminate the collision source (separate TUI/daemon counters). Marker files record ownership for legacy directory migration and state-reset recovery. Atomic writes prevent corruption during concurrent or interrupted writes.

**Output:** Production-quality marker module with full test suite, digest computation function, and fail-closed design.
</objective>

## Artifacts this phase produces

**New modules and functions:**
- `baude-core/Cargo.toml` — adds `sha2 = "0.10"` and `base64 = "0.22"` to `[dependencies]` section (verify versions in `Cargo.lock`)
- `baude-core/src/marker.rs` — new file exporting `pub fn write_marker(dir: &Path, meta: &MarkerMetadata) -> Result<MarkerWrite, MarkerError>`, `pub fn read_marker(dir: &Path) -> Result<MarkerRead, io::Error>` with enums `MarkerRead { Missing, Valid(MarkerMetadata), Invalid(InvalidReason) }` and `MarkerWrite { AlreadyOwned(MarkerMetadata), Created }`, `pub struct MarkerMetadata { canonical_common_dir: Vec<u8>, scheme_version: u32, recorded_at_ms: u64 }` (all fields serialized via serde with `canonical_common_dir` as base64-encoded string in JSON)
- `baude-core/src/marker.rs` — exports `pub enum MarkerError { OwnedByOther(MarkerMetadata), SymlinkError, OversizedError(u64), MalformedError(String), SchemeTooNewError(u32) }`
- `baude-core/src/repository.rs` — new functions `pub fn compute_repository_digest(common_dir_bytes: &[u8]) -> String` returning first 12 lowercase hex characters of sha256(common_dir_bytes) (fixed length, no fallback); `pub fn encode_common_dir(bytes: &[u8]) -> String` and `pub fn decode_common_dir(s: &str) -> Option<Vec<u8>>` for base64 round-tripping
- `baude-core/src/repository.rs` — new function `pub fn repository_display_name(common_dir: &Path) -> String` returning basename of parent directory (or basename of common_dir if not ending in `.git`)

**Module exports:**
- `baude-core/src/lib.rs` — adds `pub mod marker` declaration in alphabetical order

**Test fixtures:**
- `marker::tests` module with comprehensive tests: digest_is_deterministic, marker_roundtrip, collision_detection_reads_marker, write_marker_into_existing_dir_succeeds, write_marker_refuses_foreign_marker, non_utf8_paths_in_marker, concurrent_marker_writes (via TestRedirect), scheme_version_handling

<execution_context>
@~/.iarx/claude/gsd-core/workflows/execute-plan.md
@~/.iarx/claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-managed-worktree-identity/14-CONTEXT.md
@.planning/phases/14-managed-worktree-identity/14-RESEARCH.md
@.planning/phases/14-managed-worktree-identity/14-PATTERNS.md
@.planning/phases/13-workspace-derivation-and-new-session-open-defaults/13-01-SUMMARY.md

# Source-of-truth files
@baude-core/src/persist.rs
@baude-core/src/git.rs
@baude-core/src/repository.rs
@baude-core/src/lifecycle.rs
@baude-core/src/testing.rs
@baude-core/src/workspace.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Marker module and digest resolution: end-to-end repository identity with collision detection (RED → GREEN)</name>
  <files>baude-core/src/marker.rs, baude-core/src/repository.rs, baude-core/src/lib.rs</files>
  
  <read_first>
  - baude-core/src/persist.rs (atomic_save_current pattern: temp → sync → rename + dir sync)
  - baude-core/src/repository.rs (existing RepositoryKey, allocate_repository_key pattern)
  - baude-core/src/git.rs (discover_repository_identity and canonicalize patterns)
  - baude-core/src/testing.rs (TestRedirect fixture for isolation)
  - baude-core/src/meta.rs (now_unix_ms function signature)
  </read_first>
  
  <behavior>
    **RED commit:** Create failing tests for multiple behaviors:
    1. Test: `marker::tests::digest_is_deterministic` — calls `repository::compute_repository_digest` twice on same bytes and asserts identical 12-character hex.
    2. Test: `marker::tests::marker_roundtrip` — writes MarkerMetadata with Vec<u8> canonical_common_dir to pre-existing dir via `marker::write_marker`, reads back via `marker::read_marker`, asserts fields match exactly and non-UTF-8 bytes survive.
    3. Test: `marker::tests::write_marker_first_writer_wins_second_sees_already_owned` — two threads call write_marker into same pre-existing directory simultaneously (TestRedirect isolation), asserts one creates (MarkerWrite::Created), other reads winner and gets (MarkerRead::Valid) with AlreadyOwned status.
    4. Test: `marker::tests::write_marker_refuses_foreign_marker` — creates marker with one canonical_common_dir, calls write_marker with different canonical_common_dir, asserts returns Err(MarkerError::OwnedByOther { existing }).
    5. Test: `marker::tests::concurrent_marker_writes_exactly_one_creates` — verifies only one thread succeeds in create_new; second thread reads the winner's marker atomically.
    6. Test: `marker::tests::non_utf8_paths_in_marker` — creates marker with non-UTF-8 canonical_common_dir, serializes to JSON via serde base64, deserializes, asserts bytes match exactly.
    7. Test: `marker::tests::read_marker_symlink_is_invalid` — creates symlink at marker path, calls read_marker, asserts returns Err(InvalidReason::Symlink) or similar.
    8. Test: `marker::tests::read_marker_oversized_is_invalid` — writes > 64 KiB file as marker, calls read_marker, asserts returns Err(InvalidReason::Oversized) or MarkerRead::Invalid.
    9. Test: `marker::tests::read_marker_malformed_is_invalid` — writes garbage JSON, calls read_marker, asserts returns Err(InvalidReason::Malformed) or MarkerRead::Invalid.
    
    All tests fail with compile errors or assertion failures.
    
    **GREEN commit:** Implement the complete marker module to make all 9 tests pass:
    - `repository::compute_repository_digest(common_dir_bytes: &[u8]) -> String` — hashes with sha2::Sha256, returns first 12 hex chars (fixed length, no fallback, no Result wrapper per design answer A).
    - `marker::write_marker(dir: &Path, meta: &MarkerMetadata) -> Result<MarkerWrite, MarkerError>` — writes `.baude-marker.json` atomically INTO EXISTING directory (caller creates dir): calls `std::fs::File::create` with `create_new(true)` (O_EXCL on Unix, first writer wins atomically). On `AlreadyExists`, calls `read_marker`: if Valid with matching canonical_common_dir → Ok(MarkerWrite::AlreadyOwned(existing)); if Valid with different dir → Err(MarkerError::OwnedByOther(existing)); if Invalid → Err(MarkerError::UnknownOwner(reason)). On success, syncs file and parent dir.
    - `marker::read_marker(dir: &Path) -> Result<MarkerRead, io::Error>` — opens marker with O_NOFOLLOW (reject symlinks), reads bounded to max 64 KiB, deserializes JSON. Returns Ok(MarkerRead::Missing) on NotFound; Ok(MarkerRead::Valid(metadata)) on success; Ok(MarkerRead::Invalid(reason)) on size/parse/symlink/version issues (content errors are never Err, only I/O errors other than NotFound are).
    - `marker::MarkerMetadata` struct: `{ canonical_common_dir: Vec<u8>, scheme_version: u32, recorded_at_ms: u64 }` with serde derives (canonical_common_dir serialized as base64 string).
    - `marker::MarkerWrite` enum: `{ AlreadyOwned(MarkerMetadata), Created }`; `marker::InvalidReason` enum: `{ Symlink, Oversized(u64), Malformed(String), SchemeTooNew(u32) }`.
    - `marker::MarkerError` enum with variants: `OwnedByOther { existing: MarkerMetadata }`, `UnknownOwner(InvalidReason)`.
    - All 9 tests pass: digest determinism, marker roundtrip, concurrent writes (exactly one creates), foreign ownership rejection, symlink rejection, oversized rejection, malformed rejection, per design answer A.
  </behavior>

  <action>
    **Step 1: Add sha2 and base64 dependencies** — In baude-core/Cargo.toml, add `sha2 = "0.10"` and `base64 = "0.22"` to [dependencies] section if not already present (verify versions match Cargo.lock). Per design answer B, both are needed.

    **Step 2: Scaffold RED tests** — Create baude-core/src/marker.rs with comprehensive test module calling all marker functions. Use TestRedirect for fs isolation. Define `MarkerMetadata`, `MarkerRead`, `MarkerWrite`, `MarkerError`, `InvalidReason` enums and error variants.

    **Step 3: Add module to lib.rs** — Declare `pub mod marker;` in baude-core/src/lib.rs in alphabetical order.

    **Step 4: Implement digest computation** — In baude-core/src/repository.rs, add:
    - `pub fn compute_repository_digest(common_dir_bytes: &[u8]) -> String` that uses `sha2::{Sha256, Digest}`, creates digest, formats first 12 chars as hex string. No Result wrapper; this is deterministic per design answer A.
    - `pub fn encode_common_dir(bytes: &[u8]) -> String` using base64::engine::general_purpose::STANDARD.encode(bytes).
    - `pub fn decode_common_dir(s: &str) -> Option<Vec<u8>>` using base64::engine::general_purpose::STANDARD.decode(s).ok().
    - `pub fn repository_display_name(common_dir: &Path) -> String` that returns basename of parent (if common_dir ends in `.git`) or basename of common_dir itself. Per design answer H.

    **Step 5: Implement marker module** — In baude-core/src/marker.rs, per design answer A (create_new is the only ownership claim mechanism):
    - Define `MarkerMetadata { canonical_common_dir: Vec<u8>, scheme_version: u32, recorded_at_ms: u64 }` with `#[serde]` derives (canonical_common_dir as base64 in JSON). Note: NO physical_key field in marker (that goes in SavedRepository per design C).
    - Define `MarkerWrite` enum: `{ AlreadyOwned(MarkerMetadata), Created }`.
    - Define `InvalidReason` enum: `{ Symlink, Oversized(u64), Malformed(String), SchemeTooNew(u32) }`.
    - Define `MarkerRead` enum: `{ Missing, Valid(MarkerMetadata), Invalid(InvalidReason) }`.
    - Define `MarkerError` enum: `{ OwnedByOther(MarkerMetadata), UnknownOwner(InvalidReason) }`.
    - Implement `write_marker(dir: &Path, meta: &MarkerMetadata) -> Result<MarkerWrite, MarkerError>` that:
      - Assumes `dir` already exists (caller is responsible for `std::fs::create_dir`).
      - Calls `std::fs::File::create_new(path_to_marker)` to claim ownership atomically (O_EXCL on Unix, first writer wins, second gets AlreadyExists).
      - On success: syncs file, syncs parent dir on Unix, returns Ok(MarkerWrite::Created).
      - On `AlreadyExists`: calls `read_marker(dir)` to get existing marker. If Valid with matching canonical_common_dir → Ok(MarkerWrite::AlreadyOwned(existing)); if Valid with different dir → Err(MarkerError::OwnedByOther(existing)); if Invalid → Err(MarkerError::UnknownOwner(reason)).
      - Serializes marker to JSON with serde (base64-encoded canonical_common_dir).
    - Implement `read_marker(dir: &Path) -> Result<MarkerRead, io::Error>` that:
      - Opens marker with O_NOFOLLOW to reject symlinks. On symlink or other non-regular file, return Ok(MarkerRead::Invalid(InvalidReason::Symlink)).
      - Reads max 64 KiB (bounded size). On size > 64 KiB, return Ok(MarkerRead::Invalid(InvalidReason::Oversized(actual_size))).
      - Deserializes JSON. On NotFound, return Ok(MarkerRead::Missing); on malformed JSON, return Ok(MarkerRead::Invalid(InvalidReason::Malformed(error_string))); on success, return Ok(MarkerRead::Valid(metadata)).
      - Content errors (symlink, oversized, malformed) are never Err; only I/O errors other than NotFound are returned as Err per design answer A.

    **Commits:**
    1. `test(14-01): add failing tests for atomic marker ownership claim, bounds checking, and digest determinism` — all 9 tests fail at compile time or assertion
    2. `feat(14-01): implement marker module with create_new atomicity, bounded reads, and helper functions for common-dir encoding` — adds all implementations; tests pass; no behavior changes after GREEN
  </action>

  <verify>
    <automated>cd /Users/joese/.local/share/baude/worktrees/smoke/repository-1/primary-3 && set -o pipefail && cargo test -p baude-core --lib marker -- --nocapture 2>&1 | tee /tmp/marker_tests.log && [ "$(grep -c '^test ' /tmp/marker_tests.log)" -ge 9 ] && grep -q "test result: ok" /tmp/marker_tests.log && cargo build --workspace</automated>
    <fails_when>Exit code non-zero, fewer than 9 marker tests, "test result: ok" not found, or build fails</fails_when>
  </verify>

  <done>
    - baude-core/Cargo.toml has `sha2 = "0.10"` and `base64 = "0.22"` in [dependencies]
    - baude-core/src/marker.rs exists and is tracked in git
    - Exports pub fn write_marker(dir: &Path, meta: &MarkerMetadata) -> Result<MarkerWrite, MarkerError>
    - Exports pub fn read_marker(dir: &Path) -> Result<MarkerRead, io::Error>
    - Exports pub struct MarkerMetadata with fields canonical_common_dir: Vec<u8>, scheme_version: u32, recorded_at_ms: u64 (NO physical_key); derives Serialize, Deserialize
    - Exports pub enum MarkerRead { Missing, Valid(MarkerMetadata), Invalid(InvalidReason) }
    - Exports pub enum MarkerWrite { AlreadyOwned(MarkerMetadata), Created }
    - Exports pub enum MarkerError { OwnedByOther(MarkerMetadata), UnknownOwner(InvalidReason) }
    - write_marker writes into EXISTING directory (caller creates dir) using create_new(true) for atomic ownership claim (O_EXCL on Unix)
    - write_marker returns Ok(MarkerWrite::Created) on successful creation; Ok(MarkerWrite::AlreadyOwned(existing)) if marker already exists with matching canonical_common_dir
    - write_marker returns Err(MarkerError::OwnedByOther(existing)) if foreign marker exists (canonical_common_dir differs)
    - write_marker syncs file and parent directory on Unix
    - read_marker opens marker with O_NOFOLLOW flag to reject symlinks
    - read_marker reads max 64 KiB (bounded size), returns MarkerRead::Invalid(Oversized) if > 64 KiB
    - read_marker returns MarkerRead::Missing for missing marker, MarkerRead::Valid(...) for valid marker, MarkerRead::Invalid(reason) for content errors
    - read_marker returns Err only for I/O errors other than NotFound (content errors are Ok(Invalid(...)))
    - baude-core/src/repository.rs exports pub fn compute_repository_digest(common_dir_bytes: &[u8]) -> String
    - compute_repository_digest returns 12 lowercase hex characters (fixed length, deterministic, no Result wrapper, no fallback)
    - baude-core/src/repository.rs exports pub fn encode_common_dir(bytes: &[u8]) -> String and pub fn decode_common_dir(s: &str) -> Option<Vec<u8>>
    - baude-core/src/repository.rs exports pub fn repository_display_name(common_dir: &Path) -> String
    - baude-core/src/lib.rs declares pub mod marker in alphabetical order
    - `cargo test -p baude-core --lib marker` passes with at least 9 tests (digest determinism, roundtrip, concurrent creation, foreign ownership, symlink rejection, oversized rejection, malformed rejection)
    - `cargo test -p baude-core --lib repository` passes (digest, encode/decode, display_name)
    - `cargo build --workspace` succeeds with no compiler errors
    - Clippy passes on baude-core with no warnings
  </done>

  <assumption_delta_decision>
    **Noun becoming primary:** Repository identity is now derived from canonical git common directory digest (stable content-based hash) rather than per-state-file monotonic counter.
    
    **Decision:** Promote digest to the canonical key for new repositories; keep legacy counter dirs readable. Digest computation and marker ownership are the foundation; resolver (14-02) builds on this.
    
    **Rationale:** Digest is deterministic across TUI/daemon processes and survives state file resets. Marker files record ownership for legacy migration and recovery. Collision detection (14-02) allocates distinct paths non-destructively. Marker write uses atomic temp→rename pattern to prevent corruption.
    
    **Invariant test:** Compute digest twice for same common dir → identical. Marker write succeeds into existing dir; foreign marker detected and rejected. Non-UTF-8 paths survive base64 round-trip.
  </assumption_delta_decision>

## Review Dispositions (Cycle 2)

| Finding | Severity | Disposition | Action in Plan |
|---------|----------|-------------|-----------------|
| Marker write semantics contradictory (create_dir fails on migration) | HIGH | RESOLVED | Changed design: write_marker now writes INTO EXISTING directory (caller creates); removed create_dir call. Returns Err(OwnedByOther) if foreign marker exists (per design answer 1). |
| Collision fallback over-engineered (12→16→…→64 ladder) | HIGH | RESOLVED | Removed compute_repository_digest_with_fallback and fallback logic entirely. Fixed 12-hex digest always; collision fallback delegated to ensure_repository (design answer 2). |
| repository_identity module not needed without fallback | HIGH | RESOLVED | Removed repository_identity.rs module. Simple compute_repository_digest(12 hex) stays in repository.rs. Collision continuation handled in 14-02. |
| Marker read bound (1 KiB) too restrictive | MEDIUM | RESOLVED | Raised bounded read from 1 KiB to 64 KiB (design answer 6); tests verify 60 KiB passes, 65 KiB rejected. |
| HashMap<Vec<u8>, String> unsupported by serde_json | HIGH | RESOLVED | Deferred to 14-02: state.common_dir_to_physical_key uses BTreeMap<String, String> with canonical_common_dir encoded (design answer 4). |
| Marker foreign ownership not detected | HIGH | RESOLVED | Added test write_marker_refuses_foreign_marker; write_marker now reads existing marker and returns Err(MarkerError::OwnedByOther) if canonical_common_dir differs. |
| Non-UTF-8 path preservation accepted | MEDIUM | RESOLVED | Preserved as Vec<u8> in MarkerMetadata; serialized base64 in JSON (design answer 9, already in plan). |

## Review Dispositions (Cycle 3)

| Finding | Severity | Disposition | Action in Plan |
|---------|----------|-------------|-----------------|
| Atomic replacement does not provide exclusive ownership claim | HIGH | RESOLVED | Changed design per answer A: use `File::create_new()` with O_EXCL flag (atomic exclusive create, first writer wins). On AlreadyExists, read marker and compare. Tests: `write_marker_first_writer_wins_second_sees_already_owned`, `concurrent_marker_writes_exactly_one_creates` verify exactly-once semantics. |
| Artifact set cannot compile (sha2 missing in baude-core) | HIGH | RESOLVED | Added both `sha2 = "0.10"` and `base64 = "0.22"` to baude-core/Cargo.toml per answer B. Both dependencies are now explicitly in the plan's files_modified. |
| Digest input lacks canonical-path invariant doc | MEDIUM | RESOLVED | Per answer L: document in function signature that `compute_repository_digest` requires canonical bytes from git discovery. No newtype wrapper this phase; inline documentation sufficient. |
| Marker JSON deserialization unbounded | MEDIUM | RESOLVED | Bounded read to 64 KiB; malformed marker returns MarkerRead::Invalid, never panics. Per answer A, content errors are Ok(Invalid(...)), not Err. |
| Matching-owner rewrite weakens marker immutability | MEDIUM | RESOLVED | Marker is never rewritten or replaced. On matching canonical_common_dir, write_marker returns AlreadyOwned without touching the file. Immutability preserved. |

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Marker file write/read | Untrusted or corrupted .baude-marker.json could lead to incorrect ownership claims. File is written atomically inside the managed directory with exclusive create_dir claim. |
| Marker file path resolution | Symlink-based attacks: marker file itself could be a symlink; O_NOFOLLOW rejects symlinks. Parent directory canonicalization is insufficient. |
| Canonical common dir storage | Raw bytes must round-trip losslessly (non-UTF-8 paths valid on Unix); stored as base64 in JSON. |
| Marker JSON deserialization | Unbounded input size could cause memory exhaustion or parse failures; bounded read to 1 KiB maximum. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | Marker symlink attack | high | mitigate | Use `std::fs::File::open(..., O_NOFOLLOW)` to reject symlinks; return Err(SymlinkError) if target is symlink. |
| T-14-02 | Tampering | Marker JSON deserialization | medium | mitigate | Bounded read max 64 KiB before parsing; treat malformed marker as "ownership unknown", not fatal. Use `#[serde(deny_unknown_fields)]`. |
| T-14-03 | Tampering | Marker write race between concurrent TUI/daemon | high | mitigate | Use `File::create_new()` with O_EXCL flag to atomically claim ownership. First writer creates and succeeds (MarkerWrite::Created); second sees AlreadyExists, reads winner's marker, compares canonical_common_dir, and returns MarkerWrite::AlreadyOwned or MarkerError::OwnedByOther. No temp file, no race window after one succeeds. |
| T-14-04 | Elevation of Privilege | Marker write permissions | medium | accept | Marker lives inside worktree dir under ~/.local/share/baude/; inherits parent directory permissions. Single-user, non-networked scope. |
| T-14-05 | Information Disclosure | Canonical dir paths in logs | medium | mitigate | Canonical dir bytes logged only in debug contexts; user-facing output displays only "repository-<digest>" display. JSON `/info` response includes canonical_common_dir for admin inspection only. |
| T-14-06 | Denial of Service | Marker read bounded | low | mitigate | Bounded read to 64 KiB; no unbounded allocation or infinite loop on malformed JSON. |
</threat_model>

<verification>
End-to-end: digest computation is deterministic (same input → same output across processes). Marker writes into existing directories atomically and refuses foreign markers. Marker reads are bounded (64 KiB max) and reject symlinks. Atomic write ensures no corruption on crash (temp → sync → rename pattern matching persist.rs).
</verification>

<success_criteria>
- Digest computation is deterministic: same canonical common dir always returns same 12 hex characters (fixed length, no fallback)
- Marker write assumes existing directory (caller creates); returns Err(OwnedByOther) if foreign marker exists
- Marker write uses atomic temp → sync → rename + parent fsync pattern
- Marker read rejects symlinks via O_NOFOLLOW flag, returns Err(SymlinkError)
- Marker read bounces inputs > 64 KiB (bounded deserialization)
- Canonical common dir bytes (non-UTF-8 safe) survive round-trip via base64 JSON encoding
- Marker read is fail-closed: missing marker returns Ok(None), malformed/oversized returns Err (not panic)
- All 9 tests pass under TestRedirect isolation (deterministic, roundtrip, write_into_existing, refuse_foreign, non-UTF-8, concurrent, scheme version, bounded read)
- `cargo build --all` and `cargo test --workspace --lib` both succeed with no errors
- Clippy passes with no warnings on baude-core
</success_criteria>

<output>
Create `.planning/phases/14-managed-worktree-identity/14-01-SUMMARY.md` when done
</output>
