---
phase: 14
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - baude/src/app.rs
  - bauded/src/manager.rs
  - bauded/src/api.rs
  - baude/src/remote.rs
  - baude/src/main.rs
  - baude-core/src/worktree_scan.rs
  - README.md
autonomous: true
requirements:
  - WTID-03
  - WTID-04

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - TUI admit_repository returns collision report non-destructively; collision does not block admission, is surfaced in status/notes
    - Daemon manager admission handles digest keys and collision reports the same way as TUI, logs warnings
    - baude worktrees scan --json output includes owner field per managed repository directory
    - baude worktrees scan text output displays ownership information per repository-directory row (per design answer M)
    - Daemon /info endpoint reports collisions accumulated since daemon start, with count
    - README documents the digest-based identity scheme, marker files, legacy migration, and collision handling
  artifacts:
    - baude/src/app.rs admit_repository returns Result<(Option<u64>, Option<CollisionReport>)> and calls set_message/notes
    - bauded/src/manager.rs admission flow calls ensure_repository, logs collisions at info level, stores in collisions vector
    - bauded/src/api.rs Manager keeps collisions: Vec<CollisionReport>; /info endpoint returns JSON with "collisions" and "collision_count"
    - baude/src/remote.rs Info struct gains collision_count: u32; header appends "⚠ N collisions"
    - baude-core/src/worktree_scan.rs Candidate struct has owner: Option<OwnershipInfo> field (canonical_common_dir, display_name)
    - baude/src/main.rs print_scan_summary includes owner column in text output (displays "repository-<key>" or display_name, per design answer H)
    - README.md Worktrees section documents identity scheme, marker files, legacy recognition, collisions
  key_links:
    - app.rs admit_repository calls lifecycle::ensure_repository which returns collision report
    - manager.rs admission follows same path as app.rs but through daemon state persistence
    - worktree_scan Candidate and ScanReport are serialized to JSON; serde includes owner field
    - README documents the architectural choices and user-visible behavior
  prohibitions:
    - Never treat collision as fatal in TUI or daemon; both must continue with distinct path for newcomer
    - Never expose canonical dir paths in user-facing output (TUI status line, scan text); use "repository-<digest>" display only
    - Never omit owner information from scan --json output; it is required for WTID-04
    - Never delete or move legacy repository-<counter> directories during scan or admission
---

<objective>
**Admission UX, collision reporting, and ownership visibility:** Wire collision reports into TUI and daemon frontends; surface collisions to users (TUI message, daemon API, logs); add owner field to scan output; document the identity scheme.

**Purpose:** TUI users see collision reports in status. Daemon /info endpoint exposes collisions. Scan output shows repository owner. Both frontends handle digests and non-destructive collisions identically. README documents the full migration and recovery flow.

**Output:** Production-quality admission UX, collision visibility, ownership reporting in scan, and user documentation.
</objective>

## Artifacts this phase produces

**TUI integration:**
- `baude/src/app.rs` — `admit_repository()` returns `Result<(Option<u64>, Option<CollisionReport>)>` (collisions already computed in 14-02 ensure_repository); calls set_status or notes.push to surface collision to user

**Daemon integration and API (per design answer J):**
- `bauded/src/manager.rs` — Manager struct gains `pub collisions: Vec<CollisionReport>` field; admission paths call ensure_repository, append collision to vec, log at info level
- `bauded/src/api.rs` — GET `/info` response gains `"collisions": Vec<CollisionReport>` and `"collision_count": usize` fields (JSON-serialized)
- `baude/src/remote.rs` — Info struct gains `#[serde(default)] collisions: Vec<CollisionReport>` and `collision_count: u32` fields; remote header appends `⚠ N collisions` when count > 0

**Scan and reporting (per design answer H, M):**
- `baude-core/src/worktree_scan.rs` — New `OwnershipInfo` struct: `pub struct OwnershipInfo { pub canonical_common_dir: PathBuf, pub display_name: Option<String> }` (stores canonical path as PathBuf, display_name optional)
- `baude-core/src/worktree_scan.rs` — `Candidate` struct gains `pub owner: Option<OwnershipInfo>` field (per-repository-directory, not per-checkout per design answer H); collision flag not needed (already reported separately)
- `baude/src/main.rs` — `print_scan_summary` text output includes owner column per candidate showing "repository-<key> (owner: <display_name>)" or canonical path per design answer H (REVERSED prohibition)

**Documentation:**
- `README.md` — Worktrees section documents digest scheme, marker files, legacy migration, collision handling (including user-visible resolutions), state reset

<execution_context>
@~/.iarx/claude/gsd-core/workflows/execute-plan.md
@~/.iarx/claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-managed-worktree-identity/14-CONTEXT.md
@.planning/phases/14-managed-worktree-identity/14-PATTERNS.md
@.planning/phases/14-managed-worktree-identity/14-02-SUMMARY.md

# Source files
@baude/src/app.rs
@bauded/src/manager.rs
@baude/src/main.rs
@baude-core/src/worktree_scan.rs
@baude-core/src/lifecycle.rs
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TUI and daemon admission flows handle and surface collision reports</name>
  <files>baude/src/app.rs, bauded/src/manager.rs, bauded/src/api.rs, baude/src/remote.rs</files>
  
  <read_first>
  - baude/src/app.rs lines 1914-1947 (admit_repository call site), lines 4750-4767 (admission error display), search for set_status/set_message pattern
  - bauded/src/manager.rs lines 77-90 (Manager struct), lines 309-330 (initialization), lines 450-467 (state save), admission paths
  - bauded/src/api.rs lines 117-125 (/info endpoint structure), lines 163 (POST /sessions response)
  - baude/src/remote.rs lines 150-210 (Info struct, remote header display)
  - baude-core/src/lifecycle.rs (ensure_repository now returns collision report)
  </read_first>
  
  <action>
    **TUI (baude/src/app.rs) per design answer I:** Update admit_repository signature and implementation:
    1. Return type already Result<(Option<u64>, Option<CollisionReport>)> from 14-02 (no change to signature).
    2. When collision report is returned, extract it and surface to user: grep app.rs for how Phase 13 shows startup_notes or messages (search for set_status, set_message, notes.push).
    3. Call that function to append collision details to status line or notes.
    4. Continue with normal admission (collision does not block).
    
    **Daemon (bauded/src/manager.rs) per design answer J:** Update Manager struct and admission:
    1. Add field to Manager: `pub collisions: Vec<CollisionReport>` initialized to Vec::new().
    2. Update admission paths (find both record_checkout_intent and branch activation): call ensure_repository (already in place from 14-02).
    3. If collision report is returned, append to manager.collisions and log at info level: `tracing::warn!("Collision: {} (repo {}) collides with {} (repo {})", newcomer_path, newcomer_display, owner_path, owner_display)`.
    4. Continue with state persistence (collision does not block).

    **Daemon API (bauded/src/api.rs) per design answer J:** Update GET /info endpoint (NOT POST /sessions per design answer J):
    1. Find /info endpoint (api.rs line ~117-125 or similar), currently returns Json<serde_json::Value>.
    2. In the response object, add `"collisions": [ ... ]` (array of CollisionReport serialized), and `"collision_count": manager.collisions.len()`.
    3. CollisionReport is already Serialize from 14-02, so serde_json::to_value handles it.
    4. Test: /info response includes collisions array when manager has collisions.

    **Remote client (baude/src/remote.rs) per design answer J:** Update Info struct and display:
    1. Add fields to Info struct: `#[serde(default)] collisions: Vec<CollisionReport>`, `#[serde(default)] collision_count: u32`.
    2. When Info is deserialized from daemon /info, collision_count is populated.
    3. In remote header display (search for "status line" or "header"), append `⚠ {collision_count} collisions` when collision_count > 0.
    4. Test: remote header shows collision warning when daemon reports collisions.

    **Parity tests (baude-core, per design answer K):** Add to lifecycle::tests:
    1. Test `ensure_repository_tui_and_daemon_compute_same_physical_key` — same repo admitted via ensure_repository twice (separate state instances via TestRedirect), both compute identical physical_key.
    2. Test `ensure_repository_tui_and_daemon_collision_identical` — both face same collision (pre-marked directory), both return identical owner info and both allocate distinct paths.

    Do NOT add cross-binary tests (cannot instantiate both binaries in unit tests without adding workspace dependencies).
  </action>

  <verify>
    <automated>cd /Users/joese/.local/share/baude/worktrees/smoke/repository-1/primary-3 && set -o pipefail && cargo test -p baude-core --lib -- --nocapture ensure_repository_tui_and_daemon_compute_same_physical_key ensure_repository_tui_and_daemon_collision_identical 2>&1 | tee /tmp/parity_tests.log && [ "$(grep -c '^test ' /tmp/parity_tests.log)" -ge 2 ] && grep -q "test result: ok" /tmp/parity_tests.log && cargo test -p baude --lib -- --nocapture admission_collision_sets_status 2>&1 && cargo test -p bauded --lib -- --nocapture info_reports_collisions manager_admission_collision_recorded 2>&1 && cargo build --workspace</automated>
    <fails_when>Exit code non-zero, parity tests not found, or "test result: ok" not in log, or build fails</fails_when>
  </verify>

  <done>
    - baude/src/app.rs admit_repository returns Result<(Option<u64>, Option<CollisionReport>)> (signature from 14-02)
    - TUI surfaces collision via set_status/set_message/notes (matching Phase 13 pattern per design answer I)
    - bauded/src/manager.rs admission flow calls ensure_repository (already in place from 14-02)
    - Daemon Manager struct has `pub collisions: Vec<CollisionReport>` field (Vec::new() default)
    - Daemon admission appends collisions and logs at info level via tracing::warn!
    - bauded/src/api.rs GET /info response includes `"collisions": [ ... ]` and `"collision_count"` fields
    - baude/src/remote.rs Info struct gains `#[serde(default)] collisions: Vec<CollisionReport>` and `collision_count: u32`
    - Remote header appends `⚠ N collisions` when collision_count > 0 (design answer J)
    - Test `ensure_repository_tui_and_daemon_compute_same_physical_key` in baude-core passes (parity)
    - Test `ensure_repository_tui_and_daemon_collision_identical` in baude-core passes (collision handling parity)
    - Test `admission_collision_sets_status_line` in baude passes (TUI surfaces collision)
    - Test `info_reports_collisions` in bauded passes (/info includes collisions)
    - Test `manager_admission_collision_recorded_and_logged` in bauded passes (daemon logs)
    - `cargo build --workspace` succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Scan output reports ownership per managed repository directory (per design answer H, M)</name>
  <files>baude-core/src/worktree_scan.rs, baude/src/main.rs</files>
  
  <read_first>
  - baude-core/src/worktree_scan.rs lines 744-799 (Candidate and ScanReport structs)
  - baude-core/src/worktree_scan.rs lines 2300-2450 (scan implementation, candidate classification loop, verdict logic)
  - baude/src/main.rs lines 736-995 (print_scan_summary, run_worktrees_scan)
  - baude-core/src/marker.rs (MarkerMetadata, repository_display_name from Plan 01)
  </read_first>
  
  <action>
    **Define ownership info structure (worktree_scan.rs) per design answer H, M:**
    1. Define `OwnershipInfo` struct: `pub struct OwnershipInfo { pub canonical_common_dir: PathBuf, pub display_name: Option<String> }` with serde derives (canonical_common_dir stored as PathBuf, serializable).
    2. Add to Candidate struct: `pub owner: Option<OwnershipInfo>` field (one per repository-* managed directory, empty for non-managed or unresolved ownership).

    **Scan implementation (worktree_scan.rs) per design answer M:**
    3. During candidate classification loop, when handling a repository-* managed directory:
       a. Call marker::read_marker(directory) to get directory ownership.
       b. If marker is Valid(meta), extract canonical_common_dir_bytes and convert to PathBuf via decode_common_dir or OsStr::from_bytes.
       c. Call repository::repository_display_name(canonical_path) to get display_name.
       d. Construct OwnershipInfo and assign to candidate.owner.
       e. If marker is Invalid/Missing and checkouts exist, use first checkout's git::discover_repository_identity to populate owner (per design answer M: one per-directory discovery, not per-checkout).
    4. ScanReport already uses serde; owner field serializes automatically.
    
    **Text output (main.rs print_scan_summary) per design answer H (REVERSED prohibition):**
    5. For each Candidate with owner, include in text row: "repository-<key> (owner: /path/to/canonical/common/dir)" OR "(owner: <display_name>)" if display_name is set.
    6. Per design answer H: canonical_common_dir IS SHOWN in text output (REVERSED cycle 2 prohibition that contradicted locked CONTEXT).
    7. Integrate into existing verdict logic naturally.
    
    **JSON output (main.rs run_worktrees_scan):**
    8. No changes needed; serde serializes canonical_common_dir as PathBuf (string), display_name optional.
  </action>

  <verify>
    <automated>cd /Users/joese/.local/share/baude/worktrees/smoke/repository-1/primary-3 && set -o pipefail && cargo test -p baude-core --lib -- --nocapture scan_ownership_reads_marker scan_ownership_discovered_from_checkout 2>&1 | tee /tmp/owner_tests.log && [ "$(grep -c '^test ' /tmp/owner_tests.log)" -ge 2 ] && grep -q "test result: ok" /tmp/owner_tests.log && cargo test -p baude --lib -- --nocapture scan_output_includes_owner 2>&1 && cargo build --workspace</automated>
    <fails_when>Exit code non-zero, or "test result: ok" not found, or build fails</fails_when>
  </verify>

  <done>
    - baude-core/src/worktree_scan.rs OwnershipInfo struct defined with canonical_common_dir: PathBuf and optional display_name: String
    - baude-core/src/worktree_scan.rs Candidate struct has `pub owner: Option<OwnershipInfo>` field (per repository-directory, per design answer H, M)
    - Scan implementation reads marker for managed directories and populates owner from marker canonical_common_dir
    - Fallback: if marker invalid/missing, owner derived from checkout git discovery (one per directory, not per-checkout)
    - baude/src/main.rs text output includes owner in candidate row: "repository-<key> (owner: /path or display_name)"
    - Per design answer H (REVERSED): canonical_common_dir IS shown in text output (contradicts cycle 2 prohibition, follows locked CONTEXT)
    - baude/src/main.rs --json output includes owner field in Candidate JSON (serde automatic)
    - JSON structure: each Candidate has owner: { canonical_common_dir, display_name? } or null
    - Test `scan_ownership_reads_marker` passes (owner from marker)
    - Test `scan_ownership_discovered_from_checkout` passes (owner from checkout fallback)
    - Test `scan_output_includes_owner` in baude passes (text/JSON output verification)
    - `cargo build --workspace` succeeds
  </done>
</task>

<task type="auto">
  <name>Task 3: Documentation of managed worktree identity scheme and migration</name>
  <files>README.md</files>
  
  <read_first>
  - README.md lines 316-325 (existing Worktrees section)
  - baude-core/src/marker.rs (MarkerMetadata, marker file shape)
  - baude-core/src/repository.rs (compute_repository_digest — 12 hex)
  </read_first>
  
  <action>
    Update README.md Worktrees section to document (per design answer 5 ownership policy):
    
    1. **Identity scheme:** Managed worktree directories are identified by a stable digest of the repository's canonical git common directory (output of `git rev-parse --git-common-dir`). The digest is the first 12 characters of the SHA256 hash, formatted as `repository-<12 hex digits>/<role>-<checkout_key>`. This ensures two processes admitting the same repository derive the same path without coordination.
    
    2. **Legacy migration:** Existing counter-based directories (`repository-1`, `repository-2`, etc.) from earlier baude versions are recognized in place. The system reads a small `.baude-marker.json` file inside each managed directory to record and verify ownership. On first admission of a legacy directory, baude automatically writes a marker; after a state file reset, markers enable recovery without a scan.
    
    3. **Collision handling:** If two repositories converge on the same path (rare but possible under legacy counters), baude detects the collision via the marker file and allocates the newcomer a distinct digest-keyed directory (`repository-<12hex>-2`, etc.). The collision is reported non-destructively in `baude worktrees scan --json` output with the owning repository named. Users see only the assigned repository key in text output (not the canonical git path).
    
    4. **Path stability:** After a state file reset, the same repository is found again by computing its digest from the canonical git common directory; no scan is required for recovery.
    
    Integrate this into the existing Worktrees section without breaking the existing prose about repository rows, worktree removal (x and X), and git semantics.
  </action>

  <verify>
    <automated>grep -q "Managed worktree directories are identified by" README.md && grep -q "repository-<12 hex digits>" README.md && grep -q "legacy.*counter-based" README.md && grep -q ".baude-marker.json" README.md && grep -q "collision.*allocat" README.md</automated>
    <fails_when>Any grep fails (required phrases not found in README)</fails_when>
  </verify>

  <done>
    - README.md Worktrees section documents the digest-based identity scheme
    - Documents how legacy counter dirs are migrated in place via marker files
    - Explains collision detection and non-destructive resolution
    - Mentions state reset recovery (no scan required)
    - Prose is integrated with existing documentation on worktree semantics
    - All five grep checks pass (key phrases found)
    - No markdown syntax errors; section reads cohesively
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admission flow | TUI and daemon both call ensure_repository; collision reports must be handled identically. |
| Scan ownership reporting | Ownership metadata displayed to user; canonical paths now shown per design answer H (reversed cycle 2 prohibition). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-10 | Tampering | Collision report accuracy | medium | mitigate | Marker is source of truth; comparison with newcomer's canonical dir is straightforward. |
| T-14-11 | Information Disclosure | Canonical dir path exposure | medium | mitigate | Canonical dir shown in text and JSON output (per locked CONTEXT design answer H, reversing cycle 2 prohibition). Ownership discovery once per directory, not per checkout (design answer M). |
| T-14-12 | Denial of Service | Scan ownership discovery loops | low | accept | Discovery once per repository directory; if fallback to checkout discovery, loops once over children. Checkouts typically <10. |
| T-14-SC | Supply Chain | Daemon collisions persistence | medium | accept | Collisions stored in Manager runtime state (ephemeral, cleared on daemon restart). No persistent record unless /info is polled. User must check /info or monitor logs for ongoing collisions. |
</threat_model>

<verification>
TUI and daemon admission flows both handle collision reports non-destructively, returning them alongside the allocated key. Scan output includes ownership information from markers or discovered checkouts. README documents the identity scheme and migration strategy. All tests pass in isolation and under TestRedirect.
</verification>

<success_criteria>
- TUI admit_repository returns (Option<u64>, Option<CollisionReport>) and handles collisions non-destructively
- Daemon manager admission follows same pattern and logs collisions
- Scan Candidate struct has owner field populated from marker or discovered checkouts
- Text output includes owner information per candidate
- JSON output includes owner field for each candidate
- README documents digest-based identity, legacy migration via markers, collision handling, and state reset recovery
- All eight tests pass (app admission, daemon admission, ownership reading, scan output, README phrases)
- `cargo build --all` and `cargo test --all --lib` both succeed
</success_criteria>

## Review Dispositions (Cycle 2)

| Finding | Severity | Disposition | Action in Plan |
|---------|----------|-------------|-----------------|
| WTID-03 not satisfied for daemon/PWA users | HIGH | RESOLVED | Design answer J: Daemon /info endpoint includes collision array and count; remote header shows warning. PWA deferred (recorded in ledger). |
| TUI behavior underspecified | HIGH | RESOLVED | Design answer I: admit_repository captures and returns CollisionReport; TUI calls set_status/notes per Phase 13 pattern. |
| Scan model doesn't represent per-checkout ownership | HIGH | RESOLVED | Design answer H, M: simplified to one owner per repository-directory (not per-checkout). Add owner: OwnershipInfo to Candidate. |
| Per-checkout ownership too complex | MEDIUM | RESOLVED | Design answer H: keep minimal, add owner to Candidate only (no nested ManagedCheckoutOwnership structs). |
| API compatibility churn risk | MEDIUM | RESOLVED | Design answer J: /info gains collisions field (additive, no change to POST /sessions). Backward compatible. |
| Canonical path exposure contradiction | MEDIUM | RESOLVED | Design answer H (REVERSED cycle 2): canonical_common_dir IS shown in text output (contradicts cycle 2 prohibition, honors locked CONTEXT lines 30, 35). |
| Test coverage for parity | MEDIUM | RESOLVED | Design answer K: parity tests moved to baude-core::lifecycle::tests (not cross-binary). TUI/daemon each keep one local admission test. |

## Review Dispositions (Cycle 3)

| Finding | Severity | Disposition | Action in Plan |
|---------|----------|-------------|-----------------|
| TUI admission doesn't surface collision | HIGH | RESOLVED | Design answer I: TUI calls set_status/set_message/notes.push (matching Phase 13 pattern for startup notes). Collision is visible in status line. |
| POST /sessions unchanged but collision promised | HIGH | RESOLVED | Design answer J: decision made to NOT change POST (PWA client only navigates on id, would ignore field). Collision exposed via GET /info instead (persistent, queryable endpoint). |
| Scan ownership per-checkout vs per-directory | HIGH | RESOLVED | Design answer M: per-repository-directory only (one owner per Candidate, not per child checkout). Design is simpler and sufficient. |
| Remote client doesn't show daemon collisions | HIGH | RESOLVED | Design answer J: remote.rs Info struct gains collision_count field; header appends `⚠ N collisions` when non-zero. Users see the warning. |
| Canonical dir prohibition contradicts locked context | HIGH | RESOLVED | Design answer H (REVERSED): CONTEXT lines 30, 35 are locked; they require canonical_common_dir in collision reports. Cycle 2 prohibition removed. Text output includes both canonical path and display_name. |
| Daemon collisions epimeral | MEDIUM | ACCEPTED | Design answer J: Manager.collisions is runtime state, cleared on daemon restart. /info provides point-in-time view; logs retain history. Accepted for Phase 14; persistent collision recording deferred. |

<output>
Create `.planning/phases/14-managed-worktree-identity/14-03-SUMMARY.md` when done
</output>
