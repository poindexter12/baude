---
phase: 07-local-tui-dogfood-release
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - baude/src/app.rs
  - baude/src/ui.rs
autonomous: true
requirements: [SURF-01, SURF-02]
must_haves:
  truths:
    - "Repository, checkout, worktree, unavailable/recovery, and remote selections render the exact row, hint, confirmation, and refusal semantics authorized by 07-UI-SPEC."
    - "The exact 160x40, 100x30, 79x24, 59x20, and 40x12 matrix never panics, writes outside the viewport, or resizes a hidden PTY to zero."
    - "At width below 60 the focused pane is full width; at height below 13 shell focus transfers immediately to agent or sidebar with exact copy and ordinary input never reaches a hidden shell."
    - "All applicable open, clone, shell, editor, info/activity/GSD, resume/recovery, archive, attention, and session-cycle behavior remains keyboard reachable without reordering local children."
    - "Wide and combining Unicode labels, branch names, paths, padding, truncation, selection bands, and modal lines use terminal cell width rather than bytes or scalar count."
  artifacts:
    - path: "baude/src/ui.rs"
      provides: "Exact hierarchy row anatomy, responsive modes, Unicode-cell-safe layout, contextual hints/help, and separate close/remove modals"
      contains: "LayoutRects"
    - path: "baude/src/app.rs"
      provides: "Visibility-aware resize, tiny-height focus transfer, and preserved existing local actions"
      contains: "sync_sizes"
  key_links:
    - from: "baude/src/app.rs"
      to: "baude/src/ui.rs"
      via: "layout visibility and positive content rectangle drive PTY resize/focus"
      pattern: 'layout\(|sync_sizes'
    - from: "baude/src/ui.rs"
      to: "baude/src/hierarchy.rs"
      via: "row/action view controls exact contextual hints and role/status presentation"
      pattern: "ActionView|LocalRow"
---

<objective>
Complete SURF-01 and SURF-02 with the exact Ratatui anatomy, responsive viewport behavior, capability-gated hints, and preserved applicable session controls.

Purpose: Make the safe hierarchy usable at every contracted terminal size without redesigning or allowing hidden panes/stale hints to capture input.
Output: Deterministic render matrix, cell-correct Unicode layout, exact modals/help/hints, and visibility-aware PTY focus/resize.
</objective>

<execution_context>
@/Users/joese/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/joese/.config/opencode/get-shit-done/templates/summary.md
@/Users/joese/.config/opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-local-tui-dogfood-release/07-UI-SPEC.md
@.planning/phases/07-local-tui-dogfood-release/07-RESEARCH.md
@.planning/phases/07-local-tui-dogfood-release/07-PATTERNS.md
@.planning/phases/07-local-tui-dogfood-release/07-VALIDATION.md
@.planning/phases/07-local-tui-dogfood-release/07-01-SUMMARY.md
@.planning/phases/07-local-tui-dogfood-release/07-02-SUMMARY.md
@baude/src/app.rs
@baude/src/ui.rs
</context>

<tasks>

<task id="07-03-01" type="auto" tdd="true">
  <name>Task 1: Enforce the viewport matrix, cell width, and visible-pane focus contract</name>
  <reversibility rating="reversible">Layout breakpoints and width helpers are source-local presentation decisions behind the existing layout seam.</reversibility>
  <files>baude/src/app.rs, baude/src/ui.rs</files>
  <behavior>
    - Exact test `ui::tests::hierarchy_viewport_matrix_renders_without_panic_and_preserves_semantics` renders all required fixtures at 160x40, 100x30, 79x24, 59x20, and 40x12 and asserts exact borders, connectors, glyphs, continuous selected band, narrow `? more`, single-pane switching, and smallest-modal target/action visibility.
    - Exact test `ui::tests::hierarchy_unicode_width_scroll_and_selection_band_are_cell_correct` covers slash branches, spaces, wide/combining labels, duplicate basenames, scroll visibility, middle clipping, and no border overwrite.
    - Exact test `app::tests::hierarchy_resize_never_sends_zero_dimensions_and_transfers_hidden_shell_focus` records resize/input destinations, preserves last valid hidden PTY dimensions, enforces remote 2x10 minimum, and checks both exact tiny-height focus-transfer messages.
  </behavior>
  <action>Write the three exact failing tests before behavior edits. Extend `LayoutRects` with explicit responsive mode/visibility. Implement the exact width breakpoints (42 at >=120; clamp(width/3,28,38) at 80-119; 26 at 60-79; single-pane below 60) and height rules (footer/two-line normal >=20, footer hidden and compact as needed 13-19, one-line and shell visually hidden below 13). Use saturating centering and bounded Ratatui rendering. Replace hierarchy-related `chars().count()`/byte assumptions with composed `Line::width()` and grapheme/cell-safe clipping; preserve branch leaf/path basename per spec. Keep selected row in view and parent context when possible. Change `sync_sizes` to resize only visible positive inner rects, retain last valid dimensions while hidden, and at height below 13 immediately transfer Shell focus to a live agent or sidebar with exact specified message; ordinary keys from that frame must not reach hidden shell, while ctrl+backslash may close it. Growing does not steal focus. Do not add a snapshot crate or UI dependency.</action>
  <verify>
    <automated>name='ui::tests::hierarchy_viewport_matrix_renders_without_panic_and_preserves_semantics'; cargo test -p baude -- --list | rg -Fx -- "$name: test" &amp;&amp; cargo test -p baude "$name" -- --exact --nocapture</automated>
    <fails_when>The exact viewport test is absent, the list guard matches zero tests, any contracted viewport panics/overwrites, required compact/single-pane semantics disappear, or the exact test exits nonzero.</fails_when>
    <automated>name='ui::tests::hierarchy_unicode_width_scroll_and_selection_band_are_cell_correct'; cargo test -p baude -- --list | rg -Fx -- "$name: test" &amp;&amp; cargo test -p baude "$name" -- --exact --nocapture</automated>
    <fails_when>The exact Unicode test is absent, the list guard matches zero tests, cell width is computed naively, wide/combining text overwrites reserved cells, scrolling loses selection/context, or the exact test exits nonzero.</fails_when>
    <automated>name='app::tests::hierarchy_resize_never_sends_zero_dimensions_and_transfers_hidden_shell_focus'; cargo test -p baude -- --list | rg -Fx -- "$name: test" &amp;&amp; cargo test -p baude "$name" -- --exact --nocapture</automated>
    <fails_when>The exact resize test is absent, the list guard matches zero tests, any hidden PTY receives zero dimensions, tiny-height input reaches hidden shell, focus/message behavior differs, remote minimums regress, or the exact test exits nonzero.</fails_when>
  </verify>
  <done>Every specified viewport and Unicode fixture is cell-correct and non-panicking, hidden PTYs retain valid sizes, and tiny-mode focus/input behavior exactly matches the UI contract.</done>
</task>

<task id="07-03-02" type="auto" tdd="true">
  <name>Task 2: Finish exact rows, contextual hints, confirmations, help, and applicable actions</name>
  <reversibility rating="reversible">Copy/layout and action-hint presentation can be changed locally while the durable action contract remains stable.</reversibility>
  <files>baude/src/app.rs, baude/src/ui.rs</files>
  <behavior>
    - Exact test `ui::tests::hierarchy_modals_name_exact_targets_and_distinguish_close_from_remove` covers close cyan/non-destructive copy and remove red/exact target/full ref/path/retained branch copy at wide and tiny sizes, including wrapping/clipping and safe default keys.
    - The viewport fixture contains waiting, working, completed, exited, closed, archived, unavailable, and recovery children in one unchanged first-seen sequence plus a configured separate flat remote section.
    - Hints exactly follow capability/selection and narrow overflow; help says repository or checkout, x keeps checkout, X removes only verified clean managed worktree and retains branch.
  </behavior>
  <action>Commit the exact modal RED test before finishing presentation. Implement the complete 07-UI-SPEC row anatomy: parent aggregate only for waiting/unavailable, normal two-line and compact children, exact connector/role/status matrix, selected background across both lines, no local archive regrouping, and full unavailable/recovery cause in Info/status. Render context hints in the prescribed order and width behavior from ActionView; never advertise X for main/external/remote or r without capability. Split old combined worktree modal fully: x opens title `close checkout session`, exact body/hints, cyan border, y/Enter close and n/Esc preserve; X opens only after first preflight and renders the exact six-line red remove body with target, full ref, path, branch-retained statement, and y/Enter vs n/Esc. Map existing editor/shell/info/activity/GSD/archive/attention/session-cycle to checkout-to-runtime/path context exactly as 07-UI-SPEC permits; repository editor/GSD use canonical main, repository shell/archive use exact refusals, and remote behavior remains existing flat compatibility. Preserve established colors, two weights, animations, mouse behavior, transient 5-second messages, and design system.</action>
  <verify>
    <automated>name='ui::tests::hierarchy_modals_name_exact_targets_and_distinguish_close_from_remove'; cargo test -p baude -- --list | rg -Fx -- "$name: test" &amp;&amp; cargo test -p baude "$name" -- --exact --nocapture</automated>
    <fails_when>The exact modal test is absent, the list guard matches zero tests, close and remove share destructive copy/style/keys, target/ref/path/branch retention is omitted, tiny mode clips action identity, or the exact test exits nonzero.</fails_when>
    <automated>name='app::tests::hierarchy_action_matrix_dispatches_only_authorized_local_actions'; cargo test -p baude -- --list | rg -Fx -- "$name: test" &amp;&amp; cargo test -p baude "$name" -- --exact --nocapture</automated>
    <fails_when>The guarded action regression is absent, contextual action routing or preserved session behavior regresses, or the exact test exits nonzero.</fails_when>
  </verify>
  <done>Rows, hints, help, confirmations, refusals, animations, and applicable legacy actions match the exact UI contract across all selection and viewport states without redesign.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| action capability → displayed hint | Stale or inferred hints can induce unsafe dispatch expectations. |
| viewport visibility → input/resize target | Hidden panes must not receive input or invalid dimensions. |
| durable target → confirmation copy | Truncation must not obscure or substitute destructive target identity. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking mitigation |
|-----------|----------|----------|-----------|-------------|---------------------|
| T-07-08 | Spoofing/Tampering | High | remove/close presentation | mitigate | Distinct keys, colors, titles, exact target/ref/path, safe defaults, and direct buffer tests at tiny viewport. |
| T-07-09 | Elevation/Tampering | High | capability hints | mitigate | Hints consume typed ActionView only; action matrix remains defensive when hidden. |
| T-07-10 | Spoofing/DoS | High | hidden shell focus | mitigate | Same-frame focus transfer, positive visible resize only, input-destination test. |
| T-07-11 | Spoofing | Medium | Unicode truncation | mitigate | Cell-width measurement and preserved target leaf/basename prevent misleading overwrite. |
| T-07-SC | Tampering | High | UI dependency supply chain | mitigate | Existing Ratatui TestBackend/Buffer only; no new dependency. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Stable context-aware local TUI workflow | 07-03 | COVERED | Exact responsive local surface. |
| REQ | SURF-01 | Context actions, hints, confirmations name actual target | 07-03 | COVERED | Task 2 exact matrix/copy. |
| REQ | SURF-02 | Existing applicable local behaviors remain reachable | 07-03 | COVERED | Task 2 checkout-to-runtime/path adaptation. |
| RESEARCH | responsive/Unicode | Exact viewport matrix, visible resize, Line width | 07-03 | COVERED | Task 1. |
| CONTEXT | 07-UI-SPEC | Row anatomy, colors, density, focus, hints, exact copy | 07-03 | COVERED | Tasks 1-2. |
| CONTEXT | no redesign/dependency | Preserve current design system | 07-03 | COVERED | Explicit action guard. |
</source_coverage_audit>

<estimate>
raw_tokens: 32000
calibration_factor: 1
estimated_tokens: 32000
confidence: low
reason: Two coupled Rust files, three render/resize RED tests, and exact viewport/copy matrix; kept separate from lifecycle and release work.
</estimate>

<verification>
- Every exact test is list-guarded with `rg -Fx`; every runnable command has immediate failure criteria.
- Run the full workspace format/Clippy/test gate after the wave.
</verification>

<success_criteria>
- Exact row/modal/hint/help semantics render across all required states and viewports.
- Unicode cell width, scrolling, and selection bands are correct.
- Hidden shell focus and PTY resize are safe at tiny dimensions.
- Existing applicable local controls and flat remote presentation remain usable.
</success_criteria>

<output>
Create `.planning/phases/07-local-tui-dogfood-release/07-03-SUMMARY.md` with viewport matrix, buffer-style, focus/input, and preserved-action evidence. Keep manual visual dogfood pending.
</output>
