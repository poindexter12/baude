---
phase: 07-local-tui-dogfood-release
plan: 04
type: tdd
wave: 4
depends_on: ["07-03"]
files_modified:
  - baude/src/app.rs
  - bauded/src/api.rs
autonomous: true
requirements: [SURF-05, REL-01]
must_haves:
  truths:
    - "An isolated real Git flow admits one parent/default, creates or activates a branch checkout, closes, restarts App, reopens exactly once, and safely removes while retaining branch and avoiding duplicate parent/child/runtime keys."
    - "The flow asserts persisted structural order, deterministic first-parent selection after restart, explicit durable-key reselection, exact Git inventory, no writes outside the fixture, and user work preservation at each lifecycle boundary."
    - "Flat remote TUI rows and daemon /sessions APIs remain non-hierarchical and non-destructive; compatibility DELETE remains retained close and no hierarchy/remove-worktree endpoint exists."
  artifacts:
    - path: "baude/src/app.rs"
      provides: "Isolated real-Git restart/dedup dogfood and flat remote compatibility tests"
      contains: "local_tui_dogfood_real_git_flow_survives_restart_without_duplicates"
    - path: "bauded/src/api.rs"
      provides: "Flat non-hierarchical API compatibility contract test"
      contains: "flat_session_api_remains_a_non_hierarchical_compatibility_projection"
  key_links:
    - from: "baude/src/app.rs"
      to: "baude-core lifecycle/Git/persist"
      via: "real App production methods over an isolated bare origin, main checkout, state root, and fake backend"
      pattern: "admit_repository|activate_branch_worktree|reopen_checkout|confirm_remove_worktree"
    - from: "bauded/src/api.rs"
      to: "bauded Manager"
      via: "existing /sessions list and compatibility DELETE only"
      pattern: 'route\("/sessions|delete_session'
---

<objective>
Prove REL-01 end to end with real Git and lock SURF-05 flat compatibility against accidental hierarchy/destructive expansion.

Purpose: Convert unit-level hierarchy/action confidence into restart/dedup evidence while preserving the explicitly separate daemon contract.
Output: One isolated real-Git dogfood test plus local/daemon flat compatibility tests.
</objective>

<execution_context>
@/Users/joese/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/joese/.config/opencode/get-shit-done/templates/summary.md
@/Users/joese/.config/opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-local-tui-dogfood-release/07-UI-SPEC.md
@.planning/phases/07-local-tui-dogfood-release/07-RESEARCH.md
@.planning/phases/07-local-tui-dogfood-release/07-PATTERNS.md
@.planning/phases/07-local-tui-dogfood-release/07-VALIDATION.md
@.planning/phases/07-local-tui-dogfood-release/07-03-SUMMARY.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-07-SUMMARY.md
@baude/src/app.rs
@bauded/src/api.rs
</context>

<tasks>

<task id="07-04-01" type="auto" tdd="true">
  <name>Task 1: Automate the isolated real-Git restart and dedup dogfood flow</name>
  <reversibility rating="reversible">The harness factors existing test fixtures and exercises stable production APIs without changing persisted or public contracts.</reversibility>
  <files>baude/src/app.rs</files>
  <behavior>
    - Exact test `app::tests::local_tui_dogfood_real_git_flow_survives_restart_without_duplicates` uses a temporary bare origin/main checkout, isolated state root, sleeping fake active backend, and production App lifecycle methods.
    - It executes open/admit -> verify main plus distinct default when applicable -> create or activate -> retained close -> drop/reload App -> verify persisted order and deterministic first-local-parent initial selection -> explicitly reselect the retained CheckoutKey -> reopen exactly once -> first/second-preflight safe remove.
    - At every stage it asserts one canonical parent, unique CheckoutKeys, unchanged first_seen order, at most one runtime per CheckoutKey, exact worktree inventory, retained user file/branch ref, deterministic post-remove selection, and no fixture-external writes.
  </behavior>
  <action>Commit the exact RED dogfood test first. Factor the existing `admission_repo` and `removal_app` helpers instead of creating a second Git-fixture dialect. Use process-unique roots and the test-only persistence root; configure both backends with the harmless sleeping command and kill all spawned processes during cleanup. Drive only production App admission/hierarchy/action/lifecycle methods, including an eligible existing-local activation case or a complete new-branch case plus existing activation regression coverage. Drop the first App and construct/restore a second against the same isolated state. Assert durable RepositoryKey/CheckoutKey identity and row order before and after restart, assert that the new process initially selects the first local repository parent in rendered order rather than restoring the unpersisted prior child, explicitly reselect the retained CheckoutKey, then assert one runtime association after reopen, dirty/user files unchanged until explicitly cleaned by the test, and `git show-ref --verify` branch survival after safe remove. The test must not depend on HOME, network, user credentials, global Git config, or write outside its root.</action>
  <verify>
    <automated>name='app::tests::local_tui_dogfood_real_git_flow_survives_restart_without_duplicates'; cargo test -p baude -- --list | rg -Fx -- "$name: test" &amp;&amp; cargo test -p baude "$name" -- --exact --nocapture</automated>
    <fails_when>The exact dogfood test is absent, the list guard matches zero tests, it mocks Git/App lifecycle instead of using real production paths, restart duplicates or loses identity/order, restart claims to restore unpersisted child selection or violates first-local-parent initialization, explicit reselection/reopen duplicates runtime, work or branch is lost, fixture isolation leaks, or the exact test exits nonzero.</fails_when>
  </verify>
  <done>The complete local dogfood lifecycle passes in isolated real Git across restart with durable identity/order, deterministic initial selection plus explicit durable reselection, branch/work preservation, and runtime dedup proven.</done>
</task>

<task id="07-04-02" type="auto" tdd="true">
  <name>Task 2: Lock flat remote and daemon compatibility against hierarchy or destructive drift</name>
  <reversibility rating="costly">The existing flat daemon/session wire behavior is published compatibility surface, so changing it would affect remote clients; this task preserves rather than changes that contract.</reversibility>
  <files>baude/src/app.rs, bauded/src/api.rs</files>
  <behavior>
    - Exact test `app::tests::hierarchy_flat_remote_compatibility_has_no_local_parent_or_remove_action` proves remote rows remain under a separate flat header, retain existing order/attach/restart/close behavior, and cannot dispatch local w/X/hierarchy actions.
    - Exact test `api::tests::flat_session_api_remains_a_non_hierarchical_compatibility_projection` proves GET /sessions remains an array of flat SessionInfo, DELETE remains retained close, and candidate hierarchy/remove-worktree routes are 404/unregistered.
    - Existing `api::tests::real_atomic_persistence_failures_are_503_for_every_mutation` remains green.
  </behavior>
  <action>Commit both exact RED compatibility tests before any adjustment. Exercise the existing App remote snapshot and Axum router/Manager harness directly. Preserve production routes and RemoteInfo shape; changes to production daemon/PWA code are forbidden unless needed solely to restore already-existing compatibility. Assert no repository parent is synthesized for Remote(u64), no Shift+X or local lifecycle dispatch is reachable, and compatibility DELETE retains repository/checkout state rather than removing Git topology. Do not add a daemon hierarchy endpoint, remove-worktree endpoint, branch browser, or destructive remote action.</action>
  <verify>
    <automated>name='app::tests::hierarchy_flat_remote_compatibility_has_no_local_parent_or_remove_action'; cargo test -p baude -- --list | rg -Fx -- "$name: test" &amp;&amp; cargo test -p baude "$name" -- --exact --nocapture</automated>
    <fails_when>The exact App compatibility test is absent, the list guard matches zero tests, a remote parent/local destructive action appears, existing flat behavior regresses, or the exact test exits nonzero.</fails_when>
    <automated>name='api::tests::flat_session_api_remains_a_non_hierarchical_compatibility_projection'; cargo test -p bauded -- --list | rg -Fx -- "$name: test" &amp;&amp; cargo test -p bauded "$name" -- --exact --nocapture</automated>
    <fails_when>The exact API compatibility test is absent, the list guard matches zero tests, /sessions ceases to be flat, DELETE becomes destructive, a hierarchy/remove-worktree route exists, or the exact test exits nonzero.</fails_when>
    <automated>name='api::tests::real_atomic_persistence_failures_are_503_for_every_mutation'; cargo test -p bauded -- --list | rg -Fx -- "$name: test" &amp;&amp; cargo test -p bauded "$name" -- --exact --nocapture</automated>
    <fails_when>The existing atomic compatibility test is absent, the list guard matches zero tests, fail-closed persistence/status behavior regresses, or the exact test exits nonzero.</fails_when>
  </verify>
  <done>Real local dogfood is restart/dedup green and both local remote rows and daemon APIs remain explicitly flat, non-hierarchical, and non-destructive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| isolated fixture → host filesystem/processes | Dogfood automation must not mutate user repositories, state, credentials, or installed binaries. |
| persisted restart → runtime ownership | Restore must not duplicate a parent, child, or process for one CheckoutKey. |
| remote identity/API → local lifecycle | Flat remote IDs must not become a confused deputy for local Git/destructive actions. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking mitigation |
|-----------|----------|----------|-----------|-------------|---------------------|
| T-07-12 | Tampering/DoS | High | dogfood fixture isolation | mitigate | Process-unique root, explicit state root/fake backend, no HOME/network/global config, cleanup and outside-write assertions. |
| T-07-13 | Spoofing/Tampering | High | restart durable identity | mitigate | RepositoryKey/CheckoutKey/order/runtime cardinality asserted at every stage. |
| T-07-14 | Elevation/Tampering | High | remote/local confused deputy | mitigate | Distinct Remote(u64), exhaustive no-local-action test, no new route. |
| T-07-15 | Tampering | High | compatibility DELETE | mitigate | Retained-close persistence assertions and existing atomic-failure regression. |
| T-07-SC | Tampering | High | Cargo supply chain | mitigate | No install/dependency change in this plan. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Complete local flow survives restart without duplicates or lost work | 07-04 | COVERED | Real Git production-path test. |
| REQ | REL-01 | Isolated open/create-or-activate/close/restart/reopen/remove | 07-04 | COVERED | Task 1. |
| REQ | SURF-05 | Flat daemon/session compatibility remains non-destructive | 07-04 | COVERED | Task 2. |
| RESEARCH | dogfood harness | Reuse App real-Git fixture and isolated persistence root | 07-04 | COVERED | No second fixture dialect. |
| CONTEXT | remote boundary | No daemon hierarchy endpoint or destructive behavior | 07-04 | COVERED | App/API exact tests. |
</source_coverage_audit>

<estimate>
raw_tokens: 30000
calibration_factor: 1
estimated_tokens: 30000
confidence: low
reason: Two existing large test modules, one process/Git-heavy RED cycle and one compatibility RED cycle; no production subsystem expansion.
</estimate>

<verification>
- Every exact test is list-guarded with `rg -Fx`; immediate failure conditions reject zero-match and fixture leakage.
- Run the full workspace format/Clippy/test gate after the wave.
</verification>

<success_criteria>
- Isolated real-Git local dogfood completes across restart with no duplicate identity/runtime or lost work.
- The removed worktree's branch remains.
- Remote rows and daemon API stay flat and cannot reach local destructive hierarchy actions.
</success_criteria>

<output>
Create `.planning/phases/07-local-tui-dogfood-release/07-04-SUMMARY.md` with exact real-Git stages, key/order/runtime assertions, branch proof, isolation evidence, and compatibility results. Manual visual dogfood remains pending.
</output>
