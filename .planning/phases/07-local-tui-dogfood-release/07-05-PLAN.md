---
phase: 07-local-tui-dogfood-release
plan: 05
type: execute
wave: 5
depends_on: ["07-04"]
files_modified:
  - Cargo.toml
  - baude-core/Cargo.toml
  - baude/Cargo.toml
  - bauded/Cargo.toml
  - Cargo.lock
  - release-please-config.json
  - .github/workflows/ci.yml
  - README.md
  - CHANGELOG.md
  - docs/local-tui-dogfood.md
  - .planning/phases/07-local-tui-dogfood-release/07-VALIDATION.md
autonomous: true
requirements: [REL-02, REL-03]
must_haves:
  truths:
    - "All three crates, the exact path dependency, lockfile, binary version output, release proposal config, changelog, and source-install docs consistently target 2.0.0-beta readiness."
    - "The last-published release manifest remains exactly 0.14.0 and no command publishes, pushes, tags, creates a PR/release, logs into a registry, or uploads to an existing release."
    - "Local format, workspace Clippy/all-targets, full tests, package assembly, release build, archive/extract/version, and isolated cargo-install checks pass."
    - "CI mirrors the four supported release targets and two-binary archive/version checks as a non-publishing readiness job."
    - "Validation records only observed overnight evidence while manual dogfood, Linux/runtime certification, independent deep review, phase verification, Nyquist approval, requirement/phase completion, publication decision, and UI-SPEC approval remain pending for morning."
  artifacts:
    - path: "Cargo.toml"
      provides: "Exact versioned local baude-core dependency"
      contains: "version = \"=2.0.0-beta\""
    - path: ".github/workflows/ci.yml"
      provides: "Four-target non-publishing binary artifact readiness matrix"
      contains: "aarch64-apple-darwin"
    - path: "docs/local-tui-dogfood.md"
      provides: "HOME/state/repository/install-isolated manual wide/narrow dogfood runbook and morning evidence checklist"
    - path: ".planning/phases/07-local-tui-dogfood-release/07-VALIDATION.md"
      provides: "Observed implementation evidence and honest pending certification"
  key_links:
    - from: "baude/Cargo.toml"
      to: "Cargo.toml"
      via: "workspace baude-core path plus exact prerelease version"
      pattern: "baude-core.workspace"
    - from: ".github/workflows/ci.yml"
      to: ".github/workflows/release.yml"
      via: "copies only target/tar layout, never publication permissions/actions"
      pattern: "aarch64-unknown-linux-gnu|x86_64-unknown-linux-gnu"
    - from: "README.md"
      to: "docs/local-tui-dogfood.md"
      via: "readiness-safe local source/install instructions"
      pattern: "local-tui-dogfood"
---

<objective>
Finish REL-02 and REL-03 source readiness for target v2.0.0-beta and execute the complete non-publishing local implementation gate.

Purpose: Make the tested source package/artifact/install-ready without falsifying published history or morning certification state.
Output: Synchronized beta metadata, non-publishing CI artifacts, accurate docs/runbook/changelog, complete local evidence, and pending morning gates.
</objective>

<execution_context>
@/Users/joese/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/joese/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-local-tui-dogfood-release/07-UI-SPEC.md
@.planning/phases/07-local-tui-dogfood-release/07-RESEARCH.md
@.planning/phases/07-local-tui-dogfood-release/07-PATTERNS.md
@.planning/phases/07-local-tui-dogfood-release/07-VALIDATION.md
@.planning/phases/07-local-tui-dogfood-release/07-04-SUMMARY.md
@Cargo.toml
@baude-core/Cargo.toml
@baude/Cargo.toml
@bauded/Cargo.toml
@Cargo.lock
@release-please-config.json
@.release-please-manifest.json
@.github/workflows/ci.yml
@.github/workflows/release.yml
@README.md
@CHANGELOG.md
@docs/deploy.md
</context>

<tasks>

<task id="07-05-01" type="auto">
  <name>Task 1: Synchronize beta package metadata and add non-publishing artifact CI</name>
  <reversibility rating="reversible">No beta has been published; source versions, release proposal config, and CI readiness job can be reverted without registry or migration effects.</reversibility>
  <files>Cargo.toml, baude-core/Cargo.toml, baude/Cargo.toml, bauded/Cargo.toml, Cargo.lock, release-please-config.json, .github/workflows/ci.yml</files>
  <action>Set all three literal package versions to `2.0.0-beta`; change workspace `baude-core` to `{ path = "baude-core", version = "=2.0.0-beta" }` without adding a dependency; regenerate Cargo.lock through Cargo so only workspace package/version relationships change. Configure release-please's existing root package for exact `release-as: 2.0.0-beta`, prerelease beta behavior, and existing three TOML extra-files, but do not run release-please. Leave `.release-please-manifest.json` untouched at 0.14.0 because it is published history. Extend CI with a non-publishing readiness matrix for `aarch64-apple-darwin`, `x86_64-apple-darwin`, `x86_64-unknown-linux-gnu`, and `aarch64-unknown-linux-gnu`: build locked release binaries, archive both `baude` and `bauded`, extract, assert exact `--version`, and optionally upload ordinary CI artifacts. Do not copy release permissions, event trigger, Docker login/push, digest/manifest, tag, checksum-for-release, `gh release`, or publication steps. Preserve existing check/docker CI behavior and add no dependency.</action>
  <verify>
    <automated>cargo metadata --locked --no-deps --format-version 1 | python3 -c 'import json,sys; d=json.load(sys.stdin); p={x["name"]:x["version"] for x in d["packages"]}; assert p=={"baude":"2.0.0-beta","baude-core":"2.0.0-beta","bauded":"2.0.0-beta"},p'</automated>
    <fails_when>Cargo metadata is not locked/valid, any workspace package differs from 2.0.0-beta, an extra package appears in the assertion, or the command exits nonzero.</fails_when>
    <automated>python3 -c 'from pathlib import Path; s=Path("Cargo.toml").read_text(); assert "baude-core = { path = \"baude-core\", version = \"=2.0.0-beta\" }" in s; import json; c=json.load(open("release-please-config.json")); p=c["packages"]["."]; assert p["release-as"]=="2.0.0-beta" and p["prerelease"] is True; m=json.load(open(".release-please-manifest.json")); assert m=={".":"0.14.0"}'</automated>
    <fails_when>The exact versioned path dependency is absent, prerelease/release-as is inconsistent, the last-published manifest differs from 0.14.0, JSON is invalid, or the command exits nonzero.</fails_when>
    <automated>python3 -c 'from pathlib import Path; s=Path(".github/workflows/ci.yml").read_text(); targets=["aarch64-apple-darwin","x86_64-apple-darwin","x86_64-unknown-linux-gnu","aarch64-unknown-linux-gnu"]; assert all(x in s for x in targets); assert "baude bauded" in s; block=s[s.index("artifact-readiness"):]; assert "gh release" not in block and "docker/login-action" not in block and "push: true" not in block and "release: published" not in block'</automated>
    <fails_when>Any supported target or two-binary archive is absent, the readiness block contains publication/login/push behavior, workflow text is unreadable, or the command exits nonzero.</fails_when>
    <automated>git diff --exit-code -- .release-please-manifest.json .github/workflows/release.yml</automated>
    <fails_when>Published-history manifest or publication workflow changed, or the command exits nonzero.</fails_when>
  </verify>
  <done>Package/lock/release proposal metadata and four-target CI consistently describe 2.0.0-beta readiness without changing published history or adding a publication path/dependency.</done>
</task>

<task id="07-05-02" type="auto">
  <name>Task 2: Document the durable hierarchy and isolated morning dogfood without release claims</name>
  <reversibility rating="reversible">Documentation and unreleased changelog wording can be corrected locally before any publication.</reversibility>
  <files>README.md, CHANGELOG.md, docs/local-tui-dogfood.md</files>
  <action>Update README's overview, install, Keys, status, archive, Worktrees, and remote sections to describe repository parents, persisted oldest-first children, durable selection, x retained close, X verified clean managed removal with branch retained, applicable existing actions, and separate flat non-destructive remote rows. Source installation/dogfood must use the explicitly built local source or `cargo install --path baude --root <isolated-root> --locked`; do not imply a GitHub beta release exists or overwrite the user's installed baude. Add an unreleased `2.0.0-beta readiness` changelog section above 0.14.0 covering durable hierarchy/lifecycle/release checks without a compare URL or released/published claim. Create `docs/local-tui-dogfood.md` as a command-first isolated runbook: temporary HOME/config/data/state/install roots, temporary bare origin/main repository, harmless fake backend, local locked build/install, open/create-or-activate/close/restart/reopen/safe-remove sequence, wide and narrow screenshots, RepositoryKey/CheckoutKey/order and before/after worktree inventory evidence, branch-ref survival, cleanup, and explicit morning pending checklist. Include no push, tag, release-please execution, gh, publish, Docker push, PR, fetch, force removal, auto-clean, or branch-deletion command.</action>
  <verify>
    <automated>rg -F 'v2.0.0-beta' README.md CHANGELOG.md docs/local-tui-dogfood.md &amp;&amp; rg -F 'X' docs/local-tui-dogfood.md &amp;&amp; rg -F 'branch' docs/local-tui-dogfood.md &amp;&amp; rg -F '40x12' docs/local-tui-dogfood.md</automated>
    <fails_when>Beta target, distinct remove key, branch-retention evidence, or tiny/wide dogfood coverage is absent, or the command exits nonzero.</fails_when>
    <automated>python3 -c 'from pathlib import Path; ps=[Path("README.md"),Path("CHANGELOG.md"),Path("docs/local-tui-dogfood.md")]; t="\n".join(p.read_text().lower() for p in ps); forbidden=["git push","gh release","cargo publish","release-please manifest","docker push","git tag"]; assert not [x for x in forbidden if x in t]; c=Path("CHANGELOG.md").read_text().lower().split("## [0.14.0]",1)[0]; assert "released" not in c and "published" not in c'</automated>
    <fails_when>A forbidden publication/push/tag command exists, the unreleased beta section claims released/published status, a document is unreadable, or the command exits nonzero.</fails_when>
  </verify>
  <done>Users can understand and locally install/dogfood the exact hierarchy safely, while all text remains readiness-only and morning evidence/publication remain pending.</done>
</task>

<task id="07-05-03" type="auto">
  <name>Task 3: Run the complete local package/artifact/install gate and record observed evidence only</name>
  <reversibility rating="reversible">Validation evidence records observed local commands and can be corrected; it does not approve certification, requirements, or publication.</reversibility>
  <files>.planning/phases/07-local-tui-dogfood-release/07-VALIDATION.md</files>
  <action>Run every command below against the final source. Inspect `git diff --check`, `git status --short`, and tracked-file membership for every existing source/config/doc path changed by the phase; preserve unrelated `graphify-out/` and `opencode.json`. Fully verify the publishable core package, assemble all workspace packages with `--no-verify` because dependents cannot registry-resolve unpublished beta core, then separately prove full workspace tests/builds and host artifacts. Build and extract a two-binary host tar, assert exact versions, and perform an isolated `cargo install --path baude --root <temp> --locked` version check without changing global installation. Update 07-VALIDATION.md only with exact observed pass/fail output and test counts. Keep `nyquist_compliant: false`, approval pending, and manual wide/narrow dogfood, supported CI/Linux/runtime certification (including Phase 6 process checks), independent deep review, phase verification, Nyquist approval, requirement completion, phase completion, UI-SPEC checker sign-off, and publication decision pending. Do not edit REQUIREMENTS checkboxes, Phase 6/7 completion state, or published history; do not push, open a PR, publish, tag, upload a release, or claim readiness if any local gate fails.</action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <fails_when>Formatting differs or the command exits nonzero.</fails_when>
    <automated>cargo clippy --workspace --all-targets -- -D warnings</automated>
    <fails_when>Any warning/error is emitted or the command exits nonzero.</fails_when>
    <automated>cargo test --workspace</automated>
    <fails_when>Any workspace test fails or the command exits nonzero.</fails_when>
    <automated>cargo package -p baude-core --locked</automated>
    <fails_when>The core package cannot assemble and pristine-verify from locked metadata, or the command exits nonzero.</fails_when>
    <automated>cargo package --workspace --locked --no-verify</automated>
    <fails_when>Any workspace package cannot assemble, path/version metadata is invalid, package contents are incomplete, or the command exits nonzero.</fails_when>
    <automated>cargo build --workspace --release --locked &amp;&amp; test "$(target/release/baude --version)" = 'baude 2.0.0-beta' &amp;&amp; test "$(target/release/bauded --version)" = 'bauded 2.0.0-beta'</automated>
    <fails_when>Locked release build fails, either binary is absent, either version differs exactly, or the command exits nonzero.</fails_when>
    <automated>tmp=$(mktemp -d); trap 'rm -rf "$tmp"' EXIT; tar -C target/release -czf "$tmp/baude-v2.0.0-beta-host.tar.gz" baude bauded &amp;&amp; mkdir "$tmp/extracted" &amp;&amp; tar -C "$tmp/extracted" -xzf "$tmp/baude-v2.0.0-beta-host.tar.gz" &amp;&amp; test "$("$tmp/extracted/baude" --version)" = 'baude 2.0.0-beta' &amp;&amp; test "$("$tmp/extracted/bauded" --version)" = 'bauded 2.0.0-beta'</automated>
    <fails_when>The host archive omits either binary, extraction fails, extracted versions differ, temporary cleanup setup fails, or the command exits nonzero.</fails_when>
    <automated>tmp=$(mktemp -d); trap 'rm -rf "$tmp"' EXIT; cargo install --path baude --root "$tmp/install" --locked &amp;&amp; test "$("$tmp/install/bin/baude" --version)" = 'baude 2.0.0-beta'</automated>
    <fails_when>Isolated local install changes global state, install fails, installed version differs, temporary cleanup setup fails, or the command exits nonzero.</fails_when>
    <automated>git diff --check &amp;&amp; git ls-files --error-unmatch baude-core/src/lifecycle.rs baude/src/main.rs baude/src/app.rs baude/src/ui.rs bauded/src/api.rs .github/workflows/ci.yml Cargo.toml baude-core/Cargo.toml baude/Cargo.toml bauded/Cargo.toml Cargo.lock release-please-config.json README.md CHANGELOG.md .release-please-manifest.json .github/workflows/release.yml</automated>
    <fails_when>Whitespace errors exist, any existing source/config/release path is not tracked, published history/workflow is missing, or the command exits nonzero.</fails_when>
    <automated>python3 -c 'from pathlib import Path; s=Path(".planning/phases/07-local-tui-dogfood-release/07-VALIDATION.md").read_text(); required=["nyquist_compliant: false","Manual wide/narrow local TUI dogfood","Linux/runtime certification","Independent deep review","Phase verification and Nyquist approval","Release publication decision"]; assert all(x in s for x in required); assert "status: complete" not in s.lower()'</automated>
    <fails_when>Validation claims completion/Nyquist approval, omits any morning pending gate, or the command exits nonzero.</fails_when>
  </verify>
  <done>All local source/package/artifact/install gates pass and validation contains only observed evidence; every human/platform/review/verification/completion/publication gate remains explicitly pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source metadata → package/artifact label | Inconsistent versions can mislabel binaries or dependencies. |
| readiness CI/docs → publication systems/users | A readiness check must not create or imply a release. |
| local install/package checks → user machine | Verification must not overwrite global installs or publish credentials/artifacts. |
| observed results → validation state | Unrun morning gates must not be represented as passed. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking mitigation |
|-----------|----------|----------|-----------|-------------|---------------------|
| T-07-16 | Spoofing/Tampering | High | package/version metadata | mitigate | Exact metadata, lock, binary, archive, install, and docs assertions all require 2.0.0-beta. |
| T-07-17 | Spoofing/Repudiation | High | published release history | mitigate | `.release-please-manifest.json` fixed at 0.14.0 and release workflow diff gate. |
| T-07-18 | Elevation/Tampering | High | accidental publication | mitigate | CI readiness block forbids publication credentials/actions; executor is prohibited from push/tag/PR/publish/upload. |
| T-07-19 | Tampering/DoS | Medium | local install | mitigate | Temporary isolated root and cleanup; no global installation. |
| T-07-20 | Repudiation | High | validation claims | mitigate | Evidence-only updates and explicit pending-gate assertion; no requirement/phase checkbox edits. |
| T-07-SC | Tampering | High | package supply chain | mitigate | No new dependency; locked existing graph, package content verification, tracked-path audit. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Source tree ready for target v2.0.0-beta without publishing | 07-05 | COVERED | Metadata, CI, docs, local gate. |
| REQ | REL-02 | Format, lint, full tests, package checks, supported artifact readiness | 07-05 | COVERED | Tasks 1 and 3. |
| REQ | REL-03 | Version/package/changelog/install docs consistent and non-publishing | 07-05 | COVERED | Tasks 1-3. |
| RESEARCH | package path fix | Exact versioned path dependency; core verify plus workspace no-verify | 07-05 | COVERED | No dependency added. |
| RESEARCH | release history | Manifest remains 0.14.0; readiness CI copies only build/tar layout | 07-05 | COVERED | Diff and forbidden-action gates. |
| CONTEXT | overnight boundary | Update validation only from observation; all certification/completion/publication gates pending | 07-05 | COVERED | Task 3. |
| CONTEXT | deferred | Publication, push, PR, dormant/remote/PWA hierarchy | — | EXCLUDED | Explicitly forbidden. |
</source_coverage_audit>

<phase_source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status |
|--------|----|---------------------|------|--------|
| GOAL | — | Complete local durable hierarchy/lifecycle dogfood plus beta source readiness | 07-01..07-05 | COVERED |
| REQ | REPO-05, HIER-01, HIER-02, HIER-03, HIER-04 | Durable visible stable hierarchy | 07-01 | COVERED |
| REQ | WORK-01, WORK-02, WORK-03, WORK-04, WORK-05, WORK-06 | Safe lifecycle actions | 07-02 | COVERED |
| REQ | SURF-01, SURF-02 | Exact responsive contextual local surface and preserved actions | 07-03 | COVERED |
| REQ | SURF-05, REL-01 | Flat compatibility and isolated restart/dedup dogfood | 07-04 | COVERED |
| REQ | REL-02, REL-03 | Package/artifact/version/docs readiness without publication | 07-05 | COVERED |
| RESEARCH | architecture | Pure projection, core capabilities, App dispatch, Ratatui, real Git, non-publishing release gate | 07-01..07-05 | COVERED |
| CONTEXT | locked scope | Exact UI spec, durable keys/order, Phase 6 authority, no new dependency, morning gates pending | 07-01..07-05 | COVERED |
| CONTEXT | deferred | Dormant rows/deletion, remote hierarchy, PWA, publication/push/PR, force/auto cleanup/fetch/main switching | — | EXCLUDED |
</phase_source_coverage_audit>

<estimate>
raw_tokens: 34000
calibration_factor: 1
estimated_tokens: 34000
confidence: low
reason: Metadata/CI/docs plus a long but mechanical local gate; source implementation is complete before this plan and no new dependency is introduced.
</estimate>

<verification>
- This is the final implementation plan and contains the full local gate plus non-publishing package/archive/install checks.
- No exact Cargo filter is used without a prior `--list | rg -Fx` guard anywhere in the phase; broad workspace commands intentionally run all tests.
- Manual dogfood, supported CI/Linux/runtime certification, independent review, phase verification, Nyquist approval, requirement/phase completion, UI checker approval, and publication decision remain pending for morning.
</verification>

<success_criteria>
- All local code, package, archive, version, and isolated install gates pass for 2.0.0-beta.
- Four-target CI artifact readiness has no publication path.
- README, changelog, and runbook consistently describe readiness, not release.
- Published manifest remains 0.14.0 and no push/PR/tag/release/publication occurs.
- Validation is evidence-based and all morning gates remain pending.
</success_criteria>

<output>
Create `.planning/phases/07-local-tui-dogfood-release/07-05-SUMMARY.md` with exact local gate/package/archive/install evidence and changed metadata/docs. Do not check requirements, complete Phase 6/7, approve Nyquist/UI, push, create a PR, tag, publish, or alter last-published history.
</output>
