---
phase: 07-local-tui-dogfood-release
plan: 05
type: execute
wave: 5
depends_on: ["07-04"]
files_modified:
  - Cargo.toml
  - baude-core/Cargo.toml
  - baude/Cargo.toml
  - bauded/Cargo.toml
  - Cargo.lock
  - release-please-config.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [REL-02]
must_haves:
  truths:
    - "All three crates, the exact path dependency, lockfile, and binary version metadata consistently target 2.0.0-beta."
    - "The release-please root package preserves the current simple-project pattern and sets exact beta behavior: release-as 2.0.0-beta, versioning prerelease, prerelease true, and prerelease-type beta."
    - "The last-published release manifest remains exactly 0.14.0 and neither release workflow nor any command publishes, pushes, tags, creates a PR/release, or logs into a registry."
    - "CI mirrors the four supported release targets and two-binary archive/extract/version checks as a non-publishing readiness job."
  artifacts:
    - path: "Cargo.toml"
      provides: "Exact versioned local baude-core dependency"
      contains: "version = \"=2.0.0-beta\""
    - path: "release-please-config.json"
      provides: "Exact beta prerelease proposal configuration over the existing simple root-package pattern"
      contains: "prerelease-type"
    - path: ".github/workflows/ci.yml"
      provides: "Four-target non-publishing binary artifact readiness matrix"
      contains: "aarch64-apple-darwin"
  key_links:
    - from: "baude/Cargo.toml"
      to: "Cargo.toml"
      via: "workspace baude-core path plus exact prerelease version"
      pattern: "baude-core.workspace"
    - from: "release-please-config.json"
      to: ".release-please-manifest.json"
      via: "proposal fields target beta while last-published manifest remains unchanged"
      pattern: "release-as|versioning|prerelease-type"
    - from: ".github/workflows/ci.yml"
      to: ".github/workflows/release.yml"
      via: "copies only supported target and tar layout, never publication permissions/actions"
      pattern: "aarch64-unknown-linux-gnu|x86_64-unknown-linux-gnu"
---

<objective>
Complete REL-02 metadata and non-publishing CI artifact readiness for target v2.0.0-beta.

Purpose: Make Cargo, release proposal configuration, and supported-target CI exact and internally consistent without changing published history or invoking release machinery.
Output: Synchronized package metadata, explicit release-please beta fields, and a four-target two-binary readiness matrix.
</objective>

<execution_context>
@/Users/joese/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/joese/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-local-tui-dogfood-release/07-RESEARCH.md
@.planning/phases/07-local-tui-dogfood-release/07-PATTERNS.md
@.planning/phases/07-local-tui-dogfood-release/07-VALIDATION.md
@.planning/phases/07-local-tui-dogfood-release/07-04-SUMMARY.md
@Cargo.toml
@baude-core/Cargo.toml
@baude/Cargo.toml
@bauded/Cargo.toml
@Cargo.lock
@release-please-config.json
@.release-please-manifest.json
@.github/workflows/ci.yml
@.github/workflows/release-please.yml
@.github/workflows/release.yml

<interfaces>
The current project uses one release-please package at `.` with `release-type: simple`, `changelog-path: CHANGELOG.md`, component-free tags, pre-major bump disabled, non-draft releases, and three targeted TOML extra-files. Official current release-please schema/docs define prerelease generation through `versioning: prerelease` together with `prerelease: true` and `prerelease-type: beta`; `release-as: 2.0.0-beta` pins this proposal. `.release-please-manifest.json` is last-published history and remains `{ ".": "0.14.0" }`.
</interfaces>
</context>

<tasks>

<task id="07-05-01" type="auto">
  <name>Task 1: Synchronize exact beta Cargo metadata</name>
  <reversibility rating="reversible">No beta has been published; source versions can be reverted without registry or data migration effects.</reversibility>
  <files>Cargo.toml, baude-core/Cargo.toml, baude/Cargo.toml, bauded/Cargo.toml, Cargo.lock</files>
  <action>Set all three literal package versions to `2.0.0-beta`; change workspace `baude-core` to `{ path = "baude-core", version = "=2.0.0-beta" }` without adding a dependency; regenerate Cargo.lock through Cargo so only workspace package/version relationships change.</action>
  <verify>
    <automated>cargo metadata --locked --no-deps --format-version 1 | python3 -c 'import json,sys; d=json.load(sys.stdin); p={x["name"]:x["version"] for x in d["packages"]}; assert p=={"baude":"2.0.0-beta","baude-core":"2.0.0-beta","bauded":"2.0.0-beta"},p'</automated>
    <fails_when>Cargo metadata is not locked/valid, any workspace package differs from 2.0.0-beta, an extra package appears in the assertion, or the command exits nonzero.</fails_when>
    <automated>python3 -c 'from pathlib import Path; s=Path("Cargo.toml").read_text(); assert "baude-core = { path = \"baude-core\", version = \"=2.0.0-beta\" }" in s'</automated>
    <fails_when>The exact versioned path dependency is absent, Cargo.toml is unreadable, or the command exits nonzero.</fails_when>
  </verify>
  <done>All workspace package and lock metadata exactly describe 2.0.0-beta and the local core dependency has an exact registry-compatible version.</done>
</task>

<task id="07-05-02" type="auto">
  <name>Task 2: Prescribe exact release-please beta proposal behavior</name>
  <reversibility rating="reversible">The proposal config can be reverted before any release-please execution; published history remains unchanged.</reversibility>
  <files>release-please-config.json</files>
  <action>In the existing `packages["."]` release-please object preserve exact current fields `release-type: simple`, `changelog-path: CHANGELOG.md`, `include-component-in-tag: false`, `bump-minor-pre-major: false`, `draft: false`, and the three existing TOML `extra-files`; set exact values `release-as: 2.0.0-beta`, `versioning: prerelease`, `prerelease: true`, and `prerelease-type: beta`. These values follow the official current prerelease strategy while retaining this project's working simple/extra-files pattern. Do not change strategy to rust, add plugins, run release-please, or edit `.release-please-manifest.json`; its exact `{ ".": "0.14.0" }` value is last-published history.</action>
  <verify>
    <automated>python3 -c 'import json; c=json.load(open("release-please-config.json")); assert set(c["packages"])=={"."}; p=c["packages"]["."]; exact={"release-type":"simple","changelog-path":"CHANGELOG.md","include-component-in-tag":False,"bump-minor-pre-major":False,"draft":False,"release-as":"2.0.0-beta","versioning":"prerelease","prerelease":True,"prerelease-type":"beta"}; assert all(p.get(k)==v for k,v in exact.items()),p; assert p["extra-files"]==[{"type":"toml","path":"baude-core/Cargo.toml","jsonpath":"$.package.version"},{"type":"toml","path":"baude/Cargo.toml","jsonpath":"$.package.version"},{"type":"toml","path":"bauded/Cargo.toml","jsonpath":"$.package.version"}]; assert json.load(open(".release-please-manifest.json"))=={".":"0.14.0"}'</automated>
    <fails_when>Root package shape, preserved current fields, any of release-as/versioning/prerelease/prerelease-type, or the three extra-files differ; the manifest differs from 0.14.0; JSON is invalid; or the command exits nonzero.</fails_when>
    <automated>git diff --exit-code -- .release-please-manifest.json .github/workflows/release-please.yml .github/workflows/release.yml</automated>
    <fails_when>Published-history manifest or either publication workflow changed, or the command exits nonzero.</fails_when>
  </verify>
  <done>Release-please exactly proposes a beta prerelease using the current project pattern while published history and release workflows remain unchanged.</done>
</task>

<task id="07-05-03" type="auto">
  <name>Task 3: Add supported-target non-publishing artifact readiness CI</name>
  <reversibility rating="reversible">The readiness job is isolated CI configuration with no release credentials or publication side effects.</reversibility>
  <files>.github/workflows/ci.yml</files>
  <action>Extend CI with a non-publishing `artifact-readiness` matrix for exactly `aarch64-apple-darwin`, `x86_64-apple-darwin`, `x86_64-unknown-linux-gnu`, and `aarch64-unknown-linux-gnu`. Follow the current release workflow's build target and tar layout only: build locked release binaries, archive both `baude` and `bauded`, extract the archive, and assert each exact `--version` is 2.0.0-beta. Ordinary CI artifact upload is allowed. Preserve existing check/docker jobs. Do not copy release permissions, release event triggers, GitHub App/registry authentication, Docker login/push, digest/manifest, tag, checksum-for-release, `gh release`, release-please invocation, PR creation, or existing-release upload behavior.</action>
  <verify>
    <automated>python3 -c 'from pathlib import Path; s=Path(".github/workflows/ci.yml").read_text(); targets=["aarch64-apple-darwin","x86_64-apple-darwin","x86_64-unknown-linux-gnu","aarch64-unknown-linux-gnu"]; assert "artifact-readiness" in s; block=s[s.index("artifact-readiness"):]; assert all(block.count(x)>=1 for x in targets); assert "baude bauded" in block; assert "2.0.0-beta" in block; forbidden=["gh release","docker/login-action","docker buildx","push: true","release: published","release-please-action","RELEASE_PLEASE_APP","git tag"]; assert not [x for x in forbidden if x in block]'</automated>
    <fails_when>The readiness job, any supported target, the two-binary archive, extraction/version contract, or exact beta value is absent; a publication/login/push/release token appears; workflow text is unreadable; or the command exits nonzero.</fails_when>
    <automated>git diff --exit-code -- .release-please-manifest.json .github/workflows/release-please.yml .github/workflows/release.yml</automated>
    <fails_when>The last-published manifest or publication workflows changed while adding readiness CI, or the command exits nonzero.</fails_when>
  </verify>
  <done>CI checks all four supported target archives and both binary versions without acquiring publication authority or changing existing release automation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source metadata → package/artifact label | Inconsistent versions can mislabel binaries or local dependencies. |
| proposal config → release automation | Incorrect prerelease fields can propose a stable or malformed release. |
| readiness CI → publication systems | A build check must not gain credentials or create/push release state. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking mitigation |
|-----------|----------|----------|-----------|-------------|---------------------|
| T-07-16 | Spoofing/Tampering | High | Cargo/package metadata | mitigate | Exact locked metadata and path-version assertions require 2.0.0-beta everywhere. |
| T-07-17 | Spoofing/Repudiation | High | release-please config/history | mitigate | Assert exact simple/prerelease fields and three updaters while manifest stays 0.14.0. |
| T-07-18 | Elevation/Tampering | High | readiness CI | mitigate | Forbidden-token/action assertions and unchanged publication workflows. |
| T-07-SC | Tampering | High | package supply chain | mitigate | No new dependency; locked existing graph only. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Supported source/artifact readiness without publishing | 07-05 | COVERED | Exact metadata and CI matrix. |
| REQ | REL-02 | Format/build/package-supported artifact readiness inputs | 07-05 | COVERED | Tasks 1-3; final sampling runs in 07-06. |
| RESEARCH | release-please | Preserve simple project pattern; exact release-as/versioning/prerelease/prerelease-type | 07-05 | COVERED | Official schema/docs and current project config inspected. |
| CONTEXT | release boundary | Manifest remains 0.14.0; no publish/push/tag/PR/release | 07-05 | COVERED | Exact diff and forbidden-action gates. |
| CONTEXT | deferred | Publication and release execution | — | EXCLUDED | Explicitly forbidden. |
</source_coverage_audit>

<estimate>
raw_tokens: 26000
calibration_factor: 1
estimated_tokens: 26000
confidence: medium
reason: Two bounded configuration tasks: synchronized Cargo/proposal metadata and isolated CI readiness.
</estimate>

<verification>
- Every command has immediate failure criteria; no filtered Cargo test occurs in this plan.
- Publication history/workflows remain unchanged and no readiness command invokes release-please, push, tag, PR, release, or registry login.
</verification>

<success_criteria>
- Cargo and release-please fields exactly target 2.0.0-beta prerelease readiness.
- The four-target CI matrix builds/extracts/checks both binaries without a publication path.
- `.release-please-manifest.json` remains 0.14.0 and both existing release workflows remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/07-local-tui-dogfood-release/07-05-SUMMARY.md` with exact metadata/config/CI assertions. Do not run release-please, push, create a PR/release, tag, publish, or alter last-published history.
</output>
