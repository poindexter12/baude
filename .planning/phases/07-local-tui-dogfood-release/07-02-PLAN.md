---
phase: 07-local-tui-dogfood-release
plan: 02
type: tdd
wave: 2
depends_on: ["07-01"]
files_modified:
  - baude-core/src/lifecycle.rs
  - baude/src/hierarchy.rs
  - baude/src/app.rs
autonomous: true
requirements: [WORK-01, WORK-02, WORK-03, WORK-04, WORK-05, WORK-06]
must_haves:
  truths:
    - "From a repository or local child, w creates a valid branch worktree or activates/focuses an eligible existing local branch through Phase 6 lifecycle authority."
    - "x only confirms retained close; Enter/r reopens only when core capability permits; Shift+X alone starts separately confirmed safe managed-worktree removal."
    - "Invalid refs, remote-only refs, collisions, occupied/protected targets, dirty/conflicted/locked/submodule/indeterminate removal, and stale second preflight produce exact target-specific refusals with zero optimistic mutation."
    - "Hints and dispatch use an explicit core RetryReopen/RetryRecovery capability, never glyphs, missing runtime, visibility, cause strings, or stale row state."
  artifacts:
    - path: "baude-core/src/lifecycle.rs"
      provides: "Pure dispatchable LifecycleCapability projection"
      contains: "LifecycleCapability"
    - path: "baude/src/hierarchy.rs"
      provides: "Selection-kind plus capability action view and typed presentation mapping"
      contains: "ActionView"
    - path: "baude/src/app.rs"
      provides: "Durable-target action dispatch into existing activate/close/reopen/prepare-remove/confirm-remove authority"
      contains: "handle_sidebar_key"
  key_links:
    - from: "baude/src/app.rs"
      to: "baude-core/src/lifecycle.rs"
      via: "RepositoryKey/CheckoutKey requests and LifecycleCapability; no UI-selected transition"
      pattern: "LifecycleCapability|activate_branch_worktree|reopen_checkout|prepare_remove_worktree"
    - from: "baude/src/hierarchy.rs"
      to: "baude-core/src/lifecycle.rs"
      via: "typed capability and typed failure mapping"
      pattern: "RetryReopen|RetryRecovery"
---

<objective>
Expose the complete WORK-01 through WORK-06 lifecycle flow from durable hierarchy context without weakening Phase 6 authority.

Purpose: Turn the hierarchy tracer into a safe dogfood workflow while preventing target, capability, and destructive-intent confusion.
Output: Core-derived retry capability, exhaustive durable-target action matrix, exact refusals, and distinct create/close/reopen/remove dispatch.
</objective>

<execution_context>
@/Users/joese/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/joese/.config/opencode/get-shit-done/templates/summary.md
@/Users/joese/.config/opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-local-tui-dogfood-release/07-UI-SPEC.md
@.planning/phases/07-local-tui-dogfood-release/07-RESEARCH.md
@.planning/phases/07-local-tui-dogfood-release/07-PATTERNS.md
@.planning/phases/07-local-tui-dogfood-release/07-VALIDATION.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-04-SUMMARY.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-05-SUMMARY.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-06-SUMMARY.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-07-SUMMARY.md
@.planning/phases/07-local-tui-dogfood-release/07-01-SUMMARY.md
@baude-core/src/lifecycle.rs
@baude-core/src/git.rs
@baude/src/hierarchy.rs
@baude/src/app.rs

<interfaces>
Reuse `App::activate_branch_worktree`, `App::reopen_checkout`, `App::prepare_remove_worktree`, `App::confirm_remove_worktree`, retained-close lifecycle engine events, `RemovalConfirmation`, `RemovalFailure`, `RemovalBlocker`, `CheckoutLifecycle`, and existing App recovery methods. Branch/path data is literal argv/process data; UI code must not call Git or parse Display text to authorize effects.
</interfaces>
</context>

<tasks>

<task id="07-02-01" type="auto" tdd="true">
  <name>Task 1: Project only manually dispatchable lifecycle capabilities</name>
  <reversibility rating="costly">The capability enum becomes a shared core-to-presentation authorization contract; changing it requires coordinated core, App, hierarchy, hint, and test edits but no persisted migration.</reversibility>
  <files>baude-core/src/lifecycle.rs, baude/src/hierarchy.rs</files>
  <behavior>
    - Exact test `lifecycle::tests::lifecycle_capabilities_expose_only_dispatchable_reopen_and_recovery_actions` covers Inactive/eligible retained RetryReopen, implemented activation/teardown/stopped-active RetryRecovery paths, and negative removal-tombstone/generic-unavailable/running states.
    - Every exposed capability has a concrete App dispatch path; absence means `r` is hidden and defensively refused.
    - ActionView is derived from durable selection kind, managed role, runtime association, and explicit capability—not status color, cause text, or stale hint visibility.
  </behavior>
  <action>Commit the exact RED test first. Add a small pure `LifecycleCapability` projection beside `startup_recovery_program` in core. Map only lifecycle states with an implemented manual App path: eligible retained state to RetryReopen and only implemented activation/teardown/stopped-active recovery to RetryRecovery; do not advertise removal tombstones or generic unavailable topology absent a concrete safe dispatcher. Add hierarchy `ActionView` derivation for repository, main, managed, external, unavailable/recovery, and remote selection. Keep capability UI-free and side-effect-free; do not expose lifecycle mutation setters or infer permission from presentation.</action>
  <verify>
    <automated>name='lifecycle::tests::lifecycle_capabilities_expose_only_dispatchable_reopen_and_recovery_actions'; cargo test -p baude-core -- --list | rg -Fx -- "$name: test" &amp;&amp; cargo test -p baude-core "$name" -- --exact --nocapture</automated>
    <fails_when>The exact capability test is absent, the list guard matches zero tests, a shown capability lacks a real App dispatcher, a protected/destructive state is inferred retryable, or the exact test exits nonzero.</fails_when>
  </verify>
  <done>Core supplies the only retry-capability truth and hierarchy action views cannot make stale presentation state authoritative.</done>
</task>

<task id="07-02-02" type="auto" tdd="true">
  <name>Task 2: Dispatch the exhaustive hierarchy lifecycle action matrix</name>
  <reversibility rating="costly">Key/action semantics affect every local keyboard flow and exact safety copy, so undo requires a coordinated App/UI contract change while remaining source-only.</reversibility>
  <files>baude/src/hierarchy.rs, baude/src/app.rs</files>
  <behavior>
    - Exact test `app::tests::hierarchy_action_matrix_dispatches_only_authorized_local_actions` covers repository/main/managed/external/unavailable/recovery/remote selection × Enter, w, x, r, Shift+X, t, e, i, v, g, a and proves every refusal leaves RepositoryState, runtime map, process observations, Git inventory, order, and selection unchanged.
    - Valid new and existing-local branches dispatch through existing activation; close retains the child; reopen creates/focuses one runtime; remove runs first preflight before a distinct confirmation and second preflight only on confirmation.
    - The test asserts the exact 07-UI-SPEC confirmations/refusals and success patterns, including actual repository/target/path/full-ref names and no `r` suggestion without capability.
  </behavior>
  <action>Commit the exact exhaustive RED table first. Refactor local target resolution around RepositoryKey/CheckoutKey: repository context resolves the durable canonical parent/main facts, child context resolves its durable parent, and runtime lookup is optional decoration. Route `w` to existing activation using the durable parent context; Enter to parent default focus/reopen, live child focus, or retained child reopen; `x`/ctrl+x to a non-destructive y/Enter close confirmation that calls retained close; `r` only to the projected RetryReopen/RetryRecovery dispatcher; and uppercase/Shift+X only to first-preflight managed removal before opening a distinct confirmation. Preserve the second-preflight transaction and local branch. Translate typed branch, reconciliation, lifecycle, RemovalFailure, and RemovalBlocker variants centrally into every exact 07-UI-SPEC refusal; diagnostic Display strings are fallback detail only, never authorization. Preserve actual selection and row order on all failures. Keep lowercase x and uppercase X distinct even when modifiers vary; unrelated confirmation keys do nothing. Do not add direct Git mutation, force flags, branch deletion, remote destructive behavior, daemon hierarchy, or dormant rows.</action>
  <verify>
    <automated>name='app::tests::hierarchy_action_matrix_dispatches_only_authorized_local_actions'; cargo test -p baude -- --list | rg -Fx -- "$name: test" &amp;&amp; cargo test -p baude "$name" -- --exact --nocapture</automated>
    <fails_when>The exact action test is absent, the list guard matches zero tests, any selection/key pair is uncovered, lowercase close reaches removal, stale capability dispatches, exact target/refusal copy differs, a blocked action mutates state/process/Git/selection/order, or the exact test exits nonzero.</fails_when>
    <automated>name='app::tests::lifecycle_remove_clean_local_rechecks_after_stop_and_compensates_a_race'; cargo test -p baude -- --list | rg -Fx -- "$name: test" &amp;&amp; cargo test -p baude "$name" -- --exact --nocapture</automated>
    <fails_when>The exact Phase 6 removal regression is absent, the list guard matches zero tests, second-preflight/compensation or branch retention regresses, or the exact test exits nonzero.</fails_when>
    <automated>name='app::tests::lifecycle_reopen_local_targets_retained_checkout_once_and_obeys_save_boundary'; cargo test -p baude -- --list | rg -Fx -- "$name: test" &amp;&amp; cargo test -p baude "$name" -- --exact --nocapture</automated>
    <fails_when>The exact reopen regression is absent, the list guard matches zero tests, durable save-before-one-runtime semantics regress, or the exact test exits nonzero.</fails_when>
  </verify>
  <done>Every hierarchy action is target-specific and capability-gated, all lifecycle effects remain Phase 6-authoritative, and blocked or canceled operations are observably non-mutating.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| key selection → lifecycle request | A stale or spoofed display target must not select a different repository/checkout. |
| branch text → Git authority | User input must remain a literal local ref request through existing typed validation and argv-only Git. |
| hint/capability → process/lifecycle effect | Presentation may advertise only implemented core-authorized dispatch. |
| close/remove confirmation → destructive effect | Ordinary close must never become physical worktree removal; confirmed remove must name and freshly recheck the exact target. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking mitigation |
|-----------|----------|----------|-----------|-------------|---------------------|
| T-07-04 | Tampering/Elevation | High | Git branch target | mitigate | Durable repository resolution, literal existing Git validation, no shell expansion, exhaustive invalid/remote/collision tests. |
| T-07-05 | Spoofing/Tampering | High | destructive confirmation | mitigate | Shift+X-only entry, exact CheckoutKey/path/full ref, distinct red modal, fresh second preflight, branch-retention assertion. |
| T-07-06 | Elevation/Tampering | High | stale capability dispatch | mitigate | Core-derived enum plus exhaustive selection × key matrix; hidden actions are still refused. |
| T-07-07 | Tampering/DoS | High | process/lifecycle bypass | mitigate | UI invokes existing App adapters and shared LifecycleEngine only; no direct Git/process transition in hierarchy/UI. |
| T-07-SC | Tampering | High | Cargo supply chain | mitigate | No installs or dependency changes; use existing Rust/Ratatui/Git stack. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Create/activate, close, reopen, and safely remove from local hierarchy | 07-02 | COVERED | Tasks 1-2. |
| REQ | WORK-01, WORK-02 | Valid create/activate and typed unsafe refusal | 07-02 | COVERED | Durable parent dispatch and branch failure mapping. |
| REQ | WORK-03, WORK-04 | Retained close and exact one-runtime reopen | 07-02 | COVERED | Existing Phase 6 authority. |
| REQ | WORK-05, WORK-06 | Separate confirmed clean managed removal and all fail-closed blockers | 07-02 | COVERED | Distinct key path and two preflights. |
| RESEARCH | capability authority | Retry only when a concrete App path exists | 07-02 | COVERED | Core projection. |
| CONTEXT | exact action/refusal contract | Exhaustive selection × action copy and no partial mutation | 07-02 | COVERED | Exact table test. |
| CONTEXT | deferred | Force cleanup, branch deletion, fetch, main switching, remote/PWA hierarchy | — | EXCLUDED | Explicitly absent. |
</source_coverage_audit>

<estimate>
raw_tokens: 34000
calibration_factor: 1
estimated_tokens: 34000
confidence: low
reason: Three files, two RED/GREEN behavior slices, and a broad exact action matrix; targeted below 50% context.
</estimate>

<verification>
- All filtered Cargo commands have exact list guards using `rg -Fx` and immediate failure semantics.
- After the wave run the full workspace format, Clippy, and test gate before responsive UI expansion.
</verification>

<success_criteria>
- WORK-01 through WORK-06 are reachable from durable hierarchy context.
- Close, reopen, and remove are distinct, target-naming, and shared-core-authorized.
- Every blocked category preserves state, process, Git, selection, and order.
- No dormant row, remote hierarchy, PWA, branch deletion, force cleanup, or new dependency exists.
</success_criteria>

<output>
Create `.planning/phases/07-local-tui-dogfood-release/07-02-SUMMARY.md` with capability mapping, exact action-table evidence, and Phase 6 regression results. Leave requirement completion pending.
</output>
