---
phase: 06-safe-managed-worktree-lifecycle
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - baude-core/src/git.rs
  - baude-core/src/lifecycle.rs
  - baude-core/src/lib.rs
  - baude/src/app.rs
  - bauded/src/manager.rs
autonomous: true
requirements: [WORK-01]
must_haves:
  truths:
    - "A valid named branch can be created from the repository's verified default branch and opened in the active backend."
    - "An eligible existing local branch can be activated without resetting it, while an occupied same-repository branch reuses its existing checkout."
  artifacts:
    - path: "baude-core/src/lifecycle.rs"
      provides: "UI-free activation requests, transition outcomes, and repository mutation reservation"
      exports: ["ActivationRequest", "LifecycleOutcome", "RepositoryReservations"]
    - path: "baude-core/src/git.rs"
      provides: "Literal branch classification and verified worktree add/reuse"
      contains: "BranchActivation"
    - path: "baude/src/app.rs"
      provides: "Local production activation tracer"
    - path: "bauded/src/manager.rs"
      provides: "Daemon-owner production activation tracer without new API surface"
  key_links:
    - from: "baude/src/app.rs"
      to: "baude-core/src/lifecycle.rs"
      via: "repository-key activation request and typed outcome"
      pattern: "ActivationRequest|LifecycleOutcome"
    - from: "baude-core/src/lifecycle.rs"
      to: "baude-core/src/git.rs"
      via: "fresh classify/add/reuse operation"
      pattern: "classify_branch|activate_branch"
    - from: "bauded/src/manager.rs"
      to: "RepositoryState"
      via: "verified checkout child saved before backend spawn"
      pattern: "active_intent"
---

<objective>
Deliver the production tracer for WORK-01: repository context reaches one shared, Git-verified create-or-activate contract and then one durable active-backend runtime.

Purpose: Replace caller-HEAD and speculative worktree creation with exact local-ref semantics while proving the same lifecycle meaning in the local App and daemon Manager.
Output: Shared lifecycle module, explicit Git activation primitives, and thin App/Manager production adapters.
</objective>

## Artifacts this phase produces

- `baude-core::lifecycle` shared lifecycle contract and reservation vocabulary.
- Git-owned literal branch classification plus explicit new/existing/occupied activation.
- Local and daemon-owner activation paths keyed by durable repository/checkout identity.

<execution_context>
@/Users/joese/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/joese/.config/opencode/get-shit-done/templates/summary.md
@/Users/joese/.config/opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-CONTEXT.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-RESEARCH.md
@.planning/phases/06-safe-managed-worktree-lifecycle/06-PATTERNS.md
@.planning/phases/05-durable-repository-admission/05-03-SUMMARY.md
</context>

<tasks>

<task id="06-01-01" type="auto" tdd="true">
  <name>Task 1: Trace literal branch activation through shared core and real Git</name>
  <files>baude-core/src/git.rs, baude-core/src/lifecycle.rs, baude-core/src/lib.rs</files>
  <read_first>baude-core/src/git.rs:8-357,465-943,968-1023,1126-end; baude-core/src/repository.rs:11-127,191-351; baude-core/src/lib.rs; 06-RESEARCH.md Pattern 1 through Pattern 3; 06-PATTERNS.md lifecycle.rs and git.rs assignments</read_first>
  <behavior>
    - RED: a valid absent branch requested from any repository child produces a managed linked worktree whose HEAD OID equals the freshly verified default local ref OID.
    - RED: an existing local branch activates at its unchanged OID with plain explicit `worktree add` semantics.
    - RED: an existing same-repository occupied branch returns a reuse outcome and never invokes `worktree add`; an external occupied checkout remains `managed_by_baude=false`.
    - RED: remote-only branch names are rejected rather than inferred or tracked.
  </behavior>
  <action>Write the focused real-Git and pure transition tests first and confirm they fail for the missing lifecycle API, then implement `baude-core::lifecycle` and export it from `lib.rs`. In `git.rs`, validate the literal with `git check-ref-format --branch` and require returned text equality so `@{-1}` cannot expand; classify exact `refs/heads/<name>` with `show-ref --verify --quiet` and inspect matching remote refs only to reject remote-only requests. For a new branch, freshly resolve and commit-verify the repository default and pass its exact full ref as the start point; for an existing local branch, pass the exact full local ref. If fresh inventory already contains the full branch ref, return the existing record instead of adding. Use `Command` argv only, explicit paths/refs, and no `--force`, `-B`, remote guessing, fetching, or branch deletion. Add an explicit per-repository reservation type with release-on-drop behavior; the readable branch label is never identity. This implements the locked Worktree Creation decisions without adding hierarchy or surface UI.</action>
  <acceptance_criteria>
    - The RED tests fail because activation/classification is absent, then pass after implementation.
    - New branches always start at the verified default OID independent of the child supplying context.
    - Existing local and occupied branches follow distinct typed outcomes; remote-only branches cannot activate.
    - Lifecycle command tests prove forbidden force/reset/fetch/delete forms are absent.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p baude-core git::tests::lifecycle::branch_activation -- --nocapture</automated>
  </verify>
  <done>Shared core can classify and execute the valid WORK-01 activation cases against real Git with stable typed outcomes.</done>
</task>

<task id="06-01-02" type="auto" tdd="true">
  <name>Task 2: Wire one durable activation to App and Manager backend dispatch</name>
  <files>baude-core/src/lifecycle.rs, baude/src/app.rs, bauded/src/manager.rs</files>
  <read_first>baude/src/app.rs:310-355,560-849,1723-1749; bauded/src/manager.rs:38-99,399-445,513-685; baude-core/src/repository.rs:81-127,191-351; 05-03-SUMMARY.md patterns-established; 06-PATTERNS.md Shared Core → App/Manager Data Flow</read_first>
  <behavior>
    - RED: a local activation request records one verified `ManagedBranch` child with active intent before one active-backend spawn and associates runtime by `CheckoutKey`.
    - RED: Manager consumes the same activation transition/outcome and does not allocate a duplicate child for occupied reuse.
    - RED: repeated same-branch activation focuses/returns the existing runtime, while a conflicting same-repository reservation returns a typed busy result.
  </behavior>
  <action>First add matching App and Manager contract vectors that drive the production methods and fail against the legacy direct helper paths. Then replace local `InputKind::NewWorktreeBranch` and Manager's worktree branch arm with thin adapters over the shared lifecycle request. Each adapter must reserve by `RepositoryKey`, rediscover immediately before Git mutation, create or register the exact checkout, validate `RepositoryState`, persist active intent before spawn, and associate the runtime by `CheckoutKey`. Preserve App focus behavior and Manager's existing flat compatibility response; do not add daemon hierarchy/removal endpoints. Existing occupied external worktrees may be registered and opened but remain unmanaged. Reuse current commit-stage persistence semantics and release reservations on every error or success path.</action>
  <acceptance_criteria>
    - Local and Manager contract vectors observe the same domain outcome and aggregate delta.
    - Exactly one durable child and runtime result from a successful activation or occupied reuse.
    - Persistence precedes spawn, and runtime identity never uses display name or cwd.
    - No Phase 7 hierarchy rendering or Phase 8 API expansion appears.
  </acceptance_criteria>
  <verify>
    <automated>cargo test lifecycle_create_activate -- --nocapture</automated>
  </verify>
  <done>A real repository-context request can create or activate one managed branch worktree and run it through the active backend in either owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| branch text → Git CLI | Untrusted branch input crosses into Git argv and durable ref identity. |
| Git inventory → lifecycle state | External topology facts authorize child registration and mutation. |
| lifecycle plan → App/Manager effects | Shared ordering must survive owner-specific persistence and PTY effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering / Elevation | `git.rs` branch activation | mitigate | Git literal validation, exact full refs, argv-only commands, explicit default start ref, and no force/fetch/delete forms. |
| T-06-02 | Spoofing / Tampering | lifecycle/App/Manager activation | mitigate | Fresh common-dir inventory, durable keys, per-repository reservation, save-before-spawn, and external reuse kept unmanaged. |
| T-06-SC | Tampering | Cargo dependency boundary | accept | Research requires no packages; keep manifests and `Cargo.lock` unchanged and verify the diff before phase completion. |
</threat_model>

<verification>
- Focused activation tests pass in under 30 seconds where the host permits.
- `git diff -- Cargo.toml Cargo.lock` is empty.
- No direct production caller remains on legacy `git::create_worktree` after this tracer is wired.
</verification>

<success_criteria>
- WORK-01 is observable through local App and daemon Manager owners with one shared semantic contract.
- Branch creation is default-based, local-existing activation is exact, occupied reuse is idempotent, and remote-only activation is refused.
- Every production code task completed RED → GREEN → REFACTOR as needed.
</success_criteria>

<output>
Create `.planning/phases/06-safe-managed-worktree-lifecycle/06-01-SUMMARY.md` when done.
</output>
