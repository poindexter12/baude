---
phase: 11-negotiated-multiline-input
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - baude/src/ui.rs
  - README.md
autonomous: true
requirements: [TKEY-03]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A user opening the help overlay sees the shift+enter binding and where to learn about terminal support (TKEY-03, D-12)"
    - "A user on a terminal that cannot distinguish Shift+Enter finds documented setup/fallback guidance in the README: supported terminals, what legacy behavior does, and the child programs' own fallback bindings (TKEY-03, D-03)"
  artifacts:
    - path: "baude/src/ui.rs"
      provides: "shift+enter line in the Modal::Help global section + render test"
      contains: "shift+enter"
    - path: "README.md"
      provides: "Keys-table row + supported-terminals/fallback caveat subsection"
      contains: "shift+enter"
  key_links:
    - from: "baude/src/ui.rs"
      to: "README.md"
      via: "help line points users at the README for terminal setup detail"
      pattern: "README"
  prohibitions:
    - statement: "baude never guesses a missing modifier (guidance documents fallbacks; no detection promises beyond the real probe)"
      verification: flagged-unverified
---

<objective>
Surface the TKEY-03 guidance the locked decisions require: the help overlay and
README document which terminals support Shift+Enter newline insertion, what
happens on terminals that cannot report the modifier (legacy behavior stands —
Shift+Enter arrives as plain Enter and submits), and what fallbacks exist.

Purpose: D-03 makes guidance part of the unsupported-terminal contract — legacy
byte-identical behavior alone is not enough; the user must be able to find out
why and what to do.

Output: help-overlay line + render test; README Keys row + caveat subsection.

## User-decision mapping

| ID | Decision | Where honored |
|----|----------|---------------|
| D-03 | Unsupported terminal: documented setup/fallback guidance surfaced (help/README) | Tasks 1, 2 |
| D-12 | Documentation lands in the help overlay and README: supported terminals and fallback guidance | Tasks 1, 2 |

## Planner assumptions (flagged)

- TKEY-03 relies on **probe-or-legacy**: the guidance describes real behavior —
  on a non-kitty terminal the SHIFT bit never reaches baude, so Shift+Enter
  submits exactly as before this phase. Wording must not promise detection
  beyond the probe (no TERM-based claims — D-01 forbids allowlist detection).
- Fallback wording is Claude's discretion per CONTEXT; the content below follows
  RESEARCH Open Question 2's suggestion.
</objective>

<execution_context>
@~/.poindexter/claude/gsd-core/workflows/execute-plan.md
@~/.poindexter/claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-negotiated-multiline-input/11-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Help-overlay shift+enter line + render test</name>
  <read_first>
    - baude/src/ui.rs:2173-2229 (Modal::Help arm — the fixed Paragraph, section headers, two-space key column; the "global (any pane)" section at 2201-2211 is where the line goes; the fixed `centered(area, 60, 35)` height at 2174)
    - baude/src/ui.rs:2269+ (mod tests — existing TestBackend + App test precedent to copy for the render assert)
  </read_first>
  <files>baude/src/ui.rs</files>
  <behavior>
    - Test: rendering the UI with the Help modal open produces a buffer containing "shift+enter" (test name `help_overlay_lists_shift_enter`, in the existing ui tests module, using TestBackend per the module's own precedent)
  </behavior>
  <action>
    Add one `Line::raw` row to the Modal::Help "global (any pane)" section
    following the existing two-space-indent key-column format, reading:
    "  shift+enter  newline in claude pane (kitty-capable terminals; see README)".
    Match the surrounding rows' column alignment. The overlay height is fixed at
    `centered(area, 60, 35)` — count the resulting lines and bump the height
    argument if the new row would clip (check by rendering in the test with a
    tall TestBackend and asserting the closing "press any key to close" line
    still appears). Add `help_overlay_lists_shift_enter` to the existing ui
    tests module: construct the App the way neighboring tests do, set the Help
    modal open, render to a TestBackend, assert the buffer text contains
    "shift+enter" and still contains the overlay's closing line. Write the test
    first and observe it fail on the missing text before adding the line.
  </action>
  <verify>
    <automated>cargo test -p baude help_overlay</automated>
    <fails_when>nonzero cargo exit: the buffer does not contain the shift+enter row, or the overlay clipped its closing line after the height change</fails_when>
  </verify>
  <acceptance_criteria>
    - Help overlay renders the shift+enter row in the global section without clipping (D-12)
    - Test asserts rendered buffer content, not source text
  </acceptance_criteria>
  <done>cargo test -p baude help_overlay green; overlay visually consistent with existing rows.</done>
</task>

<task type="auto">
  <name>Task 2: README Keys row + supported-terminals and fallback caveat</name>
  <read_first>
    - README.md:108-146 (the `## Keys` table + the existing alt+arrow terminal-configuration caveat prose at 141-145 — the exact register to match)
  </read_first>
  <files>README.md</files>
  <action>
    Add a `shift+enter` row to the `## Keys` table (Key: `shift+enter`, Where:
    claude pane, Action: insert newline without submitting). After the table's
    existing caveat prose, add a short subsection (matching the alt+arrow
    caveat's tone) covering, per D-03/D-12: (1) Shift+Enter newline insertion is
    negotiated at startup via the kitty keyboard protocol — verified terminals:
    Ghostty, kitty, iTerm2, WezTerm, foot, Alacritty; (2) on terminals without
    the protocol, baude keeps today's behavior exactly — the terminal reports
    Shift+Enter as plain Enter, so it submits; baude never infers the modifier;
    (3) fallbacks on unsupported terminals: use the child program's own
    multiline mechanisms (Claude Code: backslash-then-Enter, or its
    /terminal-setup command; OpenCode: ctrl+j); (4) tmux/screen users: the
    negotiation talks to the multiplexer, not the outer terminal — tmux needs
    its extended-keys support enabled (point at tmux's extended-keys option)
    for Shift+Enter to pass through. Do not claim detection by terminal name
    or TERM value anywhere (D-01 forbids allowlist language — the list above is
    "verified to work", not "how baude decides").
  </action>
  <verify>
    <automated>grep -qi 'shift+enter' README.md && grep -q 'extended-keys' README.md</automated>
    <fails_when>nonzero exit: the Keys row or the tmux extended-keys guidance is absent from README.md</fails_when>
  </verify>
  <acceptance_criteria>
    - Keys table row present; caveat covers verified terminals, legacy behavior, child-program fallbacks, and tmux guidance (D-03, D-12)
    - No TERM/terminal-name detection claims (D-01)
  </acceptance_criteria>
  <done>README documents supported terminals + fallback guidance in the existing Keys section's register.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → user expectations | no code trust boundary; documentation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07 | Information Disclosure / Repudiation | misleading guidance leading users to force terminal modes by hand | low | accept | Guidance lists only verified terminals, states legacy behavior truthfully, and recommends child-program fallbacks rather than terminal reconfiguration hacks |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs this phase |
</threat_model>

<verification>
- `cargo test -p baude help_overlay` green
- README greps pass (shift+enter row, extended-keys guidance)
- `cargo test --workspace --locked` green (no regressions from the ui change)
</verification>

<success_criteria>
- A user can discover the shift+enter capability in-app and the full support/fallback story in the README
- Unsupported-terminal users get accurate legacy-behavior explanation plus actionable fallbacks
</success_criteria>

<output>
## Artifacts this phase produces (this plan's share)

- `baude/src/ui.rs`: help-overlay shift+enter row (docs entry) + `help_overlay_lists_shift_enter` test
- `README.md`: Keys row + supported-terminals/fallback caveat (docs entry)

Create `.planning/phases/11-negotiated-multiline-input/11-03-SUMMARY.md` when done.
</output>
