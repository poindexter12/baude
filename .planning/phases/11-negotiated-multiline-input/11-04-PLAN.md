---
phase: 11-negotiated-multiline-input
plan: 04
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - baude/src/app.rs
  - baude-core/src/pty.rs
autonomous: true
requirements: [TKEY-01, TKEY-02, TKEY-05]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Shift+Enter encodes to the enhanced sequence CSI 13;2u ONLY after the focused child's parser has observed a kitty push; otherwise the Claude-pane fallback ESC CR (or shell-pane CR) stands (TKEY-01, TKEY-05, D-04)"
    - "The local branch and the remote-attach branch of forward_key derive the encode context through the same helper, so remote sessions cannot diverge (TKEY-02 parity)"
    - "A remote-attach mirror parser converges on the same child kitty state: subscribe()'s snapshot replays an observed active push (TKEY-05 across attach)"
  artifacts:
    - path: "baude/src/app.rs"
      provides: "encode_ctx(screen, to_shell) helper; forward_key reads kitty_keyboard() beside application_cursor() in both branches"
      contains: "kitty_keyboard"
    - path: "baude-core/src/pty.rs"
      provides: "subscribe() snapshot replays active child kitty push"
      contains: "kitty_keyboard"
  key_links:
    - from: "baude/src/app.rs"
      to: "vendor/vt100/src/screen.rs"
      via: "encode_ctx reads screen.kitty_keyboard() != 0 (the DECCKM application_cursor precedent)"
      pattern: "kitty_keyboard\\(\\)"
    - from: "baude-core/src/pty.rs"
      to: "vendor/vt100/src/screen.rs"
      via: "subscribe() conditions a CSI > flags u replay on screen.kitty_keyboard()"
      pattern: "kitty_keyboard\\(\\)"
  prohibitions:
    - statement: "enhanced bytes are never sent on an unverified path"
      verification: flagged-unverified
    - statement: "legacy key bytes never change"
      verification: flagged-unverified
---

<objective>
Complete D-04's child-verification leg: replace plan 11-01's fail-closed
`kitty_child: false` constant with the real observation — forward_key consults
`screen().kitty_keyboard() != 0` exactly the way it already consults
`application_cursor()`, in BOTH the local and remote-attach branches — and make
remote mirrors converge by replaying an active child kitty push in subscribe()'s
snapshot (RESEARCH Open Question 3: include).

Purpose: with this plan, both ends of TKEY-05's "verified outer-terminal/child
input path" are wired: outer = probe + gated push (11-01); child = observed push
(11-02's accessor, consumed here). Enhanced passthrough becomes reachable only
on the doubly-verified path.

Output: encode_ctx helper + forward_key wiring + ctx-derivation tests;
subscribe() kitty replay + round-trip test.

## User-decision mapping

| ID | Decision | Where honored |
|----|----------|---------------|
| D-04 | Enhanced sequences only when BOTH ends verified | Task 1 (child end goes live) |
| D-09 | Enhanced passthrough when the child itself enabled enhanced mode on the PTY | Task 1 |
| D-10 | Enhanced mode changes ONLY Shift+Enter | Task 1 (ctx feeds only the Enter arm; 11-01's corpus locks the rest) |

## Planner assumptions (flagged)

- TKEY-05's child leg relies on **dual verification**: outer probe (11-01) AND
  observed child push (11-02). This plan is pure wiring between proven parts.
- Test strategy follows the phase's established pattern (CONTEXT: "byte-level
  regression tests with injected seams rather than live spawns"): ctx derivation
  is tested against a directly-fed vt100 Parser (the app.rs:5758 precedent), and
  the subscribe replay via the pure snapshot round-trip precedent
  (pty.rs:1120). Live end-to-end verification on a real terminal folds into
  Phase 12 SHIP-03 per CONTEXT.
</objective>

<execution_context>
@~/.poindexter/claude/gsd-core/workflows/execute-plan.md
@~/.poindexter/claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-negotiated-multiline-input/11-RESEARCH.md
@.planning/phases/11-negotiated-multiline-input/11-PATTERNS.md
@.planning/phases/11-negotiated-multiline-input/11-01-SUMMARY.md
@.planning/phases/11-negotiated-multiline-input/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: forward_key consults the observed child kitty state (both branches)</name>
  <read_first>
    - baude/src/app.rs:3923-3964 (forward_key as left by 11-01 — the EncodeCtx construction sites; the application_cursor lock-map-unwrap shape), 3930-3942 (remote-attach branch), 5754+ (clipboard_tests) and 5758 (direct vt100::Parser construction in tests — the precedent for ctx tests)
    - .planning/phases/11-negotiated-multiline-input/11-02-SUMMARY.md (kitty_keyboard() semantics as shipped)
  </read_first>
  <files>baude/src/app.rs</files>
  <behavior>
    - Test 1: encode_ctx over a parser fed b"\x1b[>1u" yields kitty_child == true; a fresh parser yields false (fail-closed default)
    - Test 2: encode_ctx over a parser fed b"\x1b[>1u\x1b[<1u" yields kitty_child == false (pop returns to unverified)
    - Test 3: encode_ctx over a parser fed b"\x1b[?1h" yields app_cursor == true and kitty_child == false (independent modes)
    - Test 4: composing with encode_key — for a Shift+Enter KeyEvent, ctx from a pushed parser yields b"\x1b[13;2u"; ctx from a fresh parser with to_shell:false yields b"\x1b\r"; with to_shell:true yields b"\r" (the full TKEY-01 decision matrix through the real derivation path)
  </behavior>
  <action>
    Write the behavior tests first in a new `#[cfg(test)] mod forward_ctx_tests`
    in app.rs (pure — vt100::Parser::new + process + asserts, per the app.rs:5758
    precedent; no PTY, no App). RED fails to compile (encode_ctx does not exist);
    record and commit. GREEN: add `fn encode_ctx(screen: &vt100::Screen,
    to_shell: bool) -> EncodeCtx` (module-level in app.rs, near forward_key)
    returning EncodeCtx { app_cursor: screen.application_cursor(), kitty_child:
    screen.kitty_keyboard() != 0, to_shell }. Rewire forward_key so BOTH
    branches derive their ctx through this one helper under their existing
    parser locks: local branch `pty.parser.lock().map(|p| encode_ctx(p.screen(),
    to_shell))`, remote-attach branch the same over `a.parser`, each with an
    `unwrap_or` fallback of EncodeCtx { app_cursor: false, kitty_child: false,
    to_shell } (poisoned lock degrades to legacy — fail-closed, D-04). Delete
    the 11-01 constant-false construction; the helper is now the only EncodeCtx
    producer in forward_key. No changes to handle_key routing, chords, or the
    input-modal path (D-11 from the phase context still binds).
  </action>
  <verify>
    <automated>cargo test -p baude forward_ctx</automated>
    <fails_when>nonzero cargo exit: a ctx derivation case or the composed encode_key matrix case fails, or the module fails to compile</fails_when>
  </verify>
  <verify>
    <automated>cargo test -p baude keys</automated>
    <fails_when>nonzero cargo exit: the 11-01 byte corpus regressed — some key other than Shift+Enter changed bytes (TKEY-02 violation)</fails_when>
  </verify>
  <acceptance_criteria>
    - encode_ctx is the single ctx producer for both forward_key branches (remote parity by construction)
    - kitty_child comes only from the parser's observed push state; every fallback (fresh parser, poisoned lock) is false (D-04 fail-closed)
    - Composed matrix test proves the doubly-gated enhanced path and both fallback encodings (D-09)
    - RED commit precedes GREEN commit
  </acceptance_criteria>
  <done>Enhanced passthrough is live behind the observed-push gate; corpus still green; both branches share one derivation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: subscribe() snapshot replays active child kitty state</name>
  <read_first>
    - baude-core/src/pty.rs:389-422 (subscribe — the mode-replay list at 398-417 being extended), 1120-1160 (subscribe_snapshot_replays_pre_attach_links — the pure snapshot round-trip test analog), 1015-1055 (subscribe_snapshot_then_live_bytes — the fixture-PTY analog, for shape reference only)
  </read_first>
  <files>baude-core/src/pty.rs</files>
  <behavior>
    - Test 1 (round-trip, pure): a source parser fed b"\x1b[>1u" plus ordinary text; snapshot bytes built the way subscribe() builds them; a fresh mirror parser fed the snapshot reports kitty_keyboard() != 0 (mirror converges on child kitty state)
    - Test 2: same round-trip with no kitty push — the snapshot contains no CSI > u bytes and the mirror reports kitty_keyboard() == 0 (inactive state adds nothing to the snapshot)
  </behavior>
  <action>
    Write the two round-trip tests first in pty.rs's existing #[cfg(test)]
    module, mirroring subscribe_snapshot_replays_pre_attach_links exactly
    (build the snapshot inline the way subscribe() does — including the new
    replay line — feed a fresh vt100::Parser, assert). RED: Test 1's mirror
    reports 0 because subscribe()'s construction (which the test mirrors) lacks
    the replay; record and commit. GREEN: extend the mode-replay list in
    subscribe() (pty.rs:403-416): after the hide_cursor replay, when
    `screen.kitty_keyboard()` is nonzero, append the push bytes
    `\x1b[>{flags}u` (format the current flags value into the sequence — a
    fresh mirror parser has an empty stack, so replaying one push converges it
    on the same current-flags value). Follow the existing pattern: literal
    escape bytes, conditioned on the accessor, appended to the snapshot Vec,
    with a one-line comment in the existing "Terminal modes aren't part of
    contents_formatted" list style. write_input and the subscriber channel are
    untouched.
  </action>
  <verify>
    <automated>cargo test -p baude-core subscribe</automated>
    <fails_when>nonzero cargo exit: a round-trip assert fails (mirror does not converge, or inactive state leaks replay bytes), or an existing subscribe test regressed</fails_when>
  </verify>
  <verify>
    <automated>cargo test --workspace --locked</automated>
    <fails_when>nonzero cargo exit: any workspace test regressed by this plan's wiring</fails_when>
  </verify>
  <acceptance_criteria>
    - Active child kitty state rides the snapshot; inactive state adds zero bytes (byte-identical snapshots for non-kitty children)
    - Test mirrors subscribe()'s real construction so drift between test and production construction is the failure mode being guarded
    - RED commit precedes GREEN commit
  </acceptance_criteria>
  <done>cargo test -p baude-core subscribe and cargo test --workspace --locked green; remote mirrors converge on child kitty state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child PTY output → parser state → encode decision | attacker-controlled child output now influences which bytes baude writes back to that same child |
| source parser → attach mirror parser | replayed snapshot bytes carry child state across the attach transport |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-08 | Spoofing | a child emitting CSI > u solely to receive enhanced encodings | low | accept | The child controls only its own input encoding, per-child, per-parser — an observed push entitling that child to CSI-u Shift+Enter is exactly the D-04 contract, not a bypass; no other key's encoding is affected (corpus-locked) |
| T-11-09 | Tampering | mirror-parser divergence: remote attach encodes against stale child state | medium | mitigate | Task 2's snapshot replay conditions on the accessor; round-trip tests pin convergence for both active and inactive states |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs this phase |
</threat_model>

<verification>
- `cargo test -p baude forward_ctx` green (derivation + composed matrix)
- `cargo test -p baude keys` green (TKEY-02 corpus unchanged)
- `cargo test -p baude-core subscribe` green (replay round-trips)
- `cargo test --workspace --locked` green
</verification>

<success_criteria>
- Enhanced Shift+Enter bytes flow only on the doubly-verified path (outer probe true AND child push observed); all fallbacks fail closed to legacy encodings
- Local and remote forwarding derive identical contexts through one helper
- Remote mirrors reconstruct child kitty state from the subscribe snapshot
</success_criteria>

<output>
## Artifacts this phase produces (this plan's share)

- `baude/src/app.rs`: `encode_ctx(screen, to_shell) -> EncodeCtx` helper; forward_key kitty_keyboard() reads (both branches); `forward_ctx_tests`
- `baude-core/src/pty.rs`: subscribe() kitty push replay + round-trip tests

Create `.planning/phases/11-negotiated-multiline-input/11-04-SUMMARY.md` when done.
</output>

> Note (checker advisory): RESEARCH's TKEY-02 fixture-PTY forward_key integration-test row is deliberately superseded by the pure keys.rs byte corpus (11-01 Task 2) plus the encode_ctx derivation/matrix tests here — per CONTEXT's injected-seams-over-live-spawns pattern; byte-equivalent coverage.
