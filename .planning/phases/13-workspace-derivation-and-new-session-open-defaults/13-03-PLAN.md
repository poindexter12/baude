---
phase: 13
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
files_modified:
  - baude-core/src/folder_workspace.rs
  - baude/src/app.rs
  - bauded/src/main.rs
  - bauded/src/manager.rs
  - README.md
autonomous: true
requirements:
  - WSPC-03
  - WSPC-04
  - OPEN-03
  - OPEN-04

estimate:
  tokens: 35000
  raw_tokens: 17500
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Daemon parity: bauded resolves workspace by same rule as baude for same launch directory, using shared resolver in baude-core"
    - "Daemon startup canonicalizes current_dir() before calling folder_workspace::plan_launch, obtaining hint from ancestor walk"
    - "Daemon startup passes plan.hint to workspace::initialize(), same as TUI startup flow"
    - "Daemon records derived bindings: after claiming workspace lock, calls folder_workspace::record with plan.repo_root (same path used by baude)"
    - "Daemon workspace lock semantics: lock refusal prevents binding write (no half-written state)"
    - "Repository deduplication test verified in 13-02; single app instance admits from root and subfolder, same row created"
    - "Clone-on-demand destination remains unchanged: clone_base_dir config semantics not affected by new-session prefill changes"
    - "README Workspaces section documents full precedence chain: BAUDE_WORKSPACE > folder binding > config > derived > BAUDE_BACKEND > config backend > claude"
    - "README Workspaces section explains when derivation applies (inside git repo) and when it doesn't (non-git folder)"
    - "README Folder context section documents ancestor walk: nearest binding used, walk stops at home"
    - "README examples cover: repo subfolder derivation, bound parent, unbound non-git folder"
    - "README New-session defaults section documents n prefill uses repo root when inside repo"
    - "README TUI title section documents that title shows workspace name and source label"
  artifacts:
    - "bauded/src/main.rs startup code (around line 182) calls folder_workspace::plan_launch with config.folder_context_enabled(), canonical launch_dir, and passes plan.hint to workspace::initialize()"
    - "bauded/src/main.rs startup calls folder_workspace::record with plan.repo_root after claiming workspace lock, recording derived bindings at repo_root (mirrors baude/src/main.rs:399-411)"
    - "baude-core/src/folder_workspace.rs test_daemon_parity_same_launch_dir (or similar) verifies daemon and app resolve same workspace when given same launch_dir and state"
    - "baude/src/app.rs tests section includes test_admit_repository_same_from_root_and_subfolder verifying deduplication works (from 13-02)"
    - "README.md Workspaces section includes full precedence order and explains derivation rule with examples"
    - "README.md Folder context section documents ancestor walk stopping at home"
    - "README.md New-session defaults section explains n prompt prefill uses repo root when inside repo"
    - "README.md TUI title section documents title shows workspace name and source label"
    - "Clone-on-demand section in README unchanged; no modifications to clone destination logic"
  key_links:
    - "Daemon startup (bauded/src/main.rs) mirrors TUI startup (baude/src/main.rs lines 343-370): canonicalize launch_dir, call plan_launch, initialize with hint, claim lock, record binding"
    - "Daemon recording (bauded/src/manager.rs line 444) guards state load/save; binding written after lock claim so lock refusal prevents partial write"
    - "Shared resolver (workspace + folder_workspace in baude-core) ensures both binaries get same result"
    - "Repository deduplication (from 13-02) verified; single app deduplicates by common dir; no new daemon-specific dedup logic needed"
    - "Clone-on-demand (clone_base_dir) lives in separate code path from new-session prefill; no cross-contamination"
  prohibitions:
    - statement: "Never modify daemon workspace resolution to deviate from TUI resolution; both must call the same baude-core functions"
      status: verified
      verification: explicit
    - statement: "Never change clone_base_dir semantics or destination logic when updating new-session prefill; these are independent"
      status: verified
      verification: explicit
    - statement: "Never claim that folder memory is read if folder_context is disabled; document the kill switch in README"
      status: verified
      verification: explicit

---

<objective>
Complete daemon parity by adding folder-workspace lookup to daemon startup, verify repository deduplication works, and document the full derivation rule and precedence chain in README so users understand how workspace selection works.

Purpose: Daemon consistency ensures that `bauded` selects the same workspace as `baude` for the same launch directory. Documentation teaches users how to use and override derivation. README clarity reduces support burden.

Output: Daemon calls shared resolver; repository deduplication test passes; README sections document precedence, derivation rule, examples, and new-session defaults.
</objective>

<execution_context>
@~/.iarx/claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-workspace-derivation-and-new-session-open-defaults/13-RESEARCH.md
@.planning/phases/13-workspace-derivation-and-new-session-open-defaults/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Add daemon workspace resolution parity and repository deduplication test</name>
  <files>
    bauded/src/main.rs
    baude-core/src/folder_workspace.rs
  </files>
  <read_first>
    bauded/src/main.rs (lines 1-200, daemon startup)
    baude/src/main.rs (lines 343-370, TUI startup for reference)
    baude-core/src/folder_workspace.rs (plan_launch signature and usage)
    baude-core/src/folder_workspace.rs (test patterns, lines 148-214)
  </read_first>
  <action>
    **bauded/src/main.rs:**
    Update daemon startup (around line 174 where workspace::initialize is called) to mirror TUI startup flow (baude/src/main.rs:359-370):
    1. Before calling workspace::initialize, add explicit canonicalization of current_dir() (e.g., let launch_dir = std::fs::canonicalize(std::env::current_dir()?)?) to ensure the daemon's launch_dir is canonical
    2. Call folder_workspace::plan_launch(config.folder_context_enabled(), ws_env, backend_env, config_root, &launch_dir) to get both hint and repo_root from ancestor walk
    3. Pass plan.hint.as_deref() to workspace::initialize() instead of None
    4. After claiming workspace lock (in Manager::restore or similar), call folder_workspace::record(config_root, plan.repo_root.as_deref().or(Some(&launch_dir)), &ws) to record derived bindings at repo_root, matching TUI behavior

    **Reasoning:** Daemon uses same resolver, same canonicalization, and same recording as TUI. When TUI spawns daemon with BAUDE_WORKSPACE set, explicit env takes precedence. When daemon is spawned from shell with no env, it consults folder memory and derives workspace same as TUI. Recording at repo_root ensures daemon-derived bindings are stable and found by future TUI launches.

    **bauded/src/manager.rs:**
    Verify lock claim occurs before binding write (Manager::restore at line 444 loads and locks before recording). If recording logic is missing, add it after lock claim, before state save.

    **baude-core/src/folder_workspace.rs tests:**
    Add unit test test_plan_launch_parity (or test_daemon_parity_same_launch_dir) that verifies: call plan_launch twice with the same launch_dir and folder-workspaces.json state, both calls return the same hint and repo_root. Use TestRedirect to set up fixtures with recorded bindings; assert hints match.

    **baude/src/app.rs (repository deduplication from 13-02 - verify test exists):**
    Test test_admit_repository_same_from_root_and_subfolder should already exist from 13-02. If not, add it:
    - One app instance launches from repo root, admits repo
    - Same app instance then launches from repo subfolder (or calls admit_repository for same path)
    - Assert same repository row created (no duplicate); git::discover_repository returns same common dir for both paths, deduplication prevents duplicate rows
    - Verify second admit sees existing row and focuses it

    **Test isolation:** Use TestRedirect for all fixtures; no real ~/.config/baude or git repositories.
  </action>
  <verify>
    <automated>cargo test -p baude-core folder_workspace::tests::daemon_parity --lib -- --nocapture</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in output</fails_when>
  </verify>
  <acceptance_criteria>
    - bauded/src/main.rs canonicalizes current_dir() before calling folder_workspace::plan_launch at daemon startup with canonical launch_dir and config.folder_context_enabled()
    - bauded/src/main.rs passes plan.hint to workspace::initialize() (per D-03)
    - bauded/src/main.rs calls folder_workspace::record(config_root, plan.repo_root, &ws) after claiming workspace lock to record derived bindings at repo_root (per WSPC-04)
    - bauded/src/manager.rs lock claim occurs before binding write; lock refusal prevents half-written binding file
    - baude-core/src/folder_workspace.rs includes unit test test_plan_launch_parity verifying plan_launch returns same hint and repo_root when called twice with same launch_dir and state
    - baude/src/app.rs includes integration test test_admit_repository_same_from_root_and_subfolder verifying repository is deduplicated (same repo row created, second admit focuses existing row) when admitted from root and subfolder within one app instance
    - Daemon startup mirrors TUI startup (baude/src/main.rs:343-370) for folder_workspace and recording logic
    - Daemon workspace lock semantics preserved; lock refusal prevents binding write
    - Tests use TestRedirect; no real home or config dir touched
    - README documents that daemon uses same workspace derivation rule as TUI
  </acceptance_criteria>
  <done>
    Daemon calls shared resolver, tests verify parity and deduplication, repository is not duplicated when admitted from different levels of the same repo.
  </done>
</task>

<task type="auto">
  <name>Update README with workspace derivation, precedence, and new-session defaults documentation</name>
  <files>
    README.md
  </files>
  <read_first>
    README.md (existing Workspaces section)
    README.md (existing Folder context section)
    README.md (existing New-session defaults section, if any)
    13-CONTEXT.md (Locked decisions section)
  </read_first>
  <action>
    **README.md — Workspaces section:**
    Rewrite to document the full precedence chain and derivation rule:
    1. Start with: "Workspace selection follows a precedence order..."
    2. List in order: BAUDE_WORKSPACE env var (explicit), folder binding (ancestor walk result), config workspace key, derived repo-root name, BAUDE_BACKEND env var, config backend key, default (claude)
    3. Explain each level: "explicit" is highest priority, "folder binding" walks up from launch dir to find nearest recorded binding (stops at home), "derived" applies when launched inside a git repository, etc.
    4. Add "When does derivation apply?" subsection: derivation applies only when launched inside a git repository and no explicit env var or folder binding is found. Unbound non-git folders use the rest of the chain without derivation.
    5. Add examples: (a) launch from ~/Code/github.com/iarx-com/baude subfolder → derives "baude" workspace, (b) launch from ~/Code/github.com/poindexter12/baude with binding recorded → uses "poindexter12", (c) launch from ~/Documents (not a repo) with no binding → uses config workspace or default
    6. Document the `folder_context` config option: when false, disables both reading and recording folder memory; existing bindings remain in the file but are not consulted

    **README.md — Folder context section:**
    Update to explain the ancestor walk:
    - "Folder memory records which workspace to use for a given directory, allowing baude to remember your choice across launches."
    - Explain the walk: "When you launch baude, it walks up from your launch directory to find the nearest recorded binding; the first binding found is used."
    - Document the boundary: "The walk stops at your home directory and does not continue above it."
    - Explain recording: "When you launch from a new repository, baude derives the workspace name from the repo root's folder name and records the binding for that repo, so future launches from that repository use the same workspace."
    - Document how to override or rebind: "To use a different workspace for a folder, set BAUDE_WORKSPACE env var, or use the config key workspace in ~/.config/baude/config.json. To rebind a folder, delete the entry from ~/.config/baude/folder-workspaces.json and re-launch to record a new binding."

    **README.md — New-session defaults section (new or update existing):**
    Document the `n` (new-session) prompt prefill:
    - "When you press `n` to create a new session, the prompt prefills a default path."
    - "If you launched baude inside a git repository, the default path is that repository's root, allowing you to easily create sessions in the same repo."
    - "If you launched outside a repository, the default path is the `new_session_dir` config option (if set), else your launch directory."
    - "You can always type a different path; the prefill is a convenience."

    **README.md — TUI title section (if exists, or update as needed):**
    Document that the title includes the active workspace and how it was chosen:
    - "The top title shows baude's version, the active workspace name, and how it was selected in parentheses: (explicit) for BAUDE_WORKSPACE or config, (folder binding) for ancestor walk result, (derived) for repo-root derivation, or (default) for implicit default."
    - "This allows you to see at a glance which workspace you're in and understand why that workspace was selected."

    **README.md — Clone-on-demand section (if exists):**
    Verify clone destination is still documented correctly; no changes needed, but if clone_base_dir is documented, note that `n` prefill changes do not affect clone destination (it still uses clone_base_dir or ~/Code/<host>/<owner>/<repo>).

    **Exact placement:** These updates go into existing README sections (Workspaces, Folder context) or create new subsections if needed. Keep consistent with existing README tone and style.
  </action>
  <verify>
    <automated>grep -E "BAUDE_WORKSPACE.*folder.*binding.*config.*derived.*BAUDE_BACKEND" README.md</automated>
    <fails_when>no matches of full precedence order in README.md</fails_when>
  </verify>
  <acceptance_criteria>
    - README.md Workspaces section documents full precedence chain in correct order: BAUDE_WORKSPACE > folder binding > config workspace > derived > BAUDE_BACKEND > config backend > default
    - README Workspaces explains when derivation applies (launched inside git repo) and when it doesn't (non-git folder uses rest of chain without derivation)
    - README Workspaces includes at least 3 examples covering: repo subfolder (derives from basename), bound parent folder (uses binding), unbound non-git folder (uses config/default)
    - README Folder context section explains ancestor walk: nearest recorded binding is used, walk stops at $HOME
    - README Folder context documents the kill switch: folder_context: false disables both reading and recording folder memory
    - README Folder context documents how to rebind or override: delete entry from folder-workspaces.json or set BAUDE_WORKSPACE env var
    - README New-session defaults section (new or updated) explains `n` prefill uses repository root when inside git repo, config new_session_dir when outside repo
    - README TUI title section (new or updated) documents that title shows workspace name and source label in parentheses: (explicit), (folder binding), (derived), or (blank)
    - README documents that daemon uses same workspace derivation rule as TUI and records derived bindings at repository root
    - Clone-on-demand documentation unchanged; clone_base_dir semantics unaffected by new-session prefill changes
    - README has no markdown syntax errors; grep finds precedence order text in Workspaces section
  </acceptance_criteria>
  <done>
    README.md updated with full workspace derivation rule, precedence chain, examples, and new-session defaults. Users can understand and predict workspace selection behavior.
  </done>
</task>

</tasks>

## Review Dispositions (Cross-AI Review Cycle 1)

| Finding ID | Severity | Status | Action Taken | Rationale |
|-----------|----------|--------|--------------|-----------|
| 13-03-R1 | HIGH | Incorporated | Added explicit daemon recording logic: after lock claim, call folder_workspace::record(config_root, plan.repo_root, &ws) | WSPC-04 requires daemon to record derived bindings via same code path; recording now happens post-lock |
| 13-03-R2 | HIGH | Incorporated | Changed parity test to verify plan_launch determinism: same launch_dir and state returns same hint and repo_root (not cross-binary subprocess test) | Determinism test appropriate for unit level; daemon invocation tested implicitly via startup parity |
| 13-03-R3 | HIGH | Incorporated | Added baude/src/app.rs to files_modified; deduplication test now explicitly in 13-02 Task 3 using one app instance | Ownership metadata accurate; test verifies expected behavior within single app |
| 13-03-R4 | HIGH | Incorporated | Clarified that deduplication test uses ONE app instance admitting from root then subfolder; no cross-instance state sharing | Single app instance shares repository_state; test correctly verifies deduplication mechanism |
| 13-03-R5 | HIGH | Incorporated | Added explicit current_dir() canonicalization in bauded startup before calling plan_launch | Daemon launch_dir now canonical, matches TUI canonicalization flow |
| 13-03-R6 | MEDIUM | Incorporated | Clarified in must-haves: binding write occurs after lock claim; lock refusal prevents write | Write ordering and lock semantics documented explicitly |
| 13-03-R7 | MEDIUM | Incorporated | README written after precedence tests pass in 13-02; documents tested contract not speculative behavior | Documentation follows tested implementation |
| 13-03-R8 | LOW | Incorporated | Replaced generic grep with targeted precedence order search: grep "BAUDE_WORKSPACE.*folder.*binding.*config" | Meaningful verification of documentation content |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Daemon startup → Workspace resolution | Daemon uses same resolver as TUI; launch_dir canonicalized before calling plan_launch |
| Documentation → User expectations | README explains behavior accurately; users understand when derivation applies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-08 | Information | README documentation gap | low | mitigate | Document full precedence chain and examples so users understand workspace selection |
| T-13-09 | Denial | Deduplication failure | low | mitigate | Integration test verifies same repo admitted from root and subfolder creates one row (no duplicate) |

</threat_model>

<verification>
1. Verify daemon startup code:
   - bauded/src/main.rs canonicalizes current_dir before calling folder_workspace::plan_launch
   - bauded/src/main.rs passes plan.hint to workspace::initialize()
   - bauded/src/main.rs calls folder_workspace::record(config_root, plan.repo_root, &ws) after lock claim
2. Run daemon parity test: `cargo test -p baude-core folder_workspace::tests::plan_launch_parity --lib`
3. Run repository deduplication test: `cargo test -p baude app::tests::admit_repository_same_from_root_and_subfolder --lib`
4. Run full test suite: `cargo test --all --lib` (includes new tests and all existing tests)
5. Read README.md Workspaces section and verify:
   - Full precedence order stated: BAUDE_WORKSPACE > binding > config > derived > BAUDE_BACKEND > backend > default
   - At least 3 examples provided (repo subfolder, bound parent, unbound non-git folder)
6. Read README.md Folder context section and verify: ancestor walk stops at home, kill switch documented
7. Read README.md for New-session defaults and TUI title sections explaining prefill and title rendering
8. Grep README for precedence order substring: `grep "BAUDE_WORKSPACE.*folder.*binding.*config" README.md` should find match
9. Verify clone_base_dir documentation unchanged
10. Run markdown syntax check if available (no broken links, valid formatting)
</verification>

<success_criteria>
- bauded/src/main.rs canonicalizes launch_dir and calls folder_workspace::plan_launch with config.folder_context_enabled()
- bauded/src/main.rs passes plan.hint to workspace::initialize()
- bauded/src/main.rs calls folder_workspace::record(config_root, plan.repo_root, &ws) after lock claim to record derived bindings
- plan_launch parity test passes: same launch_dir and state returns same hint and repo_root
- Repository deduplication test passes: same repo admitted from root and subfolder within one app creates one row
- README.md Workspaces section documents full precedence chain in correct order with at least 3 examples
- README.md Folder context section explains ancestor walk stops at home; documents kill switch and rebinding
- README.md New-session section explains `n` prefill uses repo root inside repo, config/launch_dir outside
- README.md TUI title section documents workspace name and source label display
- README.md mentions daemon uses same derivation rule and records bindings at repo root
- Clone-on-demand documentation unchanged; no regression
- Full test suite passes; no existing test regressions from precedence reorder
</success_criteria>

<output>
Create `.planning/phases/13-workspace-derivation-and-new-session-open-defaults/13-03-SUMMARY.md` when done
</output>
