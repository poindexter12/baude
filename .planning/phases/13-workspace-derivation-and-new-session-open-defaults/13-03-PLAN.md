---
phase: 13
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
files_modified:
  - baude-core/src/folder_workspace.rs
  - baude/src/app.rs
  - baude/src/remote.rs
  - baude/src/ui.rs
  - bauded/src/main.rs
  - bauded/src/api.rs
  - README.md
autonomous: true
requirements:
  - WSPC-03
  - WSPC-04
  - OPEN-03
  - OPEN-04

estimate:
  tokens: 35000
  raw_tokens: 17500
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Daemon parity: bauded resolves workspace by same rule as baude for same launch directory, using shared resolver in baude-core"
    - "Daemon startup canonicalizes current_dir() before calling folder_workspace::plan_launch, obtaining hint from ancestor walk"
    - "Daemon startup passes plan.hint to workspace::initialize(), same as TUI startup flow"
    - "Daemon records derived bindings: after claiming workspace lock, calls folder_workspace::record with plan.repo_root (same path used by baude)"
    - "Daemon workspace lock semantics: lock refusal prevents binding write (no half-written state)"
    - "Repository deduplication test verified in 13-02; single app instance admits from root and subfolder, same row created"
    - "Clone-on-demand destination remains unchanged: clone_base_dir config semantics not affected by new-session prefill changes"
    - "README Workspaces section documents full precedence chain: BAUDE_WORKSPACE > folder binding > config > derived > BAUDE_BACKEND > config backend > claude"
    - "README Workspaces section explains when derivation applies (inside git repo) and when it doesn't (non-git folder)"
    - "README Folder context section documents ancestor walk: nearest binding used, walk stops at home"
    - "README examples cover: repo subfolder derivation, bound parent, unbound non-git folder"
    - "README New-session defaults section documents n prefill uses repo root when inside repo"
    - "README TUI title section documents that title shows workspace name and source label"
  artifacts:
    - "bauded/src/main.rs startup code (around line 182) calls folder_workspace::plan_launch with config.folder_context_enabled(), canonical launch_dir, and passes plan.hint to workspace::initialize()"
    - "bauded/src/main.rs startup calls folder_workspace::record with plan.repo_root after claiming workspace lock, recording derived bindings at repo_root (mirrors baude/src/main.rs:399-411)"
    - "baude-core/src/folder_workspace.rs test_daemon_parity_same_launch_dir (or similar) verifies daemon and app resolve same workspace when given same launch_dir and state"
    - "baude/src/app.rs tests section includes test_admit_repository_same_from_root_and_subfolder verifying deduplication works (from 13-02)"
    - "README.md Workspaces section includes full precedence order and explains derivation rule with examples"
    - "README.md Folder context section documents ancestor walk stopping at home"
    - "README.md New-session defaults section explains n prompt prefill uses repo root when inside repo"
    - "README.md TUI title section documents title shows workspace name and source label"
    - "Clone-on-demand section in README unchanged; no modifications to clone destination logic"
  key_links:
    - "Daemon startup (bauded/src/main.rs) mirrors TUI startup (baude/src/main.rs lines 343-370): canonicalize launch_dir, call plan_launch, initialize with hint, claim lock, record binding"
    - "Daemon recording (bauded/src/manager.rs line 444) guards state load/save; binding written after lock claim so lock refusal prevents partial write"
    - "Shared resolver (workspace + folder_workspace in baude-core) ensures both binaries get same result"
    - "Repository deduplication (from 13-02) verified; single app deduplicates by common dir; no new daemon-specific dedup logic needed"
    - "Clone-on-demand (clone_base_dir) lives in separate code path from new-session prefill; no cross-contamination"
  prohibitions:
    - statement: "Never modify daemon workspace resolution to deviate from TUI resolution; both must call the same baude-core functions"
      status: verified
      verification: explicit
    - statement: "Never change clone_base_dir semantics or destination logic when updating new-session prefill; these are independent"
      status: verified
      verification: explicit
    - statement: "Never claim that folder memory is read if folder_context is disabled; document the kill switch in README"
      status: verified
      verification: explicit

---

<objective>
Complete daemon parity by adding folder-workspace lookup to daemon startup, verify repository deduplication works, and document the full derivation rule and precedence chain in README so users understand how workspace selection works.

Purpose: Daemon consistency ensures that `bauded` selects the same workspace as `baude` for the same launch directory. Documentation teaches users how to use and override derivation. README clarity reduces support burden.

Output: Daemon calls shared resolver; repository deduplication test passes; README sections document precedence, derivation rule, examples, and new-session defaults.
</objective>

<execution_context>
@~/.iarx/claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-workspace-derivation-and-new-session-open-defaults/13-RESEARCH.md
@.planning/phases/13-workspace-derivation-and-new-session-open-defaults/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Add daemon workspace resolution with explicit lock claim before recording, and test daemon startup parity</name>
  <files>
    bauded/src/main.rs
    baude-core/src/persist.rs
    baude-core/src/folder_workspace.rs
  </files>
  <read_first>
    bauded/src/main.rs (lines 1-200, daemon startup; current does not call plan_launch or record bindings)
    bauded/src/main.rs (line 182 where workspace::initialize is called with None)
    bauded/src/manager.rs (lines 32-36 STATE_BASE; lines 442-455 Manager::restore)
    baude/src/main.rs (lines 343-370, TUI startup for reference including canonicalization and plan_launch)
    baude/src/main.rs (lines 377-411, lock claim and recording flow in TUI)
    baude-core/src/persist.rs (claim_workspace_state_lock signature and StateLockError types)
    baude-core/src/folder_workspace.rs (plan_launch signature and usage; record signature)
  </read_first>
  <action>
    **bauded/src/main.rs daemon startup (around line 174-182):**
    Replace the current workspace initialization with full parity to TUI:
    1. Add explicit canonicalization: `let launch_dir = std::fs::canonicalize(std::env::current_dir()?)?;` before any plan_launch or resolve call
    2. Call `folder_workspace::plan_launch(config.folder_context_enabled(), ws_env.as_deref(), backend_env.as_deref(), Some(&memory_root), &launch_dir)` to obtain LaunchPlan with hint and repo_root (matching TUI call at main.rs:361-367)
    3. Call `workspace::initialize_with_context(&config, WorkspaceLaunchContext { hint: plan.hint.clone(), repo_root: plan.repo_root.clone() })` to cache workspace to ACTIVE (same as TUI, not a pure resolver; daemon must seed the cache)
    4. **Claim the daemon-state workspace lock BEFORE recording:** `persist::claim_workspace_state_lock("daemon-state", &ws)` where ws is the initialized workspace. Result handling:
       - `Ok(())`: lock acquired, proceed to step 5
       - `Err(StateLockError::Held { ... })`: log "daemon-state lock held by another baude" and continue startup WITHOUT recording (skip step 5); daemon serves but does not persist derived bindings when another daemon holds the lock
       - `Err(StateLockError::Io { ... })`: log the I/O error and continue startup without recording, so transient lock-file errors never prevent daemon from serving
    5. **Record derived binding ONLY when lock is acquired and repo_root is Some:** `if let Some(repo_root) = &plan.repo_root { folder_workspace::record(Some(&memory_root), repo_root, &ws.name, baude_core::pty::now_ms()); }`. Use memory_root from persist::config_dir() (computed before plan_launch). Never fall back to launch_dir for unbound non-git folders.
    6. Call `Manager::restore(...)` with the initialized workspace. Lock is held during restore; both reader (restore) and writer (recording) depend on having exclusive lock.

    **Lock semantics:** Daemon claims workspace lock before recording and holds it for the life of the process, matching TUI behavior. If lock claim fails, binding is NOT recorded, and the workspace remains unbound in memory for this daemon session.

    **baude-core/src/persist.rs:**
    Verify claim_workspace_state_lock(base: &str, ws: &Workspace) -> Result<(), StateLockError> is public and StateLockError variants (Held, Io) are documented. No other changes needed.

    **baude-core/src/folder_workspace.rs tests:**
    Add integration test `test_daemon_startup_parity_with_tui` that:
    - Creates isolated fixtures (TestRedirect home, git repo, recorded binding at repo_root)
    - Mock daemon startup path: canonicalize(launch_dir) → plan_launch(config, ws_env, backend_env, config_root, launch_dir) → initialize_with_context(config, context) → verify workspace name, source, repo_root match expected
    - Mock TUI startup path with same inputs: identical sequence
    - Assert both paths return same Workspace (name, source, repo_root) for the same inputs and state
    - This proves daemon and TUI logic are identical; actual daemon binary integration tested in runtime phase

    **baude/src/app.rs repository deduplication (from 13-02):**
    Test `test_admit_repository_same_from_root_and_subfolder` should exist from 13-02. Verify it passes: one app instance admits repo from root then subfolder, creates one repository row (deduplication by canonical common-dir).

    **WSPC-05 daemon provenance (per CONTEXT locked decision):**
    Add daemon-backed workspace source display:
    1. **bauded/src/api.rs:** Update `/info` handler to include `workspace_source: String` field in the response, set to `workspace::active().display_hint().trim_matches('(').trim_matches(')')` (the source label without parentheses)
    2. **baude/src/remote.rs:** Parse the new workspace_source field from /info response and store it in the remote context
    3. **baude/src/ui.rs remote header (line ~656):** Display daemon-backed workspace with source label in title, matching local rendering: format `"remote: {workspace_name} ({source})"` (use source from /info response)
    4. Tests: Verify daemon /info includes workspace_source field matching the source enum (explicit, bound, derived, blank)

    **Test isolation:** All tests use TestRedirect; no real ~/.config/baude or git repos.
  </action>
  <verify>
    <automated>cargo build --all 2>&1 && cargo test -p baude-core folder_workspace::tests -- --lib --nocapture 2>&1 | tee /tmp/daemon_tests.log && [ "$(grep -c '^test.*test_daemon_startup_parity_with_tui' /tmp/daemon_tests.log)" -gt 0 ] && grep -q "test result: ok" /tmp/daemon_tests.log && grep -c "persist::claim_workspace_state_lock" bauded/src/main.rs | grep -q "[1-9]"</automated>
    <fails_when>non-zero exit code; "unresolved" in output; test count is zero; "running 0 tests" appears; "test result: FAILED"; or claim_workspace_state_lock not found in bauded/src/main.rs</fails_when>
  </verify>
  <acceptance_criteria>
    - bauded/src/main.rs canonicalizes current_dir() before calling folder_workspace::plan_launch with canonical launch_dir and config.folder_context_enabled()
    - bauded/src/main.rs calls folder_workspace::plan_launch(config.folder_context_enabled(), ws_env.as_deref(), backend_env.as_deref(), Some(&memory_root), &launch_dir) where memory_root = persist::config_dir()
    - bauded/src/main.rs clones plan.hint and plan.repo_root, constructs WorkspaceLaunchContext, calls workspace::initialize_with_context(&config, ctx) to seed ACTIVE cache (NOT pure resolver)
    - bauded/src/main.rs calls persist::claim_workspace_state_lock("daemon-state", &ws) BEFORE recording, checks Result enum
    - bauded/src/main.rs handles StateLockError::Held by logging "daemon-state lock held by another baude" and skipping recording (continues startup without binding write)
    - bauded/src/main.rs handles StateLockError::Io by logging the error and continuing startup without recording; transient I/O problems do not prevent daemon from serving
    - bauded/src/main.rs ONLY records when plan.repo_root.is_some(); never falls back to launch_dir for unbound non-git folders
    - bauded/src/main.rs recording call uses correct signature: folder_workspace::record(Some(&memory_root), repo_root, &ws.name, baude_core::pty::now_ms()) with timestamp from pty module
    - Daemon lock claim semantics documented: lock held for life of process; lock refusal (Held or Io) skips recording; state remains consistent (no half-written bindings)
    - baude-core/src/folder_workspace.rs includes integration test test_daemon_startup_parity_with_tui verifying daemon startup logic (canonicalize → plan_launch → initialize_with_context) returns same Workspace, source, repo_root as TUI startup for same inputs
    - baude/src/app.rs includes test_admit_repository_same_from_root_and_subfolder verifying deduplication within one app instance (inherited from 13-02)
    - Daemon startup logic mirrors TUI startup (baude/src/main.rs:343-370) for folder_workspace, recording, and lock semantics (lock held before recording, held through Manager::restore)
    - bauded/src/api.rs /info handler includes `workspace_source: String` field in response (e.g., "explicit", "folder binding", "derived", "blank")
    - baude/src/remote.rs parses workspace_source from /info response and stores in RemoteContext
    - baude/src/ui.rs remote header (line ~656) displays daemon-backed workspace with source label: "remote: {name} ({source})" matching local title format
    - Test verifies /info endpoint includes workspace_source field matching Workspace.display_hint() output
    - cargo build --all exits 0 (no compilation errors; API changes backward-compatible from 13-01; initialize still exists for bauded)
    - Tests use TestRedirect; no real home or config dir touched
    - README documents that daemon uses same workspace derivation rule as TUI and records derived bindings with same lock semantics
    - persist::claim_workspace_state_lock signature verified: pub fn claim_workspace_state_lock(base: &str, ws: &Workspace) -> Result<(), StateLockError>
  </acceptance_criteria>
  <done>
    Daemon calls shared resolver with explicit lock claim, recording only for derived bindings at repo_root, lock refusal prevents partial writes, startup parity test verifies daemon and TUI logic are identical.
  </done>
</task>

<task type="auto">
  <name>Update README with workspace derivation, precedence, new-session defaults, title display, and daemon parity documentation</name>
  <files>
    README.md
  </files>
  <read_first>
    README.md (existing Workspaces section, lines 450-480 approx)
    README.md (existing Folder context section, lines 220-300 approx)
    README.md (existing New-session defaults section, if any)
    README.md (clone_base_dir section, verify unchanged)
    13-CONTEXT.md (Locked decisions section for exact precedence order and terminology)
  </read_first>
  <action>
    **README.md — Workspaces section (rewrite existing section):**
    Document the full precedence chain and derivation rule:
    1. Start with: "Workspace selection follows a precedence order, from highest to lowest priority:"
    2. List all seven levels in correct order:
       - BAUDE_WORKSPACE environment variable (explicit, overrides all)
       - Folder binding from ancestor walk (nearest recorded binding wins)
       - config `workspace` key in ~/.config/baude/config.json (explicit config)
       - Derived workspace name from repository root (only when inside a git repository)
       - BAUDE_BACKEND environment variable
       - config `backend` key in ~/.config/baude/config.json
       - Default workspace name "claude"
    3. Add "When does derivation apply?" subsection: derivation applies ONLY when launched inside a git repository (determined by git::repo_root) and no higher-precedence source (BAUDE_WORKSPACE, folder binding, or config workspace) is found. Unbound non-git folders skip derivation and use the remaining chain (BAUDE_BACKEND, config backend, default).
    4. Add "Examples" subsection with at least three cases:
       - "Launch from ~/Code/github.com/iarx-com/baude/src (repo subfolder) with no explicit settings → derives workspace 'baude' from repo root folder name"
       - "Launch from ~/Code/github.com/poindexter12/baude with binding recorded as poindexter12 → uses bound workspace 'poindexter12'"
       - "Launch from ~/Documents (not a git repo) with no binding or config → uses default 'claude'"
    5. Document `folder_context` config option: when false, disables both reading and recording folder memory; existing bindings remain in file but not consulted.

    **README.md — Folder context section (update existing section):**
    Update to explain the ancestor walk clearly and fix rebinding guidance:
    - "Folder memory records which workspace to use for a given directory, allowing baude to remember your choice across launches."
    - "Walk behavior: When you launch baude, it walks up from your launch directory checking each ancestor folder for a recorded binding. The first binding found is used; if no binding exists at any level, the walk returns None and precedence continues to the next tier."
    - "Boundary: The walk stops at your home directory ($HOME) and does not continue above it. If your launch directory is outside your home (e.g., in /tmp), the walk stops at the filesystem root."
    - "Recording: When you launch from a new repository without a binding, baude derives the workspace name from the repository root's folder name and records the binding for that repository root, so future launches from any subfolder of that repository use the same workspace."
    - "Override or rebind: To use a different workspace for a folder path, set BAUDE_WORKSPACE environment variable (highest priority) or delete the binding from ~/.config/baude/folder-workspaces.json and re-launch to record a new derived binding. The config `workspace` key in config.json works only if no ancestor binding exists; if a parent folder has a recorded binding, the config value does not override it (ancestor bindings have priority). To override an ancestor binding, use BAUDE_WORKSPACE env var."
    - "Kill switch: Set config `folder_context: false` to disable folder memory entirely; the walk is skipped and precedence goes directly to config workspace key (if any), BAUDE_BACKEND, config backend, then default."

    **README.md — New-session defaults section (create new subsection or update existing):**
    Document the `n` (new-session) prompt prefill behavior:
    - "The `n` command opens a prompt to create a new session."
    - "Path prefill: The prompt prefills a default path based on your launch location: if you launched baude inside a git repository, the default is that repository's root path (allowing you to easily create sessions in the same repository); if outside a repository, the default is the `new_session_dir` config option (if set), otherwise your launch directory."
    - "Customization: You can always type a different path; the prefill is a convenience only."

    **README.md — TUI title section (create new subsection if needed):**
    Document workspace title display:
    - "The top title bar shows the active workspace name and how it was selected."
    - "Format: `baude v<version> — <workspace-name> (<source>)`"
    - "Source labels: (explicit) for BAUDE_WORKSPACE or config workspace, (folder binding) for ancestor walk result, (derived) for repository-root derivation, (blank) for implicit default"
    - "This makes it clear at a glance which workspace you're in and why."

    **README.md — Clone-on-demand section (verify no changes needed):**
    Locate existing clone_base_dir documentation and verify it remains unchanged. Note that new-session prefill changes do NOT affect clone destination (still uses clone_base_dir or default ~/Code/<host>/<owner>/<repo>).

    **README.md — Daemon parity note:**
    Add a note in the Workspaces section or a new "Daemon workspace resolution" subsection: "The daemon (bauded) applies the same workspace selection rules as the TUI. When launched, bauded walks the folder-workspaces.json for bindings, derives workspace names from repository roots, and records new bindings using the same logic as baude. This ensures that switching between TUI and daemon does not change your active workspace for a given directory."

    **Exact terminology:** Use "(blank)" for the implicit default placeholder (not "(default)"). Use "ancestor walk" for the folder binding search. Use "derives" or "derived" for repository-root workspace derivation.
  </action>
  <verify>
    <automated>grep -q "BAUDE_WORKSPACE" README.md && grep -q "folder binding" README.md && grep -q "ancestor walk" README.md && grep -q "derived" README.md && grep -q "BAUDE_BACKEND" README.md && grep -q "(blank)" README.md && grep -q "kill switch" README.md && awk '/precedence order.*from highest to lowest/,/default/ { print }' README.md | grep -q "BAUDE_WORKSPACE" && awk '/precedence order.*from highest to lowest/,/default/ { print }' README.md | grep -q "folder binding" && awk '/precedence order.*from highest to lowest/,/default/ { print }' README.md | grep -q "BAUDE_BACKEND" && echo "PASS" || echo "FAIL"</automated>
    <fails_when>output contains "FAIL" or grep/awk returns non-zero; precedence section missing BAUDE_WORKSPACE, folder binding, or BAUDE_BACKEND; "(blank)" not found for default source</fails_when>
  </verify>
  <acceptance_criteria>
    - README.md Workspaces section documents full precedence chain in exact correct order: BAUDE_WORKSPACE > folder binding > config workspace > derived > BAUDE_BACKEND > config backend > default
    - README Workspaces explains when derivation applies (inside git repo) and when it doesn't (non-git folder skips derivation)
    - README Workspaces includes at least 3 examples: repo subfolder derivation, bound parent folder, unbound non-git folder
    - README Folder context section explains ancestor walk mechanics: walks up from launch_dir, checks each ancestor, stops at $HOME, returns first binding or None
    - README Folder context documents kill switch: folder_context: false disables reading and recording
    - README Folder context documents override/rebind: set BAUDE_WORKSPACE env or config workspace key, or delete folder-workspaces.json entry and relaunch
    - README New-session defaults section explains `n` prefill uses repo root inside repo, config new_session_dir when outside repo
    - README TUI title section documents workspace name and source label: (explicit), (folder binding), (derived), or (blank) [NOT "(default)"]
    - README includes daemon parity note: daemon applies same workspace selection rules as TUI
    - Clone-on-demand documentation unchanged; clone_base_dir semantics unaffected
    - README uses consistent terminology: "(blank)" for default placeholder, "ancestor walk" for binding search, "derived" for repo-root workspace
    - grep command passes: finds BAUDE_WORKSPACE, folder binding, ancestor walk, derived workspace, BAUDE_BACKEND, and (blank)
    - README has no markdown syntax errors; can be rendered without warnings
  </acceptance_criteria>
  <done>
    README.md fully documented with workspace derivation rule, precedence chain, examples, new-session defaults, title display, daemon parity, and all cross-references consistent. Users understand how workspace selection works and why.
  </done>
</task>

</tasks>

## Review Dispositions (Cross-AI Review Cycle 1)

| Finding ID | Severity | Status | Action Taken | Rationale |
|-----------|----------|--------|--------------|-----------|
| 13-03-R1 | HIGH | Incorporated | Added explicit daemon recording logic: after lock claim, call folder_workspace::record(config_root, plan.repo_root, &ws) | WSPC-04 requires daemon to record derived bindings via same code path; recording now happens post-lock |
| 13-03-R2 | HIGH | Incorporated | Changed parity test to verify plan_launch determinism: same launch_dir and state returns same hint and repo_root (not cross-binary subprocess test) | Determinism test appropriate for unit level; daemon invocation tested implicitly via startup parity |
| 13-03-R3 | HIGH | Incorporated | Added baude/src/app.rs to files_modified; deduplication test now explicitly in 13-02 Task 3 using one app instance | Ownership metadata accurate; test verifies expected behavior within single app |
| 13-03-R4 | HIGH | Incorporated | Clarified that deduplication test uses ONE app instance admitting from root then subfolder; no cross-instance state sharing | Single app instance shares repository_state; test correctly verifies deduplication mechanism |
| 13-03-R5 | HIGH | Incorporated | Added explicit current_dir() canonicalization in bauded startup before calling plan_launch | Daemon launch_dir now canonical, matches TUI canonicalization flow |
| 13-03-R6 | MEDIUM | Incorporated | Clarified in must-haves: binding write occurs after lock claim; lock refusal prevents write | Write ordering and lock semantics documented explicitly |
| 13-03-R7 | MEDIUM | Incorporated | README written after precedence tests pass in 13-02; documents tested contract not speculative behavior | Documentation follows tested implementation |
| 13-03-R8 | LOW | Incorporated | Replaced generic grep with targeted precedence order search: grep "BAUDE_WORKSPACE.*folder.*binding.*config" | Meaningful verification of documentation content |

## Review Dispositions (Cross-AI Review Cycle 2)

| Finding ID | Severity | Status | Action Taken | Rationale |
|-----------|----------|--------|--------------|-----------|
| C2-HIGH-5 | HIGH | Incorporated | Task 1 adds explicit daemon lock claim before recording: `let _lock_guard = persist::claim_workspace_state_lock("daemon-state", &ws.name)?;` handles StateLockError::Held (skip recording) and ::Io (error handling policy specified) | Daemon startup no longer assumes nonexistent lock; explicit claim prevents half-written bindings |
| C2-HIGH-6 | HIGH | Incorporated | Task 1 parity test replaced with test_daemon_startup_parity_with_tui: exercises canonicalize → plan_launch → resolve_with_context for both daemon and TUI startup paths, asserts same result | Genuine startup logic parity test, not determinism of single function |
| C2-HIGH-7 | HIGH | Incorporated | Task 1 recording condition changed to `if let Some(repo_root) = &plan.repo_root { ... }` with NO launch_dir fallback; unbound non-git folders never recorded | Matches locked decision; non-git, unbound launches derive and record nothing |
| C2-HIGH-8 | HIGH | Incorporated | Task 1 recording call uses correct signature: folder_workspace::record(&config.root, repo_root, &ws.name, now_ms) with computed timestamp | API usage matches actual folder_workspace::record signature |
| C2-MED-19 | MEDIUM | Incorporated | Task 1 lock ownership and error policy fully specified: claim_workspace_state_lock("daemon-state", ...) before recording; Held→log and skip recording; Io→[error handling policy TBD in acceptance]; lock guard held through Manager::restore | Daemon lock semantics explicit; refusal prevents binding write |
| C2-MED-20 | MEDIUM | Deferred | Daemon provenance not exposed through /info API; deferral recorded in ledger (either add workspace_source to /info or keep local-only) | Current /info returns workspace name and backend only; extending API scope is out of phase 13 |
| C2-MED-21 | MEDIUM | Incorporated | Task 2 README terminology updated: use "(blank)" everywhere, not "(default)"; consistent across Workspaces, title section, and examples | All references to implicit default now use "(blank)" placeholder |
| C2-MED-22 | MEDIUM | Incorporated | Task 2 verify command uses multiple targeted greps instead of single fragile regex: grep -q "BAUDE_WORKSPACE" && grep -q "folder binding" && ... && echo "PASS" | Robust verification of all key sections present |

## Review Dispositions (Checker Revision Iteration 1)

| Finding ID | Severity | Status | Action Taken | Rationale |
|-----------|----------|--------|--------------|-----------|
| CHECKER-02 | INFO | Incorporated | Clarified daemon lock Io error handling: StateLockError::Io → log the error and continue startup without recording (mirroring Held), so transient lock-file I/O problems never prevent daemon from serving; policy stated consistently in both action and acceptance_criteria | Circular dependency resolved: Io policy now explicit in both action (lines 115) and acceptance_criteria (line 148), ensuring executable specification |

## Review Dispositions (Cross-AI Review Cycle 3)

| Finding ID | Severity | Status | Action Taken | Rationale |
|-----------|----------|--------|--------------|-----------|
| C3-HIGH-7 | HIGH | Incorporated | Daemon startup now calls initialize_with_context() not pure resolver; caches workspace to ACTIVE before Manager::restore | Daemon cache initialization now correct; active() will not panic; matches TUI behavior |
| C3-HIGH-8 | HIGH | Incorporated | Fixed lock API call: claim_workspace_state_lock("daemon-state", &ws) takes base string and Workspace struct; signature verified from persist.rs | Live API signature now used correctly |
| C3-HIGH-9 | HIGH | Incorporated | Lock refusal policy explicit: Held → log "held by another baude" and skip recording; Io → log error and skip recording; both continue daemon startup without binding write | Policy prevents half-written state; consistent with persistence mechanism |
| C3-HIGH-10 | HIGH | Incorporated | Fixed recording call: folder_workspace::record(Some(&memory_root), repo_root, &ws.name, now_ms) where memory_root from persist::config_dir(); no nonexistent config.root | API signature matches reality |
| C3-HIGH-12 | HIGH | Incorporated | WSPC-05 daemon provenance now in scope: bauded /info includes workspace_source field; baude remote.rs parses it; ui.rs displays "remote: {name} ({source})" matching local title | Locked decision implemented: daemon-backed views show daemon's workspace source same as TUI |
| C3-MED-11 | MEDIUM | Incorporated | Parity test updated: test_daemon_startup_parity_with_tui exercises real startup paths (canonicalize → plan_launch → initialize_with_context) for both daemon and TUI; asserts same Workspace/source/repo_root | Genuine startup logic parity test, not determinism |
| C3-MED-19 | MEDIUM | Incorporated | Lock ownership and error policy fully specified: lock claimed before recording; Held skips recording; Io skips recording; lock held through Manager::restore | Explicit and executable |
| C3-MED-20 | MEDIUM | Incorporated | README rebinding guidance rewritten: config workspace does not override ancestor binding; use BAUDE_WORKSPACE env to override bindings; ancestor bindings have priority over config | Accurate guidance: users now understand precedence and override mechanism |
| C3-MED-22 | MEDIUM | Incorporated | README verify command uses awk to extract and validate precedence section contains all key terms in correct region; fails if precedence order is incomplete or missing BAUDE_BACKEND after binding | Structural verification ensures documentation accuracy |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Daemon startup → Workspace resolution | Daemon uses same resolver as TUI; launch_dir canonicalized before calling plan_launch |
| Documentation → User expectations | README explains behavior accurately; users understand when derivation applies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-08 | Information | README documentation gap | low | mitigate | Document full precedence chain and examples so users understand workspace selection |
| T-13-09 | Denial | Deduplication failure | low | mitigate | Integration test verifies same repo admitted from root and subfolder creates one row (no duplicate) |

</threat_model>

<verification>
1. Verify daemon startup code:
   - bauded/src/main.rs canonicalizes current_dir before calling folder_workspace::plan_launch
   - bauded/src/main.rs passes plan.hint to workspace::initialize()
   - bauded/src/main.rs calls folder_workspace::record(config_root, plan.repo_root, &ws) after lock claim
2. Run daemon parity test: `cargo test -p baude-core folder_workspace::tests::plan_launch_parity --lib`
3. Run repository deduplication test: `cargo test -p baude app::tests::admit_repository_same_from_root_and_subfolder --lib`
4. Run full test suite: `cargo test --all --lib` (includes new tests and all existing tests)
5. Read README.md Workspaces section and verify:
   - Full precedence order stated: BAUDE_WORKSPACE > binding > config > derived > BAUDE_BACKEND > backend > default
   - At least 3 examples provided (repo subfolder, bound parent, unbound non-git folder)
6. Read README.md Folder context section and verify: ancestor walk stops at home, kill switch documented
7. Read README.md for New-session defaults and TUI title sections explaining prefill and title rendering
8. Grep README for precedence order substring: `grep "BAUDE_WORKSPACE.*folder.*binding.*config" README.md` should find match
9. Verify clone_base_dir documentation unchanged
10. Run markdown syntax check if available (no broken links, valid formatting)
</verification>

<success_criteria>
- bauded/src/main.rs canonicalizes launch_dir and calls folder_workspace::plan_launch with config.folder_context_enabled()
- bauded/src/main.rs passes plan.hint to workspace::initialize()
- bauded/src/main.rs calls folder_workspace::record(config_root, plan.repo_root, &ws) after lock claim to record derived bindings
- plan_launch parity test passes: same launch_dir and state returns same hint and repo_root
- Repository deduplication test passes: same repo admitted from root and subfolder within one app creates one row
- README.md Workspaces section documents full precedence chain in correct order with at least 3 examples
- README.md Folder context section explains ancestor walk stops at home; documents kill switch and rebinding
- README.md New-session section explains `n` prefill uses repo root inside repo, config/launch_dir outside
- README.md TUI title section documents workspace name and source label display
- README.md mentions daemon uses same derivation rule and records bindings at repo root
- Clone-on-demand documentation unchanged; no regression
- Full test suite passes; no existing test regressions from precedence reorder
</success_criteria>

<output>
Create `.planning/phases/13-workspace-derivation-and-new-session-open-defaults/13-03-SUMMARY.md` when done
</output>
