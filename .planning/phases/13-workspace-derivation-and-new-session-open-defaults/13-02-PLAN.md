---
phase: 13
plan: 02
type: tdd
wave: 2
depends_on:
  - 13-01
files_modified:
  - baude-core/src/folder_workspace.rs
  - baude-core/src/workspace.rs
  - baude/src/app.rs
  - baude/src/ui.rs
autonomous: true
requirements:
  - WSPC-01
  - WSPC-02
  - WSPC-03
  - WSPC-05
  - OPEN-01
  - OPEN-02
  - OPEN-03

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Ancestor walk: binding found on immediate parent is returned before checking grandparent"
    - "Ancestor walk: binding on grandparent is found when parent has no binding"
    - "Ancestor walk: walk terminates at $HOME without walking above it"
    - "Ancestor walk: when no binding exists at any level, returns None"
    - "Sanitization: sanitize() applies [A-Za-z0-9_-] rule; non-matching chars become hyphens; empty result becomes empty string (not persisted)"
    - "Sanitization: unicode characters (non-ASCII) are converted to hyphens"
    - "Sanitization: workspace names with spaces, dots, slashes become hyphenated safe names"
    - "Precedence full matrix: BAUDE_WORKSPACE > bound > config workspace > derived > BAUDE_BACKEND > config backend > default"
    - "Precedence: explicit BAUDE_WORKSPACE env var takes priority over all other sources (source=Explicit)"
    - "Precedence: folder binding from ancestor walk (source=Bound) overrides config workspace and derived"
    - "Precedence: config workspace key (explicit config) is treated as Explicit and overrides derived"
    - "Precedence: derived repo-root name (source=Derived) comes after config but before backend env"
    - "Precedence: BAUDE_BACKEND env var now sits AFTER derived binding (precedence reordered per D-02)"
    - "WSPC-02 end-to-end: no binding exists → repo root basename sanitized → workspace resolved as Derived → binding persisted at repo_root → subsequent launch recalls binding"
    - "WSPC-05 title rendering: when source=Explicit shows '(explicit)', source=Bound shows '(folder binding)', source=Derived shows '(derived)', source=Default shows '(blank)' not the workspace name"
    - "New-session prefill inside repo: git::repo_root returns Some, prefill shows repo root path with trailing /"
    - "New-session prefill outside repo: git::repo_root returns None, prefill falls back to config new_session_dir or launch_dir with trailing /"
    - "Repository admission from subfolder and root: same repository identified by canonical common dir via git::discover_repository, no duplicate rows created"
  artifacts:
    - "baude-core/src/folder_workspace.rs tests section contains tests for find_binding with parent, grandparent, home-boundary, no-binding, and end-to-end derivation+persistence+recall cases"
    - "baude-core/src/workspace.rs tests section contains precedence matrix test covering all seven precedence levels (BAUDE_WORKSPACE, bound, config ws, derived, BAUDE_BACKEND, config backend, default)"
    - "baude-core/src/workspace.rs tests section contains display_hint() tests for all four WorkspaceSource variants; display_hint returns correct labels"
    - "baude/src/ui.rs tests section contains new UI rendering tests for title construction with all four source labels; verifies '(blank)' appears for Default"
    - "baude/src/app.rs tests section contains open_new_session_modal prefill tests for inside-repo and outside-repo cases"
    - "baude/src/app.rs tests section contains repository deduplication test: same repo admitted from root and subfolder creates one row"
  key_links:
    - "find_binding() tests exercise ancestor walk at function level; end-to-end derivation test exercises full WSPC-02 flow"
    - "Precedence matrix test verifies correct ordering (BAUDE_WORKSPACE before BAUDE_BACKEND per D-02); replaces old tests that encoded suppression behavior"
    - "display_hint() and UI rendering tests verify all four source labels work; '(blank)' shows for default (WSPC-05 requirement)"
    - "App tests verify prefill logic and deduplication; git::discover_repository uses canonical common dir to prevent duplicates"
  prohibitions:
    - statement: "Tests never write to real ~/.config/baude; all use TestRedirect fixture"
      status: verified
      verification: explicit
    - statement: "find_binding() never reads outside the folder-workspaces.json schema; no external files consulted"
      status: verified
      verification: explicit
    - statement: "New-session prefill logic never modifies app state or side effects; pure path selection logic"
      status: verified
      verification: explicit

---

<objective>
Add comprehensive unit and integration tests to verify that workspace derivation, ancestor walk, display hints, new-session prefill, and repository deduplication all work correctly under the requirements.

Purpose: TDD catches regressions early and documents expected behavior. Tests validate edge cases: bindings at multiple levels, paths at filesystem boundaries, outside-repository launches, and parallel admits.

Output: Red→Green test suite covering WSPC-01/02/03, OPEN-01/02/03; test failures clearly indicate which requirement failed.
</objective>

<execution_context>
@~/.iarx/claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-workspace-derivation-and-new-session-open-defaults/13-RESEARCH.md
@.planning/phases/13-workspace-derivation-and-new-session-open-defaults/13-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Feature: Ancestor walk finds bindings at multiple levels, stops at home; end-to-end derivation persists and recalls</name>
  <files>
    baude-core/src/folder_workspace.rs
  </files>
  <read_first>
    baude-core/src/folder_workspace.rs (lines 148-214, existing test patterns)
    baude-core/src/folder_workspace.rs (new find_binding function and LaunchPlan.repo_root from 13-01)
    baude-core/src/testing.rs (TestRedirect fixture)
    baude-core/src/workspace.rs (sanitize function, lines 99-111)
  </read_first>
  <behavior>
    - Test: find_binding at parent level returns parent's workspace name
    - Test: find_binding at grandparent level when parent has no binding returns grandparent's workspace
    - Test: find_binding nearest wins when both parent and grandparent have bindings
    - Test: find_binding stops at home boundary, does not walk above
    - Test: find_binding returns None when no binding exists at any level
    - Test (WSPC-02 end-to-end): no binding exists → derive workspace from repo root name → persist binding at repo_root → subsequent call from different subfolder recalls binding
    - Edge case: launch_dir == home returns None
    - Edge case: walk outside home boundary (launch_dir in /tmp, home in /home) stops at filesystem root
  </behavior>
  <implementation>
    RED: Write test stubs in baude-core/src/folder_workspace.rs::tests module:
    - test_find_binding_at_parent
    - test_find_binding_nearest_wins
    - test_find_binding_stops_at_home
    - test_find_binding_returns_none_when_no_binding
    - test_find_binding_at_home_returns_none
    - test_wspc02_end_to_end_derive_persist_recall: no binding exists for a repo root → plan_launch populates repo_root → derive and persist binding → second plan_launch from different subfolder reads binding and returns it

    Fixtures use TestRedirect and scratch() helper to create isolated temp repos and home dirs.

    GREEN: Implement each test with actual find_binding calls and FolderWorkspaceFile setup; verify end-to-end flow persists and retrieves bindings correctly.

    REFACTOR: Extract fixture setup helper.
  </implementation>
  <verify>
    <automated>cargo test -p baude-core folder_workspace::tests::find_binding --lib -- --nocapture</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in output</fails_when>
  </verify>
  <done>
    find_binding() tests pass for all ancestor levels; end-to-end WSPC-02 test verifies derivation, persistence, and recall work together.
  </done>
</task>

<task type="tdd">
  <name>Feature: Complete precedence matrix, display_hint source labels, UI rendering with all four sources</name>
  <files>
    baude-core/src/workspace.rs
    baude/src/ui.rs
  </files>
  <read_first>
    baude-core/src/workspace.rs (lines 656-810, existing resolution tests; lines 116-167 precedence chain)
    baude-core/src/workspace.rs (WorkspaceSource enum and resolve function from 13-01)
    baude/src/ui.rs (lines 2482-2500, TestBackend render helper)
    baude/src/ui.rs (lines 220-230, title construction)
  </read_first>
  <behavior>
    - Test: Precedence matrix covering all seven levels: (1) BAUDE_WORKSPACE env → Explicit, (2) bound from ancestor walk → Bound, (3) config workspace → Explicit, (4) derived repo root → Derived, (5) BAUDE_BACKEND env → Explicit, (6) config backend → (backend binding), (7) default → Default
    - Test: Each precedence level sets correct WorkspaceSource
    - Test: display_hint returns "(explicit)" for Explicit, "(folder binding)" for Bound, "(derived)" for Derived, "(blank)" for Default (not the workspace name)
    - Test: UI title rendering includes workspace name and display_hint() label; all four labels appear correctly in the rendered title
    - Test: When source=Default, title shows "(blank)" not the workspace name (WSPC-05 requirement)
    - Edge case: When both BAUDE_WORKSPACE and BAUDE_BACKEND are set, BAUDE_WORKSPACE wins (Explicit overrides all)
  </behavior>
  <implementation>
    RED: Write test stubs in baude-core/src/workspace.rs::tests module:
    - test_precedence_matrix: table-driven test covering all seven precedence cases with different combinations of env vars, config, binding, repo
    - test_display_hint_explicit, test_display_hint_bound, test_display_hint_derived, test_display_hint_default
    - test_display_hint_default_returns_blank_not_name: verify Default returns "(blank)" not the workspace name

    Write test stubs in baude/src/ui.rs::tests module:
    - test_title_rendering_with_explicit_source: mock workspace::active() to return Explicit source, verify title includes "(explicit)"
    - test_title_rendering_with_bound_source: Bound → "(folder binding)"
    - test_title_rendering_with_derived_source: Derived → "(derived)"
    - test_title_rendering_with_default_source_shows_blank: Default → "(blank)"

    Use TestBackend render helper (line 2482) for UI tests; set up fixture workspaces with each source variant.

    GREEN: Implement precedence matrix test logic; implement display_hint() method; update title formatting in ui.rs to include display_hint(); update existing precedence tests to reflect reordered BAUDE_BACKEND position.

    REFACTOR: Consolidate display_hint string constants if needed; clean up precedence test setup.
  </implementation>
  <verify>
    <automated>cargo test -p baude-core workspace::tests::precedence --lib -- --nocapture</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in output</fails_when>
  </verify>
  <done>
    Precedence matrix test passes all seven levels; display_hint returns correct labels; UI title rendering test passes for all four sources, including "(blank)" for Default.
  </done>
</task>

<task type="tdd">
  <name>Feature: New-session prefill and repository deduplication (root vs subfolder)</name>
  <files>
    baude/src/app.rs
  </files>
  <read_first>
    baude/src/app.rs (lines 8348-8482, existing app admission tests)
    baude/src/app.rs (open_new_session_modal and admit_repository from 13-01)
    baude-core/src/git.rs (git::repo_root and discover_repository functions)
  </read_first>
  <behavior>
    - Test (OPEN-01/02): open_new_session_modal inside repo prefills git root; outside repo prefills config new_session_dir or launch_dir
    - Test (OPEN-01/02): all prefills end with / for consistency
    - Test (OPEN-03): launching baude from repo root and repo subfolder admits same repository once, focuses existing row (no duplicate)
    - Test (OPEN-03): git::discover_repository uses canonical common dir for identity; deduplication prevents duplicate rows
    - Edge case: repo root and subfolder have same canonical common dir even if paths differ
  </behavior>
  <implementation>
    RED: Write test stubs in baude/src/app.rs::tests module:
    - test_open_new_session_prefill_inside_repo: launch_dir is repo subfolder, prefill shows repo root path
    - test_open_new_session_prefill_outside_repo_with_config: launch_dir is non-repo, config.new_session_dir set, prefill shows config dir
    - test_open_new_session_prefill_outside_repo_no_config: launch_dir is non-repo, no config, prefill shows launch_dir
    - test_open_new_session_prefill_ends_with_slash: all cases end with /
    - test_admit_repository_same_from_root_and_subfolder: one app instance, admit repo from root, admit same repo from subfolder, expect same repository row (one entry), second admit focuses existing row

    Fixtures use TestRedirect to set up isolated git repos and app config.

    GREEN: Implement prefill logic in open_new_session_modal (try git::repo_root, fallback to config/launch_dir); implement deduplication test to verify one app shares repository_state and deduplicates by common dir.

    REFACTOR: Extract prefill path formatting if needed.
  </implementation>
  <verify>
    <automated>cargo test -p baude app::tests::open_new_session --lib -- --nocapture</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in output</fails_when>
  </verify>
  <done>
    open_new_session_modal prefill tests pass; repository deduplication test passes (same repo from root and subfolder creates one row).
  </done>
</task>

</tasks>

## Review Dispositions (Cross-AI Review Cycle 1)

| Finding ID | Severity | Status | Action Taken | Rationale |
|-----------|----------|--------|--------------|-----------|
| 13-02-R1 | HIGH | Incorporated | Added Task 2 with complete precedence matrix test covering all seven levels and source assignments | Old tests encoded suppression behavior; new matrix verifies reordered precedence (binding/derived before BAUDE_BACKEND) |
| 13-02-R2 | HIGH | Incorporated | Added Task 1 with end-to-end WSPC-02 test: no binding → derive → persist → recall from different subfolder | Full derivation lifecycle verified, not just find_binding in isolation |
| 13-02-R3 | HIGH | Incorporated | Added Task 3 with repository deduplication test: same repo admitted from root and subfolder within one app creates one row | Verifies git::discover_repository canonicalization prevents duplicates |
| 13-02-R4 | HIGH | Incorporated | Added ui.rs to files_modified; Task 2 includes UI rendering test for title with all four source labels | WSPC-05 rendering verified; "(blank)" shows for Default source |
| 13-02-R5 | MEDIUM | Incorporated | Moved "daemon startup" assertion to 13-03 where daemon code is modified | Plan 02 is TDD/testing only; Plan 03 wires daemon startup |
| 13-02-R6 | MEDIUM | Incorporated | Clarified in must-haves: empty sanitization yields empty string (not persisted as binding) | Behavior defined; edge case documented |
| 13-02-R7 | MEDIUM | Incorporated | Noted in action: trailing / is intentional normalization for consistency with prefill UI | Changed behavior now explicitly called out, not silent |
| 13-02-R8 | MEDIUM | Incorporated | Split cargo test command into separate invocations (Task 1, Task 2, Task 3) with individual filters | Valid cargo test syntax |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → Real filesystem | TestRedirect intercepts all home/config dir reads; no test writes to real ~/.config/baude |
| Test app instances → Shared state | Each test app is isolated; no interference between tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-06 | Denial | Test isolation bypass | low | mitigate | All tests use TestRedirect; containment guard scripts run after test suite (existing phase 8 pattern) |
| T-13-07 | Elevation | Test app state pollution | low | mitigate | Each test creates a fresh app instance; no shared mutable state between tests |

</threat_model>

<verification>
Red→Green→Refactor cycle for each TDD task:
1. RED: Write test stubs with descriptive names, compile to ensure test harness find them
2. GREEN: Implement the feature so all tests pass
3. REFACTOR: Clean up test code and implementation if needed, re-run to ensure green

Run full test suite before task completion:
- `cargo test -p baude-core folder_workspace::tests::find_binding --lib` (Task 1)
- `cargo test -p baude-core workspace::tests::precedence --lib` (Task 2)
- `cargo test -p baude app::tests::open_new_session --lib` (Task 3)
- `cargo test -p baude app::tests::admit_repository_same_from_root --lib` (Task 3)
- Full phase gate: `cargo test --all --lib` passes with all new tests green
</verification>

<success_criteria>
- Task 1: All find_binding() tests pass: ancestor walk finds nearest binding, stops at home, returns None when no binding
- Task 1: WSPC-02 end-to-end test passes: derivation + persistence + recall works together
- Task 2: Precedence matrix test passes all seven levels; BAUDE_BACKEND now sits after derived (per D-02)
- Task 2: All display_hint() tests pass: returns "(explicit)", "(folder binding)", "(derived)", "(blank)" based on source
- Task 2: UI title rendering tests pass; "(blank)" shown for Default source (not workspace name)
- Task 3: All open_new_session prefill tests pass: git root inside repo, config/launch_dir outside, paths end with /
- Task 3: Repository deduplication test passes: same repo from root and subfolder creates one row
- No test writes to real ~/.config/baude (TestRedirect active)
- All existing tests still pass after precedence reorder (old BAUDE_BACKEND suppression tests replaced)
</success_criteria>

<output>
Create `.planning/phases/13-workspace-derivation-and-new-session-open-defaults/13-02-SUMMARY.md` when done
</output>
