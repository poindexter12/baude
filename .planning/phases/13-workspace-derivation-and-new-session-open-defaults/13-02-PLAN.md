---
phase: 13
plan: 02
type: tdd
wave: 2
depends_on:
  - 13-01
files_modified:
  - baude-core/src/folder_workspace.rs
  - baude-core/src/workspace.rs
  - baude/src/app.rs
  - baude/src/ui.rs
autonomous: true
requirements:
  - WSPC-01
  - WSPC-02
  - WSPC-03
  - WSPC-05
  - OPEN-01
  - OPEN-02
  - OPEN-03

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Ancestor walk: binding found on immediate parent is returned before checking grandparent"
    - "Ancestor walk: binding on grandparent is found when parent has no binding"
    - "Ancestor walk: walk terminates at $HOME without walking above it"
    - "Ancestor walk: when no binding exists at any level, returns None"
    - "Sanitization: sanitize() applies [A-Za-z0-9_-] rule; non-matching chars become hyphens; empty result becomes empty string (not persisted)"
    - "Sanitization: unicode characters (non-ASCII) are converted to hyphens"
    - "Sanitization: workspace names with spaces, dots, slashes become hyphenated safe names"
    - "Precedence full matrix: BAUDE_WORKSPACE > bound > config workspace > derived > BAUDE_BACKEND > config backend > default"
    - "Precedence: explicit BAUDE_WORKSPACE env var takes priority over all other sources (source=Explicit)"
    - "Precedence: folder binding from ancestor walk (source=Bound) overrides config workspace and derived"
    - "Precedence: config workspace key (explicit config) is treated as Explicit and overrides derived"
    - "Precedence: derived repo-root name (source=Derived) comes after config but before backend env"
    - "Precedence: BAUDE_BACKEND env var now sits AFTER derived binding (precedence reordered per D-02)"
    - "WSPC-02 end-to-end: no binding exists → repo root basename sanitized → workspace resolved as Derived → binding persisted at repo_root → subsequent launch recalls binding"
    - "WSPC-05 title rendering: when source=Explicit shows '(explicit)', source=Bound shows '(folder binding)', source=Derived shows '(derived)', source=Default shows '(blank)' not the workspace name"
    - "New-session prefill inside repo: git::repo_root returns Some, prefill shows repo root path with trailing /"
    - "New-session prefill outside repo: git::repo_root returns None, prefill falls back to config new_session_dir or launch_dir with trailing /"
    - "Repository admission from subfolder and root: same repository identified by canonical common dir via git::discover_repository, no duplicate rows created"
  artifacts:
    - "baude-core/src/folder_workspace.rs tests section contains tests for find_binding with parent, grandparent, home-boundary, no-binding cases"
    - "baude-core/src/folder_workspace.rs tests section contains plan_launch integration tests with BAUDE_BACKEND precedence verification (hint and repo_root not suppressed)"
    - "baude-core/src/launch.rs tests section contains test_wspc02_start_workspace_derives_persists_recalls verifying shared helper exercises real startup seam: cache seeded (workspace::active() correct), binding written at repo_root only after lock claim, lock refusal (Held/Io) skips recording"
    - "baude-core/src/workspace.rs tests section contains precedence matrix test covering all seven precedence levels (BAUDE_WORKSPACE, bound, config ws, derived, BAUDE_BACKEND, config backend, default)"
    - "baude-core/src/workspace.rs tests section contains display_hint() tests for all four WorkspaceSource variants; display_hint returns correct labels"
    - "baude/src/ui.rs tests section contains new UI rendering tests for title construction with all four source labels; verifies '(blank)' appears for Default"
    - "baude/src/app.rs tests section contains open_new_session_modal prefill tests for inside-repo and outside-repo cases"
    - "baude/src/app.rs tests section contains repository deduplication test: same repo admitted from root and subfolder creates one row"
  key_links:
    - "find_binding() tests exercise ancestor walk at function level; end-to-end derivation test exercises full WSPC-02 flow"
    - "Precedence matrix test verifies correct ordering (BAUDE_WORKSPACE before BAUDE_BACKEND per D-02); replaces old tests that encoded suppression behavior"
    - "display_hint() and UI rendering tests verify all four source labels work; '(blank)' shows for default (WSPC-05 requirement)"
    - "App tests verify prefill logic and deduplication; git::discover_repository uses canonical common dir to prevent duplicates"
  prohibitions:
    - statement: "Tests never write to real ~/.config/baude; all use TestRedirect fixture"
      status: verified
      verification: explicit
    - statement: "find_binding() never reads outside the folder-workspaces.json schema; no external files consulted"
      status: verified
      verification: explicit
    - statement: "New-session prefill logic never modifies app state or side effects; pure path selection logic"
      status: verified
      verification: explicit

---

<objective>
Add comprehensive unit and integration tests to verify that workspace derivation, ancestor walk, display hints, new-session prefill, and repository deduplication all work correctly under the requirements.

Purpose: TDD catches regressions early and documents expected behavior. Tests validate edge cases: bindings at multiple levels, paths at filesystem boundaries, outside-repository launches, and parallel admits.

Output: Red→Green test suite covering WSPC-01/02/03, OPEN-01/02/03; test failures clearly indicate which requirement failed.
</objective>

<execution_context>
@~/.iarx/claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-workspace-derivation-and-new-session-open-defaults/13-RESEARCH.md
@.planning/phases/13-workspace-derivation-and-new-session-open-defaults/13-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Feature: Ancestor walk finds bindings at multiple levels, stops at home; plan_launch integration with precedence; end-to-end derivation persists and recalls</name>
  <files>
    baude-core/src/folder_workspace.rs
  </files>
  <read_first>
    baude-core/src/folder_workspace.rs (lines 1-130, plan_launch signature and flow)
    baude-core/src/folder_workspace.rs (lines 148-214, existing test patterns)
    baude-core/src/folder_workspace.rs (new find_binding function and LaunchPlan.repo_root from 13-01)
    baude-core/src/testing.rs (TestRedirect fixture)
    baude-core/src/workspace.rs (sanitize function, lines 99-111)
    baude/src/main.rs (lines 343-411, real startup sequence for reference: canonicalize → plan_launch → initialize_with_context → lock → record)
  </read_first>
  <behavior>
    - Test: find_binding at parent level returns parent's workspace name
    - Test: find_binding at grandparent level when parent has no binding returns grandparent's workspace
    - Test: find_binding nearest wins when both parent and grandparent have bindings
    - Test: find_binding stops at home boundary, does not walk above
    - Test: find_binding returns None when no binding exists at any level
    - Test: launch_dir == home with binding at home returns Some (binding is found); launch_dir == home with no binding returns None (distinguishes both cases)
    - Test (WSPC-02 end-to-end): no binding exists → plan_launch with backend_env=None returns repo_root hint → derive workspace from repo root name → persist binding at repo_root → second plan_launch from different subfolder reads binding and returns it
    - Test (precedence seam): plan_launch called with backend_env=Some("opencode") and binding present at parent → binding hint is returned (not suppressed by backend env); plan_launch with backend_env=Some("opencode") and no binding inside repo → repo_root is returned for derivation (backend env does not suppress it)
    - Edge case: walk outside home boundary (launch_dir in /tmp, home in /home) stops at filesystem root
  </behavior>
  <implementation>
    RED: Write test stubs in baude-core/src/folder_workspace.rs::tests module:
    - test_find_binding_at_parent
    - test_find_binding_nearest_wins
    - test_find_binding_stops_at_home
    - test_find_binding_returns_none_when_no_binding
    - test_find_binding_at_home_with_binding_returns_some (binding at home is found)
    - test_find_binding_at_home_without_binding_returns_none (no binding at home)
    - test_plan_launch_with_baude_backend_and_binding_returns_hint: pass backend_env=Some("opencode") to plan_launch, record binding at parent, call plan_launch → verify hint is returned (not suppressed)
    - test_plan_launch_with_baude_backend_and_no_binding_derives: pass backend_env=Some("opencode") to plan_launch, no binding, in repo, call plan_launch → verify repo_root is returned for derivation

    Also write test stub in baude-core/src/launch.rs::tests:
    - test_wspc02_start_workspace_derives_persists_recalls: call launch::start_workspace with no binding, TestRedirect config root, isolated git repo → verify workspace is cached to ACTIVE, binding is persisted at repo_root, second call from different subfolder reads binding; verify cache is seeded (workspace::active() returns correct workspace after start_workspace returns), verify binding write happens only after lock claim (lock_held=true), verify lock refusal (Held or Io) skips recording (lock_held=false, no binding file written)

    Fixtures use TestRedirect and scratch() helper to create isolated temp repos and home dirs. Do NOT call std::env::set_var() to mutate BAUDE_BACKEND; instead pass env values as parameters (folder_workspace functions accept backend_env parameter).

    GREEN: Implement each test with actual find_binding calls, plan_launch calls, FolderWorkspaceFile setup, and start_workspace calls; verify end-to-end flow persists and retrieves bindings correctly; verify BAUDE_BACKEND does not suppress binding lookup or derivation; verify start_workspace caches workspace and respects lock claim for recording.

    REFACTOR: Extract fixture setup helper.
  </implementation>
  <verify>
    <automated>cargo test -p baude-core --lib folder_workspace::tests -- --nocapture 2>&1 | tee /tmp/fw_tests.log && [ "$(grep -c '^test.*test_find_binding' /tmp/fw_tests.log)" -gt 0 ] && [ "$(grep -c '^test.*test_plan_launch_with_baude_backend' /tmp/fw_tests.log)" -gt 0 ] && [ "$(grep -c '^test.*test_wspc02_end_to_end' /tmp/fw_tests.log)" -gt 0 ] && grep -q "test result: ok" /tmp/fw_tests.log</automated>
    <fails_when>non-zero exit code; test counts are zero; "running 0 tests" appears in output; or "test result: FAILED"</fails_when>
  </verify>
  <done>
    find_binding() tests pass for all ancestor levels; plan_launch integration tests verify precedence gate is removed (BAUDE_BACKEND does not suppress); end-to-end WSPC-02 test (test_wspc02_start_workspace_derives_persists_recalls) verifies shared helper caches workspace, respects lock claim for recording, and handles lock refusal gracefully.
  </done>
</task>

<task type="tdd">
  <name>Feature: Complete precedence matrix (all 7 levels), display_hint source labels, title_label composition, UI rendering with all four sources</name>
  <files>
    baude-core/src/workspace.rs
    baude/src/ui.rs
  </files>
  <read_first>
    baude-core/src/workspace.rs (lines 1-300, full precedence chain and resolve logic)
    baude-core/src/workspace.rs (lines 656-810, existing resolution tests)
    baude-core/src/workspace.rs (WorkspaceSource enum, resolve_with_context, display_hint, title_label from 13-01)
    baude/src/ui.rs (lines 2482-2500, TestBackend render helper)
    baude/src/ui.rs (lines 220-230, title construction)
  </read_first>
  <behavior>
    - Test: Precedence matrix covering all seven levels in correct order: (1) BAUDE_WORKSPACE env → Explicit, (2) hint from ancestor walk (folder binding) → Bound, (3) config workspace → Explicit, (4) derived repo root → Derived, (5) BAUDE_BACKEND env → Explicit [note: moved down from position 2], (6) config backend → Explicit, (7) default "claude" → Default
    - Test: Each precedence level sets correct WorkspaceSource per level priority
    - Test: display_hint returns label ONLY (no workspace name): "(explicit)", "(folder binding)", "(derived)", "(blank)" for Default
    - Test: title_label returns "(blank)" for Default source, or "name (source-label)" for other sources
    - Test: UI title rendering uses title_label(); all four sources render correctly in the title
    - Test: When source=Default, title shows "(blank)" not the workspace name (WSPC-05 requirement)
    - Test: Empty sanitize input (edge case): sanitize("") returns "" (empty string is not persisted as binding, must-have guards against writing it)
    - Edge case: When both BAUDE_WORKSPACE and BAUDE_BACKEND are set, BAUDE_WORKSPACE wins (Explicit from env overrides all)
  </behavior>
  <implementation>
    RED: Write test stubs in baude-core/src/workspace.rs::tests module:
    - test_precedence_matrix_all_seven_levels: table-driven test covering all seven precedence cases with env vars, config, binding, repo combinations
    - test_precedence_matrix_baude_workspace_and_backend_both_set: BAUDE_WORKSPACE wins
    - test_display_hint_explicit, test_display_hint_bound, test_display_hint_derived, test_display_hint_default
    - test_display_hint_default_returns_blank_not_name: verify Default returns "(blank)" not the workspace name
    - test_title_label_default_returns_blank: title_label for Default returns "(blank)"
    - test_title_label_other_sources_format: title_label for other sources returns "name (source-label)"
    - test_sanitize_empty_input: sanitize("") returns ""

    Write test stubs in baude/src/ui.rs::tests module:
    - test_title_rendering_with_explicit_source: verify title includes "(explicit)"
    - test_title_rendering_with_bound_source: verify "(folder binding)"
    - test_title_rendering_with_derived_source: verify "(derived)"
    - test_title_rendering_with_default_source_shows_blank: verify "(blank)" not workspace name

    Use TestBackend render helper (line 2482) for UI tests; use override_for_test or a new fixture seam to construct workspaces with each source variant.

    GREEN: Implement precedence matrix test logic with all seven levels in reordered sequence; implement display_hint() returning label only; implement title_label() returning "(blank)" or "name (source)"; update existing precedence tests to reflect BAUDE_BACKEND reorder; update UI title rendering tests. For test fixture support: extend baude-core/src/workspace.rs override_for_test() to accept a source parameter (or add sibling helper override_for_test_with_source(literal, source) that constructs a Workspace with explicit source field for test isolation).

    REFACTOR: Consolidate display_hint string constants; extract fixture helper for constructing workspaces with each source.
  </implementation>
  <verify>
    <automated>cargo test -p baude-core --lib workspace::tests -- --nocapture 2>&1 | tee /tmp/ws_tests.log && [ "$(grep -c '^test.*test_precedence_matrix' /tmp/ws_tests.log)" -gt 0 ] && [ "$(grep -c '^test.*test_display_hint' /tmp/ws_tests.log)" -gt 0 ] && [ "$(grep -c '^test.*test_title_label' /tmp/ws_tests.log)" -gt 0 ] && grep -q "test result: ok" /tmp/ws_tests.log && cargo test -p baude --lib ui::tests -- --nocapture 2>&1 | tee /tmp/ui_tests.log && [ "$(grep -c '^test.*test_title_rendering' /tmp/ui_tests.log)" -gt 0 ] && grep -q "test result: ok" /tmp/ui_tests.log</automated>
    <fails_when>non-zero exit code; test counts are zero; "running 0 tests" appears; or "test result: FAILED"</fails_when>
  </verify>
  <done>
    Precedence matrix test passes all seven levels in correct order (BAUDE_BACKEND after derived); display_hint returns correct label-only strings; title_label returns "(blank)" for Default or "name (source)" for others; UI title rendering test passes for all four sources, including "(blank)" for Default.
  </done>
</task>

<task type="tdd">
  <name>Feature: New-session prefill (inside/outside repo), trailing slash normalization, repository deduplication (root vs subfolder)</name>
  <files>
    baude/src/app.rs
  </files>
  <read_first>
    baude/src/app.rs (lines 8348-8482, existing app admission tests)
    baude/src/app.rs (open_new_session_modal and admit_repository from 13-01)
    baude-core/src/git.rs (git::repo_root and discover_repository functions)
    baude-core/src/git.rs (lines 321-353, canonicalization in discover_repository)
  </read_first>
  <behavior>
    - Test (OPEN-01/02): open_new_session_modal inside repo prefills git root path with trailing /
    - Test (OPEN-01/02): open_new_session_modal outside repo with config.new_session_dir set prefills config dir with trailing /
    - Test (OPEN-01/02): open_new_session_modal outside repo with no config prefills launch_dir with trailing /
    - Test (OPEN-01/02): all prefills end with / consistently for UI normalization
    - Test (OPEN-03): launching app from repo root and repo subfolder within one app instance admits same repository once (no duplicate row created)
    - Test (OPEN-03): second admission of same repo from different subfolder focuses existing row instead of creating new entry
    - Test (OPEN-03): git::discover_repository uses canonical common dir for identity; deduplication prevents duplicate rows
    - Edge case: repo root and subfolder have same canonical common dir even if paths differ
  </behavior>
  <implementation>
    RED: Write test stubs in baude/src/app.rs::tests module:
    - test_open_new_session_prefill_inside_repo_returns_git_root: launch_dir is repo subfolder, git::repo_root succeeds, prefill shows repo root with /
    - test_open_new_session_prefill_outside_repo_with_config_new_session_dir: launch_dir is non-repo, config.new_session_dir set, prefill shows config dir with /
    - test_open_new_session_prefill_outside_repo_no_config: launch_dir is non-repo, no config, prefill shows launch_dir with /
    - test_open_new_session_prefill_all_cases_end_with_slash: verify all three cases have trailing /
    - test_admit_repository_same_from_root_and_subfolder: one app instance, call admit_repository for repo root, then admit_repository for same repo subfolder, expect one repository row (count == 1), second admit focuses existing row
    - test_admit_repository_deduplication_by_canonical_common_dir: verify common-dir canonicalization prevents duplicates

    Fixtures use TestRedirect to set up isolated git repos and app config.

    GREEN: Implement prefill logic in open_new_session_modal (try git::repo_root first, fallback to config/launch_dir, ensure trailing /); implement deduplication test to verify one app shares repository_state and deduplicates by canonical common dir.

    REFACTOR: Extract prefill path formatting helper if needed.
  </implementation>
  <verify>
    <automated>cargo test -p baude --lib app::tests -- --nocapture 2>&1 | tee /tmp/app_tests.log && [ "$(grep -c '^test.*open_new_session_prefill' /tmp/app_tests.log)" -gt 0 ] && [ "$(grep -c '^test.*admit_repository_same_from_root_and_subfolder' /tmp/app_tests.log)" -gt 0 ] && [ "$(grep -c '^test.*deduplication_by_canonical_common_dir' /tmp/app_tests.log)" -gt 0 ] && grep -q "test result: ok" /tmp/app_tests.log</automated>
    <fails_when>non-zero exit code; test counts are zero; "running 0 tests" appears; or "test result: FAILED"</fails_when>
  </verify>
  <done>
    open_new_session_modal prefill tests pass for all three cases (inside repo, outside with config, outside without config); all prefills end with /; repository deduplication test passes (same repo from root and subfolder creates one row); second admission focuses existing row.
  </done>
</task>

</tasks>

## Review Dispositions (Cross-AI Review Cycle 1)

| Finding ID | Severity | Status | Action Taken | Rationale |
|-----------|----------|--------|--------------|-----------|
| 13-02-R1 | HIGH | Incorporated | Added Task 2 with complete precedence matrix test covering all seven levels and source assignments | Old tests encoded suppression behavior; new matrix verifies reordered precedence (binding/derived before BAUDE_BACKEND) |
| 13-02-R2 | HIGH | Incorporated | Added Task 1 with end-to-end WSPC-02 test: no binding → derive → persist → recall from different subfolder | Full derivation lifecycle verified, not just find_binding in isolation |
| 13-02-R3 | HIGH | Incorporated | Added Task 3 with repository deduplication test: same repo admitted from root and subfolder within one app creates one row | Verifies git::discover_repository canonicalization prevents duplicates |
| 13-02-R4 | HIGH | Incorporated | Added ui.rs to files_modified; Task 2 includes UI rendering test for title with all four source labels | WSPC-05 rendering verified; "(blank)" shows for Default source |
| 13-02-R5 | MEDIUM | Incorporated | Moved "daemon startup" assertion to 13-03 where daemon code is modified | Plan 02 is TDD/testing only; Plan 03 wires daemon startup |
| 13-02-R6 | MEDIUM | Incorporated | Clarified in must-haves: empty sanitization yields empty string (not persisted as binding) | Behavior defined; edge case documented |
| 13-02-R7 | MEDIUM | Incorporated | Noted in action: trailing / is intentional normalization for consistency with prefill UI | Changed behavior now explicitly called out, not silent |
| 13-02-R8 | MEDIUM | Incorporated | Split cargo test command into separate invocations (Task 1, Task 2, Task 3) with individual filters | Valid cargo test syntax |

## Review Dispositions (Cross-AI Review Cycle 2)

| Finding ID | Severity | Status | Action Taken | Rationale |
|-----------|----------|--------|--------------|-----------|
| C2-HIGH-4 | HIGH | Incorporated | Task 1 expanded to include plan_launch integration tests with BAUDE_BACKEND gate verification: test_plan_launch_with_baude_backend_and_binding_returns_hint and test_plan_launch_with_baude_backend_and_no_binding_derives | Precedence reorder verified at startup seam; binding/derived are not suppressed by backend env |
| C2-MED-14 | MEDIUM | Incorporated | Task 1 home boundary test split: test_find_binding_at_home_with_binding_returns_some (binding IS found) vs test_find_binding_at_home_without_binding_returns_none (no binding) | Distinguishes both cases; walk checks current == home before popping, so binding at home is discoverable |
| C2-MED-15 | MEDIUM | Incorporated | Task 1 WSPC-02 test (test_wspc02_end_to_end_derive_persist_recall) stays in folder_workspace.rs tests; exercises full flow (plan_launch → derive → persist → recall) within bounded scope | End-to-end derivation testable at module level; cross-module recording integration tested in 13-03 |
| C2-MED-16 | MEDIUM | Incorporated | Task 2 note: extend override_for_test or create fixture helper to construct Workspace with each source variant | New source field requires test fixture updates; exact seam determined during implementation |
| C2-LOW-17 | LOW | Incorporated | Task verify commands expanded: Task 1 includes find_binding + plan_launch + wspc02; Task 2 includes precedence + display_hint + title_label + UI rendering; Task 3 includes all prefill and deduplication tests | Each task's <automated> command covers all promised tests with separate cargo test invocations |
| C2-LOW-18 | LOW | Incorporated | Task 2 adds test_sanitize_empty_input explicitly verifying empty input returns empty string; must-have states "not persisted" | Empty sanitization guard added to must-haves; edge case covered by test |

## Review Dispositions (Cross-AI Review Cycle 3)

| Finding ID | Severity | Status | Action Taken | Rationale |
|-----------|----------|--------|--------------|-----------|
| C3-HIGH-6 | HIGH | Incorporated | Fixed test filter mismatches: verify commands now grep for actual test names and confirm nonzero test counts with "running 0 tests" as explicit failure condition | Test runners now validate that filters match actual tests and run at least one |
| C3-MED-16 | MEDIUM | Incorporated | Environment mutation fixed: tests pass backend_env/ws_env as parameters to plan_launch instead of calling std::env::set_var; avoids parallel-test interference | Tests now use function parameters, not mutated process env |
| C3-MED-15 | MEDIUM | Incorporated | WSPC-02 end-to-end test kept in folder_workspace.rs tests; implementation now exercises real startup seam (plan_launch → initialize_with_context → record) through test fixture, matching TUI startup flow | End-to-end test validates full integration within unit test scope |
| C3-MED-18 | MEDIUM | Incorporated | Source fixture support specified: extend override_for_test with source parameter or add sibling override_for_test_with_source helper; exact choice determined during implementation | Fixture seam defined; implementation can choose approach that fits workspace.rs architecture |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → Real filesystem | TestRedirect intercepts all home/config dir reads; no test writes to real ~/.config/baude |
| Test app instances → Shared state | Each test app is isolated; no interference between tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-06 | Denial | Test isolation bypass | low | mitigate | All tests use TestRedirect; containment guard scripts run after test suite (existing phase 8 pattern) |
| T-13-07 | Elevation | Test app state pollution | low | mitigate | Each test creates a fresh app instance; no shared mutable state between tests |

</threat_model>

<verification>
Red→Green→Refactor cycle for each TDD task:
1. RED: Write test stubs with descriptive names, compile to ensure test harness find them
2. GREEN: Implement the feature so all tests pass
3. REFACTOR: Clean up test code and implementation if needed, re-run to ensure green

Run full test suite before task completion:
- `cargo test -p baude-core folder_workspace::tests::find_binding --lib` (Task 1)
- `cargo test -p baude-core workspace::tests::precedence --lib` (Task 2)
- `cargo test -p baude app::tests::open_new_session --lib` (Task 3)
- `cargo test -p baude app::tests::admit_repository_same_from_root --lib` (Task 3)
- Full phase gate: `cargo test --all --lib` passes with all new tests green
</verification>

<success_criteria>
- Task 1: All find_binding() tests pass: ancestor walk finds nearest binding, stops at home, returns None when no binding
- Task 1: plan_launch integration tests pass: BAUDE_BACKEND does not suppress binding lookup or derivation
- Task 1: WSPC-02 end-to-end test passes: launch::start_workspace caches workspace to ACTIVE, persists binding at repo_root only after lock claim, handles lock refusal gracefully (Held or Io → skip recording)
- Task 1: grep -c "pub fn start_workspace" baude-core/src/launch.rs equals 1 (shared helper exists in 13-01 tracer)
- Task 2: Precedence matrix test passes all seven levels; BAUDE_BACKEND now sits after derived (per D-02)
- Task 2: All display_hint() tests pass: returns "(explicit)", "(folder binding)", "(derived)", "(blank)" based on source
- Task 2: UI title rendering tests pass; "(blank)" shown for Default source (not workspace name)
- Task 3: All open_new_session prefill tests pass: git root inside repo, config/launch_dir outside, paths end with /
- Task 3: Repository deduplication test passes: same repo from root and subfolder creates one row
- No test writes to real ~/.config/baude (TestRedirect active)
- All existing tests still pass after precedence reorder (old BAUDE_BACKEND suppression tests replaced)
</success_criteria>

<output>
Create `.planning/phases/13-workspace-derivation-and-new-session-open-defaults/13-02-SUMMARY.md` when done
</output>
