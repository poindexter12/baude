---
phase: 13
plan: 02
type: tdd
wave: 1
depends_on:
  - 13-01
files_modified:
  - baude-core/src/folder_workspace.rs
  - baude-core/src/workspace.rs
  - baude/src/app.rs
autonomous: true
requirements:
  - WSPC-01
  - WSPC-02
  - WSPC-03
  - OPEN-01
  - OPEN-02
  - OPEN-03

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Ancestor walk: binding found on immediate parent is returned before checking grandparent"
    - "Ancestor walk: binding on grandparent is found when parent has no binding"
    - "Ancestor walk: walk terminates at $HOME without walking above it"
    - "Ancestor walk: when path does not exist (launch_dir outside any recorded binding), returns None"
    - "Sanitization: empty folder name after removing non-safe chars becomes empty string, then a placeholder or single hyphen"
    - "Sanitization: unicode characters (non-ASCII) are converted to hyphens"
    - "Sanitization: workspace names with spaces, dots, slashes become hyphenated safe names"
    - "Precedence: explicit BAUDE_WORKSPACE overrides any folder binding"
    - "Precedence: folder binding (from ancestor walk) overrides config workspace key"
    - "Precedence: explicit config workspace overrides derived workspace name"
    - "Precedence: BAUDE_BACKEND bindings do not override folder bindings"
    - "New-session prefill inside repo: git::repo_root returns Some, prefill shows repo root path"
    - "New-session prefill outside repo: git::repo_root returns None, prefill falls back to config new_session_dir or launch_dir"
    - "New-session prefill: path ends with / for consistency with existing behavior"
    - "Repository admission from subfolder and root: same repository identified by canonical common dir, no duplicate rows created"
    - "Daemon startup: bauded calls plan_launch with same launch_dir as baude, resolves same workspace"
  artifacts:
    - "baude-core/src/folder_workspace.rs tests section contains tests for find_binding with parent, grandparent, home-boundary, and no-binding cases"
    - "baude-core/src/workspace.rs tests section contains tests for display_hint returning correct source label for all four WorkspaceSource variants"
    - "baude/src/app.rs tests section contains open_new_session_modal prefill tests for inside-repo and outside-repo cases"
    - "baude/src/app.rs tests section contains admission deduplication tests launching from subfolder and root"
  key_links:
    - "Unit tests for find_binding exercise the ancestor walk at the function level; integration tests for new-session exercise the whole app flow"
    - "Workspace source field determines display_hint() output; tests verify all four labels appear correctly"
    - "App tests verify that open_new_session_modal prefill logic (git::repo_root first, fallback to config) matches design"
    - "Admission tests verify that git::discover_repository deduplication by common dir prevents duplicates"
  prohibitions:
    - statement: "Tests never write to real ~/.config/baude; all use TestRedirect fixture"
      status: verified
      verification: explicit
    - statement: "find_binding() never reads outside the folder-workspaces.json schema; no external files consulted"
      status: verified
      verification: explicit
    - statement: "New-session prefill logic never modifies app state or side effects; pure path selection logic"
      status: verified
      verification: explicit

---

<objective>
Add comprehensive unit and integration tests to verify that workspace derivation, ancestor walk, display hints, new-session prefill, and repository deduplication all work correctly under the requirements.

Purpose: TDD catches regressions early and documents expected behavior. Tests validate edge cases: bindings at multiple levels, paths at filesystem boundaries, outside-repository launches, and parallel admits.

Output: Red→Green test suite covering WSPC-01/02/03, OPEN-01/02/03; test failures clearly indicate which requirement failed.
</objective>

<execution_context>
@~/.iarx/claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-workspace-derivation-and-new-session-open-defaults/13-RESEARCH.md
@.planning/phases/13-workspace-derivation-and-new-session-open-defaults/13-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Feature: Ancestor walk finds folder bindings at multiple levels, stops at home</name>
  <files>
    baude-core/src/folder_workspace.rs
  </files>
  <read_first>
    baude-core/src/folder_workspace.rs (lines 148-214, existing test patterns)
    baude-core/src/folder_workspace.rs (new find_binding function from 13-01)
    baude-core/src/testing.rs (TestRedirect fixture)
  </read_first>
  <behavior>
    - Test: find_binding called with binding at parent returns parent's workspace name
    - Test: find_binding called with binding at grandparent and parent returns parent's (nearest wins)
    - Test: find_binding called with binding above home returns None (walk stops at home)
    - Test: find_binding called with no binding anywhere returns None
    - Test: find_binding called with launch_dir == home returns None (no walk above home)
    - Edge case: when $HOME is not in the path (e.g., launch_dir is /tmp), walk stops at filesystem root
  </behavior>
  <implementation>
    RED: Write test stubs in baude-core/src/folder_workspace.rs::tests module:
    - test_find_binding_at_parent: set up fixture with binding at parent path, call find_binding from child, expect Some(parent_workspace)
    - test_find_binding_nearest_wins: set up fixtures with binding at parent and grandparent, call from child, expect parent binding
    - test_find_binding_stops_at_home: set up binding above home, call from within home, expect None
    - test_find_binding_returns_none_when_no_binding: call from folder with no binding at any ancestor, expect None

    Fixtures use scratch() helper (line 137 of folder_workspace tests) to create temp dirs; set home via TestRedirect.

    GREEN: Implement each test scenario by setting up the FolderWorkspaceFile with entries, calling find_binding(), asserting result matches expected workspace name or None.

    REFACTOR: Extract common fixture setup into a helper (e.g., fn setup_bindings_fixture(home, entries) -> (root, folders)).
  </implementation>
  <verify>
    <automated>cargo test -p baude-core folder_workspace::tests::find_binding --lib -- --nocapture</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in output</fails_when>
  </verify>
  <done>
    find_binding() ancestor walk tests pass; walk finds nearest binding, stops at home, returns None when no binding exists.
  </done>
</task>

<task type="tdd">
  <name>Feature: Workspace display_hint returns correct source label for explicit/bound/derived/default</name>
  <files>
    baude-core/src/workspace.rs
  </files>
  <read_first>
    baude-core/src/workspace.rs (lines 656-810, existing resolution tests)
    baude-core/src/workspace.rs (Workspace struct and display_hint method from 13-01)
  </read_first>
  <behavior>
    - Test: Workspace with source=Explicit returns "(explicit)"
    - Test: Workspace with source=Bound returns "(folder binding)"
    - Test: Workspace with source=Derived returns "(derived)"
    - Test: Workspace with source=Default returns "(default)"
    - Edge case: display_hint wording is short (one line, no punctuation inside parens)
  </behavior>
  <implementation>
    RED: Write test stubs in baude-core/src/workspace.rs::tests module:
    - test_display_hint_explicit: create Workspace with source=Explicit, call display_hint(), expect "(explicit)"
    - test_display_hint_bound: source=Bound, expect "(folder binding)"
    - test_display_hint_derived: source=Derived, expect "(derived)"
    - test_display_hint_default: source=Default, expect "(default)"

    GREEN: Implement display_hint() method on Workspace that matches on self.source and returns the corresponding string.

    REFACTOR: (if needed) Move string constants to a method or const array for DRY.
  </implementation>
  <verify>
    <automated>cargo test -p baude-core workspace::tests::display_hint --lib -- --nocapture</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in output</fails_when>
  </verify>
  <done>
    display_hint() method returns correct source labels for all four WorkspaceSource variants.
  </done>
</task>

<task type="tdd">
  <name>Feature: New-session prefill uses git repo root when inside a repository, config/launch_dir outside</name>
  <files>
    baude/src/app.rs
  </files>
  <read_first>
    baude/src/app.rs (lines 8348-8482, existing app admission tests)
    baude/src/app.rs (open_new_session_modal from 13-01)
    baude-core/src/git.rs (git::repo_root function, used by open_new_session_modal)
  </read_first>
  <behavior>
    - Test: open_new_session_modal called with launch_dir inside repo, git::repo_root returns Some, prefill shows repo root path
    - Test: open_new_session_modal called with launch_dir outside repo (not a git dir), git::repo_root returns None, prefill falls back to config.new_session_dir
    - Test: open_new_session_modal called outside repo with config.new_session_dir None, prefill shows launch_dir
    - Test: prefill path always ends with / for consistency
    - Integration: after admitting path from new-session modal, same repository row is focused (deduplication)
  </behavior>
  <implementation>
    RED: Write test stubs in baude/src/app.rs::tests module:
    - test_open_new_session_prefill_inside_repo: set up test app with launch_dir inside a fixture git repo, call open_new_session_modal(), check modal.buf contains repo root path
    - test_open_new_session_prefill_outside_repo_with_config: set up test app outside any repo with config.new_session_dir set, call open_new_session_modal(), check modal.buf contains config dir
    - test_open_new_session_prefill_outside_repo_no_config: set up test app outside repo with no config.new_session_dir, call open_new_session_modal(), check modal.buf contains launch_dir
    - test_open_new_session_prefill_ends_with_slash: all three cases above end with /

    Fixtures use TestRedirect to set up isolated git repos and config.

    GREEN: Update open_new_session_modal() to match the pattern in 13-01: try git::repo_root first, fallback to config, fallback to launch_dir; ensure trailing slash.

    REFACTOR: (if needed) Extract path formatting logic into a helper.
  </implementation>
  <verify>
    <automated>cargo test -p baude app::tests::open_new_session --lib -- --nocapture</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in output</fails_when>
  </verify>
  <done>
    open_new_session_modal() prefill tests pass; git root used inside repo, config/launch_dir used outside, paths end with /.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → Real filesystem | TestRedirect intercepts all home/config dir reads; no test writes to real ~/.config/baude |
| Test app instances → Shared state | Each test app is isolated; no interference between tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-06 | Denial | Test isolation bypass | low | mitigate | All tests use TestRedirect; containment guard scripts run after test suite (existing phase 8 pattern) |
| T-13-07 | Elevation | Test app state pollution | low | mitigate | Each test creates a fresh app instance; no shared mutable state between tests |

</threat_model>

<verification>
Red→Green→Refactor cycle for each TDD task:
1. RED: Write test stubs with descriptive names, compile to ensure test harness finds them
2. GREEN: Implement the feature so tests pass
3. REFACTOR: Clean up test code and implementation if needed, re-run to ensure green

Run full test suite before task completion:
- `cargo test -p baude-core workspace:: folder_workspace:: --lib`
- `cargo test -p baude app::tests::open_new_session --lib`
</verification>

<success_criteria>
- All find_binding() tests pass: ancestor walk finds nearest binding, stops at home, returns None when no binding
- All display_hint() tests pass: source field determines output label (explicit/bound/derived/default)
- All open_new_session prefill tests pass: git root inside repo, config/launch_dir outside, paths end with /
- No test writes to real ~/.config/baude (TestRedirect active)
- Test code follows existing patterns in folder_workspace.rs:148-214 and app.rs:8348-8482
</success_criteria>

<output>
Create `.planning/phases/13-workspace-derivation-and-new-session-open-defaults/13-02-SUMMARY.md` when done
</output>
